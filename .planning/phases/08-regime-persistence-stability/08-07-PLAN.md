---
phase: 08-regime-persistence-stability
plan: 07
type: execute
wave: 2
depends_on: ["08-03"]
files_modified:
  - scripts/run_subsample_stability.py
  - tests/unit/test_platform_subsample_stability_record.py
  - outputs/reports/platform/stability/stability_rows.parquet
  - outputs/reports/platform/stability/stability_cost_matrices.parquet
  - outputs/reports/platform/stability/stability_record.json
  - .planning/phases/08-regime-persistence-stability/08-STABILITY.md
autonomous: true
requirements: [PER-06]

estimate:
  tokens: 74000
  raw_tokens: 74000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "§4.4 criterion 3 is RUN for BOTH classifiers under FOUR subsample schemes — drop first decade, drop last decade, circular block bootstrap across {6,12,24,48}, and leave-one-episode-out (ROADMAP criterion 5)."
    - "The full-sample reference fit is REPRODUCED in process and asserted elementwise against the tracked regime_labels checkpoint before any subsample is fit; a mismatch halts, because a stability table measured against a different baseline than every recorded number uses is worthless."
    - "Every output row carries subsample occupancy in months and percent, the within-state split-half null at the same n, the margin to the next-best alternative, and the subsample episode count."
    - "The only pass/fail rendered is §4.4 AMENDMENT condition (i) — at least three temporally separated episodes — which binds on classifier #1's sub-floor state 0 alone. No other threshold is invented, before or after the numbers are seen."
  artifacts:
    - "scripts/run_subsample_stability.py — the runner; no registry writes, no (K, lambda) search."
    - "outputs/reports/platform/stability/ — the row table, the full K x K cost matrices per (classifier, scheme), and the JSON record."
    - ".planning/phases/08-regime-persistence-stability/08-STABILITY.md — the pre-registered prediction quoted from commit 2363752, the results, and the verdict."
  key_links:
    - "_reference_label_columns(dev_features, lean_cols, first_decision) -> 10 frozen columns -> standardize -> fit_jump_model(K=6, lam=10.0) -> canonicalize -> elementwise identity with data/checkpoints/platform/regime_labels.parquet (VERIFIED at plan time, 695 months, 0 mismatches)."
    - "reference fit -> run_stability(schemes) -> stability_row per (classifier, scheme, state) -> 08-STABILITY.md verdict."
---

<objective>
Run the one §4.4 acceptance criterion that has never been run, for both classifiers, and record
whichever way it falls.

Purpose: ROADMAP criterion 5. `grep` for `hungarian|linear_sum_assignment|subsample_stab` across
`src/` and `tests/` returned **nothing** before plan 08-03. Criterion 3 — *"states persist with
matched emission parameters (match via Hungarian algorithm on distribution distances to defeat
label switching). A 'regime' that evaporates when 2008-09 is dropped is an episode, not a
regime"* — is the test that decides whether classifier #1's states are regimes or episodes. Both
classifiers were re-pinned on 2026-09-18 (#1 to K=6/lambda=10.0, #2 to K=5/lambda=16.0), so the
labelings are fresh and this is the right moment.

Output: the row table, the persisted cost matrices, and `08-STABILITY.md`.

**The reference fit reproduces exactly, and that was verified at plan time rather than assumed.**
`_reference_label_columns(dev_features, lean_cols, first_decision)` resolves to the ten columns
`['cape_shiller', 'credit_spread_baa_aaa', 'curve_10y3m', 'div_yield', 'oil',
'real_rate_level', 'realized_vol_1m', 'realized_vol_3m', 'trailing_return_1m',
'trailing_return_3m']` with `first_decision` 1972-01-31; `X` is **(695, 10)**, 1963-02-28 to
2020-12-31; and `fit_jump_model(K=6, lam=10.0, n_restarts=10)` followed by `canonicalize_states`
reproduces `data/checkpoints/platform/regime_labels.parquet` with **0 mismatches**. The runner
asserts that identity before it fits anything else. Without it, every subsample is being compared
against a baseline whose provenance nobody checked.

**The prediction is already pre-registered, at commit `2363752`, before any criterion-3 number
existed.** `08-RESEARCH.md` §5.1 states it:

- **State 0 (crisis, 5.76%, 40 months) will PASS.** Drop-first-decade removes one 3-month
  episode; drop-last-decade removes two (6 months). Six to seven temporally separated episodes
  remain either way, so AMENDMENT condition (i) holds. `08-CONTEXT.md` D-05 expects this test to
  adjudicate the crisis state; on the arithmetic it will **exonerate** it.
- **State 2 is the likely FAILURE** — 10.22%, **one contiguous 71-month episode, 1996-07 to
  2002-05**, never recurring — **and neither decade-drop touches it**, because 1996-2002 is
  interior. The three schemes criterion 3 names are, by construction, poorly aimed at the actual
  failure mode. Leave-one-episode-out is the design's own "drop 2008-09" example generalised, and
  for a one-episode state it is **degenerate — and that degeneracy is the answer**, reached with
  no invented threshold.

The record quotes that prediction from its commit and then says whether it held. A prediction
recovered after the fact is not a prediction.

**Whichever way it falls, it is recorded.** `08-CONTEXT.md` D-05: *"Whichever way the result
falls, it is recorded; if the crisis state fails, the exemption's condition (i) is in question and
that must be said."*

**Registry cost: zero.** Re-fitting at the pinned (K, lambda) evaluates no configuration. **No
stability result may re-pin K or lambda** — that would be a selection decision taken on a
diagnostic, after seeing it.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-regime-persistence-stability/08-CONTEXT.md
@.planning/phases/08-regime-persistence-stability/08-RESEARCH.md
@.planning/phases/08-regime-persistence-stability/08-03-SUMMARY.md
@src/trading_crab_lib/platform/labeling/stability.py
@src/trading_crab_lib/platform/labeling/classifier2.py
@scripts/run_joint_lift.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reproduce both reference labelings in process, assert identity with the tracked checkpoints, then run one scheme end to end</name>

  <files>
    scripts/run_subsample_stability.py,
    tests/unit/test_platform_subsample_stability_record.py
  </files>

  <read_first>
    - `scripts/run_joint_lift.py` lines 156-200 — `build_inputs`, which produces the frozen lists.
      Reuse it: re-deriving the frozen list independently is how the harness ends up measuring a
      different classifier than every recorded number came from.
    - `src/trading_crab_lib/platform/labeling/diagnostics.py` lines 274-315 — `label_regimes`'s
      wiring order. **Note the discrepancy the runner must resolve:** `label_regimes` selects
      `lean_feature_set(cfg)` (13 columns) while the tracked `regime_labels.parquet` (695 months,
      K=6) reproduces from `_reference_label_columns`'s **ten** frozen columns. The runner uses
      the ten, because that is what reproduces the checkpoint elementwise, and it records that
      fact rather than leaving it as an unexplained choice.
    - `src/trading_crab_lib/platform/labeling/classifier2.py` lines 140-220 and 296-340 —
      `classifier2_config` (K, lam, n_restarts, sort_column, features, with the lambda-equals-
      coefficient-times-n invariant validated at read time) and `label_leadership_regimes`'s
      holdout carve. Classifier #2's reference is `regime_labels_2.parquet`, 696 months,
      1963-01-31 to 2020-12-31, K=5.
    - `src/trading_crab_lib/platform/labeling/stability.py` — plan 08-03's public surface:
      `fit_for_stability`, `match_states`, `split_half_null`, `state_episodes`, `stability_row`,
      `run_stability`, and the four scheme functions.
  </read_first>

  <behavior>
    - `build_reference(cfg, classifier)` -> the reference `fit_for_stability` result plus the
      resolved frozen column list and the pinned `(K, lam, n_restarts, sort_column)`.
    - `assert_reference_matches_checkpoint(reference, checkpoint_name)` -> raises with the
      mismatch count and the first five mismatched dates if the reproduced states differ from the
      tracked checkpoint anywhere. **Elementwise, no tolerance.** `fit_jump_model` is
      deterministic at a fixed seed (base `random_state=42`, `n_restarts` from config) and the DP
      decode is deterministic, so equality is the correct assertion and a tolerance would hide a
      real divergence.
    - A `--schemes` flag so one scheme can be run alone for the tracer, defaulting to all four.

    Tests that must be able to fail:
    - **Reference identity.** For both classifiers, the reproduced states equal the tracked
      checkpoint elementwise: 695 months / 6 states for #1, 696 months / 5 states for #2, 0
      mismatches. Fails if the frozen list, K, lambda, `n_restarts` or `sort_column` drift from
      what produced the checkpoint — which is precisely the drift that would silently invalidate
      the whole table.
    - **The mismatch path is exercised.** Feed `assert_reference_matches_checkpoint` a
      deliberately perturbed state series and assert it raises, with the mismatch count in the
      message. Fails if the assertion is a log line rather than a raise.
    - **Frozen list identity.** The resolved list for #1 is exactly the ten columns named in this
      plan's objective, in that order, and for #2 exactly `classifier2_config(cfg)["features"]`.
      Order is load-bearing: `canonicalize_states` locates `sort_column`'s centroid by column
      position.
  </behavior>

  <action>
    Create `scripts/run_subsample_stability.py` with the `main(argv)` + `SystemExit` footer this
    repository uses. Flags: `--classifier {1,2,both}`, `--schemes`, `--out-dir` (default
    `outputs/reports/platform/stability`), `--seed` (default the module constant), `--n-bootstrap`
    (default 200), `--null-reps` (default 200).

    Build both references, assert both identities, then run **one** scheme — drop-first-decade for
    classifier #1 — end to end and print the resulting rows. That single path proves the wiring:
    reference fit, scheme mask, subsample fit at the same pinned constants and the same frozen
    columns, de-standardized centroids on both sides, Hungarian match, split-half null at the
    subsample's own per-state n, occupancy, episodes, margin.

    Log at INFO the reference identity result for each classifier with its month count and state
    count, because that line is the first thing anyone debugging this table will want.

    Runtime budget, from measured figures: one L1 fit is ~0.111 s, so 2 classifiers x (2 decade
    drops + 4 block lengths x 200 replicates + K leave-one-episode-out refits) is roughly
    **1,620 fits ≈ 3 minutes**, plus the split-half nulls which are pure arithmetic on cached
    rows. **Criterion 3 is cheap. There has never been a cost reason it was not run.** Say that in
    the script docstring.
  </action>

  <verify>
    <automated>pytest tests/unit/test_platform_subsample_stability_record.py -q -k "reference or frozen or mismatch"</automated>
    <automated>python scripts/run_subsample_stability.py --classifier 1 --schemes drop_first_decade --out-dir /tmp/stability_smoke</automated>
  </verify>

  <done>
    Both reference labelings reproduce their tracked checkpoints elementwise with 0 mismatches,
    asserted by a raise whose failure path is itself tested. One scheme runs end to end and emits
    rows carrying all seven required fields plus the evaporation flag.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: All four schemes, both classifiers, artifacts persisted with the full cost matrices</name>

  <files>
    scripts/run_subsample_stability.py,
    outputs/reports/platform/stability/stability_rows.parquet,
    outputs/reports/platform/stability/stability_cost_matrices.parquet,
    outputs/reports/platform/stability/stability_record.json,
    tests/unit/test_platform_subsample_stability_record.py
  </files>

  <read_first>
    - `08-RESEARCH.md` §5.5 — the seven per-row quantities and the instruction to persist the full
      K x K distance matrix so a later reader can re-judge without a re-run.
    - `08-RESEARCH.md` §5.6 — the circular block bootstrap, the {6, 12, 24, 48} ladder, the
      Politis-White n^(1/3) ≈ 8.9 anchor quoted-not-obeyed, and the seam caveat: the jump model
      penalises state changes **in index order**, so a block-bootstrapped series contains
      synthetic seams at which the penalty fires on artefacts. Block bootstrap is the **weakest**
      of the four schemes for a temporally penalized model specifically, and the write-up must say
      so rather than presenting the schemes as equals.
    - `08-RESEARCH.md` §5.1 — the per-state episode table for classifier #1, which the run should
      reproduce as a by-product: state 0 nine episodes, state 1 five, **state 2 one**, state 3
      four, state 4 three (72 of 84 months in one), state 5 four.
  </read_first>

  <behavior>
    The JSON record carries, per classifier: the resolved frozen columns, `(K, lambda,
    n_restarts, sort_column)`, the reference identity result, the full-sample per-state occupancy
    and episode table, the seed, `n_bootstrap`, `n_null_reps`, the block-length ladder, and a
    `rows` array. Each row: classifier, scheme, scheme parameters (block length and seam count for
    bootstrap rows; dropped episode span and `degenerate` for leave-one-episode-out rows),
    reference state id, matched partner id, `is_identity`, matched distance, split-half null
    median with its p10/p90 and its n, margin, subsample occupancy months and pct, subsample
    episode count, and `evaporated`.

    Tests that must be able to fail, run against the artifacts:
    - **Coverage.** Exactly four scheme families appear; the bootstrap family has one entry per
      ladder value; every (classifier, scheme, reference state) combination is present, so a
      silently-skipped state cannot look like a clean table. Assert the exact expected row count,
      computed from K and the scheme list, not a lower bound.
    - **Every row carries its occupancy.** No row has a null `subsample_occupancy_months`. A row
      whose occupancy is 0 has `evaporated is True`. Fails if the flag is derived from the
      distance.
    - **The null is present and non-zero on every row**, and each row's null `n` equals that
      state's own subsample month count — not the full-sample count. Fails if the null was
      computed once at full-sample n and reused, which would make it the wrong yardstick for
      exactly the small states that matter.
    - **Leave-one-episode-out is degenerate for classifier #1 state 2**, with
      `n_months_dropped == 71` and the dropped span 1996-07 to 2002-05. This is the pre-registered
      prediction's most specific consequence and it is asserted as a **structural** fact about the
      labeling, not as a verdict about whether state 2 is a regime.
    - **Bootstrap rows carry a seam count** and it is strictly greater than 0 for every ladder
      value below `len(index)`.
    - **No threshold appears.** Assert that the record contains no key matching
      `/threshold|passes|stable_if/` outside the AMENDMENT-condition-(i) episode count. Fails if
      someone adds a persistence cutoff.
  </behavior>

  <action>
    Run the full harness for real:
    `python scripts/run_subsample_stability.py --classifier both --out-dir outputs/reports/platform/stability`.
    Persist the three artifacts and commit them; they are small and the whole point of persisting
    the cost matrices is that a later reader can re-judge without a re-run.

    Emit the AMENDMENT condition (i) evaluation as a **single named field** on the rows where it
    applies — classifier #1's state 0 only, the sub-floor state invoking the recurrence exemption
    — computed as the subsample episode count being at least three, with the condition quoted in
    the field's description. Do **not** compute it for any other state: condition (i) binds only
    on a state invoking the exemption, and applying it elsewhere would be inventing a threshold by
    generalisation.

    Do not hand-edit the artifacts. If a number looks wrong, re-run; the harness is about three
    minutes.
  </action>

  <verify>
    <automated>pytest tests/unit/test_platform_subsample_stability_record.py -q</automated>
    <automated>python -c "
import json; r=json.load(open('outputs/reports/platform/stability/stability_record.json'))
rows=r['rows']; print('rows', len(rows))
loo=[x for x in rows if x['scheme']=='leave_one_episode_out' and x['classifier']==1 and x['reference_state']==2]
assert loo and loo[0]['degenerate'] is True and loo[0]['n_months_dropped']==71, loo
assert all(x.get('subsample_occupancy_months') is not None for x in rows)
assert all(x['split_half_null']['median']>0 for x in rows)
print('coverage, occupancy, null and the state-2 degeneracy all present')"</automated>
    <automated>python -c "
import json
r = json.load(open('outputs/reports/platform/stability/stability_record.json'))
def keys(o):
    if isinstance(o, dict):
        for k, v in o.items():
            yield k
            yield from keys(v)
    elif isinstance(o, list):
        for v in o: yield from keys(v)
# KEY positions only: prose values are allowed to discuss thresholds (the record
# must state that none exists), so scanning values would be self-invalidating.
bad = [k for k in keys(r) if any(s in k for s in ('threshold','stable_if','passes')) and k != 'act_threshold']
assert not bad, bad
print('no invented persistence threshold key in the record')"</automated>
  </verify>

  <done>
    All four schemes ran for both classifiers. The exact expected row count is asserted. Every row
    carries its own-n split-half null, its subsample occupancy and its evaporation flag. The
    classifier #1 state 2 leave-one-episode-out row is degenerate with 71 months dropped over
    1996-07 to 2002-05. No persistence threshold exists anywhere in the record.
  </done>
</task>

<task type="auto">
  <name>Task 3: 08-STABILITY.md — the pre-registered prediction, the result, and a verdict that can say "episode"</name>

  <files>
    .planning/phases/08-regime-persistence-stability/08-STABILITY.md
  </files>

  <read_first>
    - `08-RESEARCH.md` §5.1 (committed at `2363752`) — the prediction, to be quoted with its
      commit hash.
    - `platform_design/platform_design.md` §4.4 and its 2026-09-18 AMENDMENT — criterion 3
      verbatim, and condition (i) verbatim: *"it recurs in at least three temporally separated
      episodes, so that removing any one leaves the state intact — criterion 3 applied directly,
      not by proxy."*
    - `08-CONTEXT.md` D-05 — *"Whichever way the result falls, it is recorded; if the crisis state
      fails, the exemption's condition (i) is in question and that must be said."*
    - `.planning/phases/07-regime-representation/07-MEASUREMENTS.md` — the house format: every
      number with its denominator and window inline, in the same cell.
  </read_first>

  <action>
    Write the record. Required sections, in this order:

    1. **What criterion 3 asks**, verbatim from §4.4, and the AMENDMENT's condition (i) verbatim.
    2. **What was run** — both classifiers, their pinned constants, their frozen column lists, the
       reference identity result (695 months / 0 mismatches; 696 months / 0 mismatches), the four
       schemes, the block ladder, the seed, `n_bootstrap`, `n_null_reps`, and the runtime.
    3. **The pre-registered prediction**, quoted from `08-RESEARCH.md` §5.1 **with commit
       `2363752`** so a reader can verify it predates the numbers.
    4. **The results table**, per (classifier, scheme, state): matched partner, identity flag,
       matched distance, the **split-half null at the same n beside it**, margin, subsample
       occupancy, subsample episode count, evaporated. Every distance cell reads against its own
       null in the same row — a matched distance without an n-matched null is not interpretable,
       because the null is **not zero**.
    5. **Whether the prediction held**, stated explicitly for state 0 and for state 2, including
       the case where it did not — `08-RESEARCH.md`'s own assumption A8 says a wrong prediction
       *"is itself a recordable result"*.
    6. **The one verdict this phase may render**, and only it: AMENDMENT condition (i) on
       classifier #1's state 0. If it holds, say the exemption's condition (i) is satisfied under
       criterion 3 applied directly. If it fails, say that the exemption's condition (i) is **in
       question** and that this must be carried into `.planning/STATE.md`'s open items, per D-05.
    7. **What is NOT a verdict.** Every other state's row is a reading, and Glenn's. State
       plainly: no persistence threshold exists, none was invented, and adding one now — after
       the numbers — would be the tie-break shape the pre-registration at `298b1bc` forbids
       elsewhere and would be no more honest here. If a threshold is ever wanted, pre-register it.
    8. **Three caveats that must not be buried.**
       - An **identity** Hungarian assignment means the canonical ordering held; it does **not**
         mean the states persisted. The informative output is the distances. A **non**-identity
         assignment means the canonical ordering itself flipped, and every downstream occupancy,
         profile and lift number is keyed on those ids.
       - De-standardizing recovers **winsorized** units and the 1%/99% clip bounds also differ per
         subsample; second-order for centroids over 40+ months, not zero.
       - Block bootstrap is the **weakest** of the four for a temporally penalized model: the DP
         penalises changes in index order and the resampled series contains synthetic seams. Quote
         the seam counts. Politis-White's n^(1/3) ≈ 8.9 is cited as a variance-estimation anchor
         and explicitly **not** obeyed, because criterion 3 needs blocks long enough to preserve
         the persistence the states encode, and classifier #1's median sojourn is 9.5 months while
         classifier #2's is 29.0.
    9. **What this does not license.** No re-pin of K or lambda. No dependence statistic of any
       kind — criterion 6 is UNRESOLVED and the pre-registration at `298b1bc` forbids tie-breaks.
       No use of any 2021+ data.
  </action>

  <verify>
    <automated>bash -c "f=.planning/phases/08-regime-persistence-stability/08-STABILITY.md; test -f \$f && for s in '2363752' 'three temporally separated episodes' 'split-half null' '1996-07' 'is not a verdict' 'block bootstrap' '298b1bc'; do grep -qi \"\$s\" \$f || { echo \"MISSING: \$s\"; exit 1; }; done; echo ok"</automated>
    <automated>python -c "
import json
r=json.load(open('outputs/reports/platform/stability/stability_record.json'))
t=open('.planning/phases/08-regime-persistence-stability/08-STABILITY.md').read()
c1=[x for x in r['rows'] if x['classifier']==1 and x['reference_state']==0]
assert any(str(x['subsample_episode_count']) in t for x in c1), 'state 0 episode counts absent from the record'
print('condition (i) inputs appear in the record')"</automated>
  </verify>

  <human-check>
    Open `08-STABILITY.md`. Confirm: (1) the prediction is quoted with commit `2363752` and
    appears **before** the results; (2) every matched-distance cell has its own-n split-half null
    in the same row; (3) the only pass/fail rendered is condition (i) on classifier #1 state 0;
    (4) if classifier #1's state 0 failed condition (i), the record says the exemption is in
    question rather than softening it; (5) the identity-assignment caveat is present and not
    buried.
  </human-check>

  <done>
    The record exists with the prediction quoted from its commit ahead of the results, every
    distance read against its own-n null, one verdict rendered and its scope stated, and the three
    caveats present.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reproduced reference ↔ tracked checkpoint | Asserted elementwise before anything else runs. A table measured against an unverified baseline is worthless. |
| diagnostic ↔ selection | Re-fitting at pinned (K, lambda) selects nothing; no result may re-pin either. |
| criterion 3 ↔ criterion 6 | Disjoint. Nothing here computes, references or re-derives a dependence statistic. |
| dev ↔ 2021+ holdout | Both references end 2020-12-31 by construction; the identity assertion proves the window. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-33 | Spoofing | a stability table measured against a reference that is not the one every recorded number uses | **critical** | mitigate | `assert_reference_matches_checkpoint` raises on any elementwise mismatch for both classifiers, with the mismatch count; its failure path is itself tested on a perturbed series. |
| T-08-34 | Repudiation | a persistence threshold invented after the numbers | **critical** | mitigate | An automated check scans the JSON for threshold-shaped keys and fails; the record's §7 states the prohibition and names `298b1bc`. |
| T-08-35 | Spoofing | an evaporated state scoring stable on a frozen centroid | **critical** | mitigate | `evaporated` derives from occupancy alone (plan 08-03); artifact tests assert no null occupancy and that zero occupancy implies the flag. |
| T-08-36 | Repudiation | a matched distance read without an n-matched null | high | mitigate | Every row carries its own split-half null at that state's own subsample n; a test asserts the null's n equals that count, not the full-sample count. |
| T-08-37 | Repudiation | block bootstrap presented as equal in weight to the decade drops | medium | mitigate | Seam counts are persisted per bootstrap row and asserted non-zero; the record's caveat section states the scheme is the weakest for a temporally penalized model and quotes the seam counts. |
| T-08-38 | Tampering | hand-editing the persisted artifacts | medium | mitigate | The run is ~3 minutes; the action forbids hand-editing; the artifact tests recompute coverage from K and the scheme list. |
| T-08-39 | Information disclosure | a dependence statistic computed in passing | high | mitigate | Nothing in `stability.py` or the runner computes ARI, NMI or Cramér's V. The scope fence is restated in the record's §9. |
| T-08-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install occurs; scipy is pre-existing (`08-RESEARCH.md` §§ 10-11). |

ASVS level 1, block on high.
</threat_model>

<verification>
- `pytest tests/unit/test_platform_subsample_stability_record.py -q` — green, 0 skipped.
- `pytest tests/ -q` — full suite green.
- `python -c "from trading_crab_lib.platform.honesty.registry import total_trial_count; print(total_trial_count())"` → 42, unchanged.
- `pytest tests/unit/test_platform_legacy_import_ratchet.py -q` — ratchet still 31.
- The three artifacts under `outputs/reports/platform/stability/` are committed, not gitignored.
</verification>

<success_criteria>
- ROADMAP criterion 5 met: §4.4 criterion 3 is RUN for both classifiers under four subsample
  schemes, with centroid distance in de-standardized units, Hungarian matching, a within-state
  split-half null at the same n, and subsample occupancy on every row.
- Both reference labelings reproduce their tracked checkpoints elementwise.
- The pre-registered prediction is quoted with its commit and its outcome stated either way.
- Exactly one pass/fail is rendered — AMENDMENT condition (i) on classifier #1 state 0 — and no
  other threshold exists.
</success_criteria>

<output>
Create `.planning/phases/08-regime-persistence-stability/08-07-SUMMARY.md` when done. Record: the
reference identity results; the full results table with every distance beside its own-n null; the
condition (i) verdict for classifier #1 state 0 and, if it failed, the exact sentence to carry
into `.planning/STATE.md`'s open items; whether the pre-registered prediction held for state 0 and
state 2; the seam counts per block length; and confirmation that the registry is unchanged at 42.
</output>
