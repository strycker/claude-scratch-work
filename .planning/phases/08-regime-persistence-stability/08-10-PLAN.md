---
phase: 08-regime-persistence-stability
plan: 10
type: execute
wave: 5
depends_on: ["08-02", "08-05", "08-07", "08-09"]
files_modified:
  - src/trading_crab_lib/platform/backtest/joint_driver.py
  - scripts/run_joint_lift.py
  - tests/unit/test_docs_recorded_counts.py
  - tests/unit/test_platform_joint_diagnostics_record.py
  - CLAUDE.md
  - README.md
  - outputs/reports/platform/joint_lift/measurement_l1only.json
  - outputs/reports/platform/joint_lift/measurement_l2_observational.json
  - outputs/reports/platform/joint_lift/joint_lift_joint_l1only.parquet
  - outputs/reports/platform/joint_lift/joint_lift_baseline_l1only.parquet
  - .planning/phases/08-regime-persistence-stability/08-MEASUREMENTS.md
autonomous: true
requirements: [PER-07, PER-10]

estimate:
  tokens: 76000
  raw_tokens: 76000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Criterion 7 is re-measured under whatever changed, BOTH legs, ONE harness, with the window inline in every cell (ROADMAP criterion 6)."
    - "The decision-bearing leg's status is established by comparison, not by assumption: if the chosen §5.3 mechanism is inert there, the re-run must reproduce the tracked curve BYTE-IDENTICALLY and the registry is not touched; if it is not identical, the plan HALTS, because an inert mechanism that moved a decision-bearing number is a bug or an unregistered configuration change."
    - "Any code change implied by the A11 ruling lands BEFORE criterion 7 is re-measured, never after."
    - "Recorded test counts match reality, measured by a live collection rather than arithmetic: CLAUDE.md x2 and README.md's badge are corrected at the END of the phase, when the suite is at its final size (criterion 9)."
  artifacts:
    - "Regenerated measurement_l1only.json and measurement_l2_observational.json, both legs, one harness."
    - "tests/unit/test_docs_recorded_counts.py — the recorded counts pinned against a live pytest collection."
    - ".planning/phases/08-regime-persistence-stability/08-MEASUREMENTS.md — the phase's closing record: every criterion, every number, every window, every verdict."
  key_links:
    - "08-05's A11 ruling -> any band-tier code change -> run_joint_lift -> measurement JSONs -> 08-MEASUREMENTS.md. The ruling precedes the number it judges."
    - "pytest --collect-only -q (subprocess) -> the live count -> CLAUDE.md lines 128 and 606 and README.md's badge and line 477."
---

<objective>
Re-measure criterion 7 under everything this phase changed, close the phase's recorded numbers,
and write the record that says what each criterion actually returned.

Purpose: ROADMAP criteria 6 and 9. The prior value — `wealth_delta` **-0.123438**, `dd_delta`
**+0.024084**, over **588 steps, 1972-01-31 to 2020-12-31**, 0 degraded steps — is a **comparison
point, not a target**. A change in it is the expected consequence of changing the labeling path,
and is reported with its window either way. No target is pre-declared for either churn metric or
for the lift.

Output: both measurement JSONs, corrected recorded counts, and `08-MEASUREMENTS.md`.

**The result this plan must be willing to report, and must not dress up.** Under the corrected
causal model, the decision-bearing `ROUTING_L1_ONLY` leg is structurally untouched by §5.1 (its
probability vector is `_last_state_one_hot(states_1)`, a label) and — under mechanisms
`a-hard-gate` or `c-magnitude-scaling` — untouched by §5.3 as well, because p=1.0 clears every
admissible act threshold. **In that case criterion 7 re-measures to exactly the prior value, and
that is the correct outcome, not a failure of the phase.** Saying so plainly is the whole point;
a phase written to catch checks that can only confirm must not close by implying a number moved
when it did not.

Conversely, if mechanism `b-bounded-turnover` was chosen, the decision-bearing weight path **does**
change, the run is a **new configuration**, and it spends the last two registry rows: **42 -> 44,
the ADR-0002 ceiling exactly**. Glenn accepted that at plan 08-09's Task 1 gate or did not choose
(b). This plan does not re-open it.

**And the failure mode that would otherwise be invisible.** If the mechanism was supposed to be
inert and the decision-bearing curve nonetheless moves, that is either a bug or a configuration
change nobody registered. The plan **halts and reports** rather than registering a surprise or
quietly recording a new number. This is S-4 applied at the phase level.

**Scope fences, restated because this is the plan most likely to drift across them.** No migration
work — that is Phase 9. **No dependence statistic of any kind**; criterion 6 of Phase 7 is
UNRESOLVED and the pre-registration at `298b1bc` forbids tie-breaks. No 2021+ holdout use for any
selection decision. `legacy/` and the reference submodules untouched. The legacy-import ratchet is
**31** and may only decrease.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-regime-persistence-stability/08-CONTEXT.md
@.planning/phases/08-regime-persistence-stability/08-A11.md
@.planning/phases/08-regime-persistence-stability/08-A7.md
@.planning/phases/08-regime-persistence-stability/08-CHURN.md
@.planning/phases/08-regime-persistence-stability/08-TRACK-A.md
@.planning/phases/08-regime-persistence-stability/08-STABILITY.md
@.planning/phases/08-regime-persistence-stability/08-G6.md
@scripts/run_joint_lift.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Apply the A11 ruling's code consequence, then re-measure criterion 7 — both legs, one harness, window inline</name>

  <files>
    src/trading_crab_lib/platform/backtest/joint_driver.py,
    scripts/run_joint_lift.py,
    tests/unit/test_platform_joint_diagnostics_record.py,
    outputs/reports/platform/joint_lift/measurement_l1only.json,
    outputs/reports/platform/joint_lift/measurement_l2_observational.json,
    outputs/reports/platform/joint_lift/joint_lift_joint_l1only.parquet,
    outputs/reports/platform/joint_lift/joint_lift_baseline_l1only.parquet
  </files>

  <read_first>
    - `.planning/phases/08-regime-persistence-stability/08-A11.md` — the ruling and its explicit
      handoff: any code change the ruling implies is executed **here**, under the ruling, before
      criterion 7 is re-measured. If the ruling was `a-no-gate` there is no code change and this
      step is a no-op that must still be **stated** as one in the SUMMARY.
    - `.planning/phases/08-regime-persistence-stability/08-A7.md` — which §5.3 mechanism was
      chosen and whether it is inert on the decision-bearing leg. **This determines whether the
      decision-bearing run is a reproduction or an evaluation, and therefore whether it registers.**
    - `src/trading_crab_lib/platform/backtest/joint_driver.py` lines 62-70 — the band constants
      and the note that `DD_DELTA_UNIVERSAL` is the **revised** `[-1, 1]`, not the retired
      `[-2, 2]`, because *"the old bound was wider than the quantity's own arithmetic range
      (`max_drawdown` in `[-1, 0]` per leg) and could therefore only confirm."*
    - `scripts/run_joint_lift.py` lines 204-262 — `decision_bearing = (routing ==
      ROUTING_L1_ONLY) and not dry_run`, the registry row-count assertions at `:230-249`, and
      `ADR_0002_CEILING = 44` at `:89`.
    - `outputs/reports/platform/joint_lift/measurement_l1only.json` — the prior values:
      `lift.wealth_delta` -0.12343826162064975, `lift.dd_delta` +0.02408401236666291,
      `baseline_leg.sharpe_annualized` 0.9170725133308871, `joint_leg.sharpe_annualized`
      0.914903185594245, both DSRs below the hurdle at `n_trials` 42 with `sharpe_variance` 1.0.
  </read_first>

  <behavior>
    The run ladder, in order, and the branch is decided by `08-A7.md`, not at execution time:

    1. **Apply the A11 code consequence** (or record that there is none).
    2. **Observational leg.** `--routing l2 --dump-curves ...` — always `NO_REGISTRY`. Regenerate
       `measurement_l2_observational.json`.
    3. **Decision-bearing leg, reproduction branch** (mechanism inert per `08-A7.md`):
       `--routing l1 --dry-run --dump-curves ...`. Compare the resulting
       `joint_lift_{joint,baseline}_l1only.parquet` against the versions recovered from git
       **byte-identically**, via `pandas.testing.assert_frame_equal` with no tolerance.
       - **Identical** -> criterion 7 is re-measured as a **reproduction**: the configuration did
         not change, so this is not a new evaluation, the registry stays at **42**, and the record
         states that the decision-bearing numbers are unchanged **and why that was predicted**.
       - **Not identical** -> **HALT.** Do not register, do not record a new number, do not adjust
         a band. Report the first differing date and column. An inert mechanism that moved a
         decision-bearing number is a bug or an unregistered configuration change, and either way
         it outranks finishing the phase.
    4. **Decision-bearing leg, evaluation branch** (mechanism `b-bounded-turnover`):
       `--routing l1 --dump-curves ...` with a `trial_tag` naming the phase and the mechanism.
       This appends **exactly 2** rows: 42 -> **44**. Assert the delta is exactly 2 and that
       `total_trial_count() <= ADR_0002_CEILING` — the script already asserts both, and the plan
       asserts them again from outside so the ceiling is hit visibly rather than incidentally.

    Tests that must be able to fail:
    - **Both legs, one harness, window inline.** Assert every lift cell in each measurement JSON
      carries `n_steps`, `n_steps_joint`, `n_steps_baseline` and the window string, and that the
      two legs' `n_steps` agree. A lift quoted without its window is not quotable.
    - **The reproduction claim is checked, not asserted.** In the reproduction branch, a test
      asserts `wealth_delta` and `dd_delta` equal the prior values to **1e-12** — not to a
      tolerance that would absorb a real change.
    - **The registry delta is exactly what the branch predicts** — 0 or 2 — asserted by reading
      `total_trial_count()` before and after.
    - **Band flags are recorded, not re-tuned.** Assert the four band flags are present in the
      record and that the band constants in `joint_driver.py` are unchanged unless `08-A11.md`
      ruled a change, in which case assert they match the ruling's stated values.
  </behavior>

  <action>
    Execute the ladder. Take the "before" curves from git (`git show <rev>:<path>`) into a temp
    file rather than from the working tree, so the comparison cannot be against a file this phase
    already rewrote.

    If the halt branch fires, write the finding into `08-MEASUREMENTS.md` immediately with the
    differing dates and stop — a halted criterion 7 honestly recorded is a result; a criterion 7
    completed by adjusting something is the failure this phase exists to catch.

    Do **not** re-derive the prior values by re-running anything. They are quoted from the tracked
    `measurement_l1only.json` and from `.planning/STATE.md`, both of which predate this phase.
  </action>

  <verify>
    <automated>pytest tests/unit/test_platform_joint_diagnostics_record.py -q</automated>
    <automated>python -c "
import json
a=json.load(open('outputs/reports/platform/joint_lift/measurement_l1only.json'))
b=json.load(open('outputs/reports/platform/joint_lift/measurement_l2_observational.json'))
for name,r in (('l1only',a),('l2',b)):
    l=r['lift']; assert l['n_steps_joint']==l['n_steps_baseline']==588, (name,l)
    print(name, 'wealth_delta', l['wealth_delta'], 'dd_delta', l['dd_delta'], 'n_steps', l['n_steps'])"</automated>
    <automated>python -c "from trading_crab_lib.platform.honesty.registry import total_trial_count; n=total_trial_count(); assert n in (42,44), n; print('registry', n, 'of ceiling 44')"</automated>
  </verify>

  <done>
    The A11 ruling's code consequence is applied or explicitly recorded as none. Both legs ran
    through one harness. The decision-bearing leg is either a verified byte-identical reproduction
    at zero registry cost, or a registered evaluation at exactly 2 rows, or the plan halted with
    the differing dates recorded.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Recorded counts match a live collection — measured at the end, when the suite is at its final size</name>

  <files>
    tests/unit/test_docs_recorded_counts.py,
    CLAUDE.md,
    README.md
  </files>

  <read_first>
    - `CLAUDE.md` line 128 (`← pytest test suite (1705 tests)`) and line 606 (`**1705 tests
      collected**`); `README.md` line 5 (the `tests-1705%20passing` badge) and line 477
      (`✓ 1705 tests (unit + integration), all passing`). **Four sites, one number.**
    - `.planning/STATE.md` — the suite baseline is **2018 passed, 0 skipped, 0 xfailed**. This
      phase adds tests in eight plans, so the final number is neither 1705 nor 2018 and **must be
      measured, not computed**. `08-01-SUMMARY.md` recorded an intermediate `--collect-only`
      count precisely so nobody is tempted to do arithmetic.
    - `08-CONTEXT.md` D-07: *"Trivial — and exactly the shape of recorded number that has misled
      this project three times already."*
  </read_first>

  <behavior>
    `tests/unit/test_docs_recorded_counts.py`:
    - Runs `pytest --collect-only -q -p no:cacheprovider` in a **subprocess** against `tests/`,
      parses the collected count from its summary line, and compares it against the number parsed
      out of each of the four documentation sites. `--collect-only` executes nothing, so there is
      no recursion; set an env sentinel anyway and assert on it, so a future change that makes
      collection execute something fails loudly instead of hanging.
    - Parses each site with its own regex so a mismatch reports **which** of the four is stale.
    - Asserts **equality**, not a bound. A `>=` assertion here would be a check that can only
      confirm — the precise defect criterion 9 exists to correct, and the same shape as the
      `grep -v platform` filter this project has already recorded.

    Tests that must be able to fail:
    - Feed the parser a synthetic document containing `1705` and assert the comparison **fails**
      with a message naming the stale value and the live one. Without this arm, the test's own
      failure path is untested and a broken regex would present as a pass.
    - Assert all four sites are found. If a regex matches nothing, that is a failure, not a skip —
      a silently unmatched site is how three of the four could drift while the test stayed green.
  </behavior>

  <action>
    Run the full suite first and record the live collected count. Then update all four
    documentation sites to that number in one commit, together with the test that pins it. One
    commit, because a commit that changes the docs without the pin leaves a reader unable to tell
    which is authoritative — the same reasoning that governed F-4's denominator fix in plan 08-01.

    Also update `README.md`'s badge URL, not only its text.

    Do **not** touch `.planning/STATE.md` here; the phase's state update is the orchestrator's,
    after verification.

    **Sequencing note, to be preserved if this plan is ever re-ordered.** This task is last on
    purpose: every earlier plan in this phase adds tests, and pinning the count any earlier would
    leave the suite red for the rest of the phase. If a later plan is ever inserted after this
    one, it must update the four sites and this pin in its own commit.
  </action>

  <verify>
    <automated>pytest tests/unit/test_docs_recorded_counts.py -q</automated>
    <automated>python -c "
import re
# Region-scoped: the ADR / Development Decisions log in CLAUDE.md may legitimately
# record a historical count in a '301 -> 428'-style entry, so a whole-file scan
# would be self-invalidating. Only the four LIVE claim sites are checked.
c = open('CLAUDE.md').read(); r = open('README.md').read()
sites = re.findall(r'pytest test suite \((\d+) tests\)', c) + \
        re.findall(r'\*\*(\d+) tests collected\*\*', c) + \
        re.findall(r'tests-(\d+)%20passing', r) + \
        re.findall(r'(\d+) tests \(unit \+ integration\)', r)
assert len(sites) == 4, ('not all four recorded-count sites were found', sites)
assert len(set(sites)) == 1, ('the four sites disagree', sites)
assert sites[0] != '1705', 'the stale count survives at a live claim site'
print('four live sites agree at', sites[0])"</automated>
  </verify>

  <done>
    All four documentation sites carry the live collected count, pinned by a test that asserts
    equality, reports which site is stale, and has its own failure path exercised. No `1705`
    remains in either file.
  </done>
</task>

<task type="auto">
  <name>Task 3: 08-MEASUREMENTS.md — every criterion, its number, its window, and its verdict</name>

  <files>
    .planning/phases/08-regime-persistence-stability/08-MEASUREMENTS.md
  </files>

  <read_first>
    - All eight prior SUMMARYs in this phase, plus `08-TRACK-A.md`, `08-STABILITY.md`,
      `08-CHURN.md`, `08-A7.md`, `08-A11.md`, `08-G6.md`.
    - `.planning/phases/07-regime-representation/07-MEASUREMENTS.md` — the house format: every
      number carries its denominator and its window **inline, in the same cell**.
    - `.planning/UAT-AUDIT-2026-09-09.md` — why a shape check is not evidence.
  </read_first>

  <action>
    Write the phase's closing record. Required sections:

    1. **The ten criteria, one row each**, with: the number, its denominator, its window, and a
       verdict of MET / MET-AS-A-MEASUREMENT / NOT MET / HALTED. No criterion may be marked MET on
       a shape check.
    2. **The two tracks, side by side**, with the sentence this phase exists to make
       un-mistakable: the 41.91% is an **L1** quantity; §5.1 changes **L2**; the fix cannot move
       that number, and the fact that it did not is the **predicted** result. Track A 246/587,
       Track B's B0 and B1 with their degraded counts.
    3. **What is decision-bearing and what is not.** The l1only leg is decision-bearing; the l2
       leg is observational and firewalled per ADR-0002 decision (e), and **nothing downstream may
       change on its basis**. State which of this phase's numbers came from which. If the §5.3
       mechanism is inert on the decision-bearing leg, say so in those words.
    4. **Criterion 7 re-measured**, both legs, one harness, window inline, against the prior
       `wealth_delta` -0.123438 / `dd_delta` +0.024084 quoted as a **comparison point, not a
       target**. Include the registry count before and after and the headroom remaining against
       the ADR-0002 ceiling of 44.
    5. **The DSR status**, unchanged in kind: both legs' deflated Sharpe against
       `expected_max_sharpe(n_trials, 1.0)`, with the standing caveat that `sharpe_variance` is
       the **1.0 placeholder** until 20 independent Sharpe-bearing trials exist.
    6. **Baselines**: suite count before and after, legacy-import ratchet (must still be **31**,
       may only decrease), registry count, and 0 skipped / 0 xfailed.
    7. **Open items carried forward**, in a form `.planning/STATE.md` can take verbatim — at
       minimum: Phase 7's criterion 6 dependence verdict still UNRESOLVED with no tie-break
       permitted; ADR-0001 condition (iv)'s covariance clause still unimplemented and now pinned
       as a known non-compliance by G6; anything `08-STABILITY.md` raised about AMENDMENT
       condition (i); and the Track A verdict's consequences, including whether a lambda ruling is
       now owed.
    8. **What this phase did NOT do**, stated rather than left to inference: no lambda sweep, no
       dependence statistic, no migration work, no 2021+ holdout use, no re-pin of K or lambda, no
       target pre-declared for either churn metric, and — if applicable — no fix of the G6
       non-compliance.
    9. **One honest paragraph on the phase's own defect.** This phase was scoped on a wrong causal
       model and corrected itself before executing: the original criterion 2 would have reported
       246 -> 246 whether the fix worked or not. Record that as the sixth instance of the
       signature defect, note that it was caught by the phase's own research rather than at UAT,
       and say what changed in the process as a result — because a project that records its near
       misses is the only kind that stops repeating them.
  </action>

  <verify>
    <automated>bash -c "f=.planning/phases/08-regime-persistence-stability/08-MEASUREMENTS.md; for s in '246' '41.91' 'observational' 'comparison point, not a target' 'ratchet' '298b1bc' 'sixth'; do grep -qi \"\$s\" \$f || { echo MISSING \$s; exit 1; }; done; echo ok"</automated>
    <automated>python -c "
import re
t=open('.planning/phases/08-regime-persistence-stability/08-MEASUREMENTS.md').read()
verdicts=re.findall(r'\b(MET-AS-A-MEASUREMENT|NOT MET|HALTED|MET)\b', t)
assert len(verdicts)>=10, ('a verdict per criterion is required', len(verdicts))
print('verdicts recorded:', len(verdicts))"</automated>
    <automated>pytest tests/ -q 2>&1 | tail -3</automated>
    <automated>pytest tests/unit/test_platform_legacy_import_ratchet.py -q</automated>
  </verify>

  <human-check>
    Open `08-MEASUREMENTS.md`. Confirm: (1) every one of the ten criteria has a verdict and none
    is MET on a shape check; (2) every number carries its denominator and window in the same cell;
    (3) the decision-bearing versus observational split is explicit, and if the §5.3 mechanism is
    inert on the decision-bearing leg the record says so plainly rather than implying movement;
    (4) §9's paragraph on the phase's own defect is present and does not soften it.
  </human-check>

  <done>
    The record exists, carries a verdict per criterion, keeps every number with its denominator and
    window, separates decision-bearing from observational numbers, lists the carried-forward open
    items in STATE-ready form, and records the phase's own near miss.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reproduction ↔ evaluation | A re-run of an unchanged configuration is a reproduction and must not register; a changed configuration is an evaluation and must. The branch is decided by `08-A7.md`, before the run. |
| A11's ruling ↔ criterion 7's number | The ruling and its code consequence land first. Deciding a gate after seeing the number is a tie-break. |
| decision-bearing l1only ↔ observational l2 | Every number in the record is attributed to one or the other. |
| recorded documentation ↔ measured reality | Four sites, one number, pinned against a live collection with equality rather than a bound. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-56 | Repudiation | an unchanged decision-bearing number reported as if the phase moved it | **critical** | mitigate | The record must state, in those words, that the mechanism is inert on the decision-bearing leg where it is; the reproduction branch asserts equality to 1e-12 against the git-recovered prior curves. |
| T-08-57 | Tampering | a decision-bearing number that moved unexpectedly, registered or recorded anyway | **critical** | mitigate | The halt branch: do not register, do not record, do not adjust a band; report the first differing date and column and stop. |
| T-08-58 | Tampering | the ADR-0002 ceiling exceeded, or hit invisibly | **critical** | mitigate | The registry delta is asserted from outside the script as exactly 0 or exactly 2, and the record carries the count before and after with the remaining headroom. |
| T-08-59 | Repudiation | a recorded count asserted with `>=` | high | mitigate | The pin asserts equality, reports which of the four sites is stale, and has its own failure path exercised on a synthetic `1705` document. |
| T-08-60 | Repudiation | a band adjusted to accommodate a re-measured value | high | mitigate | Band constants are asserted unchanged unless `08-A11.md` ruled a change, in which case they must match the ruling's stated values. |
| T-08-61 | Information disclosure | a dependence statistic or a 2021+ read creeping into the closing record | high | mitigate | §8 of the record names every fence explicitly; the harness reads through `split_by_holdout_boundary` and the artifacts end 2020-12-31 by construction. |
| T-08-62 | Repudiation | a criterion marked MET on a shape check | high | mitigate | Every criterion row requires a number, a denominator, a window and a verdict; the human-check re-reads for exactly this. |
| T-08-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install occurs; `08-RESEARCH.md` § 11 records the audit as not applicable. |

ASVS level 1, block on high.
</threat_model>

<verification>
- `pytest tests/ -q` — full suite green, **0 skipped, 0 xfailed**, count equal to the number now
  recorded in `CLAUDE.md` and `README.md`.
- `pytest tests/unit/test_platform_legacy_import_ratchet.py -q` — ratchet **31**, unchanged.
- `python -c "from trading_crab_lib.platform.honesty.registry import total_trial_count; print(total_trial_count())"` → 42 (reproduction branch) or 44 (evaluation branch), matching `08-A7.md`'s prediction.
- Both `measurement_*.json` files carry both legs' KPIs, the lift, the four band flags, and both
  DSR blocks, each with its window inline.
- `git diff --stat -- legacy/ gsd-scratch-work/ trading-crab-lib/` is **empty**.
</verification>

<success_criteria>
- ROADMAP criterion 6 met: criterion 7 re-measured, both legs, one harness, window inline, against
  the prior value quoted as a comparison point and not a target.
- ROADMAP criterion 9 met: the recorded test counts match a live collection at all four sites, and
  F-4's denominator (fixed in plan 08-01) is still correct.
- The A11 ruling's code consequence landed before the measurement.
- The registry moved by exactly 0 or exactly 2 rows, as `08-A7.md` predicted, and the ceiling was
  not exceeded.
- The closing record carries a verdict per criterion, none on a shape check.
</success_criteria>

<output>
Create `.planning/phases/08-regime-persistence-stability/08-10-SUMMARY.md` when done. Record:
which branch the decision-bearing run took and why; `wealth_delta` and `dd_delta` for both legs
with their windows; the registry count before and after with remaining headroom; the live
collected test count and the four sites updated; the ratchet; and the STATE-ready open-items block
from `08-MEASUREMENTS.md` §7.
</output>
