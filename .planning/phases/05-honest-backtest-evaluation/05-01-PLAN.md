---
phase: 05-honest-backtest-evaluation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/platform_settings.yaml
  - src/trading_crab_lib/platform/backtest/__init__.py
  - src/trading_crab_lib/platform/evaluation/__init__.py
  - src/trading_crab_lib/platform/backtest/costs.py
  - tests/unit/test_platform_backtest_costs.py
autonomous: true
requirements: [EVAL-01]
must_haves:
  truths:
    - "compute_turnover(prev_weights, new_weights) returns sum(|new - prev|) over the union of both weight indices, treating a missing asset as weight 0.0"
    - "apply_transaction_cost(gross_return, turnover, cost_bps) subtracts exactly turnover * cost_bps / 1e4 (the D-03 cost = turnover x bps identity)"
    - "a frictionless run (cost_bps=0) and a costed run differ by exactly turnover x bps for the same weight sequence (the honesty invariant named in 05-CONTEXT code_context)"
    - "load_platform_config() still validates and returns without error after the new backtest: section is added (section read via cfg.get, never added to _REQUIRED_PLATFORM_SECTIONS)"
  artifacts:
    - "config/platform_settings.yaml gains a backtest: section (cost_bps, min_train_months, refit_frequency_months, crisis_windows, sixty_forty_rebalance, apply_cost_to_baselines)"
    - "src/trading_crab_lib/platform/backtest/__init__.py and src/trading_crab_lib/platform/evaluation/__init__.py (empty package scaffolds)"
    - "src/trading_crab_lib/platform/backtest/costs.py with compute_turnover + apply_transaction_cost + __main__ self-check"
    - "tests/unit/test_platform_backtest_costs.py (TestCostIdentity, TestTurnover)"
  key_links:
    - "backtest/costs.py is imported by backtest/driver.py (Plan 02) for per-step turnover + cost accounting and by baselines.py (Plan 05) for the rebalancing baselines' cost legs"
    - "every configured crisis_windows entry ends on or before DEFAULT_HOLDOUT_CUTOFF (2020-12-31) — the config default list is hard-bounded (Pitfall 4)"
  prohibitions:
    - "no key is added to _REQUIRED_PLATFORM_SECTIONS in platform/config.py — the backtest: section is optional and read defensively via cfg.get('backtest', {})"
    - "no crisis_windows default entry references any date in 2021 or later (no 2020-02 COVID / no 2022 windows — those are holdout)"
---

<objective>
Lay the Phase 5 foundation: the two new platform sub-packages (`backtest/`, `evaluation/`),
the `backtest:` config section every downstream plan reads, and the transaction-cost /
turnover arithmetic (D-03). Costs are the smallest, most-reused primitive — the driver
(Plan 02) and the baseline gauntlet (Plan 05) both depend on `compute_turnover` /
`apply_transaction_cost`. This plan owns the two package `__init__.py` files so no later
same-wave plan collides on them.

Purpose: A monthly regime tilt must not look free (D-03). The `cost = turnover x bps`
identity is a known-answer test and the honesty invariant that keeps higher-turnover
configs from being flattered. The config section is where `cost_bps` (~10 bps), the 1972
warmup (`min_train_months: 120`), the refit-cadence escape hatch (A2), and the
hard-bounded in-sample crisis windows (Pitfall 4) live.
Output: `backtest/costs.py`, both package scaffolds, the `backtest:` config block, and the
cost test file.
</objective>

<execution_context>
@/home/user/claude-scratch-work/.claude/gsd-core/workflows/execute-plan.md
@/home/user/claude-scratch-work/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-honest-backtest-evaluation/05-CONTEXT.md
@.planning/phases/05-honest-backtest-evaluation/05-RESEARCH.md
@src/trading_crab_lib/platform/config.py
@src/trading_crab_lib/platform/honesty/holdout.py
@config/platform_settings.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the backtest: config section + create both package scaffolds</name>
  <files>config/platform_settings.yaml, src/trading_crab_lib/platform/backtest/__init__.py, src/trading_crab_lib/platform/evaluation/__init__.py</files>
  <read_first>
    - config/platform_settings.yaml (the existing allocation:/report:/tripwire: sections — mirror their "read defensively via cfg.get, not in _REQUIRED_PLATFORM_SECTIONS" comment style)
    - src/trading_crab_lib/platform/config.py (confirm _REQUIRED_PLATFORM_SECTIONS is NOT to be edited)
    - src/trading_crab_lib/platform/honesty/holdout.py (DEFAULT_HOLDOUT_CUTOFF = "2020-12-31" — the bound every crisis window must respect)
  </read_first>
  <action>
    Append a `backtest:` block to config/platform_settings.yaml after the `report:` section,
    with a header comment noting it is read via cfg.get("backtest", {}) and deliberately NOT
    added to _REQUIRED_PLATFORM_SECTIONS. Keys: cost_bps: 10 (D-03, document derivation as a
    light-touch per-rebalance haircut on traded notional); min_train_months: 120 (yields the
    1972-01 first decision from the 1962-01 monthly spine); refit_frequency_months: 1 (A2
    escape hatch — monthly L1/L2 refit by default, raise to reduce wall-clock);
    apply_cost_to_baselines: true (A5 — same bps convention on every rebalancing baseline for
    an apples-to-apples Faber comparison); sixty_forty_rebalance: monthly; and crisis_windows
    as a list of name/start/end mappings hard-bounded to in-sample crises: 1973-01..1974-12,
    1980-01..1982-12, 2000-01..2002-12, 2008-01..2009-12. Every end date must be on or before
    2020-12-31 — add an inline comment stating no 2020/2022 window is permitted (holdout).
    Create empty src/trading_crab_lib/platform/backtest/__init__.py and
    src/trading_crab_lib/platform/evaluation/__init__.py (module docstring only, matching the
    one-line style of platform/honesty/__init__.py). Do NOT edit config.py.
  </action>
  <acceptance_criteria>python -c "from trading_crab_lib.platform.config import load_platform_config; c=load_platform_config(); import datetime as d; assert c['backtest']['cost_bps']==10 and c['backtest']['min_train_months']==120; assert all(str(w['end'])<= '2020-12-31' for w in c['backtest']['crisis_windows']); import importlib; importlib.import_module('trading_crab_lib.platform.backtest'); importlib.import_module('trading_crab_lib.platform.evaluation'); print('ok')" exits 0</acceptance_criteria>
  <verify>
    <automated>python -c "from trading_crab_lib.platform.config import load_platform_config; c=load_platform_config(); assert c['backtest']['cost_bps']==10; assert all(str(w['end'])<='2020-12-31' for w in c['backtest']['crisis_windows']); import importlib; importlib.import_module('trading_crab_lib.platform.backtest'); importlib.import_module('trading_crab_lib.platform.evaluation')"</automated>
  </verify>
  <done>The backtest: section loads via load_platform_config() (validation still passes), both new packages import, and every crisis window ends on or before 2020-12-31.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement costs.py (turnover + transaction-cost identity) test-first</name>
  <files>src/trading_crab_lib/platform/backtest/costs.py, tests/unit/test_platform_backtest_costs.py</files>
  <read_first>
    - src/trading_crab_lib/platform/backtest/costs.py (the file being created)
    - tests/unit/test_platform_gap_lag.py (class-per-behavior, docstring-per-test, synthetic-only convention to mirror)
    - src/trading_crab_lib/platform/allocation/tilt.py (the shape of the weight Series that vol_targeted_tilt returns under result["weights"] — the input to compute_turnover)
  </read_first>
  <behavior>
    - TestTurnover: compute_turnover(prev, new) == sum of absolute per-asset weight changes over the union of both indices; an asset present in only one Series is treated as 0.0 in the other; an empty prev (cold start) yields turnover == new.abs().sum()
    - TestCostIdentity: apply_transaction_cost(gross, turnover, cost_bps) == gross - turnover * cost_bps / 1e4 exactly for a hand-built weight sequence (e.g. turnover 0.5 at 10 bps costs 0.0005); cost_bps=0 returns gross unchanged
    - TestCostIdentity: for the same weight sequence, (frictionless_net - costed_net) == turnover * cost_bps / 1e4 to full float precision
  </behavior>
  <action>
    Write tests/unit/test_platform_backtest_costs.py FIRST (RED) with the TestTurnover and
    TestCostIdentity classes above, importing from trading_crab_lib.platform.backtest.costs.
    Then implement costs.py with `from __future__ import annotations`, a module docstring, and
    two pure functions: compute_turnover(prev_weights: pd.Series, new_weights: pd.Series) ->
    float using index-union alignment (reindex both to the sorted union, fill 0.0, sum the
    absolute difference — never positional diffing); and apply_transaction_cost(gross_return:
    float, turnover: float, cost_bps: float) -> float returning gross_return - turnover *
    cost_bps / 1e4. Document the target-vs-target turnover convention (Pitfall 3 — a defensible
    documented simplification, not drift-adjusted). Add an if __name__ == "__main__" synthetic
    self-check that prints a turnover + cost example (no network, no checkpoint).
  </action>
  <acceptance_criteria>pytest tests/unit/test_platform_backtest_costs.py -x -q passes (TestTurnover, TestCostIdentity green)</acceptance_criteria>
  <verify>
    <automated>pytest tests/unit/test_platform_backtest_costs.py -x -q</automated>
  </verify>
  <done>compute_turnover and apply_transaction_cost pass the known-answer turnover + cost-identity tests; the frictionless-minus-costed delta equals turnover x bps to float precision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config file → runtime | numeric config values (cost_bps, crisis_windows) read from a local trusted YAML via yaml.safe_load |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Information Disclosure | crisis_windows config default | high | mitigate | Hard-bound every default crisis window end to ≤ 2020-12-31; must_have prohibition + the load-time acceptance-criteria assertion forbid any 2021+ date (Pitfall 4). |
| T-05-02 | Tampering | cost_bps / crisis_windows used in arithmetic + report strings | low | accept | Values are numeric floats/dates read via cfg.get with defaults; no string interpolation into shell/paths; local trusted config only. |
| T-05-SC | Tampering | package installs | low | accept | No npm/pip/cargo installs in this phase — pure orchestration over already-pinned deps (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `pytest tests/unit/test_platform_backtest_costs.py -x -q` green.
- `load_platform_config()` returns with the new `backtest:` section present and every crisis window ≤ 2020-12-31.
- Both new packages import without error.
- `_REQUIRED_PLATFORM_SECTIONS` in platform/config.py is unchanged (git diff shows no edit to config.py).
</verification>

<success_criteria>
The `backtest:` config section exists and loads, the `backtest/` and `evaluation/` packages
are scaffolded, and the cost/turnover identity (D-03) is proven by known-answer test.
</success_criteria>

## Artifacts this phase produces

New this plan:
- Config: `backtest:` section in `config/platform_settings.yaml` — keys `cost_bps`, `min_train_months`, `refit_frequency_months`, `crisis_windows`, `sixty_forty_rebalance`, `apply_cost_to_baselines`.
- Modules: `src/trading_crab_lib/platform/backtest/__init__.py`, `src/trading_crab_lib/platform/evaluation/__init__.py`, `src/trading_crab_lib/platform/backtest/costs.py`.
- Functions: `compute_turnover`, `apply_transaction_cost`.
- Tests: `tests/unit/test_platform_backtest_costs.py`.

<output>
Create `.planning/phases/05-honest-backtest-evaluation/05-01-SUMMARY.md` when done.
</output>
