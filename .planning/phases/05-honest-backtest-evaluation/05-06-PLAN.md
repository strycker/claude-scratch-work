---
phase: 05-honest-backtest-evaluation
plan: 06
type: execute
wave: 4
depends_on: [05-02, 05-03, 05-04, 05-05]
files_modified:
  - src/trading_crab_lib/platform/evaluation/report.py
  - tests/unit/test_platform_evaluation_report.py
  - tests/integration/test_mini_backtest.py
autonomous: true
requirements: [EVAL-01, EVAL-02, EVAL-03, EVAL-04]
must_haves:
  truths:
    - "assemble_backtest_report renders the sojourn/detection-lag ratio as the FIRST metrics section (the headline go/no-go number, D-01a/§5.4), before the Faber comparison and the no-regime-ablation delta"
    - "the report includes a baseline gauntlet section (SPY, 60/40, Faber log wealth AND max drawdown per §23.1) and a no-regime-ablation delta section (strategy vs ablation side-by-side)"
    - "the report includes the smoothed-vs-filtered gap, the strategy KPI table (terminal log wealth, max drawdown + duration, CVaR(5%), turnover, in-sample crisis capture), AND a documented cash-return convention note stating the strategy cash residual and every baseline's non-invested leg both earn the same cash_ret series (review F4)"
    - "run_full_backtest_evaluation wires driver + baselines + sojourn_lag + kpis + model_metrics into markdown + parquet artifacts and runs end-to-end on a small synthetic monthly frame with no network"
    - "the smoothed reference labeling (one full-sample L1 fit) and the walk-forward filtered probs are built as DISTINCT series feeding the gap/lag diagnostic (Pitfall 1)"
    - "the headline filtered-probs input is a MULTICLASS matrix built from the driver's per_step_metrics via build_filtered_probs_matrix and passed to compute_sojourn_lag_headline — each transition is scored against P(its own target state), never a class-agnostic max (review F1)"
    - "y_true for the model-metrics artifacts is joined from the full-sample smoothed reference states aligned to per_step_metrics['dates'] (the smoothed label AT each decision month) — NOT sourced from the walk-forward loop (review F2)"
    - "run_full_backtest_evaluation passes cash_ret (from build_core_research_series -> compute_monthly_returns) into run_backtest as cash_returns so the strategy cash residual earns the same series as the baselines (review F4)"
  artifacts:
    - "src/trading_crab_lib/platform/evaluation/report.py with assemble_backtest_report + write_backtest_report + run_full_backtest_evaluation + main() CLI + __main__"
    - "tests/unit/test_platform_evaluation_report.py (TestHeadlineOrdering, TestBaselineGauntletSection, TestArtifactsWritten)"
    - "tests/integration/test_mini_backtest.py (synthetic end-to-end: driver + 4 baselines + report, no network, no real checkpoints)"
  key_links:
    - "run_full_backtest_evaluation calls run_backtest (strategy) + no_regime_ablation + spy_buy_hold/sixty_forty/faber_sma + compute_sojourn_lag_headline + kpis + report_model_metrics — every EVAL requirement flows through here"
    - "equity-curve / KPI-table parquet artifacts persist under OUTPUT_DIR/reports/platform/ (or a backtest/ subdir); the report markdown is written distinct from the incumbent report path"
  prohibitions:
    - "no 2021+ data path: the report evaluates only the holdout-bounded strategy/baseline legs; no 2021+ read anywhere"
    - "no deflated Sharpe (DSR) is computed or displayed (D-04) — the trial registry is written but never read for a DSR denominator this phase"
    - "the smoothed and filtered series must not be the same object reused for both gap/lag roles (Pitfall 1)"
    - "the headline filtered-probs input must not be a class-agnostic max series and y_true must not be loop-sourced — build_filtered_probs_matrix + per-target-state selection (review F1) and the date-joined smoothed y_true (review F2) are used"
---

<objective>
Assemble the honest backtest report and wire the whole evaluation together. `report.py`'s
`run_full_backtest_evaluation` drives the strategy backtest, the no-regime ablation, the three
price baselines, the sojourn/lag headline, the strategy KPIs, and the model-metrics artifacts,
then renders a markdown report whose FIRST metrics section is the sojourn/detection-lag ratio
(D-01a) — followed by the Faber comparison (§23.1), the no-regime-ablation delta ("does the
regime layer pay rent"), the smoothed-vs-filtered gap, and the full KPI table. A synthetic
end-to-end integration test proves the entire chain runs with no network and no real
checkpoints.

Purpose: This is where EVAL-01…04 come together into the artifact Glenn (and the later
L4-upgrade milestone) reads. The report is a diagnostic (D-01): it must RUN and REPORT every
number honestly, not pass a benchmark. The headline ordering and the ablation delta are the
scientifically load-bearing outputs.
Output: `evaluation/report.py`, its unit test, and the synthetic integration test.
</objective>

<execution_context>
@/home/user/claude-scratch-work/.claude/gsd-core/workflows/execute-plan.md
@/home/user/claude-scratch-work/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-honest-backtest-evaluation/05-CONTEXT.md
@.planning/phases/05-honest-backtest-evaluation/05-RESEARCH.md
@src/trading_crab_lib/platform/report/weekly.py
@src/trading_crab_lib/platform/backtest/driver.py
@src/trading_crab_lib/platform/backtest/baselines.py
@src/trading_crab_lib/platform/evaluation/kpis.py
@src/trading_crab_lib/platform/evaluation/sojourn_lag.py
@src/trading_crab_lib/platform/evaluation/model_metrics.py
@src/trading_crab_lib/platform/honesty/gap_lag.py
@src/trading_crab_lib/platform/splice.py
@tests/integration/test_mini_pipeline.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the report unit test (RED) — headline ordering + gauntlet + artifacts</name>
  <files>tests/unit/test_platform_evaluation_report.py</files>
  <read_first>
    - src/trading_crab_lib/platform/report/weekly.py (assemble_weekly_report is the pure-markdown-from-precomputed-inputs pattern to mirror; _build_report_inputs isolation so tests avoid real checkpoints)
    - src/trading_crab_lib/platform/evaluation/sojourn_lag.py (compute_sojourn_lag_headline return keys median_sojourn/median_lag/ratio)
    - src/trading_crab_lib/platform/evaluation/kpis.py (KPI function outputs the table renders)
    - tests/unit/test_platform_report_weekly.py (how the weekly report markdown assertions are written against section headers)
  </read_first>
  <behavior>
    - TestHeadlineOrdering: assemble_backtest_report(precomputed_inputs) returns markdown whose FIRST metrics section header is the sojourn/detection-lag ratio, appearing before any Faber or ablation section (assert by header index order)
    - TestBaselineGauntletSection: the markdown contains a baseline-gauntlet section naming SPY, 60/40, and Faber with both log wealth and max drawdown, plus a no-regime-ablation delta line
    - TestArtifactsWritten: write_backtest_report writes the markdown to a tmp path and the KPI/equity-curve parquet artifacts round-trip via read_parquet
  </behavior>
  <action>
    Write tests/unit/test_platform_evaluation_report.py FIRST (RED). Build precomputed synthetic
    inputs (a sojourn/lag headline dict, per-leg KPI dicts, a small equity-curve frame per leg,
    a gap value) and assert on the assembled markdown's section ordering and content. Import
    from trading_crab_lib.platform.evaluation.report. Classes fail because report.py is absent
    (RED).
  </action>
  <acceptance_criteria>pytest tests/unit/test_platform_evaluation_report.py -q --co collects the three classes and they fail (RED) only because report.py is not yet implemented</acceptance_criteria>
  <verify>
    <automated>pytest tests/unit/test_platform_evaluation_report.py -q --co</automated>
  </verify>
  <done>The three report test classes exist and are collected; they fail because report.py is absent (RED established).</done>
</task>

<task type="auto">
  <name>Task 2: Implement report.py — assemble markdown (headline-first) + write artifacts</name>
  <files>src/trading_crab_lib/platform/evaluation/report.py</files>
  <read_first>
    - src/trading_crab_lib/platform/evaluation/report.py (file being created)
    - src/trading_crab_lib/platform/report/weekly.py (assemble/write pattern; OUTPUT_DIR/reports/platform path convention; noqa T201 print for first-class CLI output)
    - src/trading_crab_lib/platform/evaluation/kpis.py + sojourn_lag.py + model_metrics.py (the compute functions the report renders)
    - src/trading_crab_lib/platform/honesty/gap_lag.py (compute_gap(smoothed_perf, filtered_perf) for the smoothed-vs-filtered gap line)
  </read_first>
  <action>
    Implement report.py with `from __future__ import annotations`. assemble_backtest_report(*,
    sojourn_lag: dict, strategy_kpis: dict, ablation_kpis: dict, baseline_kpis: dict, gap:
    float, ...) -> str: a pure markdown builder whose sections appear in this order — (1)
    Headline: sojourn/detection-lag ratio with the §5.4 interpretation text; (2) Faber
    comparison (log wealth AND max drawdown, marked as the standing target per D-01a, not a
    pass/fail gate); (3) No-regime-ablation delta (strategy vs ablation side-by-side — "does the
    regime layer pay rent"); (4) Smoothed-vs-filtered gap; (5) Baseline gauntlet table (SPY,
    60/40, Faber); (6) Strategy KPI table (terminal log wealth, max drawdown + duration,
    CVaR(5%), turnover, in-sample crisis capture) — including a short documented "Conventions"
    note stating the cash-return convention: the strategy's cash residual and every baseline's
    non-invested leg both earn the same cash_ret series, so the §23.1 Faber comparison is
    cost-symmetric (review F4), alongside the turnover/cost convention already documented.
    write_backtest_report(markdown, artifacts, *,
    output_dir=None) -> Path writes the markdown under (output_dir or OUTPUT_DIR / "reports" /
    "platform") and persists the per-leg equity-curve + KPI-table parquet artifacts (schema-stable
    empty-safe idiom). Do NOT compute or display the design-Phase-6 registry-denominator
    statistic (D-04) — that risk-adjusted family is out of scope this phase; neither its name
    nor its acronym appears in report.py. Implement to make the report unit test GREEN.
  </action>
  <acceptance_criteria>pytest tests/unit/test_platform_evaluation_report.py -x -q passes AND grep -niE "deflated|dsr" src/trading_crab_lib/platform/evaluation/report.py returns nothing</acceptance_criteria>
  <verify>
    <automated>pytest tests/unit/test_platform_evaluation_report.py -x -q && ! grep -niE "deflated|dsr" src/trading_crab_lib/platform/evaluation/report.py</automated>
  </verify>
  <done>assemble_backtest_report renders the sojourn/lag ratio first, includes the gauntlet + ablation delta + gap + KPI table, and writes round-trippable artifacts; no DSR anywhere.</done>
</task>

<task type="auto">
  <name>Task 3: Wire run_full_backtest_evaluation + main() CLI + synthetic end-to-end integration test</name>
  <files>src/trading_crab_lib/platform/evaluation/report.py, tests/integration/test_mini_backtest.py</files>
  <read_first>
    - src/trading_crab_lib/platform/evaluation/report.py (file being extended)
    - tests/integration/test_mini_pipeline.py (the synthetic-frame, no-network, no-checkpoint end-to-end pattern to mirror; _make_synthetic_macro style)
    - src/trading_crab_lib/platform/backtest/driver.py (run_backtest returns (equity_curve, per_step_metrics with keys dates/proba/classes) and accepts cash_returns= for the strategy cash leg, review F4)
    - src/trading_crab_lib/platform/backtest/baselines.py (no_regime_ablation + spy_buy_hold/sixty_forty/faber_sma)
    - src/trading_crab_lib/platform/evaluation/sojourn_lag.py (build_filtered_probs_matrix(per_step_metrics) -> multiclass matrix + compute_sojourn_lag_headline(smoothed_states, filtered_probs_matrix), review F1)
    - src/trading_crab_lib/platform/labeling/jump_model.py (fit_jump_model + canonicalize_states — used ONCE full-sample for the smoothed reference labeling, distinct from the walk-forward filtered probs, Pitfall 1; the same smoothed states are reindexed onto per_step_metrics['dates'] to join y_true, review F2)
    - src/trading_crab_lib/platform/splice.py (build_core_research_series -> the shared price source for baselines AND the cash_ret series fed to run_backtest, review F4)
  </read_first>
  <action>
    Implement run_full_backtest_evaluation(monthly_features, monthly_raw, cfg, *,
    registry_path=None, output_dir=None) in report.py: (a) build_core_research_series(monthly_raw,
    cfg) -> compute_monthly_returns to get equity/bond/cash return series, then run_backtest for
    the regime-tilt strategy passing cash_returns=cash_ret so the strategy cash residual earns the
    same cash series as the baselines (review F4) (collect equity_curve + per_step_metrics); (b)
    no_regime_ablation for the ablation leg; (c) the three price baselines (spy_buy_hold,
    sixty_forty, faber_sma) over the same holdout-bounded window from the same research series; (d)
    build the SMOOTHED reference states via ONE full-sample fit_jump_model + canonicalize_states
    over the dev window (a DISTINCT series from the walk-forward filtered probs collected in
    per_step_metrics — Pitfall 1); build the multiclass filtered-probs matrix via
    build_filtered_probs_matrix(per_step_metrics) and call compute_sojourn_lag_headline(
    smoothed_states, filtered_probs_matrix) so each transition is scored against P(its own target
    state), never a class-agnostic max (review F1); (e) join y_true for the metrics by reindexing
    the smoothed reference states onto per_step_metrics["dates"] (the smoothed label AT each
    decision month, review F2) and set per_step_metrics["y_true"] = that aligned list, asserting
    len == len(dates) == len(proba) before proceeding; (f) compute per-leg KPIs and
    compute_gap(smoothed_perf, filtered_perf); (g) report_model_metrics on per_step_metrics; (h)
    assemble + write the report + artifacts (include the documented cash-return convention note,
    review F4). Add main(argv) CLI that loads platform config + checkpoints and calls
    run_full_backtest_evaluation, plus an if __name__ == "__main__" hook. Then write
    tests/integration/test_mini_backtest.py: a synthetic monthly frame (>= min_train + a few steps)
    driving run_full_backtest_evaluation end-to-end with tmp output_dir and tmp registry —
    asserting the markdown + all parquet artifacts are produced, the registry gained the expected
    number of trials (strategy + ablation only; the 3 price baselines log no trial), AND
    (TestYTrueDateAlignment) the metrics y_true is aligned by date to per_step_metrics["dates"]
    (assert each scored label equals the smoothed reference state at that decision month, review
    F2). No network, no real checkpoints.
  </action>
  <acceptance_criteria>pytest tests/integration/test_mini_backtest.py -x -q passes (full synthetic end-to-end: driver + ablation + 3 baselines + multiclass sojourn/lag + date-joined y_true + kpis + model-metrics + report, no network); the integration test asserts the metrics y_true aligns by date to the smoothed reference (review F2)</acceptance_criteria>
  <verify>
    <automated>pytest tests/integration/test_mini_backtest.py -x -q</automated>
  </verify>
  <done>run_full_backtest_evaluation runs the whole EVAL-01..04 chain on a synthetic frame producing the markdown + parquet artifacts; the smoothed reference and filtered series are distinct; only strategy + ablation log registry trials.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checkpoints/config → full evaluation | the report orchestrator loads holdout-bounded dev data only; artifacts are dev-visible |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-13 | Information Disclosure | 2021+ data leaking into a dev-visible report artifact | critical | mitigate | All legs run over the holdout-bounded window (driver + baselines slice ≤ 2020-12-31); no 2021+ read; crisis windows bounded (Plan 03). |
| T-05-14 | Tampering | conflating smoothed and filtered series for the gap/lag diagnostic (silent hindsight leak) | medium | mitigate | Smoothed reference is a separate full-sample L1 fit; filtered is the walk-forward per-step series; built as distinct objects (Pitfall 1). |
| T-05-15 | Tampering | premature DSR creeping into the report/selection | high | mitigate | grep gate: no "deflated"/"dsr" token in report.py; registry is written but never read for a DSR denominator (D-04). |
| T-05-SC | Tampering | package installs | low | accept | No package-manager installs — pure orchestration over pinned deps. |
</threat_model>

<verification>
- `pytest tests/unit/test_platform_evaluation_report.py tests/integration/test_mini_backtest.py -x -q` green.
- `grep -niE "deflated|dsr" src/trading_crab_lib/platform/evaluation/report.py` returns nothing.
- Full suite `pytest tests/ -q` green (no regression across the 863+ existing tests).
</verification>

<success_criteria>
The honest backtest report renders the sojourn/lag ratio first, surfaces the gauntlet +
ablation delta + gap + KPI table, and the whole EVAL-01…04 chain runs end-to-end on a
synthetic frame with no network and no DSR.
</success_criteria>

## Artifacts this phase produces

New this plan:
- Module: `src/trading_crab_lib/platform/evaluation/report.py`.
- Functions: `assemble_backtest_report` (now emits a documented cash-return convention note, review F4), `write_backtest_report`, `run_full_backtest_evaluation` (passes cash_returns to run_backtest [F4], builds the multiclass filtered-probs matrix for the headline [F1], joins y_true from the smoothed reference by date [F2]), `main`.
- CLI entry point: `python -m trading_crab_lib.platform.evaluation.report`.
- Artifacts written: backtest report markdown + per-leg equity-curve/KPI-table parquet under `OUTPUT_DIR/reports/platform/`.
- Tests: `tests/unit/test_platform_evaluation_report.py`, `tests/integration/test_mini_backtest.py` (adds `TestYTrueDateAlignment`, review F2).

<output>
Create `.planning/phases/05-honest-backtest-evaluation/05-06-SUMMARY.md` when done.
</output>
