---
phase: 05-honest-backtest-evaluation
plan: 05
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - src/trading_crab_lib/platform/backtest/baselines.py
  - tests/unit/test_platform_backtest_baselines.py
autonomous: true
requirements: [EVAL-02]
must_haves:
  truths:
    - "spy_buy_hold returns the equity total-return series' monthly returns (single purchase, cost-free by construction)"
    - "sixty_forty blends equity/bond monthly returns at a fixed 60/40 with a documented monthly-reconstitution turnover (rebalance convention from cfg['backtest']['sixty_forty_rebalance'])"
    - "faber_sma decides the position with the 10-month SMA signal known at close of month t and acts in month t+1 — never uses the same-month price to set the same-month position (1-step lag, no look-ahead)"
    - "no_regime_ablation calls run_backtest(use_regime_tilt=False) and reproduces the vol-target baseline exactly — same weights/equity curve as the tilt-off driver path (the ablation-reproduces-vol-target invariant)"
    - "no_regime_ablation inherits the driver's skip_l1l2_for_ablation shortcut (review F5): the tilt-off path skips the discarded L1 jump-model + L2 nowcaster refits, so the gauntlet does not double the ~588-refit cost — and because those outputs are unused when the tilt is off, the ablation equity curve is unchanged (the invariant still holds)"
    - "every rebalancing baseline applies the same cfg cost_bps convention as the strategy leg when apply_cost_to_baselines is true (apples-to-apples, A5); the strategy's cash residual also earns the same cash_ret series (review F4, cash-return symmetry documented)"
  artifacts:
    - "src/trading_crab_lib/platform/backtest/baselines.py with spy_buy_hold, sixty_forty, faber_sma, no_regime_ablation + __main__ self-check"
    - "tests/unit/test_platform_backtest_baselines.py (TestNoRegimeAblationInvariant, TestFaberNoLookahead, TestSpyBuyHold, TestSixtyForty)"
  key_links:
    - "all baselines consume the same spliced core research series (splice.build_core_research_series -> equities_tr / long_duration_tr / cash) so comparisons are apples-to-apples over 1972-2020"
    - "no_regime_ablation is the SAME run_backtest code path with the tilt flag off — NOT a hand-rolled parallel equal-weight implementation (RESEARCH Anti-Pattern; D-02)"
  prohibitions:
    - "no forked/parallel allocation implementation for the ablation — it must route through run_backtest(use_regime_tilt=False)"
    - "no baseline reads 2021+ data — baselines are computed over the same holdout-bounded window as the strategy leg"
    - "the cost convention must not be applied asymmetrically (strategy costed, Faber/60-40 frictionless) — document and apply the same bps to every rebalancing baseline (A5)"
---

<objective>
Build EVAL-02: the baseline gauntlet — SPY buy-and-hold, 60/40, Faber 10-month SMA, and the
scientifically load-bearing no-regime ablation. All four share one price source (the spliced
core research series) and one cost convention so the §23.1 Faber comparison and the "does the
regime layer pay rent" delta are apples-to-apples. The ablation is the SAME `run_backtest`
code path with the regime tilt disabled — never a forked equal-weight implementation — which
is exactly what makes the "ablation reproduces the vol-target baseline" invariant hold.

Purpose: The no-regime ablation is the one baseline that attributes any edge to the regime
layer vs. plain vol-targeting (design §8.7 "the regime layer must pay rent"). Faber is the
standing target (D-01a). Faber's 1-step decision lag is a leakage invariant consistent with
the platform's causal-gating discipline.
Output: `backtest/baselines.py` + its test file.
</objective>

<execution_context>
@/home/user/claude-scratch-work/.claude/gsd-core/workflows/execute-plan.md
@/home/user/claude-scratch-work/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-honest-backtest-evaluation/05-CONTEXT.md
@.planning/phases/05-honest-backtest-evaluation/05-RESEARCH.md
@src/trading_crab_lib/platform/splice.py
@src/trading_crab_lib/platform/assets/returns.py
@src/trading_crab_lib/platform/backtest/driver.py
@src/trading_crab_lib/platform/backtest/costs.py
@src/trading_crab_lib/platform/honesty/holdout.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the baselines test file (RED) — Faber no-lookahead + ablation invariant + price baselines</name>
  <files>tests/unit/test_platform_backtest_baselines.py</files>
  <read_first>
    - src/trading_crab_lib/platform/splice.py (build_core_research_series returns equities_tr / long_duration_tr / gold / oil / cash columns; the equity/bond/cash series shapes)
    - src/trading_crab_lib/platform/backtest/driver.py (run_backtest signature + the use_regime_tilt=False path the ablation must reproduce)
    - src/trading_crab_lib/platform/assets/returns.py (compute_monthly_returns — how levels become monthly returns)
    - tests/unit/test_platform_tilt.py (how a synthetic returns_by_regime + asset_returns frame is constructed for allocation tests, to reuse for the ablation invariant)
  </read_first>
  <behavior>
    - TestFaberNoLookahead: faber_sma on a synthetic equity level series sets the month-t+1 position from the SMA signal computed through month t only; shifting the input by one month shifts the position vector by one month (no same-month peeking)
    - TestNoRegimeAblationInvariant: no_regime_ablation(features, asset_returns, cfg) equals run_backtest(features, asset_returns, cfg, use_regime_tilt=False) — identical equity curve on the same synthetic frame (byte/round-identical returns)
    - TestSpyBuyHold: spy_buy_hold returns equities_tr monthly returns unchanged (cost-free)
    - TestSixtyForty: sixty_forty blends 0.6 equity + 0.4 bond monthly returns with the documented monthly-reconstitution turnover
  </behavior>
  <action>
    Write tests/unit/test_platform_backtest_baselines.py FIRST (RED), synthetic-only, no
    network. Build a small synthetic research-series frame (equities_tr, long_duration_tr, cash
    as monthly levels) and a synthetic monthly_features + asset_returns frame reusing the
    Plan 02 driver-test fixture style for the ablation invariant. Import from
    trading_crab_lib.platform.backtest.baselines. All classes fail because baselines.py does
    not yet exist (RED).
  </action>
  <acceptance_criteria>pytest tests/unit/test_platform_backtest_baselines.py -q --co collects the four classes and they fail (RED) only because baselines.py is not yet implemented</acceptance_criteria>
  <verify>
    <automated>pytest tests/unit/test_platform_backtest_baselines.py -q --co</automated>
  </verify>
  <done>The four baseline test classes exist and are collected; they fail because baselines.py is absent (RED established).</done>
</task>

<task type="auto">
  <name>Task 2: Implement the three price-only baselines (SPY, 60/40, Faber 1-step lag)</name>
  <files>src/trading_crab_lib/platform/backtest/baselines.py</files>
  <read_first>
    - src/trading_crab_lib/platform/backtest/baselines.py (file being created)
    - src/trading_crab_lib/platform/splice.py (build_core_research_series output columns)
    - src/trading_crab_lib/platform/assets/returns.py (compute_monthly_returns)
    - src/trading_crab_lib/platform/backtest/costs.py (apply_transaction_cost / compute_turnover — the same cost convention baselines that rebalance must apply, A5)
    - .planning/phases/05-honest-backtest-evaluation/05-RESEARCH.md (Pattern 2; A4 Faber 1-step lag; A5 baseline cost symmetry)
  </read_first>
  <action>
    Implement baselines.py with `from __future__ import annotations`. spy_buy_hold(equity_ret)
    -> pd.Series returns the equity monthly return series unchanged (single purchase, cost-free
    — document). sixty_forty(equity_ret, bond_ret, *, rebalance="monthly", cost_bps=0.0) ->
    pd.Series: hold 60/40, reconstitute per the rebalance convention, compute the
    monthly-reconstitution turnover and subtract apply_transaction_cost when cost_bps>0
    (document the convention). faber_sma(equity_level, cash_ret, *, window=10, cost_bps=0.0) ->
    pd.Series: sma = equity_level.rolling(window).mean(); signal = (equity_level >
    sma).shift(1) so the decision uses data through t and acts at t+1 (the 1-step lag — no
    look-ahead, A4); return the switch series' realized monthly returns (equities_tr when
    in-market, cash_ret when out), applying the same cost convention on each switch. Read
    cost_bps and rebalance from cfg["backtest"] at the call site (report layer), not hard-coded.
    Document that the cash_ret series these baselines earn on their non-invested legs is the SAME
    series the strategy leg now earns on its cash residual (review F4) — the cash-return
    convention is symmetric across the strategy and the baselines, so the §23.1 Faber comparison
    is not quietly biased over the double-digit-yield 1970s-80s. Add a synthetic __main__ self-check.
  </action>
  <acceptance_criteria>pytest "tests/unit/test_platform_backtest_baselines.py::TestFaberNoLookahead" "tests/unit/test_platform_backtest_baselines.py::TestSpyBuyHold" "tests/unit/test_platform_backtest_baselines.py::TestSixtyForty" -x -q passes</acceptance_criteria>
  <verify>
    <automated>pytest "tests/unit/test_platform_backtest_baselines.py::TestFaberNoLookahead" "tests/unit/test_platform_backtest_baselines.py::TestSpyBuyHold" "tests/unit/test_platform_backtest_baselines.py::TestSixtyForty" -x -q</automated>
  </verify>
  <done>SPY, 60/40, and Faber baselines are green; Faber uses a strict 1-step decision lag with no same-month look-ahead.</done>
</task>

<task type="auto">
  <name>Task 3: Implement no_regime_ablation via the tilt-off driver path (invariant)</name>
  <files>src/trading_crab_lib/platform/backtest/baselines.py</files>
  <read_first>
    - src/trading_crab_lib/platform/backtest/baselines.py (file being extended)
    - src/trading_crab_lib/platform/backtest/driver.py (run_backtest use_regime_tilt=False — the exact path the ablation must call, not fork)
    - .planning/phases/05-honest-backtest-evaluation/05-RESEARCH.md (A3 ablation construction: constant single-state label fed to the UNCHANGED tilt; Anti-Pattern: no hand-rolled parallel implementation)
  </read_first>
  <action>
    Implement no_regime_ablation(monthly_features, asset_returns, cfg, *, registry_path=None) in
    baselines.py as a one-line delegation: return run_backtest(monthly_features, asset_returns,
    cfg, use_regime_tilt=False, registry_path=registry_path). Do NOT reimplement any allocation
    math — the ablation is the same driver with the tilt flag off, which routes a constant
    single-state label series through the UNCHANGED vol_targeted_tilt (A3). Document in the
    docstring that, because cfg["backtest"].skip_l1l2_for_ablation defaults True, the tilt-off
    path skips the expensive L1 jump-model + L2 nowcaster refits (their output is discarded when
    the tilt is off), so building the ablation does NOT double the ~588-refit gauntlet cost —
    while the ablation equity curve remains byte-identical to the vol-target baseline (review F5).
    Document the trial-registry scope as an explicit judgment call resolving RESEARCH Open
    Question 2 (never locked in CONTEXT, review F7): the ablation logs its OWN registry trial
    because it is a genuine evaluated configuration with a fitted (degenerate) model path, whereas
    the three price-only baselines (SPY, 60/40, Faber) are deterministic price arithmetic with no
    tunable parameters and therefore log NO trial — they are report-only comparisons, not part of
    the multiple-testing surface the registry bounds.
  </action>
  <acceptance_criteria>pytest "tests/unit/test_platform_backtest_baselines.py::TestNoRegimeAblationInvariant" -x -q passes AND a grep for call/import forms `vol_targeted_tilt\(`, `regime_tilt_weights\(`, and `import.*allocation` in baselines.py returns nothing (no forked allocation)</acceptance_criteria>
  <verify>
    <automated>pytest tests/unit/test_platform_backtest_baselines.py -x -q && ! grep -nE "vol_targeted_tilt\(|regime_tilt_weights\(|import .*allocation" src/trading_crab_lib/platform/backtest/baselines.py</automated>
  </verify>
  <done>no_regime_ablation reproduces the tilt-off driver equity curve exactly and contains no forked allocation math; the full baselines test file is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| research price series → baseline legs | same spliced source + same holdout-bounded window as the strategy |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-10 | Tampering | Faber same-month look-ahead leakage | high | mitigate | faber_sma shifts the signal by one month (decide at t, act t+1); TestFaberNoLookahead proves the lag (A4). |
| T-05-11 | Tampering | asymmetric cost convention flattering the strategy vs Faber/60-40 | medium | mitigate | Same cfg cost_bps applied to every rebalancing baseline when apply_cost_to_baselines is true; documented convention (A5). |
| T-05-12 | Tampering | forked ablation drifting from the audited allocation path | medium | mitigate | no_regime_ablation delegates to run_backtest(use_regime_tilt=False); grep gate asserts no allocation function is redefined in baselines.py (D-02 invariant). |
| T-05-SC | Tampering | package installs | low | accept | No package-manager installs — pure orchestration over pinned deps. |
</threat_model>

<verification>
- `pytest tests/unit/test_platform_backtest_baselines.py -x -q` fully green.
- `grep -nE "vol_targeted_tilt|regime_tilt_weights" src/trading_crab_lib/platform/backtest/baselines.py` returns nothing.
- Faber uses a strict 1-step decision lag; the ablation reproduces the tilt-off driver curve.
</verification>

<success_criteria>
The four-baseline gauntlet is built from one shared price source with a symmetric cost
convention; the no-regime ablation is the tilt-off driver path (invariant proven) and Faber is
look-ahead-free.
</success_criteria>

## Artifacts this phase produces

New this plan:
- Module: `src/trading_crab_lib/platform/backtest/baselines.py`.
- Functions: `spy_buy_hold`, `sixty_forty`, `faber_sma`, `no_regime_ablation` (inherits the driver's `skip_l1l2_for_ablation` shortcut, review F5).
- Tests: `tests/unit/test_platform_backtest_baselines.py`.

## Review notes (Phase 5 cross-AI review)

- **F6 (LOW, ROADMAP serialization):** This plan's frontmatter correctly lists `depends_on: [05-02]` only — 05-05 does NOT need 05-03/05-04 to finish first. The ROADMAP annotates it as "Wave 3 (blocked on Wave 2 completion)", which is a conservative wave-ordering label (wave = max(dep waves)+1 = 3), not an implied dependency on all of Wave 2. Kept as-is per the review's low-priority guidance; no frontmatter change needed.
- **F7 (LOW/informational, trial-registry scope):** Documented as an explicit judgment call in Task 3 (above): only the regime-tilt strategy and the no-regime ablation log registry trials; the three price-only baselines (SPY, 60/40, Faber) log none. This resolves RESEARCH.md Open Question 2, which was never locked in CONTEXT.md — surfaced here as a visible decision rather than an unstated default.

<output>
Create `.planning/phases/05-honest-backtest-evaluation/05-05-SUMMARY.md` when done.
</output>
