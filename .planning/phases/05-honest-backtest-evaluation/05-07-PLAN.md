---
phase: 05-honest-backtest-evaluation
plan: 07
type: execute
wave: 5
depends_on: [05-06]
files_modified: []
autonomous: false
requirements: [EVAL-01, EVAL-02, EVAL-03, EVAL-04]
must_haves:
  truths:
    - "run_full_backtest_evaluation executes end-to-end on the REAL Phase 1 monthly checkpoints (1962+ spliced data) and produces the backtest report markdown + all parquet artifacts"
    - "the walk-forward index starts at the first decision in 1972 and stops at ≤ 2020-12 — no 2021+ row is ever touched"
    - "the report surfaces the sojourn/detection-lag ratio (headline), the four-baseline gauntlet (SPY, 60/40, Faber, no-regime ablation), the smoothed-vs-filtered gap, and the model-metrics artifacts"
  artifacts:
    - "the written backtest report markdown + equity-curve / KPI-table / model-metrics parquet artifacts under OUTPUT_DIR/reports/platform/ from a REAL run"
    - "one trial-registry row each for the regime-tilt strategy run and the no-regime ablation"
  key_links:
    - "the real run is the design §14 Phase 1 exit criterion — the synthetic unit/integration tests (Plans 01-06) are necessary but not sufficient; only a real run against live checkpoints proves the phase done"
  prohibitions:
    - "no 2021+ holdout read; the index must visibly stop at ≤ 2020-12"
    - "no DSR / no holdout evaluation is run (D-04) — this is a diagnostic run only (D-01)"
---

<objective>
Execute the phase's actual exit criterion: run the full backtest evaluation against REAL
Phase 1 checkpoint data (the spliced 1962+ monthly spine) and confirm — with a human in the
loop — that the honest 1972–2020 walk-forward runs end-to-end, stops at the holdout boundary,
and produces every required number and artifact. This is a diagnostic run (D-01): it passes on
"it ran and reported correctly," NOT on beating any benchmark.

Purpose: The synthetic unit + integration tests (Plans 01–06) prove correctness of the
machinery, but design §14 Phase 1 exit is defined as a REAL honest run ("honest backtest
1972–2020 runs end to end; gap + detection lag reported; beats nothing yet — that's fine").
This checkpoint verifies exactly that against live data + the already-verified `FRED_API_KEY`.
Output: a human-confirmed real backtest report + artifacts.
</objective>

<execution_context>
@/home/user/claude-scratch-work/.claude/gsd-core/workflows/execute-plan.md
@/home/user/claude-scratch-work/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-honest-backtest-evaluation/05-CONTEXT.md
@.planning/phases/05-honest-backtest-evaluation/05-VALIDATION.md
@src/trading_crab_lib/platform/evaluation/report.py
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Human-verify the real 1972-2020 backtest run + artifacts</name>
  <action>
    Run the full backtest evaluation against real Phase 1 checkpoints via
    `python -m trading_crab_lib.platform.evaluation.report`, then hand off to the operator to
    confirm the report, artifacts, holdout boundary, and registry rows per the checklist below.
    This is a diagnostic run (D-01) — it passes on "ran and reported correctly," not on beating
    any benchmark. Do not compute DSR or read any 2021+ holdout data.
  </action>
  <what-built>
    The full Phase 5 backtest evaluation machinery (Plans 01–06): the walk-forward driver,
    the four-baseline gauntlet + no-regime ablation, the sojourn/detection-lag headline, the
    strategy KPIs, and the per-run model-metrics artifacts — all wired through
    run_full_backtest_evaluation with a CLI entry point.
  </what-built>
  <how-to-verify>
    1. Confirm the full synthetic suite is green first: `pytest tests/ -q` (no regressions across the 863+ existing tests, plus the new backtest/evaluation tests).
    2. Ensure real Phase 1 checkpoints exist (the monthly_features / monthly_raw dev-namespace checkpoints) and FRED_API_KEY is present (already verified 2026-07-23).
    3. Run the real backtest end-to-end: `python -m trading_crab_lib.platform.evaluation.report` (the run_full_backtest_evaluation CLI). Expect it to complete without crashing across ~1972-01 → 2020-12.
    4. Open the written report markdown under `outputs/reports/platform/` and confirm:
       - the sojourn/detection-lag ratio is the FIRST metrics section (with the §5.4 interpretation);
       - the four-baseline gauntlet (SPY, 60/40, Faber with log wealth AND max drawdown) and the no-regime-ablation delta are present;
       - the smoothed-vs-filtered gap and the KPI table (terminal log wealth, max drawdown + duration, CVaR(5%), turnover, in-sample crisis capture) are present.
    5. Confirm the equity-curve / KPI-table / model-metrics parquet artifacts were written and round-trip via read_parquet.
    6. Confirm the walk-forward index visibly stops at ≤ 2020-12 (no 2021+ dates in any equity curve) and that `registry/trials.jsonl` gained exactly two new rows (strategy + ablation).
    7. Confirm no deflated Sharpe and no 2021+ holdout evaluation appears anywhere (D-04).
  </how-to-verify>
  <resume-signal>Type "approved" once the real 1972–2020 run has completed and the report + artifacts + boundary + registry checks all hold; otherwise describe what failed.</resume-signal>
  <verify>
    <human-check>Operator confirms: real run completed 1972→2020; report headline is the sojourn/lag ratio; gauntlet + ablation delta + gap + KPI table present; index stops at ≤ 2020-12; two new registry rows; no DSR / no 2021+ read.</human-check>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live checkpoints → real backtest run | the real monthly checkpoints may contain 2021+ rows physically; the run must slice to ≤ 2020-12 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-16 | Information Disclosure | real run reading 2021+ holdout rows into a dev-visible report | critical | mitigate | The driver holdout-slices before expanding_steps (Plan 02); the human check explicitly confirms the index stops at ≤ 2020-12 and no 2021+ date appears in any equity curve. |
| T-05-17 | Repudiation | untracked/ad-hoc real run with no registry record | low | mitigate | run_full_backtest_evaluation logs exactly two registry trials (strategy + ablation); the human check confirms the two new rows. |
| T-05-SC | Tampering | package installs | low | accept | No package-manager installs — the real run uses already-pinned deps. |
</threat_model>

<verification>
- Full synthetic suite green (`pytest tests/ -q`).
- Real `python -m trading_crab_lib.platform.evaluation.report` completes 1972–2020 without crashing.
- Human confirms report headline ordering, gauntlet, ablation delta, gap, KPI table, artifacts, holdout boundary, and the two registry rows.
</verification>

<success_criteria>
The honest 1972–2020 walk-forward backtest runs end-to-end on real data, reports every
required number, stops at the holdout boundary, and is human-confirmed — satisfying design §14
Phase 1 exit as a diagnostic (D-01).
</success_criteria>

## Artifacts this phase produces

New this plan:
- A real-data backtest report markdown + equity-curve/KPI-table/model-metrics parquet artifacts under `outputs/reports/platform/`.
- Two new `registry/trials.jsonl` rows (regime-tilt strategy + no-regime ablation).
- (No new source files — this plan executes and human-verifies the machinery built in Plans 01–06.)

<output>
Create `.planning/phases/05-honest-backtest-evaluation/05-07-SUMMARY.md` when done.
</output>
