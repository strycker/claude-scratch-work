---
phase: 02-honesty-infrastructure
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/trading_crab_lib/platform/honesty/registry.py
  - tests/unit/test_platform_registry.py
autonomous: true
requirements: [HON-02]
must_haves:
  truths:
    - "Every call to append_trial writes exactly one JSON line and never rewrites or truncates existing lines"
    - "Each trial row carries config_hash, config, features, metrics, git_sha, and an ISO-8601 UTC timestamp"
    - "read_trials returns all logged trials as a DataFrame, one row per appended line"
    - "The ledger file lives at a git-TRACKED path (registry/trials.jsonl), never under gitignored data/"
  artifacts:
    - "src/trading_crab_lib/platform/honesty/registry.py"
    - "tests/unit/test_platform_registry.py"
  key_links:
    - "append_trial() opens the ledger in append mode ('a') only — never 'w'"
    - "config_hash = md5(json.dumps(config, sort_keys=True)) — deterministic for the multiple-testing denominator"
    - "REGISTRY_PATH resolves from config registry.path relative to trading_crab_lib.ROOT"
---

<objective>
Build the trial registry (HON-02, design §8.4 / §22, CONTEXT D-01/D-02): an append-only
JSONL ledger, committed to git, logging every evaluated configuration as an immutable
pre-registration record. This ledger is the multiple-testing denominator for the deflated
Sharpe at design freeze — losing or corrupting it invalidates the freeze evaluation, so its
append-only integrity is CORRECTNESS-CRITICAL.

Purpose: no evaluated configuration escapes the ledger; git history is the tamper-evidence
layer. Boring, inspectable JSONL over clever storage (the ledger's credibility IS the product).

Output: `registry.py` (append_trial, read_trials, config hashing, git SHA capture) and a
property-style test suite proving appends never truncate and the schema is complete.
</objective>

<execution_context>
@/home/user/claude-scratch-work/.claude/gsd-core/workflows/execute-plan.md
@/home/user/claude-scratch-work/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-honesty-infrastructure/02-CONTEXT.md
@.planning/phases/02-honesty-infrastructure/02-RESEARCH.md
@.planning/phases/02-honesty-infrastructure/02-PATTERNS.md
@src/trading_crab_lib/platform/config.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Append-only JSONL trial registry module</name>
  <files>src/trading_crab_lib/platform/honesty/registry.py, tests/unit/test_platform_registry.py</files>
  <read_first>
    - .planning/phases/02-honesty-infrastructure/02-RESEARCH.md Pattern 2 + "Code Examples" (append_trial with hashlib.md5 config hash, subprocess git SHA, JSONL append 'a' mode — the exact target implementation) and Pitfall 3 (row must carry the full config that produced the metrics, not just a score)
    - .planning/phases/02-honesty-infrastructure/02-PATTERNS.md registry.py section (I/O conventions; note the model_metrics_artifacts.py analog opens 'w' — the OPPOSITE of what D-01 requires; copy only the json.dump idiom, never the open-mode)
    - src/trading_crab_lib/__init__.py (ROOT constant — resolve registry.path against ROOT so registry/trials.jsonl is repo-root-relative and git-tracked)
    - src/trading_crab_lib/platform/config.py (load_platform_config — read registry.path from the config registry: section added in Plan 01)
  </read_first>
  <behavior>
    - append_trial(*, config, features, metrics, path=None) appends exactly one line and returns the written row dict.
    - N sequential append_trial calls produce a file with exactly N lines; the first N-1 lines are byte-identical before and after the Nth append (never truncates/rewrites).
    - Each row dict has keys: config_hash, config, features, metrics, git_sha, timestamp.
    - config_hash is a 12-char hex string, deterministic for equal config (order-independent via sort_keys), and different for a differing config.
    - timestamp parses as ISO-8601 and carries a UTC offset.
    - git_sha is a non-empty string (real SHA in this repo; "unknown" if git is unavailable — subprocess errors are caught, never raised).
    - read_trials(path=None) returns a pandas DataFrame with one row per logged line; on a missing/empty file it returns an empty DataFrame (does not raise).
    - The default REGISTRY_PATH resolves under trading_crab_lib.ROOT (repo root), NOT under DATA_DIR.
  </behavior>
  <action>
    Create registry.py: docstring + `from __future__ import annotations`; imports hashlib, json,
    logging, subprocess; from datetime import datetime, timezone; from pathlib import Path; pandas
    as pd; from trading_crab_lib import ROOT; `log = logging.getLogger(__name__)`. Define
    `DEFAULT_REGISTRY_PATH = ROOT / "registry" / "trials.jsonl"` and a helper
    `_resolve_registry_path(path)` that returns DEFAULT_REGISTRY_PATH when path is None (a
    config-driven override may pass registry.path). Implement `config_hash(config)` =
    `hashlib.md5(json.dumps(config, sort_keys=True, default=str).encode()).hexdigest()[:12]`.
    Implement `_git_sha()` via `subprocess.run(["git","rev-parse","HEAD"], capture_output=True,
    text=True)` returning stdout.strip() on success and "unknown" on CalledProcessError /
    FileNotFoundError (catch specific exceptions only — no bare except; follow CLAUDE.md error
    handling). Implement `append_trial(*, config, features, metrics, path=None)`: build the row
    (config_hash, config, features, metrics, git_sha, timestamp = datetime.now(timezone.utc).isoformat()),
    mkdir(parents=True, exist_ok=True) the parent, open the file in "a" mode with utf-8, write
    json.dumps(row, default=str) + "\n", return row. Implement `read_trials(path=None)` using
    pd.read_json(lines=True) guarded for a missing/empty file (return pd.DataFrame()).
    Create tests/unit/test_platform_registry.py mirroring the taxonomy test's class structure,
    using tmp_path for the ledger path (pass path= explicitly; never write the real
    registry/trials.jsonl in tests). Cover every behavior bullet as an explicit property test.
  </action>
  <verify>
    <automated>pytest tests/unit/test_platform_registry.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_append_never_truncates: write a sentinel trial, append 3 more, assert the file has 4 lines AND the first line's parsed content is unchanged (byte-stable prefix).
    - test_one_line_per_call: N append_trial calls yield exactly N newline-terminated lines, each valid JSON.
    - test_row_schema: every appended row has keys {config_hash, config, features, metrics, git_sha, timestamp}; timestamp parses via datetime.fromisoformat and has tzinfo.
    - test_config_hash_deterministic: equal configs (differently ordered dict literals) hash identically; a changed value hashes differently; hash length == 12.
    - test_open_mode_is_append: the module source contains `open(` in `"a"` mode and NO `"w"` mode write of the ledger (`grep -n "\"w\"\|'w'" src/trading_crab_lib/platform/honesty/registry.py` returns no ledger-write match).
    - test_read_trials_roundtrip: after K appends, read_trials(path) returns a DataFrame with K rows and a config_hash column; read_trials on a nonexistent path returns an empty DataFrame without raising.
    - test_git_sha_present: appended row's git_sha is a non-empty string.
    - test_default_path_not_under_data: DEFAULT_REGISTRY_PATH is under ROOT and its parts contain "registry" and NOT "data" (assert "data" not in DEFAULT_REGISTRY_PATH.relative_to(ROOT).parts).
  </acceptance_criteria>
  <done>append_trial is a proven append-only writer with a complete, hashable schema; read_trials reconstructs the ledger; the default ledger path is repo-root-relative (git-trackable), never under data/.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the ledger path is git-tracked (not swallowed by .gitignore)</name>
  <files>tests/unit/test_platform_registry.py</files>
  <read_first>
    - .gitignore (confirm no rule matches registry/ or registry/trials.jsonl — data/raw/ is ignored, registry/ is not)
    - .planning/phases/02-honesty-infrastructure/02-CONTEXT.md D-01 (ledger is committed to git; tamper-evidence via git history)
  </read_first>
  <action>
    Add a test `test_registry_path_is_git_trackable` to tests/unit/test_platform_registry.py that
    asserts the default ledger path is NOT git-ignored. Implement it by running
    `subprocess.run(["git","check-ignore", "registry/trials.jsonl"], ...)` from the repo root and
    asserting the return code is 1 (path is NOT ignored) — check-ignore exits 0 only when a path
    IS ignored. Skip the test gracefully (pytest.skip) if git is unavailable. Do NOT create or
    commit registry/trials.jsonl in this test; assert on the path string only.
  </action>
  <verify>
    <automated>pytest tests/unit/test_platform_registry.py::test_registry_path_is_git_trackable -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `git check-ignore registry/trials.jsonl` exits non-zero (path not ignored) — asserted by the test.
    - The test does not write registry/trials.jsonl to the working tree.
    - Running `git check-ignore registry/trials.jsonl` manually from the repo root prints nothing and exits 1.
  </acceptance_criteria>
  <done>The tracked ledger path is proven reachable by git (not gitignored), so the tamper-evidence guarantee holds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| researcher -> ledger | Every evaluated configuration must be recorded immutably; a retroactively edited metric would corrupt the multiple-testing denominator |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02 | Tampering | registry.py append_trial | medium | mitigate | Append-only JSONL (open "a", never "w"); git history as tamper-evidence (tracked path, verified not gitignored); property test proves appends never truncate existing lines |
| T-02-06 | Repudiation | registry row completeness | medium | mitigate | Every row carries config_hash + full config + git_sha + UTC timestamp (Pitfall 3), so any logged metric is reproducible and attributable |
| T-02-SC | Tampering | pip installs | low | accept | Zero new packages installed this phase (RESEARCH Package Legitimacy Audit). No install task exists. |
</threat_model>

<verification>
- `pytest tests/unit/test_platform_registry.py -x -q` green (all property tests).
- `git check-ignore registry/trials.jsonl` exits 1 (not ignored).
- `grep -n "'w'\|\"w\"" src/trading_crab_lib/platform/honesty/registry.py` shows no ledger opened for writing/truncation.
</verification>

<success_criteria>
HON-02 success criterion 2 (ROADMAP): every evaluated configuration is automatically logged to
a queryable trial registry with no manual bookkeeping. Proven by append_trial's complete schema
+ read_trials roundtrip; automatic logging is wired into the walk-forward runner in Plan 05.
</success_criteria>

## Artifacts this plan produces

- Module `src/trading_crab_lib/platform/honesty/registry.py`:
  - Constant: `DEFAULT_REGISTRY_PATH` (ROOT/registry/trials.jsonl).
  - Functions: `config_hash(config)`, `append_trial(*, config, features, metrics, path=None)`,
    `read_trials(path=None)`, `_git_sha()`, `_resolve_registry_path(path)`.
- Ledger schema (one JSON object per line): `config_hash`, `config`, `features`, `metrics`,
  `git_sha`, `timestamp`.
- Test: `tests/unit/test_platform_registry.py`.

<output>
Create `.planning/phases/02-honesty-infrastructure/02-02-SUMMARY.md` when done.
</output>
