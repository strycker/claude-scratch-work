---
phase: 02-honesty-infrastructure
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/trading_crab_lib/platform/honesty/gating.py
  - src/trading_crab_lib/platform/honesty/gap_lag.py
  - tests/unit/test_platform_gating.py
  - tests/unit/test_platform_gap_lag.py
autonomous: true
requirements: [HON-05, HON-06]
must_haves:
  truths:
    - "Supervised training paths refuse a feature set containing any forbidden non-causal (centered-window) column, raising loudly unless allow_noncausal=True is passed"
    - "allow_noncausal=True opts out with a WARNING log and a returned noncausal_used=True flag"
    - "compute_gap, compute_detection_lag, and sojourn_lag_ratio produce correct values on synthetic smoothed/filtered series"
    - "A gap/lag run prints a summary AND persists a first-class artifact under outputs/reports/model_metrics/"
  artifacts:
    - "src/trading_crab_lib/platform/honesty/gating.py"
    - "src/trading_crab_lib/platform/honesty/gap_lag.py"
    - "tests/unit/test_platform_gating.py"
    - "tests/unit/test_platform_gap_lag.py"
  key_links:
    - "select_platform_feature_path() loads monthly_features via get_platform_checkpoint_manager and asserts causal-by-construction"
    - "report_gap_lag() -> outputs/reports/model_metrics/*.parquet + stdout summary (D-05)"
    - "Phase 3 plugs real jump-model/nowcaster series into the same compute functions with no interface change"
---

<objective>
Two leaf honesty utilities that depend only on the Plan 01 foundation:

HON-06 — causal-feature gating (design R5, pitfall P1). RESEARCH Open Question 1 is RESOLVED by
02-PATTERNS.md: `monthly_features` (Phase 1) is causal-by-construction (only trailing
`rolling(3)`; no `center=True` anywhere; no `monthly_features_supervised.parquet` exists or
should be created). So gating here is NOT a two-file selector — it is an assertion guard that
refuses any forbidden centered-window column with a loud opt-out, keeping the salvaged module's
raise-loud / warn-on-opt-out shape.

HON-05 — smoothed-vs-filtered gap + detection lag (design §5.4). Per RESEARCH Pitfall 4, the
real smoothed/filtered regime series do not exist until Phase 3; this phase delivers the
GENERIC, tested compute functions plus the CLI + artifact reporting surface (D-05), proven
end-to-end on synthetic series. Phase 3 plugs in the real jump-model + nowcaster with no
interface change. Weekly-report wiring is deferred to Phase 4 (D-05) — do not touch the
incumbent report path.

Output: `gating.py`, `gap_lag.py`, and their tests.
</objective>

<execution_context>
@/home/user/claude-scratch-work/.claude/gsd-core/workflows/execute-plan.md
@/home/user/claude-scratch-work/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-honesty-infrastructure/02-CONTEXT.md
@.planning/phases/02-honesty-infrastructure/02-RESEARCH.md
@.planning/phases/02-honesty-infrastructure/02-PATTERNS.md
@ideas/gsd-salvage/prediction/feature_gating.py
@src/trading_crab_lib/platform/checkpoints.py
@src/trading_crab_lib/platform/transforms_monthly.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Causal-feature gating guard (HON-06)</name>
  <files>src/trading_crab_lib/platform/honesty/gating.py, tests/unit/test_platform_gating.py</files>
  <read_first>
    - ideas/gsd-salvage/prediction/feature_gating.py (the raise-loud / warn-on-opt-out shape and error-message tone to port — returns tuple[Path, str, bool]; DO NOT import from ideas/, port the pattern)
    - .planning/phases/02-honesty-infrastructure/02-PATTERNS.md gating.py section (RESOLVED Open Question 1: monthly_features is causal-by-construction; reshape the port to select_platform_feature_path(checkpoint_dir, *, allow_noncausal=False) that asserts no forbidden centered-window column; do NOT invent monthly_features_supervised.parquet)
    - src/trading_crab_lib/platform/transforms_monthly.py (compute_lean_features — confirm only trailing rolling(3).std is used; there is no centered smoothing; this is why the guard is an assertion, not a file selector)
    - src/trading_crab_lib/platform/checkpoints.py (get_platform_checkpoint_manager — gating loads monthly_features through THIS default manager, not through holdout.py)
  </read_first>
  <behavior>
    - assert_causal_features(columns, *, allow_noncausal=False) returns noncausal_used (bool). It scans columns for any name matching a forbidden centered-window pattern (module constant FORBIDDEN_CENTERED_SUFFIXES = a tuple such as ("_centered", "_c5", "_zerophase")). If a match is found and allow_noncausal is False, it raises a loud FileNotFoundError/ValueError naming the offending column(s) and instructing to pass allow_noncausal=True to opt out. If a match is found and allow_noncausal is True, it logs a WARNING ("NONCAUSAL_USED=true ...") and returns True. If no match, returns False.
    - select_platform_feature_path(checkpoint_dir=None, *, allow_noncausal=False) resolves the monthly_features.parquet path from the platform checkpoint manager's dir (checkpoint_dir overrides for tests), reads its column names, runs assert_causal_features, and returns (path, "monthly_features", noncausal_used).
    - The default path (allow_noncausal=False) on a clean causal monthly_features returns noncausal_used=False and does NOT raise.
  </behavior>
  <action>
    Create gating.py: docstring (state the causal-by-construction resolution + the guard's job) +
    `from __future__ import annotations` + `import logging` + `from pathlib import Path` + pandas +
    `log = logging.getLogger(__name__)`. Define FORBIDDEN_CENTERED_SUFFIXES. Implement
    assert_causal_features and select_platform_feature_path per the behavior block. For reading
    columns cheaply, read the parquet schema (pyarrow) or `pd.read_parquet(path, columns=None).columns`
    — a lightweight column read is acceptable at this scale. Reuse get_platform_checkpoint_manager
    from platform.checkpoints for the default dir. Match the salvaged module's loud, instructive
    error-message tone. Keep the concept described in prose only — do not write a literal forbidden
    token into a comment that a negative-grep acceptance check would then trip on.
    Create tests/unit/test_platform_gating.py mirroring the platform test-class structure. Build a
    tmp monthly_features.parquet: one clean (all-causal) frame and one with an injected
    "<something>_centered" column. No network. Cover: clean+default -> (path, "monthly_features",
    False), no raise; forbidden+default -> raises with the offending column named; forbidden+
    allow_noncausal=True -> WARNING logged (caplog) + returns noncausal_used=True.
  </action>
  <verify>
    <automated>pytest tests/unit/test_platform_gating.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_clean_features_pass: select_platform_feature_path on an all-causal monthly_features returns ("<path>", "monthly_features", False) and does not raise.
    - test_noncausal_blocked_by_default: a monthly_features containing a "_centered" column raises (FileNotFoundError or ValueError) whose message names the offending column and mentions allow_noncausal.
    - test_noncausal_opt_out_warns: with allow_noncausal=True the same frame returns noncausal_used=True and emits a WARNING record containing "NONCAUSAL_USED" (assert via caplog).
    - gating.py loads monthly_features via get_platform_checkpoint_manager (grep confirms the import from platform.checkpoints) and does NOT reference a "monthly_features_supervised" path (`grep -c "monthly_features_supervised" src/trading_crab_lib/platform/honesty/gating.py` returns 0).
  </acceptance_criteria>
  <done>Supervised training refuses non-causal features by default with a loud, actionable error and a logged, flagged opt-out — the P1 look-ahead sin cannot happen silently in the platform namespace.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gap/lag pure compute functions (HON-05)</name>
  <files>src/trading_crab_lib/platform/honesty/gap_lag.py, tests/unit/test_platform_gap_lag.py</files>
  <read_first>
    - platform_design/platform_design.md §5.4 (precise definitions: smoothed-vs-filtered gap = performance with smoothed labels minus performance with filtered probabilities; detection lag = periods from each ex-post transition until real-time probability crosses the action threshold; sojourn/lag ratio reported prominently)
    - .planning/phases/02-honesty-infrastructure/02-RESEARCH.md "Code Examples" (sojourn_lag_ratio reference implementation — copy verbatim; ValueError on non-positive lag) and Pitfall 4 (compute functions must be generic + tested on synthetic series; real regime data arrives in Phase 3)
    - .planning/phases/02-honesty-infrastructure/02-PATTERNS.md gap_lag.py section (pure compute functions: plain functions over pandas Series, explicit ValueError on invalid input, no classes)
  </read_first>
  <behavior>
    - compute_gap(smoothed_perf, filtered_perf) returns smoothed_perf - filtered_perf (accepts floats; documented as "measured hindsight content").
    - compute_detection_lag(transitions, filtered_probs, threshold) returns a per-transition lag list (periods from each transition index until filtered_probs first crosses >= threshold at or after that transition) plus a median; a transition whose probability never crosses is reported as unresolved (e.g., NaN / excluded from median with a documented rule). Inputs are index-aligned pandas Series / integer positions.
    - sojourn_lag_ratio(median_sojourn_months, median_detection_lag_months) returns the ratio; raises ValueError if detection lag <= 0.
    - All three are pure (no I/O), deterministic, and raise ValueError on malformed input (e.g., empty series, mismatched lengths).
  </behavior>
  <action>
    Create gap_lag.py: docstring citing design §5.4 + `from __future__ import annotations` +
    `import logging` + numpy + pandas + `log = logging.getLogger(__name__)`. Implement compute_gap,
    compute_detection_lag, sojourn_lag_ratio per the behavior block; copy sojourn_lag_ratio from
    RESEARCH Code Examples. Keep them classless pure functions with explicit ValueError guards.
    Create tests/unit/test_platform_gap_lag.py mirroring the platform test-class structure with
    hand-constructed synthetic series where the expected gap, per-transition lags, and ratio are
    known by construction (e.g., a filtered-prob series that crosses the threshold exactly k
    periods after a known transition -> lag == k). Cover the happy path plus the ValueError guards
    (non-positive lag, empty/mismatched inputs).
  </action>
  <verify>
    <automated>pytest tests/unit/test_platform_gap_lag.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_compute_gap_value: compute_gap(0.9, 0.6) == pytest.approx(0.3).
    - test_detection_lag_known_construction: for a synthetic transition at position p and a filtered-prob series first crossing the threshold at p+k, the returned per-transition lag == k and the median matches for a multi-transition case.
    - test_sojourn_lag_ratio: sojourn_lag_ratio(18, 2) == pytest.approx(9.0); sojourn_lag_ratio(5, 2) == pytest.approx(2.5); sojourn_lag_ratio(x, 0) raises ValueError.
    - test_invalid_inputs_raise: empty or length-mismatched series raise ValueError (not a silent NaN return path for structurally invalid input).
  </acceptance_criteria>
  <done>The three §5.4 honesty metrics compute correctly on synthetic series and reject malformed input — the generic core Phase 3 will feed real regime output into.</done>
</task>

<task type="auto">
  <name>Task 3: Gap/lag reporting surface — CLI summary + persisted artifact (HON-05, D-05)</name>
  <files>src/trading_crab_lib/platform/honesty/gap_lag.py, tests/unit/test_platform_gap_lag.py</files>
  <read_first>
    - ideas/gsd-salvage/prediction/model_metrics_artifacts.py (the empty-safe DataFrame + to_parquet idiom — PATTERNS lines 424-484: build pd.DataFrame(rows); if empty give it an explicit column list; df.to_parquet(path, index=False); port the I/O shape only, targeting outputs/reports/model_metrics/)
    - src/trading_crab_lib/__init__.py (OUTPUT_DIR constant — artifact dir = OUTPUT_DIR/"reports"/"model_metrics")
    - .planning/phases/02-honesty-infrastructure/02-CONTEXT.md D-05 (CLI run output + persisted artifact this phase; weekly-report wiring is Phase 4 — do NOT touch the incumbent report path)
  </read_first>
  <action>
    Extend gap_lag.py with report_gap_lag(metrics: dict, *, output_dir=None) -> Path that (a) prints
    a human-readable summary of gap / median detection lag / sojourn-lag ratio to stdout via
    logging.info AND a plain print of a one-block summary (so it is visible as CLI run output per
    D-05), and (b) writes a parquet artifact under OUTPUT_DIR/"reports"/"model_metrics/" (default;
    output_dir overrides for tests) using the empty-safe DataFrame + to_parquet idiom, returning the
    written path. Add a runnable `if __name__ == "__main__":` demo that constructs a small synthetic
    smoothed/filtered example, computes the three metrics, and calls report_gap_lag — this doubles
    as the CLI surface and a self-check runnable end-to-end on synthetic data (no network, no Phase 3
    dependency). Add tests to test_platform_gap_lag.py: report_gap_lag writes a parquet file that
    round-trips (pd.read_parquet) with the expected metric columns, and the empty-metrics case still
    writes a schema-stable (columns present) parquet.
  </action>
  <verify>
    <automated>pytest tests/unit/test_platform_gap_lag.py -x -q && python3 -m trading_crab_lib.platform.honesty.gap_lag</automated>
  </verify>
  <acceptance_criteria>
    - test_report_writes_artifact: report_gap_lag(metrics, output_dir=tmp) returns a Path under a "model_metrics" directory; pd.read_parquet(that path) yields a DataFrame carrying gap / detection-lag / sojourn-lag-ratio columns.
    - test_report_empty_metrics_schema_stable: report_gap_lag({}, output_dir=tmp) still writes a parquet with the declared columns present (empty-safe idiom).
    - `python3 -m trading_crab_lib.platform.honesty.gap_lag` exits 0 and prints a summary line containing the sojourn/detection-lag ratio (end-to-end on synthetic data, no network).
    - The artifact directory is OUTPUT_DIR/reports/model_metrics (grep confirms the path is built from OUTPUT_DIR, not a hardcoded absolute path).
  </acceptance_criteria>
  <done>Gap/lag metrics are first-class RUN outputs now (CLI summary + persisted parquet artifact), proven end-to-end on synthetic data — Phase 4 only adds weekly-report wiring later.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| feature set -> supervised trainer | Non-causal (centered/two-sided) features leaking into supervised training inflate accuracy and cannot be reproduced live (pitfall P1) |
| honesty metric -> decision | Gap/lag numbers are the go/no-go signal; they must exist as run outputs, not be silently deferred |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Information Disclosure | gating.py assert_causal_features | high | mitigate | Default path refuses any forbidden centered-window column with a loud raise; opt-out requires explicit allow_noncausal=True and emits a WARNING flag; test covers all three branches |
| T-02-07 | Information Disclosure | gap_lag reporting scope | medium | mitigate | Compute functions are generic + tested now (Pitfall 4); artifact writes under outputs/ only, never touching the incumbent report path (D-05 phase-boundary discipline) |
| T-02-SC | Tampering | pip installs | low | accept | Zero new packages this phase (RESEARCH Package Legitimacy Audit). No install task exists. |
</threat_model>

<verification>
- `pytest tests/unit/test_platform_gating.py tests/unit/test_platform_gap_lag.py -x -q` green.
- `python3 -m trading_crab_lib.platform.honesty.gap_lag` runs end-to-end on synthetic data.
- No touch of the incumbent report path: `git diff --name-only` for this plan lists only the four files_modified.
</verification>

<success_criteria>
HON-06 success criterion 5a (ROADMAP): supervised training paths load causal features by default
with a loud opt-out. HON-05 success criterion 5b: smoothed-vs-filtered gap and detection lag are
computed and reported as first-class run outputs. Both proven on synthetic data with a working
CLI + artifact surface; real regime series arrive in Phase 3 against the same interface.
</success_criteria>

## Artifacts this plan produces

- Module `src/trading_crab_lib/platform/honesty/gating.py`:
  - Constant `FORBIDDEN_CENTERED_SUFFIXES`.
  - Functions `assert_causal_features(columns, *, allow_noncausal=False)`,
    `select_platform_feature_path(checkpoint_dir=None, *, allow_noncausal=False)`.
- Module `src/trading_crab_lib/platform/honesty/gap_lag.py`:
  - Functions `compute_gap(smoothed_perf, filtered_perf)`,
    `compute_detection_lag(transitions, filtered_probs, threshold)`,
    `sojourn_lag_ratio(median_sojourn_months, median_detection_lag_months)`,
    `report_gap_lag(metrics, *, output_dir=None)`, plus a `__main__` synthetic demo/CLI.
  - Artifact: `outputs/reports/model_metrics/*.parquet`.
- Tests: `tests/unit/test_platform_gating.py`, `tests/unit/test_platform_gap_lag.py`.

<output>
Create `.planning/phases/02-honesty-infrastructure/02-04-SUMMARY.md` when done.
</output>
