---
phase: 01-monthly-data-layer-long-histories
plan: 04
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/trading_crab_lib/platform/ingestion/norgate.py
  - src/trading_crab_lib/platform/ingestion/tiingo.py
  - src/trading_crab_lib/platform/ingestion/eodhd.py
  - tests/unit/test_platform_paid_provider_stubs.py
  - docs/paid_provider_seams.md
autonomous: true
requirements: [DATA-06]
must_haves:
  truths:
    - "Each paid-provider stub module imports cleanly and, when its fetch entry point is called, raises NotImplementedError with a message pointing to docs/paid_provider_seams.md"
    - "No stub makes any network call and no new runtime dependency is added (DATA-06 is documentation/placeholder only)"
    - "docs/paid_provider_seams.md documents Norgate, Tiingo, and EODHD as future adapter seams and notes stockcharts.com/finviz.com as future FEATURE sources (not price sources)"
  artifacts:
    - src/trading_crab_lib/platform/ingestion/norgate.py
    - src/trading_crab_lib/platform/ingestion/tiingo.py
    - src/trading_crab_lib/platform/ingestion/eodhd.py
    - docs/paid_provider_seams.md
  key_links:
    - "stub module names match the cfg['paid_providers'] list authored in Plan 01"
---

<objective>
Create documentation-only adapter seams for the paid data providers (Norgate, Tiingo,
EODHD) as explicit placeholders, and document them plus the candidate feature sources.

Purpose: DATA-06 reserves the interface for a future v2 paid-data integration without
building it now. The value is a clearly-marked seam and a decision record, not runtime
code — building anything more would be speculative (YAGNI; RESEARCH "Don't Hand-Roll").

Output: three stub modules that raise NotImplementedError, a stub test, and
`docs/paid_provider_seams.md`.
</objective>

<execution_context>
@/home/user/claude-scratch-work/.claude/gsd-core/workflows/execute-plan.md
@/home/user/claude-scratch-work/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-monthly-data-layer-long-histories/01-CONTEXT.md
@.planning/phases/01-monthly-data-layer-long-histories/01-RESEARCH.md
</context>

## Artifacts this phase produces

New symbols created by this plan (exclude from source-drift verification):
- **Files:** `src/trading_crab_lib/platform/ingestion/norgate.py`, `tiingo.py`, `eodhd.py`, `tests/unit/test_platform_paid_provider_stubs.py`, `docs/paid_provider_seams.md`
- **Functions:** `fetch_prices()` in each stub module (raises `NotImplementedError`)

<tasks>

<task type="auto">
  <name>Task 1: Three provider stub modules + stub test</name>
  <files>src/trading_crab_lib/platform/ingestion/norgate.py, src/trading_crab_lib/platform/ingestion/tiingo.py, src/trading_crab_lib/platform/ingestion/eodhd.py, tests/unit/test_platform_paid_provider_stubs.py</files>
  <read_first>
    - .planning/phases/01-monthly-data-layer-long-histories/01-RESEARCH.md ("Paid-Provider Adapter Seam Notes" — stub pattern raising NotImplementedError with a docstring pointing to the research doc)
    - src/trading_crab_lib/platform/ingestion/__init__.py (package marker created by Plan 01 — stubs live alongside it)
    - config/platform_settings.yaml (cfg['paid_providers'] entries authored by Plan 01: name/module/note)
  </read_first>
  <action>
    Create `norgate.py`, `tiingo.py`, and `eodhd.py` under `platform/ingestion/`. Each has
    `from __future__ import annotations`, a module docstring naming the provider and pointing to
    `docs/paid_provider_seams.md`, a module logger, and a single public entry point
    `fetch_prices(cfg) -> pd.DataFrame` that immediately `raise NotImplementedError(...)` with a
    message stating the provider is a documented v2 seam and directing the reader to
    `docs/paid_provider_seams.md`. No imports of any provider SDK, no network client, no new
    dependency — importing the module must never fail and must never touch the network.

    Create `tests/unit/test_platform_paid_provider_stubs.py` asserting that each of the three
    modules imports cleanly and that calling `fetch_prices({})` raises `NotImplementedError` with
    a non-empty message.
  </action>
  <verify>
    <automated>cd /home/user/claude-scratch-work && python -m pytest tests/unit/test_platform_paid_provider_stubs.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/test_platform_paid_provider_stubs.py -x` passes.
    - Importing each of `norgate`, `tiingo`, `eodhd` from `trading_crab_lib.platform.ingestion` succeeds.
    - Calling `fetch_prices({})` on each raises `NotImplementedError` with a message referencing `docs/paid_provider_seams.md`.
    - `grep -rn "import requests\|import norgatedata\|tiingo\|eodhd" src/trading_crab_lib/platform/ingestion/norgate.py src/trading_crab_lib/platform/ingestion/tiingo.py src/trading_crab_lib/platform/ingestion/eodhd.py` shows no provider SDK / network imports (stubs stay inert).
  </acceptance_criteria>
  <done>Three inert, cleanly-importable provider seams exist and fail loud on use, adding zero runtime dependencies (DATA-06 placeholder code).</done>
</task>

<task type="auto">
  <name>Task 2: docs/paid_provider_seams.md — provider seam + feature-source notes</name>
  <files>docs/paid_provider_seams.md</files>
  <read_first>
    - .planning/phases/01-monthly-data-layer-long-histories/01-RESEARCH.md ("Paid-Provider Adapter Seam Notes" table — Norgate/Tiingo/EODHD offerings; stockcharts/finviz as future feature sources)
    - .planning/phases/01-monthly-data-layer-long-histories/01-CONTEXT.md (Specific Ideas — stockcharts.com/finviz.com are candidate FEATURE sources for later milestones, not price sources)
  </read_first>
  <action>
    Create `docs/paid_provider_seams.md` documenting each provider (Norgate, Tiingo, EODHD): what
    it offers, its integration shape (desktop DB vs REST), and the stub module that reserves its
    seam. State clearly that all three are v2-only placeholders with no implementation in this
    phase. Add a one-line note that stockcharts.com and finviz.com (existing paid subscriptions)
    are candidate FEATURE sources for a later milestone — explicitly NOT price-history sources for
    this phase.
  </action>
  <verify>
    <automated>cd /home/user/claude-scratch-work && python -c "t=open('docs/paid_provider_seams.md').read().lower(); [t.index(k) for k in ['norgate','tiingo','eodhd','stockcharts','finviz','placeholder']]; print('OK', len(t), 'chars')"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/paid_provider_seams.md` exists and documents Norgate, Tiingo, and EODHD as v2 placeholder seams.
    - The document notes stockcharts.com and finviz.com as future feature sources, not price sources.
  </acceptance_criteria>
  <done>DATA-06's documentation-only requirement is satisfied: paid-provider seams and candidate feature sources are recorded with no implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none active) | Stubs perform no I/O; no runtime trust boundary is crossed this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | future config-driven base_url enabling SSRF when a seam is implemented | low | accept | Out of scope — DATA-06 is docs-only; RESEARCH notes to allowlist base URLs when a real adapter lands in v2 |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages; stubs import no SDKs (RESEARCH "Package Legitimacy Audit") |
</threat_model>

<verification>
- `pytest tests/unit/test_platform_paid_provider_stubs.py -x` green.
- Both docs present; three stub modules import cleanly and raise on call.
- No incumbent file modified; no new dependency in pyproject/requirements.
- Full incumbent suite still passes: `pytest tests/ -q`.
</verification>

<success_criteria>
- Paid-provider adapter seams are documented placeholders that raise NotImplementedError, with zero new runtime dependencies (DATA-06).
</success_criteria>

<output>
Create `.planning/phases/01-monthly-data-layer-long-histories/01-04-SUMMARY.md` when done.
</output>
