---
phase: 01-monthly-data-layer-long-histories
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/trading_crab_lib/platform/splice.py
  - tests/unit/test_platform_splice.py
  - docs/splicing_rules.md
autonomous: true
requirements: [DATA-02]
must_haves:
  truths:
    - "ratio_splice(old, new, join_date) returns a continuous series whose value at join_date equals new's value at join_date (no discontinuity beyond float tolerance)"
    - "build_core_research_series(raw, cfg) returns one spliced research series per D-03 core class (equities_tr, long_duration_tr, gold, oil, cash) reaching back to ~1962"
    - "The long-duration synthetic is constructed from GS10 yields via par-bond repricing, not treated as a price level"
    - "docs/splicing_rules.md documents, per asset class, the source, date range, join date, and method (a DATA-02 deliverable)"
  artifacts:
    - src/trading_crab_lib/platform/splice.py
    - docs/splicing_rules.md
  key_links:
    - "splice.build_core_research_series reads the cfg['splice'] block written by Plan 01 and the raw columns produced by the ingestion plans"
    - "join dates are literal config/doc values, never computed dynamically (RESEARCH Pitfall 2)"
---

<objective>
Build the splicing engine that turns raw source segments into one long-history research
series per core asset class (D-03 "model the index, trade the ETF"), and document every
splice rule per DATA-02.

Purpose: The regime labeler and asset models (later phases) need ~1962+ history that no
single ETF can provide. This plan produces the five spliced/synthetic research series and
the human-readable rulebook that makes them reproducible.

Output: `platform/splice.py` (ratio-splice, Treasury total-return synthetic, per-class
assembly), its unit test, and `docs/splicing_rules.md`.
</objective>

<execution_context>
@/home/user/claude-scratch-work/.claude/gsd-core/workflows/execute-plan.md
@/home/user/claude-scratch-work/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-monthly-data-layer-long-histories/01-CONTEXT.md
@.planning/phases/01-monthly-data-layer-long-histories/01-RESEARCH.md
@.planning/phases/01-monthly-data-layer-long-histories/01-PATTERNS.md
</context>

## Artifacts this phase produces

New symbols created by this plan (exclude from source-drift verification):
- **Files:** `src/trading_crab_lib/platform/splice.py`, `tests/unit/test_platform_splice.py`, `docs/splicing_rules.md`
- **Functions:** `ratio_splice()`, `bond_price()`, `monthly_total_return()`, `build_treasury_tr_synthetic()`, `build_equity_total_return()`, `build_core_research_series()`
- **Depends on config from Plan 01:** `cfg['splice']` block, `cfg['data']['start_date']`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Splicing engine — ratio splice, Treasury TR synthetic, per-class assembly</name>
  <files>src/trading_crab_lib/platform/splice.py, tests/unit/test_platform_splice.py</files>
  <read_first>
    - .planning/phases/01-monthly-data-layer-long-histories/01-RESEARCH.md ("Code Examples" ratio_splice reference; "Pattern 3: Treasury total-return synthetic from CMT yields"; "Splicing Sources & Recommendation")
    - config/platform_settings.yaml (the cfg['splice'] block: research_name, method, source columns, maturity_years, coupon_freq per class)
    - src/trading_crab_lib/transforms.py (module docstring + section-divider + copy-before-mutate conventions to match; do NOT edit — frozen)
    - src/trading_crab_lib/config.py (fail-fast collect-all-errors validation shape for the continuity guard)
  </read_first>
  <behavior>
    - ratio_splice(old, new, join_date): result equals new at/after join_date; result at the last pre-join index equals old*scale where scale = new[join_date]/old[join_date]; the seam is continuous (|result[join_date] - new[join_date]| < 1e-9).
    - ratio_splice raises ValueError if either series lacks an observation at join_date (no silent interpolation of the scale factor — RESEARCH Pitfall 2).
    - bond_price(yield, coupon, years, freq): a par bond priced at its own coupon yield returns ~1.0 (|price-1.0| < 1e-6).
    - monthly_total_return(y0, y1, maturity): rising yield (y1>y0) → negative price component (total return < coupon accrual); falling yield → positive.
    - build_core_research_series(raw, cfg): returns a DataFrame with exactly the 5 research_name columns from cfg['splice']; equities via total-return-from-price+dividend, long_duration via par-bond repricing on the GS10 column, gold/oil passthrough of their single source, cash = TB3MS as a return series.
  </behavior>
  <action>
    Create `platform/splice.py` implementing:
    `ratio_splice(old, new, join_date) -> pd.Series` per RESEARCH "Code Examples" — scale `old`
    so `old[join_date] == new[join_date]`, concatenate scaled-old (before join_date) with
    unscaled-new (at/after), raise `ValueError` when join_date is absent from either input.
    `bond_price(yield_annual, coupon_annual, years_to_maturity, freq=2) -> float` and
    `monthly_total_return(yield_t0, yield_t1, maturity_years=10) -> float` implementing the
    par-bond repricing method from RESEARCH Pattern 3 (issue at par at yield_t0, accrue one
    month coupon, reprice at yield_t1). `build_treasury_tr_synthetic(gs10_yield_series, cfg) ->
    pd.Series` that chains `monthly_total_return` across the GS10 monthly yield series into a
    cumulative total-return index (reading maturity_years/coupon_freq from cfg['splice']
    ['long_duration']). `build_equity_total_return(price, div_yield, cfg) -> pd.Series` that
    forms monthly total return = price return + (annual div_yield / 12) and chains into an index
    (Claude's-Discretion source choice: use the already-scraped multpl price + dividend rather
    than adding a new Shiller fetcher — document this and the Shiller cross-check option in
    Task 2's doc). `build_core_research_series(raw, cfg) -> pd.DataFrame` that dispatches on each
    class's `method` (total_return_from_price_div, cmt_par_bond_repricing, single_source,
    yield_as_return) and returns one column per `research_name`. Any 2-segment splice uses
    `ratio_splice` with a literal `join_date` read from config/doc — never a computed default.
    Use `from __future__ import annotations`, `df = df.copy()` before mutation, type hints,
    module logger. Do not import or modify any incumbent transform module.

    Create `tests/unit/test_platform_splice.py` covering the five behaviors with synthetic
    Series/DataFrames (no network). Include `test_splice_continuity_at_join` (asserts the seam
    is continuous within 1e-9) and construction tests for the Treasury and equity synthetics.
  </action>
  <verify>
    <automated>cd /home/user/claude-scratch-work && python -m pytest tests/unit/test_platform_splice.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/test_platform_splice.py -x` passes.
    - `ratio_splice` output at join_date matches `new` at join_date within 1e-9, and raises `ValueError` when join_date is missing from an input.
    - `bond_price(0.05, 0.05, 10)` is within 1e-6 of 1.0 (par bond at its own yield prices to par).
    - `monthly_total_return(0.04, 0.05, 10) < 0.05/12` (rising yield hurts) and `monthly_total_return(0.05, 0.04, 10) > 0.05/12` (falling yield helps).
    - `build_core_research_series(synthetic_raw, cfg)` returns a DataFrame whose columns are exactly the 5 `research_name` values from `cfg['splice']`.
  </acceptance_criteria>
  <done>The five core research series are constructible from raw segments with a documented, tested, reproducible splice/synthesis method (DATA-02 code).</done>
</task>

<task type="auto">
  <name>Task 2: docs/splicing_rules.md — per-asset splice rulebook (DATA-02 deliverable)</name>
  <files>docs/splicing_rules.md</files>
  <read_first>
    - .planning/phases/01-monthly-data-layer-long-histories/01-RESEARCH.md ("Splicing Sources & Recommendation" table; Pattern 3 methodology caveats; Open Questions 2 on GS10-vs-TLT duration mismatch)
    - .planning/phases/01-monthly-data-layer-long-histories/01-CONTEXT.md (D-03 class→series→tradable mapping, D-04 documentation requirement)
    - src/trading_crab_lib/platform/splice.py (the methods actually implemented in Task 1, so the doc matches the code)
    - config/platform_settings.yaml (the cfg['splice'] block whose join dates / sources the doc must record)
  </read_first>
  <action>
    Create `docs/splicing_rules.md` following the CLAUDE.md ADR documentation style
    (Context / Decision / Rationale / Tradeoff). Include one section per core class (equities,
    long_duration, gold, oil, cash) recording, per DATA-02/D-04: the source(s), the date range
    each source covers, the join date (literal, matching config), and the splice/synthesis
    method. Explicitly document: (1) the equity total-return construction from multpl price +
    dividend yield and why Shiller is deferred to a cross-check (RESEARCH A3); (2) the Treasury
    par-bond repricing method, its semiannual-compounding convention, and the GS10(~10Y)-vs-
    TLT(~20Y+) duration mismatch that D-03 accepts (RESEARCH Open Question 2); (3) gold and oil
    as single-source passthroughs from macrotrends with the WTISPLC cross-check for oil;
    (4) cash as TB3MS yield used directly as a return series (no splice). State the ~1962 start
    and that every join date is a pinned config value (RESEARCH Pitfall 2).
  </action>
  <verify>
    <automated>cd /home/user/claude-scratch-work && python -c "t=open('docs/splicing_rules.md').read().lower(); [t.index(k) for k in ['equities','long_duration','gold','oil','cash','join date','par-bond','1962']]; print('OK', len(t), 'chars')"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/splicing_rules.md` exists and contains a section for each of the 5 core classes.
    - The document names, for each class, its source, date range, join date, and method (DATA-02 documentation requirement).
    - The Treasury par-bond repricing method and the accepted GS10-vs-TLT duration mismatch are stated explicitly.
    - The equity total-return source decision (multpl price+dividend; Shiller as future cross-check) is documented.
  </acceptance_criteria>
  <done>Every core asset class has a documented, reproducible splice rule — the DATA-02 documentation deliverable is complete and matches the implemented code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw data → splice engine | numeric Series from ingestion (possibly containing NaN / gaps) crosses into pure-math splice functions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | undocumented/dynamic join date shifting the seam between runs | medium | mitigate | join dates are literal config/doc values; `ratio_splice` raises on a missing join_date rather than silently interpolating (RESEARCH Pitfall 2) |
| T-02-02 | Tampering | miscalibrated Treasury synthetic from a wrong compounding convention | medium | mitigate | convention documented in `docs/splicing_rules.md` and asserted by the rising/falling-yield sign tests |
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages (RESEARCH "Package Legitimacy Audit") |
</threat_model>

<verification>
- `pytest tests/unit/test_platform_splice.py -x` green.
- `docs/splicing_rules.md` present with all 5 class sections and required fields.
- No incumbent file modified (`git status` shows only new splice.py, test, and doc).
- Full incumbent suite still passes: `pytest tests/ -q`.
</verification>

<success_criteria>
- Five core research series (equities_tr, long_duration_tr, gold, oil, cash) are constructible back to ~1962 from free sources.
- Splice continuity, Treasury synthetic sign behavior, and per-class assembly are proven by passing tests.
- Every splice rule is documented per asset (DATA-02).
</success_criteria>

<output>
Create `.planning/phases/01-monthly-data-layer-long-histories/01-02-SUMMARY.md` when done.
</output>
