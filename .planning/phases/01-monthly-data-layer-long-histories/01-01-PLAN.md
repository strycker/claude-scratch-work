---
phase: 01-monthly-data-layer-long-histories
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/trading_crab_lib/platform/__init__.py
  - src/trading_crab_lib/platform/ingestion/__init__.py
  - src/trading_crab_lib/platform/checkpoints.py
  - src/trading_crab_lib/platform/config.py
  - src/trading_crab_lib/platform/taxonomy.py
  - config/platform_settings.yaml
  - tests/unit/test_platform_taxonomy.py
autonomous: true
requirements: [DATA-04]
must_haves:
  truths:
    - "Importing trading_crab_lib.platform succeeds without importing or mutating any incumbent pipeline module (fred.py, transforms.py, pipeline.py stay byte-identical)"
    - "load_platform_config() reads config/platform_settings.yaml and returns a validated dict containing taxonomy, splice, universe, fred_monthly, fred_vintage, and paid_providers blocks"
    - "validate_taxonomy() raises ValueError listing every feature assigned to more than one tier or to no tier (collect-all-errors-then-raise-once, per D-04/DATA-04)"
    - "get_platform_checkpoint_manager() returns a CheckpointManager whose directory is data/checkpoints/platform/ (separate namespace, D-01)"
    - "lean_feature_set() returns the union of the fast and slow taxonomy tiers as the labeling feature set"
  artifacts:
    - src/trading_crab_lib/platform/__init__.py
    - src/trading_crab_lib/platform/config.py
    - src/trading_crab_lib/platform/taxonomy.py
    - src/trading_crab_lib/platform/checkpoints.py
    - config/platform_settings.yaml
  key_links:
    - "platform/checkpoints.py imports CheckpointManager from trading_crab_lib.checkpoints (reuse, never subclass) and DATA_DIR from trading_crab_lib"
    - "taxonomy.py and config.py read config/platform_settings.yaml ONLY — never config/settings.yaml (D-02: frozen incumbent schema untouched)"
---

<objective>
Build the self-contained foundation of the new monthly platform subpackage: the package
scaffold, a checkpoint-namespace factory that reuses the existing CheckpointManager, an
independent config loader + `config/platform_settings.yaml`, and the fast/slow/agency
feature taxonomy with validation.

Purpose: Everything else in Phase 1 (splicing, ALFRED vintages, ingestion, transforms)
reads this config and writes into this checkpoint namespace. Establishing it first as a
frozen, independent foundation (D-01/D-02) lets the four Wave-2 plans run in parallel
without touching each other or the incumbent's 769-test pipeline.

Output: `src/trading_crab_lib/platform/` package with `config.py`, `checkpoints.py`,
`taxonomy.py`; the complete `config/platform_settings.yaml`; and the DATA-04 taxonomy
test.
</objective>

<execution_context>
@/home/user/claude-scratch-work/.claude/gsd-core/workflows/execute-plan.md
@/home/user/claude-scratch-work/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-monthly-data-layer-long-histories/01-CONTEXT.md
@.planning/phases/01-monthly-data-layer-long-histories/01-RESEARCH.md
@.planning/phases/01-monthly-data-layer-long-histories/01-PATTERNS.md
</context>

## Artifacts this phase produces

New symbols created by this plan (exclude from source-drift verification):
- **Files:** `src/trading_crab_lib/platform/__init__.py`, `src/trading_crab_lib/platform/ingestion/__init__.py`, `src/trading_crab_lib/platform/checkpoints.py`, `src/trading_crab_lib/platform/config.py`, `src/trading_crab_lib/platform/taxonomy.py`, `config/platform_settings.yaml`, `tests/unit/test_platform_taxonomy.py`
- **Functions:** `get_platform_checkpoint_manager()`, `load_platform_config()`, `validate_platform_config()`, `validate_taxonomy()`, `classify_feature()`, `lean_feature_set()`
- **Constants:** `PLATFORM_CHECKPOINT_DIR`, `_REQUIRED_PLATFORM_SECTIONS`
- **Config blocks (in `platform_settings.yaml`):** `data`, `fred_monthly`, `fred_vintage`, `multpl_monthly`, `macrotrends_monthly`, `splice`, `universe`, `taxonomy`, `paid_providers`

<tasks>

<task type="auto">
  <name>Task 1: Package scaffold + checkpoint-namespace factory + platform config loader</name>
  <files>src/trading_crab_lib/platform/__init__.py, src/trading_crab_lib/platform/ingestion/__init__.py, src/trading_crab_lib/platform/checkpoints.py, src/trading_crab_lib/platform/config.py</files>
  <read_first>
    - src/trading_crab_lib/checkpoints.py (CheckpointManager.__init__ accepts checkpoint_dir: Path | None — reuse it; PLATFORM must NOT subclass)
    - src/trading_crab_lib/config.py (load() tri-mode dict|Path|str|None and validate_config() collect-all-errors pattern to mirror)
    - src/trading_crab_lib/__init__.py (DATA_DIR, CONFIG_DIR resolution + env-var overrides)
    - src/trading_crab_lib/ingestion/__init__.py (analog for a package marker module)
  </read_first>
  <action>
    Create `platform/__init__.py` as a package marker with a module docstring stating this
    subpackage is the Phase 6 migration unit (D-02) and imports the incumbent's fetchers but
    never edits them (D-01). No re-exports. Create `platform/ingestion/__init__.py` as an
    empty package marker.

    Create `platform/checkpoints.py` exposing constant `PLATFORM_CHECKPOINT_DIR = DATA_DIR /
    "checkpoints" / "platform"` and factory `get_platform_checkpoint_manager() ->
    CheckpointManager` that returns `CheckpointManager(checkpoint_dir=PLATFORM_CHECKPOINT_DIR)`.
    Import `CheckpointManager` from `trading_crab_lib.checkpoints` and `DATA_DIR` from
    `trading_crab_lib`. Do not reimplement save/load/is_fresh — the reused class already
    provides them (PATTERNS.md "Checkpoint reuse (not reimplementation)").

    Create `platform/config.py` mirroring the incumbent `config.py` conventions: a
    `load_platform_config(settings_path: dict | Path | str | None = None) -> dict` accepting
    the same dict|Path|str|None tri-mode (default reads `CONFIG_DIR / "platform_settings.yaml"`),
    calling `load_dotenv()` first, then `validate_platform_config(cfg)`, then injecting the FRED
    API key from `os.getenv("FRED_API_KEY")` into `cfg["fred_monthly"]["api_key"]` and
    `cfg["fred_vintage"]["api_key"]` (log WARNING if unset — reuse D-07's existing key, no new
    credential). Implement `validate_platform_config(cfg)` using the same collect-all-errors-
    then-raise-ValueError-once shape as incumbent `validate_config()`, checking that
    `_REQUIRED_PLATFORM_SECTIONS = ["data", "fred_monthly", "fred_vintage", "splice",
    "universe", "taxonomy"]` are all present. All modules use `from __future__ import
    annotations`, `log = logging.getLogger(__name__)`, type hints on public functions, and never
    read or import `config/settings.yaml` (D-02).
  </action>
  <verify>
    <automated>cd /home/user/claude-scratch-work && python -c "import trading_crab_lib.platform; from trading_crab_lib.platform.checkpoints import get_platform_checkpoint_manager, PLATFORM_CHECKPOINT_DIR; cm=get_platform_checkpoint_manager(); assert str(cm.dir).endswith('checkpoints/platform'), cm.dir; print('OK', PLATFORM_CHECKPOINT_DIR)"</automated>
  </verify>
  <acceptance_criteria>
    - `import trading_crab_lib.platform` and `import trading_crab_lib.platform.ingestion` both succeed.
    - `get_platform_checkpoint_manager().dir` path ends with `checkpoints/platform`.
    - `platform/checkpoints.py` contains no `class` definition (factory + constant only) and imports `CheckpointManager` from `trading_crab_lib.checkpoints`.
    - `grep -rn "settings.yaml" src/trading_crab_lib/platform/` returns no matches (platform reads only platform_settings.yaml).
    - `validate_platform_config({})` raises `ValueError` whose message names every missing section in `_REQUIRED_PLATFORM_SECTIONS`.
  </acceptance_criteria>
  <done>Platform package imports cleanly, checkpoint factory points at the platform namespace, and the platform config loader validates its own schema independently of the frozen incumbent config.</done>
</task>

<task type="auto">
  <name>Task 2: Author config/platform_settings.yaml (all declarative blocks)</name>
  <files>config/platform_settings.yaml</files>
  <read_first>
    - config/settings.yaml (top-level shape conventions: per-series name/shift dicts, macrotrends series list, data.start_date — mirror the SHAPE, do not import the schema)
    - .planning/phases/01-monthly-data-layer-long-histories/01-RESEARCH.md ("Feature Taxonomy Config Design", "Splicing Sources & Recommendation", "Paid-Provider Adapter Seam Notes")
    - .planning/phases/01-monthly-data-layer-long-histories/01-CONTEXT.md (D-03 class→series→tradable mapping, D-06 vintage series list, D-08…D-13 universe)
  </read_first>
  <action>
    Create `config/platform_settings.yaml` with these blocks (concrete keys — later plans read
    these exact names):

    `data`: `start_date: "1962-01-01"`, `end_date: null`, `monthly_freq: "ME"`,
    `daily_persist: true`.

    `fred_monthly.series`: market/fast-layer FRED series, each a dict with `name` and
    `shift: false`, plus `daily: true` on natively-daily series. Include at least GS10
    (fred_gs10), TB3MS (fred_tb3ms), BAA (fred_baa), AAA (fred_aaa), T10Y3M (fred_t10y3m,
    daily), T10Y2Y (fred_t10y2y, daily), VIXCLS (fred_vix, daily), WTISPLC (wti_fred — oil
    cross-check per RESEARCH Splicing Sources).

    `fred_vintage.series`: the D-06 revision-heavy agency series for ALFRED, each a dict with
    `name` and `tier: agency`: GDPC1 (fred_gdp), CPIAUCSL (fred_cpi), UNRATE (fred_unrate),
    INDPRO (fred_indpro), PAYEMS (fred_payems).

    `multpl_monthly.datasets`: list-of-lists `[name, description, url, value_type]` for sp500
    (num), div_yield (percent), cape_shiller (num) using the `/table/by-month` multpl URLs.

    `macrotrends_monthly`: `base_url: "https://www.macrotrends.net"` and `series` list of dicts
    `{name, path, resample}` for gold_spot (path /1333/..., resample mean) and wti_crude (path
    /1369/..., resample mean).

    `splice`: one block per D-03 core class (equities, long_duration, gold, oil, cash), each
    naming `research_name`, `method`, source column key(s), and `tradable`. Use methods:
    equities → `total_return_from_price_div` (price_col sp500, div_yield_col div_yield,
    tradable SPY); long_duration → `cmt_par_bond_repricing` (yield_col fred_gs10,
    maturity_years 10, coupon_freq 2, tradable TLT); gold → `single_source` (source_col
    gold_spot, tradable IAU); oil → `single_source` (source_col wti_crude, cross_check_col
    wti_fred, tradable USO); cash → `yield_as_return` (yield_col fred_tb3ms, tradable FZFXX).

    `universe`: `core_classes` (the 5 class names), `satellites` (D-09 list), `holdings` (D-10),
    `watchlist` (D-11 UNG, UEC), `no_price_ingest` (D-12 FZFXX, SPAXX).

    `taxonomy`: `fast`, `slow`, `agency` lists per RESEARCH "Feature Taxonomy Config Design".
    Ensure NO feature name appears in two tiers (Task 3's validator enforces this).

    `paid_providers`: list of dicts `{name, module, note}` for norgate.py, tiingo.py, eodhd.py
    (DATA-06 placeholder metadata).
  </action>
  <verify>
    <automated>cd /home/user/claude-scratch-work && python -c "import yaml; c=yaml.safe_load(open('config/platform_settings.yaml')); [c[k] for k in ['data','fred_monthly','fred_vintage','multpl_monthly','macrotrends_monthly','splice','universe','taxonomy','paid_providers']]; assert c['data']['start_date']=='1962-01-01'; assert set(c['splice'])=={'equities','long_duration','gold','oil','cash'}; assert len(c['fred_vintage']['series'])==5; print('OK', list(c))"</automated>
  </verify>
  <acceptance_criteria>
    - `config/platform_settings.yaml` parses as valid YAML and contains all nine top-level blocks listed above.
    - `data.start_date == "1962-01-01"` and `data.monthly_freq == "ME"`.
    - `splice` has exactly the 5 keys equities, long_duration, gold, oil, cash; each has a `research_name`, `method`, and `tradable`.
    - `fred_vintage.series` has exactly 5 entries (GDPC1, CPIAUCSL, UNRATE, INDPRO, PAYEMS).
    - `universe.no_price_ingest` contains FZFXX and SPAXX (D-12).
    - No feature string appears in more than one of taxonomy.fast/slow/agency.
  </acceptance_criteria>
  <done>The single declarative config source for the whole phase exists with concrete, documented keys that the splice, ingest, ALFRED, and transforms plans consume verbatim.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Feature taxonomy module + validation test (DATA-04)</name>
  <files>src/trading_crab_lib/platform/taxonomy.py, tests/unit/test_platform_taxonomy.py</files>
  <read_first>
    - src/trading_crab_lib/config.py (validate_config() collect-all-errors-then-raise-once shape — mirror it exactly)
    - config/platform_settings.yaml (the taxonomy block authored in Task 2)
    - tests/unit/test_config.py (existing validation-test style to match)
  </read_first>
  <behavior>
    - validate_taxonomy(cfg): a feature listed in two tiers → ValueError message names that feature and both tiers.
    - validate_taxonomy(cfg): well-formed taxonomy (each feature in exactly one tier) → returns None, no raise.
    - classify_feature("fred_unrate", cfg) → "agency"; classify_feature("cape_shiller", cfg) → "slow"; classify_feature("unknown_x", cfg) → None.
    - lean_feature_set(cfg): returns a set equal to taxonomy.fast ∪ taxonomy.slow (agency excluded from the lean labeling set per RESEARCH §"Feature Taxonomy Config Design").
    - check_columns_tagged(columns, cfg): returns the list of column names not found in any tier (empty list when all tagged).
  </behavior>
  <action>
    Create `platform/taxonomy.py` with:
    `classify_feature(name, cfg) -> str | None` returning the tier ("fast"/"slow"/"agency")
    containing `name`, else None; `lean_feature_set(cfg) -> set[str]` returning the union of the
    fast and slow tiers; `check_columns_tagged(columns, cfg) -> list[str]` returning columns not
    in any tier; and `validate_taxonomy(cfg) -> None` that collects every feature appearing in
    more than one tier into an error list and raises a single `ValueError` (collect-all-then-
    raise-once, mirroring incumbent `validate_config`). The multi-tier check is the DATA-04 "each
    feature classified into exactly one tier" guarantee. Use `from __future__ import
    annotations`, type hints, module logger.

    Create `tests/unit/test_platform_taxonomy.py` covering the five behaviors above, building
    small in-memory cfg dicts (do not hit the real file) plus one test loading the real
    `config/platform_settings.yaml` via `load_platform_config()` and asserting
    `validate_taxonomy` passes and `lean_feature_set` is non-empty. Follow the incumbent
    `tests/unit/test_config.py` structure.
  </action>
  <verify>
    <automated>cd /home/user/claude-scratch-work && python -m pytest tests/unit/test_platform_taxonomy.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/test_platform_taxonomy.py -x` passes.
    - A cfg with a feature in both `fast` and `slow` makes `validate_taxonomy` raise `ValueError` whose message contains that feature name.
    - `lean_feature_set(cfg)` equals `set(cfg["taxonomy"]["fast"]) | set(cfg["taxonomy"]["slow"])` and excludes agency-only features.
    - The real `config/platform_settings.yaml` passes `validate_taxonomy` (no feature double-tagged).
  </acceptance_criteria>
  <done>Every feature has exactly one taxonomy tag, the lean full-history labeling set is derivable from config, and the guarantee is enforced by a passing automated test (DATA-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config file → platform loader | `platform_settings.yaml` is developer-authored local input parsed by `load_platform_config()` |
| environment → loader | `FRED_API_KEY` read from env and injected into the config dict |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | malformed/incomplete platform_settings.yaml | medium | mitigate | `validate_platform_config()` fail-fast collect-all-errors raise (mirrors incumbent V5 input-validation pattern) |
| T-01-02 | Information Disclosure | FRED_API_KEY injected into cfg | low | mitigate | key is only stored in the dict, never logged; loader logs a WARNING only on *absence*, never the value |
| T-01-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages introduced (RESEARCH "Package Legitimacy Audit"); nothing to install this plan |
</threat_model>

<verification>
- `python -c "import trading_crab_lib.platform"` succeeds and no incumbent module is modified (`git status` shows only new files under platform/, config/platform_settings.yaml, and the new test).
- `pytest tests/unit/test_platform_taxonomy.py -x` green.
- Full incumbent suite unaffected: `pytest tests/ -q` still collects and passes its existing tests (this plan adds files only).
</verification>

<success_criteria>
- Platform subpackage scaffold, checkpoint factory, config loader, and taxonomy module exist and import cleanly.
- `config/platform_settings.yaml` holds every declarative block the downstream plans consume.
- DATA-04's "exactly one taxonomy tag per feature" guarantee is enforced by a passing test.
- Zero edits to any frozen incumbent file.
</success_criteria>

<output>
Create `.planning/phases/01-monthly-data-layer-long-histories/01-01-SUMMARY.md` when done.
</output>
