---
phase: 01-monthly-data-layer-long-histories
plan: 07
type: execute
wave: 3
depends_on: [01-01, 01-02, 01-03, 01-05, 01-06]
files_modified:
  - src/trading_crab_lib/platform/transforms_monthly.py
  - tests/unit/test_platform_transforms.py
autonomous: true
requirements: [DATA-01, DATA-03, DATA-04]
must_haves:
  truths:
    - "build_monthly_spine(cfg) orchestrates macro_monthly + prices_daily + splice + ALFRED into ONE monthly feature table indexed by month-end back to ~1962, persisting daily_raw, monthly_raw, and monthly_features checkpoints in the platform namespace"
    - "Agency series are aligned point-in-time via alfred.value_as_of where vintages exist and via publication-lag shift before the earliest vintage — no quarterly agency value appears in a month before it would have been published (DATA-01 + DATA-03)"
    - "The monthly feature table contains the lean full-history feature set (taxonomy fast + slow) and every produced feature column carries exactly one taxonomy tag (DATA-04)"
    - "Merges are NULL-tolerant (pd.concat axis=1): short-history satellites and post-1993 series stay NaN-padded, never truncating the 1962+ span"
  artifacts:
    - src/trading_crab_lib/platform/transforms_monthly.py
  key_links:
    - "transforms_monthly imports macro_monthly.fetch_macro_monthly, prices_daily.fetch_universe_prices, splice.build_core_research_series, alfred.fetch_all_vintages+align_with_fallback, taxonomy.check_columns_tagged+lean_feature_set, and platform checkpoints factory"
    - "agency alignment uses alfred.value_as_of / align_with_fallback (Plan 03) — the runtime application of DATA-03"
---

<objective>
Assemble the monthly feature table: orchestrate the ingestion + splice + vintage layers,
resample to the monthly spine, align agency series point-in-time, compute the lean
fast/slow feature columns, tag every column by taxonomy, and persist the platform
checkpoints. This is the integrator that turns the parts from Plans 01-06 into the DATA-01
deliverable the regime labeler will consume.

Purpose: Success criterion 1 (monthly-frequency dataset with correctly-lagged agency
series) and criterion 4 (a lean full-history feature set usable for labeling) become TRUE
here. This is the phase's vertical join point.

Output: `platform/transforms_monthly.py` and its unit test.
</objective>

<execution_context>
@/home/user/claude-scratch-work/.claude/gsd-core/workflows/execute-plan.md
@/home/user/claude-scratch-work/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-monthly-data-layer-long-histories/01-CONTEXT.md
@.planning/phases/01-monthly-data-layer-long-histories/01-RESEARCH.md
@.planning/phases/01-monthly-data-layer-long-histories/01-PATTERNS.md
</context>

## Artifacts this phase produces

New symbols created by this plan (exclude from source-drift verification):
- **Files:** `src/trading_crab_lib/platform/transforms_monthly.py`, `tests/unit/test_platform_transforms.py`
- **Functions:** `align_agency_monthly()`, `compute_lean_features()`, `tag_feature_columns()`, `build_monthly_spine()`
- **Checkpoint names produced:** `daily_raw`, `monthly_raw`, `monthly_features` (in the `data/checkpoints/platform/` namespace)
- **Depends on config from Plan 01:** all blocks; consumes symbols from Plans 02, 03, 05, 06

<tasks>

<task type="auto">
  <name>Task 1: Monthly spine assembly + point-in-time agency alignment (DATA-01, DATA-03)</name>
  <files>src/trading_crab_lib/platform/transforms_monthly.py</files>
  <read_first>
    - src/trading_crab_lib/platform/ingestion/macro_monthly.py (fetch_macro_monthly signature/output)
    - src/trading_crab_lib/platform/ingestion/prices_daily.py (fetch_universe_prices returns (daily, monthly))
    - src/trading_crab_lib/platform/ingestion/alfred.py (fetch_all_vintages, value_as_of, align_with_fallback)
    - src/trading_crab_lib/platform/splice.py (build_core_research_series signature: raw frame -> 5 research columns)
    - src/trading_crab_lib/platform/checkpoints.py (get_platform_checkpoint_manager)
    - src/trading_crab_lib/transforms.py (engineer_all "fixed step order, each step a named helper" structure to mirror; DO NOT edit — frozen)
    - .planning/phases/01-monthly-data-layer-long-histories/01-RESEARCH.md ("Pitfall 4" vintage-vs-shift; "Pitfall 5" concat merge; System Architecture Diagram data flow)
  </read_first>
  <action>
    Create `platform/transforms_monthly.py` (`from __future__ import annotations`, module logger,
    `df = df.copy()` before mutation, named-helper-per-step structure mirroring incumbent
    `engineer_all`). Implement `align_agency_monthly(monthly_index, cfg, fred_client=None) ->
    pd.DataFrame`: for each `cfg['fred_vintage']['series']` series, call `alfred.fetch_all_vintages`
    then, for each month-end in `monthly_index`, use `alfred.align_with_fallback` (value_as_of
    where vintages exist, publication-lag shift before the earliest vintage) to build a column
    with NO revision or timing look-ahead (DATA-03 runtime application; RESEARCH Pitfall 4).
    Implement `build_monthly_spine(cfg) -> pd.DataFrame` orchestrating, in order: (a)
    `macro_monthly.fetch_macro_monthly(cfg)`; (b) `prices_daily.fetch_universe_prices(cfg)` →
    (daily, monthly); (c) `splice.build_core_research_series(macro_frame, cfg)` for the 5 research
    series; (d) `align_agency_monthly(...)`; (e) merge everything with `pd.concat([...], axis=1)`
    (outer/NULL-tolerant — RESEARCH Pitfall 5) on a month-end index from `cfg['data']
    ['start_date']` forward. Persist via `get_platform_checkpoint_manager().save(...)`: the daily
    prices as `daily_raw` (D-05), the merged monthly frame as `monthly_raw`. Return the monthly
    frame. Import the Plan 02/03/05/06 modules by absolute path; do not touch any frozen incumbent
    module.
  </action>
  <verify>
    <automated>cd /home/user/claude-scratch-work && python -c "import inspect; from trading_crab_lib.platform import transforms_monthly as t; src=inspect.getsource(t); [__import__('sys').exit('missing '+n) for n in ['align_agency_monthly','build_monthly_spine'] if n not in src]; assert 'concat' in src and 'axis=1' in src; assert 'value_as_of' in src or 'align_with_fallback' in src; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `transforms_monthly.py` defines `align_agency_monthly` and `build_monthly_spine` and merges with `pd.concat(..., axis=1)`.
    - `align_agency_monthly` calls `alfred.align_with_fallback` / `value_as_of` (point-in-time), not a raw latest-revision series.
    - `build_monthly_spine` persists `daily_raw` and `monthly_raw` checkpoints via the platform checkpoint factory.
    - No frozen incumbent module (transforms.py, fred.py, assets.py, macrotrends.py) is modified.
  </acceptance_criteria>
  <done>The monthly spine is assembled from all sources with point-in-time agency alignment and NULL-tolerant merges, persisted to the platform checkpoint namespace (DATA-01 + DATA-03 runtime).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lean feature computation + taxonomy tagging + monthly_features checkpoint (DATA-01, DATA-04)</name>
  <files>src/trading_crab_lib/platform/transforms_monthly.py</files>
  <read_first>
    - src/trading_crab_lib/platform/taxonomy.py (lean_feature_set, check_columns_tagged, classify_feature)
    - config/platform_settings.yaml (taxonomy.fast/slow lists — the feature names to materialize)
    - src/trading_crab_lib/platform/transforms_monthly.py (build_monthly_spine output from Task 1 — the input to feature computation)
  </read_first>
  <behavior>
    - compute_lean_features(monthly_raw, cfg): produces the simple full-history fast/slow feature columns derivable from 1962+ data — at minimum curve_10y3m (fred_gs10 − fred_tb3ms), credit_spread_baa_aaa (fred_baa − fred_aaa), trailing_return_1m and trailing_return_3m on the equities research series, realized_vol_1m/3m, cape_shiller passthrough, div_yield_vs_baa. Column names match the taxonomy config exactly.
    - tag_feature_columns(features_df, cfg): returns a dict/metadata mapping every produced feature column to its taxonomy tier; check_columns_tagged reports zero untagged lean columns.
    - build_monthly_spine end-to-end (with synthetic ingestion mocked): the returned monthly_features frame's lean columns are a superset of lean_feature_set(cfg), and every one of those columns is tagged.
  </behavior>
  <action>
    Extend `transforms_monthly.py` with `compute_lean_features(monthly_raw, cfg) -> pd.DataFrame`
    computing the lean fast/slow feature columns whose names exactly match `cfg['taxonomy']`
    (curve slopes = yield differences, credit spread = BAA−AAA, trailing returns = pct_change over
    1/3 months on the equities research series, realized_vol = rolling std of monthly returns,
    cape passthrough, div_yield_vs_baa). Each is a short pandas derivation — keep it lean, no
    speculative multi-scale expansion beyond the taxonomy list. Add `tag_feature_columns(
    features_df, cfg) -> dict[str, str]` mapping each produced column to its tier via
    `taxonomy.classify_feature`, and assert (via `taxonomy.check_columns_tagged`) that every lean
    column is tagged — log a WARNING listing any untagged column. Wire these into
    `build_monthly_spine`: after assembling `monthly_raw`, compute lean features, join them
    (concat axis=1), verify taxonomy coverage of the lean set, and persist the result as the
    `monthly_features` checkpoint. Update `build_monthly_spine`'s return to the monthly_features
    frame.
  </action>
  <verify>
    <automated>cd /home/user/claude-scratch-work && python -m pytest tests/unit/test_platform_transforms.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `compute_lean_features` output column names are a superset of `taxonomy.lean_feature_set(cfg)` for the columns present in synthetic input.
    - `check_columns_tagged(lean_columns, cfg)` returns an empty list (every lean feature carries exactly one taxonomy tag — DATA-04).
    - `build_monthly_spine` persists a `monthly_features` checkpoint in the platform namespace.
    - curve_10y3m equals fred_gs10 − fred_tb3ms and credit_spread_baa_aaa equals fred_baa − fred_aaa on synthetic input.
  </acceptance_criteria>
  <done>The lean full-history feature set is materialized at monthly frequency and fully taxonomy-tagged (DATA-04), completing the labeling-ready feature table.</done>
</task>

<task type="auto">
  <name>Task 3: Transforms test — monthly cadence, agency alignment (no look-ahead), taxonomy coverage</name>
  <files>tests/unit/test_platform_transforms.py</files>
  <read_first>
    - tests/conftest.py (existing fixtures like quarterly_index; may add a platform_monthly_index fixture per RESEARCH Validation Architecture)
    - src/trading_crab_lib/platform/transforms_monthly.py (functions under test)
    - .planning/phases/01-monthly-data-layer-long-histories/01-RESEARCH.md ("Phase Requirements → Test Map": test_quarterly_series_alignment, test_every_feature_tagged, monthly row count)
  </read_first>
  <action>
    Create `tests/unit/test_platform_transforms.py` with all ingestion mocked (patch
    `macro_monthly.fetch_macro_monthly`, `prices_daily.fetch_universe_prices`, and
    `alfred.fetch_all_vintages` to return synthetic frames; do not hit the network or real
    checkpoints — redirect the checkpoint dir with a tmp_path / TC_CHECKPOINT_DIR monkeypatch
    like the incumbent tests). Cover: (1) `test_monthly_row_count` — the assembled monthly frame
    has ~12 rows/year over its span (monthly, not quarterly); (2) `test_quarterly_series_alignment`
    — a synthetic agency series whose vintage becomes known mid-quarter does NOT appear in the
    monthly frame in a month before its realtime_start (no look-ahead); (3) `test_pre_vintage_
    fallback_applied` — pre-vintage months use the shift fallback value; (4) `test_every_feature_
    tagged` — `check_columns_tagged` on the produced lean feature columns returns empty; (5)
    `test_short_history_satellite_null_tolerant` — a short-history price column stays NaN-padded
    in the merged frame. Redirect checkpoint writes to a temp dir so no production checkpoint is
    touched.
  </action>
  <verify>
    <automated>cd /home/user/claude-scratch-work && python -m pytest tests/unit/test_platform_transforms.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/test_platform_transforms.py -x` passes with all network/checkpoints mocked to a temp dir.
    - A test proves monthly cadence (~12 rows/year), distinct from quarterly.
    - A test proves an agency value never appears before its point-in-time availability (DATA-01/DATA-03 no look-ahead).
    - A test proves every lean feature column is taxonomy-tagged (DATA-04).
    - A test proves short-history satellites remain NaN-padded (NULL-tolerant).
    - No production checkpoint under `data/checkpoints/` is written during the test run.
  </acceptance_criteria>
  <done>The integrated monthly feature table's cadence, point-in-time agency alignment, taxonomy coverage, and NULL tolerance are all proven by mocked tests — the phase's core deliverable is verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ingestion layers → transforms | per-source frames (possibly partial/NaN) cross into merge + alignment |
| checkpoint store → downstream phases | the monthly_features table becomes the labeler's input of record |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | revision/timing look-ahead in agency alignment | high | mitigate | use alfred.value_as_of / align_with_fallback point-in-time; test asserts no pre-availability value (RESEARCH Pitfall 4) |
| T-07-02 | Tampering | inner-join truncating the 1962+ span to a short satellite's coverage | high | mitigate | `pd.concat(axis=1)` outer merge; test asserts NaN-padding + monthly row count (RESEARCH Pitfall 5) |
| T-07-03 | Tampering | untagged feature column silently entering the labeling set | medium | mitigate | `check_columns_tagged` gate + test asserting zero untagged lean columns (DATA-04) |
| T-07-04 | Tampering | test run corrupting a production checkpoint | medium | mitigate | redirect checkpoint dir to tmp_path/TC_CHECKPOINT_DIR in tests (incumbent D5 convention) |
| T-07-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages (RESEARCH "Package Legitimacy Audit") |
</threat_model>

<verification>
- `pytest tests/unit/test_platform_transforms.py -x` green with all network + checkpoints mocked.
- `git diff --stat` shows NO change to any frozen incumbent module.
- Full incumbent suite still passes: `pytest tests/ -q` (all 769 existing tests untouched).
- Phase-level integration (documented follow-up, needs FRED_API_KEY + network, not CI): a single live `build_monthly_spine(load_platform_config())` run produces a `monthly_features` checkpoint with a month-end index reaching ~1962 and materially more rows than the quarterly incumbent.
</verification>

<success_criteria>
- A monthly-frequency feature dataset exists back to ~1962 with point-in-time-aligned agency series and a fully taxonomy-tagged lean feature set (DATA-01, DATA-03 runtime, DATA-04).
- All merges NULL-tolerant; daily_raw + monthly_raw + monthly_features checkpoints persisted in the platform namespace.
</success_criteria>

<output>
Create `.planning/phases/01-monthly-data-layer-long-histories/01-07-SUMMARY.md` when done.
</output>
