---
phase: 01-monthly-data-layer-long-histories
plan: 06
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/trading_crab_lib/platform/ingestion/prices_daily.py
  - tests/unit/test_platform_prices_ingest.py
autonomous: true
requirements: [DATA-05]
must_haves:
  truths:
    - "fetch_universe_prices(cfg) fetches DAILY adjusted-close prices for the satellite/holdings/watchlist tickers (interval '1d'), persisting daily and deriving a monthly ('ME') spine (D-05)"
    - "Tickers with short histories yield NaN-padded columns in the merged frame (pd.concat axis=1), never dropped rows or a crash (DATA-05 NULL-tolerant)"
    - "Money-market tickers in universe.no_price_ingest (FZFXX, SPAXX) are excluded from price fetch (D-12 — NAV≈$1, mapped to the cash class)"
    - "Glenn's current holdings (D-10) are always included in the fetch set"
  artifacts:
    - src/trading_crab_lib/platform/ingestion/prices_daily.py
  key_links:
    - "prices_daily reuses assets._ssl_bypass_curl_session (importable session factory, no resample inside) but NOT the QE-hardcoded _batch_yfinance"
    - "the fetch ticker set = union(satellites, holdings, watchlist) minus no_price_ingest, read from cfg['universe']"
---

<objective>
Ingest daily adjusted-close prices for the tradable universe (satellites, Glenn's
holdings, watchlist) with NULL-tolerant handling of short histories, persisting daily
data and a derived monthly spine.

Purpose: DATA-05 + D-05 — the tradable instruments (many post-2000 ETFs, single names)
must be scored even though their histories are far shorter than the 1962+ research
series, and the Phase 4 tripwire consumes daily granularity. The incumbent `assets.py`
resamples to quarterly, so this reuses its SSL/fallback skeleton but targets daily +
monthly.

Output: `platform/ingestion/prices_daily.py` and its mocked unit test.
</objective>

<execution_context>
@/home/user/claude-scratch-work/.claude/gsd-core/workflows/execute-plan.md
@/home/user/claude-scratch-work/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-monthly-data-layer-long-histories/01-CONTEXT.md
@.planning/phases/01-monthly-data-layer-long-histories/01-RESEARCH.md
@.planning/phases/01-monthly-data-layer-long-histories/01-PATTERNS.md
</context>

## Artifacts this phase produces

New symbols created by this plan (exclude from source-drift verification):
- **Files:** `src/trading_crab_lib/platform/ingestion/prices_daily.py`, `tests/unit/test_platform_prices_ingest.py`
- **Functions:** `universe_fetch_tickers()`, `_batch_yfinance_daily()`, `fetch_universe_prices()`, `to_monthly_spine()`
- **Depends on config from Plan 01:** `cfg['universe']` (satellites/holdings/watchlist/no_price_ingest), `cfg['data']['start_date']`, `cfg['data']['monthly_freq']`

<tasks>

<task type="auto">
  <name>Task 1: Daily universe price fetch + monthly spine (reuse SSL session, NULL-tolerant)</name>
  <files>src/trading_crab_lib/platform/ingestion/prices_daily.py</files>
  <read_first>
    - src/trading_crab_lib/ingestion/assets.py (_ssl_bypass_curl_session is a pure importable session factory — reuse it; _batch_yfinance hardcodes interval="1mo" and resample "QE" — do NOT reuse verbatim, it defeats daily persistence; fetch_all Phase 1-5 fallback structure to mirror)
    - config/platform_settings.yaml (cfg['universe']: satellites/holdings/watchlist/no_price_ingest lists from D-09..D-13)
    - .planning/phases/01-monthly-data-layer-long-histories/01-RESEARCH.md ("Code Examples" to_monthly_spine; "Don't Hand-Roll" reuse assets fallback; Pitfall 5 concat merge)
  </read_first>
  <action>
    Create `platform/ingestion/prices_daily.py` with `from __future__ import annotations`, the
    same guarded optional-import blocks as `assets.py` (yfinance / curl_cffi availability flags),
    and a module logger. Implement `universe_fetch_tickers(cfg) -> list[str]` returning
    `union(satellites, holdings, watchlist)` minus `no_price_ingest`, preserving Glenn's holdings
    (D-10) and excluding FZFXX/SPAXX (D-12). Implement `_batch_yfinance_daily(tickers, start, end,
    session) -> dict[str, pd.Series]` mirroring `assets._batch_yfinance` but with interval set to
    one-day and NO internal resample (keep daily). Reuse `assets._ssl_bypass_curl_session()`
    directly (import it — it contains no resample). Implement `to_monthly_spine(daily_df) ->
    pd.DataFrame` that resamples to month-end using `cfg['data']['monthly_freq']` and `.last()`
    (RESEARCH Code Examples). Implement `fetch_universe_prices(cfg) -> tuple[pd.DataFrame,
    pd.DataFrame]` returning `(daily, monthly)`: batch-fetch daily, merge every ticker's Series
    with `pd.concat([...], axis=1)` (outer join → short histories become NaN-padded columns, never
    dropped), and derive the monthly spine. On total fetch failure, degrade gracefully to an
    empty/NaN frame with a WARNING (matching assets.py's graceful terminal fallback) — do NOT
    rebuild the full Stooq/OpenBB daily chain (note it as a documented future fallback). Never use
    `pd.merge`/`DataFrame.join` defaults (Pitfall 5). Do not import or modify `ingestion/assets.py`
    beyond importing `_ssl_bypass_curl_session`.
  </action>
  <verify>
    <automated>cd /home/user/claude-scratch-work && python -c "import inspect; from trading_crab_lib.platform.ingestion import prices_daily as p; src=inspect.getsource(p); assert 'concat' in src and 'axis=1' in src; from trading_crab_lib.ingestion.assets import _ssl_bypass_curl_session; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `universe_fetch_tickers(cfg)` includes every D-10 holding, includes D-09 satellites and D-11 watchlist, and excludes FZFXX and SPAXX.
    - `_batch_yfinance_daily` requests a one-day interval and performs no internal QE resample (daily preserved).
    - `fetch_universe_prices` returns a `(daily, monthly)` tuple merged with `pd.concat(..., axis=1)`.
    - `src/trading_crab_lib/ingestion/assets.py` is byte-identical (unmodified) after this task; only `_ssl_bypass_curl_session` is imported from it.
  </acceptance_criteria>
  <done>Daily universe prices are fetched with a reused SSL session, a monthly spine is derived, and money-market tickers are excluded — all NULL-tolerant.</done>
</task>

<task type="auto">
  <name>Task 2: Universe price ingestion test (short-history NULL tolerance, exclusions, daily→monthly)</name>
  <files>tests/unit/test_platform_prices_ingest.py</files>
  <read_first>
    - tests/unit/test_ingestion.py (assets/yfinance mock approach; _FakeResponse style; skip-when-optional-dep-missing convention)
    - src/trading_crab_lib/platform/ingestion/prices_daily.py (functions under test)
    - config/platform_settings.yaml (universe lists to build the expected fetch set)
  </read_first>
  <action>
    Create `tests/unit/test_platform_prices_ingest.py` with all network mocked. Patch the
    yfinance download call used by `_batch_yfinance_daily` (e.g.
    `@patch("trading_crab_lib.platform.ingestion.prices_daily.yf")` or the batch function
    itself) to return synthetic daily Close data where one ticker has a SHORT history (data only
    for recent dates). Assert: (1) `universe_fetch_tickers` excludes FZFXX/SPAXX and includes the
    holdings; (2) the merged daily/monthly frame keeps the short-history ticker as a NaN-padded
    column for its pre-inception dates rather than dropping rows or raising (DATA-05); (3)
    `to_monthly_spine` on synthetic daily data returns month-end-indexed rows. Use the incumbent
    skip-if-optional-dep-missing pattern for any yfinance-dependent path.
  </action>
  <verify>
    <automated>cd /home/user/claude-scratch-work && python -m pytest tests/unit/test_platform_prices_ingest.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/test_platform_prices_ingest.py -x` passes with all network mocked.
    - A test proves a short-history ticker becomes a NaN-padded column (no dropped rows, no crash).
    - A test proves FZFXX and SPAXX are excluded from the fetch set.
    - A test proves `to_monthly_spine` yields month-end frequency from daily input.
  </acceptance_criteria>
  <done>DATA-05 NULL-tolerant ingestion of variable-length universe histories and money-market exclusion are proven by mocked tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Yahoo Finance → prices_daily | remote price frames of variable shape/coverage cross into merge logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | inner-join merge silently dropping short-history tickers | high | mitigate | standardize on `pd.concat(axis=1)` outer join; test asserts NaN-padding (RESEARCH Pitfall 5) |
| T-06-02 | Denial of Service | Yahoo rate-limit / SSL failure | low | mitigate | reuse assets._ssl_bypass_curl_session + single batch request; graceful WARNING + NaN frame on failure |
| T-06-03 | Tampering | accidental QE resample defeating D-05 daily persistence | medium | mitigate | daily kept unresampled; monthly derived separately; test asserts daily→monthly |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages; yfinance/curl_cffi already dependencies (RESEARCH "Package Legitimacy Audit") |
</threat_model>

<verification>
- `pytest tests/unit/test_platform_prices_ingest.py -x` green with network mocked.
- `git diff --stat` shows NO change to `ingestion/assets.py`.
- Full incumbent suite still passes: `pytest tests/ -q`.
</verification>

<success_criteria>
- Daily universe prices ingest with NULL-tolerant short-history handling and money-market exclusion, plus a derived monthly spine (DATA-05, D-05).
</success_criteria>

<output>
Create `.planning/phases/01-monthly-data-layer-long-histories/01-06-SUMMARY.md` when done.
</output>
