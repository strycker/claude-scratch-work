---
phase: 03-regime-labeling-prediction
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/trading_crab_lib/platform/labeling/__init__.py
  - src/trading_crab_lib/platform/labeling/jump_model.py
  - config/platform_settings.yaml
  - tests/unit/test_platform_labeling.py
autonomous: true
requirements: [L1-01]
must_haves:
  truths:
    - "decode_states_dp returns the exact global minimizer of Σ d[t,s_t] + λ·Σ 1[s_t≠s_{t-1}] — matches brute-force enumeration on small (T,K)"
    - "fit_jump_model on a synthetic 13-column lean-shaped frame returns exactly K=5 states, deterministic given random_state"
    - "labels are canonicalized (sorted by ascending trailing_return_1m centroid) so state numbering is stable across restarts"
    - "an empty state mid-alternation freezes its centroid instead of producing NaN"
  artifacts:
    - "src/trading_crab_lib/platform/labeling/jump_model.py with decode_states_dp, fit_jump_model, canonicalize_states, soft_confidences, standardize_features"
    - "config/platform_settings.yaml labeling: section (K, lambda, n_restarts, embargo_months)"
    - "tests/unit/test_platform_labeling.py (DP-exact + fit + determinism + canonicalization)"
  key_links:
    - "fit_jump_model reads labeling.{K,lambda,n_restarts} defensively via cfg.get('labeling', {}) — no _REQUIRED_PLATFORM_SECTIONS extension"
    - "canonicalize_states depends on trailing_return_1m being in the lean feature set (taxonomy fast tier)"
---

<objective>
Build the correctness-critical core of the L1 jump-model regime labeler (L1-01, design §4.1,
R2): a self-contained `platform/labeling/jump_model.py` implementing the exact
dynamic-programming state decode, the k-means-warm-started multi-restart alternation, state
canonicalization, and softmax soft confidences — plus the `labeling:` config section that
supplies default λ=52, K=5.

Purpose: This is the algorithmic heart of the phase. Everything downstream (persistence,
churn, diagnostics, the nowcaster's targets) consumes the label sequence this module
produces. The DP decode must be provably exact — the design mandates "exact DP decode", and
CONTEXT specifics forbid greedy/heuristic shortcuts — so this plan is TDD with a brute-force
enumeration test as the RED gate.

Output: `jump_model.py` (5 pure/compute functions), the `labeling:` YAML block, and the
labeling test file seeded with the DP-exactness and fit invariants.
</objective>

<execution_context>
@/home/user/claude-scratch-work/.claude/gsd-core/workflows/execute-plan.md
@/home/user/claude-scratch-work/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-regime-labeling-prediction/03-CONTEXT.md
@.planning/phases/03-regime-labeling-prediction/03-RESEARCH.md
@.planning/phases/03-regime-labeling-prediction/03-PATTERNS.md
@src/trading_crab_lib/platform/honesty/cv.py
@src/trading_crab_lib/platform/taxonomy.py
@config/platform_settings.yaml
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Exact DP state decode + soft confidences (RED brute-force gate)</name>
  <files>tests/unit/test_platform_labeling.py, src/trading_crab_lib/platform/labeling/__init__.py, src/trading_crab_lib/platform/labeling/jump_model.py</files>
  <read_first>
    - .planning/phases/03-regime-labeling-prediction/03-RESEARCH.md — Pattern 1 (decode_states_dp, O(TK) min/second-min recurrence) and Pattern 4 (soft_confidences temperature-free softmax); Pitfall 2 (empty-state freeze).
    - .planning/phases/03-regime-labeling-prediction/03-PATTERNS.md — jump_model.py section (module docstring style, copy Patterns 1 & 4 verbatim, do not re-derive) and the test-file structure section (class-per-behavior, `_make_synthetic_monthly` local helper, `if __name__ == "__main__": pytest.main([__file__, "-x", "-q"])` footer).
    - src/trading_crab_lib/platform/honesty/cv.py — module docstring + `from __future__ import annotations` + no-silent-default conventions to mirror.
    - src/trading_crab_lib/platform/honesty/__init__.py — the minimal package-init shape to replicate for labeling/__init__.py.
  </read_first>
  <behavior>
    - decode_states_dp(d, lam): given (T,K) squared-distance matrix and scalar λ, returns (states, total_cost) that EXACTLY minimize Σ_t d[t, s_t] + λ·Σ_t 1[s_t≠s_{t-1}]. Test: on random small cases (e.g. T∈{4,5}, K∈{2,3}) the returned states and cost match an itertools.product brute-force enumeration over all K^T sequences, for several λ values (including λ=0 → pure argmin per row, and large λ → constant sequence).
    - decode_states_dp with λ large enough to forbid all jumps returns a single constant state equal to argmin of column sums.
    - soft_confidences(d): returns a (T,K) row-stochastic matrix (each row sums to 1.0, all entries in [0,1]); the argmax column per row equals the argmin distance column per row; invariant to a per-row additive shift of -d (numeric-stability property).
  </behavior>
  <action>
    Create `platform/labeling/__init__.py` (module docstring only, mirroring
    `honesty/__init__.py`) and `platform/labeling/jump_model.py` with `from __future__ import
    annotations`, `import numpy as np`, module-level `log = logging.getLogger(__name__)`, and a
    `Usage::` docstring block per the cv.py convention.

    Implement `decode_states_dp(d: np.ndarray, lam: float) -> tuple[np.ndarray, float]` copying
    RESEARCH Pattern 1 verbatim: forward pass builds `cost` and `backptr` (T,K) arrays; per
    timestep compute idx1/min1 (cheapest predecessor) and idx2/min2 (second-cheapest, masking
    idx1 to inf) so "stay in idx1" cannot illegally reuse its own value as the jump-from option;
    the flat per-jump penalty `lam` is added once per state change; backtrack from
    argmin(cost[-1]). Keep the inner `for k in range(K)` loop explicit (readable-exact beats
    vectorized at K=5).

    Implement `soft_confidences(d: np.ndarray) -> np.ndarray` per RESEARCH Pattern 4: subtract
    per-row max for numeric stability, exponentiate `-d`, row-normalize. No temperature `/T`
    knob (D-03: temperature-free v1 — do not add one).

    Write the test file first (RED) with `_make_synthetic_monthly(...)` helper, a
    `_brute_force_decode(d, lam)` local reference that enumerates all K^T sequences via
    `itertools.product`, `class TestDPDecodeExact`, and `class TestSoftConfidences`. Confirm the
    tests fail before the implementation exists, then implement to GREEN.
  </action>
  <verify>
    <automated>python3 -m pytest tests/unit/test_platform_labeling.py::TestDPDecodeExact tests/unit/test_platform_labeling.py::TestSoftConfidences -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m pytest tests/unit/test_platform_labeling.py::TestDPDecodeExact -x -q` passes: DP output equals brute-force enumeration (states AND total_cost) across the parametrized (T,K,λ) grid.
    - `TestSoftConfidences` passes: rows sum to 1.0 (pytest.approx), argmax==argmin-distance, shift-invariance holds.
    - `platform/labeling/jump_model.py` and `platform/labeling/__init__.py` both begin with `from __future__ import annotations` (after the module docstring) and jump_model.py has no `import trading_crab_lib.clustering`.
    - No temperature parameter appears in `soft_confidences`.
  </acceptance_criteria>
  <done>decode_states_dp is provably exact against brute force; soft_confidences is row-stochastic and temperature-free.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Multi-restart alternation, warm start, canonicalization + labeling config</name>
  <files>src/trading_crab_lib/platform/labeling/jump_model.py, config/platform_settings.yaml, tests/unit/test_platform_labeling.py</files>
  <read_first>
    - .planning/phases/03-regime-labeling-prediction/03-RESEARCH.md — Pattern 2 (fit_jump_model alternation + multi-restart), Pattern 3 (canonicalize_states economic sort), Pitfall 2 (empty-state freeze), Pitfall 5 (λ = 4 × len(lean_feature_set) derivation).
    - .planning/phases/03-regime-labeling-prediction/03-PATTERNS.md — jump_model.py config-read pattern (cfg.get('labeling', {})), keyword-only args convention, and the "Suggested new YAML block" style-matched to the existing cv: section.
    - config/platform_settings.yaml — existing cv:/holdout:/registry: section comment-header style (lines ~203-227) to match for the new labeling: block; taxonomy: block to confirm trailing_return_1m is in fast tier.
    - src/trading_crab_lib/platform/config.py — confirm `labeling` is NOT added to `_REQUIRED_PLATFORM_SECTIONS` (defensive .get() only).
  </read_first>
  <behavior>
    - standardize_features(X): winsorizes each column to [1%,99%] via pandas clip(lower=quantile(0.01), upper=quantile(0.99)) then StandardScaler; returns a numpy array with zero-mean unit-variance columns (approx) and the fitted column order preserved.
    - fit_jump_model(X, K, lam, *, n_restarts, max_iter, random_state) returns a dict with keys states (len-T int array), centroids ((K,d) array), total_cost (float), restart (int). Determinism: two calls with the same random_state return np.array_equal states.
    - fit_jump_model with K=5 on a synthetic frame that has 5 separable blobs returns exactly 5 distinct states.
    - An alternation iteration where a state has zero assigned timesteps keeps that state's previous centroid (no NaN in centroids). Test: construct/seed a case exercising the freeze path and assert no NaN and len(unique(states)) does not silently collapse via NaN poisoning.
    - canonicalize_states(states, centroids, feature_names) relabels so state indices are ordered by ascending centroid coordinate of trailing_return_1m; applying it twice is idempotent; two permuted-but-equivalent fits canonicalize to the same numbering.
  </behavior>
  <action>
    Add to `jump_model.py`: `standardize_features(X: pd.DataFrame) -> np.ndarray` (winsorize via
    `.clip(lower=X.quantile(0.01), upper=X.quantile(0.99), axis=1)`-style per-column clip then
    `sklearn.preprocessing.StandardScaler().fit_transform`; pandas-only winsorization, no scipy
    per RESEARCH Standard Stack note); `fit_jump_model(X, K, lam, *, n_restarts=10, max_iter=50,
    random_state=42)` copying RESEARCH Pattern 2 — per restart `KMeans(n_clusters=K, n_init=1,
    init="k-means++", random_state=random_state + r)` for warm start, then the alternation loop
    (recompute (T,K) squared distances via broadcasting → `decode_states_dp` → recompute
    centroids as per-state means with the freeze-on-empty guard from Pitfall 2 → break on
    `np.array_equal(states, prev_states)` or max_iter), keeping the lowest-total_cost restart;
    `canonicalize_states(states, centroids, feature_names)` copying Pattern 3 (economic sort on
    `feature_names.index("trailing_return_1m")`; if trailing_return_1m is absent, fall back to
    sorting on centroid column 0 and log a WARNING). Keyword-only optional args after K, lam per
    CLAUDE.md Function Design.

    Add the `labeling:` block to `config/platform_settings.yaml` matching the cv: section
    comment-header style, with keys `K: 5`, `lambda: 52.0`, `n_restarts: 10`,
    `embargo_months: 12`, and a comment documenting λ = 4 × len(lean_feature_set) = 4 × 13
    (RESEARCH Pitfall 5 — re-derive, do not reuse the literal, if the lean set column count
    changes). Do NOT add "labeling" to `_REQUIRED_PLATFORM_SECTIONS`.

    Extend the test file with `class TestFitJumpModel` (K=5 on separable blobs, determinism
    given random_state, empty-state freeze produces no NaN) and `class TestCanonicalize`
    (ordering by trailing_return_1m, idempotence, permutation-stability). Add a `class
    TestLabelingConfig` asserting `load_platform_config()` exposes labeling defaults via .get()
    and that platform config still validates without labeling being a required section.
  </action>
  <verify>
    <automated>python3 -m pytest tests/unit/test_platform_labeling.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m pytest tests/unit/test_platform_labeling.py -x -q` passes all classes.
    - `TestFitJumpModel::test_determinism` proves np.array_equal states across two same-seed calls.
    - `TestFitJumpModel` empty-state test asserts `np.isnan(result["centroids"]).any() == False`.
    - `python3 -c "import yaml; c=yaml.safe_load(open('config/platform_settings.yaml')); assert c['labeling']=={'K':5,'lambda':52.0,'n_restarts':10,'embargo_months':12}, c['labeling']"` exits 0.
    - `python3 -c "from trading_crab_lib.platform.config import _REQUIRED_PLATFORM_SECTIONS as s; assert 'labeling' not in s"` exits 0.
  </acceptance_criteria>
  <done>fit_jump_model produces deterministic canonicalized K=5 labels with warm start and empty-state safety; λ=52/K=5 defaults live in config, read defensively.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checkpointed parquet → in-process labeler | Untrusted-only in the sense of malformed/degenerate numeric data; no network, no auth, single-user local batch compute |
| git-tracked config → fit parameters | λ/K/n_restarts are human-edited YAML consumed by fit_jump_model |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Denial of Service | fit_jump_model centroid recompute | medium | mitigate | Freeze-on-empty guard (RESEARCH Pitfall 2): an empty state keeps its previous centroid rather than producing NaN that poisons every subsequent DP iteration; asserted by the empty-state test |
| T-03-02 | Tampering | labeling.{K,lambda,n_restarts} via cfg.get() | low | accept | Read defensively with sane defaults (K=5, λ=52); no schema validation added per CONTEXT "don't extend _REQUIRED_PLATFORM_SECTIONS" — malformed values surface as a fit-time TypeError/ValueError, an accepted v1 tradeoff (02-01 precedent) |
| T-03-03 | Information Disclosure | labeler batch fit mistaken for causal | low | accept | The DP is globally-optimizing (non-causal at batch level) BY DESIGN (D4/D5 two-stage split); documented in the module docstring so no downstream consumer gates the labeler input through assert_causal_features expecting failure |
| T-03-SC | Tampering | pip installs | low | accept | Zero new packages this phase (numpy/pandas/sklearn already installed) — Package Legitimacy Gate not applicable; no [ASSUMED]/[SUS] packages to verify |
</threat_model>

<verification>
- `python3 -m pytest tests/unit/test_platform_labeling.py -x -q` green.
- Full platform suite unaffected: `python3 -m pytest tests/unit/test_platform_*.py -q`.
- Manual/deferred (non-blocking, real-data gap per VALIDATION Manual-Only + STATE blocker):
  once FRED_API_KEY is configured and the real 1962+ `monthly_features` checkpoint exists,
  run `fit_jump_model` on the real lean feature set and eyeball that it returns 5 populated
  states — deferred, does NOT block this plan (develop/test on synthetic frames per Phase 1/2).
</verification>

<success_criteria>
- decode_states_dp is exact (brute-force match); soft_confidences row-stochastic, temperature-free.
- fit_jump_model deterministic, K=5, warm-started, empty-state-safe; canonicalization stable.
- labeling: config section present with documented λ derivation; not a required section.
</success_criteria>

## Artifacts this plan produces

**New files:**
- `src/trading_crab_lib/platform/labeling/__init__.py` (package init, module docstring)
- `src/trading_crab_lib/platform/labeling/jump_model.py`
- `tests/unit/test_platform_labeling.py` (seeded here; extended by Plan 03-02)

**Functions (symbols):**
- `standardize_features(X: pd.DataFrame) -> np.ndarray`
- `decode_states_dp(d: np.ndarray, lam: float) -> tuple[np.ndarray, float]`
- `fit_jump_model(X, K, lam, *, n_restarts=10, max_iter=50, random_state=42) -> dict`
- `canonicalize_states(states, centroids, feature_names) -> tuple[np.ndarray, np.ndarray]`
- `soft_confidences(d: np.ndarray) -> np.ndarray`

**Config keys (new):**
- `labeling.K` (5), `labeling.lambda` (52.0), `labeling.n_restarts` (10), `labeling.embargo_months` (12)

<output>
Create `.planning/phases/03-regime-labeling-prediction/03-01-SUMMARY.md` when done.
</output>
