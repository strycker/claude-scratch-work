---
phase: 03-regime-labeling-prediction
plan: 04
type: tdd
wave: 2
depends_on: ["03-03"]
files_modified:
  - src/trading_crab_lib/platform/prediction/transition_matrix.py
  - tests/unit/test_platform_transition_matrix.py
autonomous: true
requirements: [L2-02]
must_haves:
  truths:
    - "empirical_transition_matrix returns a row-normalized K×K table P(next=j | current=i) whose occupied rows sum to 1.0"
    - "a state that is never a 'from' state produces a 0-sum (or absent) row, not a NaN/ZeroDivision crash"
    - "the matrix is a pure function of the label sequence — no model state, no I/O"
  artifacts:
    - "src/trading_crab_lib/platform/prediction/transition_matrix.py with empirical_transition_matrix"
    - "tests/unit/test_platform_transition_matrix.py"
  key_links:
    - "consumes the labeler's full smoothed state sequence (Plan 03-02 regime_labels) — diagnostic, NOT embargoed (L2-02)"
    - "lives in the prediction/ package created by Plan 03-03 (depends_on for the package __init__.py)"
---

<objective>
Add the L2-02 empirical transition matrix diagnostic: a single pure function
`empirical_transition_matrix(states)` in `platform/prediction/transition_matrix.py` that
returns the row-normalized K×K forward-transition table implied by the label history (design
R3). This is the v1 transition machinery — the feature-conditional TVTP model is explicitly
deferred to v2 (L2-V2-02).

Purpose: Phase 4's allocation and weekly report need to know the forward regime distribution
implied by history. This diagnostic supplies exactly that, computed from the labeler's full
(non-embargoed) smoothed state sequence — it is a descriptive diagnostic, not a training
target, so no embargo applies to it.

Output: `transition_matrix.py` (one function) and its dedicated test file.
</objective>

<execution_context>
@/home/user/claude-scratch-work/.claude/gsd-core/workflows/execute-plan.md
@/home/user/claude-scratch-work/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-regime-labeling-prediction/03-CONTEXT.md
@.planning/phases/03-regime-labeling-prediction/03-RESEARCH.md
@.planning/phases/03-regime-labeling-prediction/03-PATTERNS.md
@src/trading_crab_lib/platform/honesty/gap_lag.py
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Empirical transition matrix (row-normalized K×K crosstab)</name>
  <files>src/trading_crab_lib/platform/prediction/transition_matrix.py, tests/unit/test_platform_transition_matrix.py</files>
  <read_first>
    - .planning/phases/03-regime-labeling-prediction/03-RESEARCH.md — Pattern 8 (empirical_transition_matrix via pd.crosstab of from/to pairs, then counts.div(counts.sum(axis=1), axis=0)); System Architecture Diagram note ("from full labeler output, diagnostic, not embargoed").
    - .planning/phases/03-regime-labeling-prediction/03-PATTERNS.md — transition_matrix.py section (gap_lag.py compute_* analog; plain pure function, full docstring, type hints on param and return; copy Pattern 8 verbatim).
    - platform_design/platform_design.md §5.1 (transition machinery context; R3 — TVTP deferred).
    - src/trading_crab_lib/platform/honesty/gap_lag.py — pure compute_* function style + __main__ self-check footer to mirror.
  </read_first>
  <behavior>
    - empirical_transition_matrix(states) returns a DataFrame indexed by 'from' state, columns 'to' state, values P(next=j | current=i); each occupied row sums to 1.0 (pytest.approx).
    - On a hand-built sequence with known transition counts, the returned probabilities match the hand-computed row-normalized counts.
    - A state that never appears as a 'from' (e.g. only the final label, or absent) does not cause a ZeroDivision/NaN crash — its row is absent or zero-sum by construction; the function returns without raising.
    - Pure function: no file I/O, no checkpoint access, no model state (import-and-call on a plain Series works).
  </behavior>
  <action>
    Create `platform/prediction/transition_matrix.py` with `from __future__ import annotations`,
    `import pandas as pd`, a module docstring `(L2-02, design §5.1/R3)` noting this is the v1
    diagnostic and TVTP is v2. Implement `empirical_transition_matrix(states: pd.Series) ->
    pd.DataFrame` copying RESEARCH Pattern 8: build a from/to pair DataFrame from
    `states.iloc[:-1]` and `states.iloc[1:]`, `pd.crosstab`, then row-normalize via
    `counts.div(counts.sum(axis=1), axis=0)`. Full docstring with type hints on param and return
    (CLAUDE.md). Add a `__main__` synthetic self-check that builds a tiny state Series and prints
    the matrix (no network), mirroring gap_lag.py's footer.

    Write the test file first (RED): `tests/unit/test_platform_transition_matrix.py` with
    `class TestEmpiricalTransitionMatrix` — row-sum-to-1 on a random sequence, hand-computed
    known-count case, and the never-a-from-state no-crash case. Add the
    `if __name__ == "__main__": pytest.main([__file__, "-x", "-q"])` footer.
  </action>
  <verify>
    <automated>python3 -m pytest tests/unit/test_platform_transition_matrix.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m pytest tests/unit/test_platform_transition_matrix.py -x -q` passes.
    - Occupied rows sum to 1.0 (pytest.approx) in the row-sum test.
    - The hand-computed known-count case matches expected probabilities to pytest.approx.
    - The never-a-from-state case returns without raising (no ZeroDivision/NaN crash).
    - `transition_matrix.py` begins with `from __future__ import annotations` and does not import `trading_crab_lib.prediction` (incumbent) or any incumbent module.
  </acceptance_criteria>
  <done>The empirical transition matrix is a pure, row-normalized K×K diagnostic that degrades gracefully on never-observed 'from' states.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| labeler state sequence → transition matrix | Purely descriptive diagnostic input; no training target, no embargo required |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-12 | Denial of Service | empirical_transition_matrix normalization | low | mitigate | A never-observed 'from' state produces a zero-sum/absent row rather than a ZeroDivision/NaN crash; asserted by the never-a-from-state test |
| T-03-13 | Information Disclosure | transition matrix mistaken for a training input | low | accept | It is a descriptive diagnostic over the FULL smoothed label sequence (not embargoed); documented in the module docstring so no consumer treats it as a leak-safe supervised feature (feature-conditional TVTP is v2) |
| T-03-SC | Tampering | pip installs | low | accept | Zero new packages this phase — Package Legitimacy Gate not applicable |
</threat_model>

<verification>
- `python3 -m pytest tests/unit/test_platform_transition_matrix.py -x -q` green.
- Full platform suite: `python3 -m pytest tests/unit/test_platform_*.py -q`.
</verification>

<success_criteria>
- Row-normalized K×K matrix; occupied rows sum to 1.0.
- Graceful on never-observed 'from' states; pure function, no I/O.
</success_criteria>

## Artifacts this plan produces

**New files:**
- `src/trading_crab_lib/platform/prediction/transition_matrix.py`
- `tests/unit/test_platform_transition_matrix.py`

**Functions (symbols):**
- `empirical_transition_matrix(states: pd.Series) -> pd.DataFrame`

<output>
Create `.planning/phases/03-regime-labeling-prediction/03-04-SUMMARY.md` when done.
</output>
