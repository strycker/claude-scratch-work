---
phase: 04-asset-prediction-allocation
plan: 03
type: tdd
wave: 2
depends_on: [04-02]
files_modified:
  - src/trading_crab_lib/platform/allocation/__init__.py
  - src/trading_crab_lib/platform/allocation/hysteresis.py
  - src/trading_crab_lib/platform/allocation/tilt.py
  - tests/unit/test_platform_hysteresis.py
  - tests/unit/test_platform_tilt.py
autonomous: true
requirements: [L4-01]
must_haves:
  truths:
    - "Hysteresis tracks P(currently-HELD regime), NOT argmax(P): a probability path oscillating 0.65↔0.72 NEVER switches active_regime (the headline no-flip invariant of L4-01)"
    - "The active regime only unwinds once its OWN probability drops below 0.40, and only then switches to a competitor that has itself crossed 0.70; otherwise it holds"
    - "vol_target_scale = min(1, target_vol/σ̂_port) — long-only, capped at 1.0 (never levers), degrades to 0.0 on zero/negative portfolio vol; cash absorbs 1 − scale"
    - "hysteresis state is persisted load-BEFORE-save so the state machine reads the prior run's active_regime, not its own just-written value (Pitfall 3)"
  artifacts:
    - "src/trading_crab_lib/platform/allocation/hysteresis.py with load_active_regime, save_active_regime, update_active_regime"
    - "src/trading_crab_lib/platform/allocation/tilt.py with vol_target_scale, portfolio_vol, regime_tilt_weights, vol_targeted_tilt"
    - "tests/unit/test_platform_hysteresis.py (no-flip invariant + cross-below-0.40 switch) and tests/unit/test_platform_tilt.py (scale cap + zero-vol degrade)"
  key_links:
    - "tilt.portfolio_vol imports ewma_vol from platform/assets/vol.py (Plan 02) — no duplicate EWMA math"
    - "hysteresis state persisted via get_platform_checkpoint_manager() checkpoint 'hysteresis_state' (parquet, never hand-rolled JSON)"
    - "act/unwind thresholds and target_vol/halflife read via cfg.get('allocation', {}) defaults matching Plan 01 YAML (0.70/0.40/0.10/6)"
---

<objective>
Build the L4-01 naive vol-targeted regime-tilt allocation with anti-flicker hysteresis bands.
Two correctness-critical modules, both TDD (RED→GREEN): the hysteresis state machine (the
Schmitt-trigger on the HELD regime's own probability, §5.3) and the vol-targeting math
(scale ∝ target_vol/σ̂_port, long-only, cash residual, §6.2/§7). The headline no-flip invariant
— a probability path oscillating 0.65↔0.72 must NOT flip the mix — is the RED gate for
hysteresis; the leverage cap and zero-vol degrade are the RED gate for the scale math.

Purpose: This is where regime probabilities become an actual target portfolio. The tilt comes
from historical regime-conditional stats (Plan 02's returns-by-regime table blended by the
nowcaster's probabilities), never from raw point-forecast optimization (§7 "shrink brutally").
Hysteresis is what keeps Glenn from churning his Fidelity account on every noisy month.

Output: `allocation/hysteresis.py`, `allocation/tilt.py`, and two test files whose invariants
are written FIRST and fail before implementation.

Holdout note (for the verifier): the hysteresis/target-vol thresholds are literature-derived
config DEFAULTS (§5.3 0.7/0.4; D-03 0.10), NOT tuned against post-2020 data — so no trial-registry
logging is required (RESEARCH Pitfall 4). If any future iteration TUNES a threshold against a
computed metric, that becomes an evaluated configuration and MUST be registry-logged.
</objective>

<execution_context>
@/home/user/claude-scratch-work/.claude/gsd-core/workflows/execute-plan.md
@/home/user/claude-scratch-work/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-asset-prediction-allocation/04-CONTEXT.md
@.planning/phases/04-asset-prediction-allocation/04-RESEARCH.md
@.planning/phases/04-asset-prediction-allocation/04-PATTERNS.md
@src/trading_crab_lib/platform/labeling/diagnostics.py
@src/trading_crab_lib/platform/assets/vol.py
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Hysteresis state machine (hysteresis.py) — no-flip invariant, load-before-save</name>
  <files>src/trading_crab_lib/platform/allocation/__init__.py, src/trading_crab_lib/platform/allocation/hysteresis.py, tests/unit/test_platform_hysteresis.py</files>
  <read_first>
    - src/trading_crab_lib/platform/labeling/diagnostics.py (lines ~159-177 — _get_checkpoint_manager + load-before-save ordering, "Pitfall 3")
    - .planning/phases/04-asset-prediction-allocation/04-RESEARCH.md (Pitfall 3 — the EXACT 3-branch state machine; Assumption A1 cold-start rule; Pattern 2 load/save)
    - .planning/phases/04-asset-prediction-allocation/04-CONTEXT.md (carried-forward hysteresis act 0.7 / unwind 0.4; §5.3)
    - src/trading_crab_lib/platform/checkpoints.py (get_platform_checkpoint_manager)
  </read_first>
  <behavior>
    - RED test (headline no-flip): given `prev_active=A` and a sequence of probability dicts where P[A] oscillates 0.65 ↔ 0.72 (and a competitor B sits at ~0.30 ↔ 0.28), `update_active_regime` returns A on EVERY step — the mix never flips.
    - RED test (competitor spike must NOT steal): P[A]=0.45 (still ≥ 0.40) while P[B]=0.72 (≥ act) → returns A, NOT B (Pitfall 3: only unwind once the HELD regime's own P collapses).
    - RED test (unwind + switch): P[A]=0.35 (< 0.40) AND P[B]=0.75 (≥ 0.70) → returns B (switches once).
    - RED test (unwind to neutral): P[A]=0.35 (< 0.40) and no competitor ≥ 0.70 → returns None (cash-heavy neutral posture).
    - RED test (cold start): prev_active=None → returns argmax(P) if max(P) ≥ 0.70, else None.
    - Test (load-before-save): `save_active_regime` then `load_active_regime` round-trips through the checkpoint manager; loading before any save returns None (cold start, FileNotFoundError swallowed).
  </behavior>
  <action>
    Create `allocation/__init__.py` and `allocation/hysteresis.py` with `from __future__ import annotations`.
    Write the tests FIRST (they must fail before implementation). Then implement:
    - `load_active_regime(cm=None) -> int | None` — `cm = cm or get_platform_checkpoint_manager()`;
      `try: state = cm.load("hysteresis_state") except FileNotFoundError: return None`;
      return `int(state["active_regime"].iloc[0])` (None-safe if stored value is null).
    - `save_active_regime(active_regime: int | None, cm=None) -> None` — save a one-row DataFrame
      `[{"active_regime": active_regime}]` under `"hysteresis_state"`.
    - `update_active_regime(probs, prev_active, *, act_threshold=0.70, unwind_threshold=0.40) -> int | None`
      — PURE function implementing exactly the Pitfall-3 branches:
        1. cold start (`prev_active is None`): `argmax(probs)` if `max(probs) >= act_threshold` else None.
        2. if `probs[prev_active] >= unwind_threshold`: return `prev_active` unchanged (HOLD), regardless
           of any other regime's probability.
        3. else (`probs[prev_active] < unwind_threshold`): among regimes with `probs[k] >= act_threshold`,
           switch to the argmax of those; if none qualify, return None (neutral/cash-heavy).
      Accept `probs` as a `pd.Series` or dict keyed by regime id. Document the A1 cold-start assumption
      in the docstring (flag for report visibility).
    Thresholds default in code to the Plan-01 YAML values (0.70/0.40); the orchestration layer passes
    `cfg.get("allocation", {}).get("hysteresis", {})` overrides.
  </action>
  <verify>
    <automated>python3 -m pytest tests/unit/test_platform_hysteresis.py -x -q</automated>
  </verify>
  <done>
    All hysteresis tests green; the 0.65↔0.72 oscillation never flips active_regime; the below-0.40 +
    competitor-≥0.70 case switches exactly once; load-before-save round-trips.
  </done>
  <acceptance_criteria>
    - `update_active_regime` returns `prev_active` whenever `probs[prev_active] >= 0.40`, even if a competitor is ≥ 0.70 (no-flip invariant test passes).
    - Switches to competitor B only when `probs[prev_active] < 0.40` AND `probs[B] >= 0.70`; returns None when active collapses and no competitor qualifies.
    - Cold start returns argmax only if `max(probs) >= 0.70`, else None.
    - `load_active_regime` returns None on missing checkpoint (FileNotFoundError caught, no bare except); `save`→`load` round-trips through checkpoint `"hysteresis_state"`.
    - `update_active_regime` is a pure function (no checkpoint I/O inside it — grep: no `cm.load`/`cm.save` in its body).
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Vol-targeted tilt (tilt.py) — scale cap, cash residual, naive regime tilt</name>
  <files>src/trading_crab_lib/platform/allocation/tilt.py, tests/unit/test_platform_tilt.py</files>
  <read_first>
    - src/trading_crab_lib/platform/assets/vol.py (Plan 02 — import ewma_vol; do NOT re-implement EWMA)
    - .planning/phases/04-asset-prediction-allocation/04-RESEARCH.md (Pattern 5 portfolio_vol + ponytail fallback; "Vol-targeted position scale" example; §6.2/§7)
    - .planning/phases/04-asset-prediction-allocation/04-PATTERNS.md (tilt.py section — vol_target_scale/portfolio_vol verbatim + ponytail comment)
    - .planning/phases/04-asset-prediction-allocation/04-CONTEXT.md (D-03 target_vol=0.10, long-only, no leverage, cash residual; §16.7 cash sleeve FZFXX/SPAXX; Claude's-discretion tilt construction)
  </read_first>
  <behavior>
    - RED test: `vol_target_scale(0.10, 0.20) == 0.5`; `vol_target_scale(0.10, 0.05) == 1.0` (capped, never levers); `vol_target_scale(0.10, 0.0) == 0.0` and `vol_target_scale(0.10, -1.0) == 0.0` (degrade to all-cash, no divide-by-zero).
    - RED test: `portfolio_vol` with ≥ min_obs joint history uses the blended-return EWMA path; with too few overlapping non-NaN months falls back to the conservative linear sum Σ wᵢ·σ̂ᵢ (which over-estimates, so it under-levers — safe).
    - Test: `regime_tilt_weights` clips negative Sharpe-rank weights to ≥ 0, sums to 1.0 (before scaling), and is deterministic given inputs.
    - Test: `vol_targeted_tilt` output weights are all ≥ 0, sum of asset weights == scale (≤ 1.0), and `cash == 1 − scale` (cash absorbs the residual).
  </behavior>
  <action>
    Create `allocation/tilt.py` with `from __future__ import annotations`; `from trading_crab_lib.platform.assets.vol import ewma_vol`. Write tests FIRST. Implement:
    - `vol_target_scale(target_vol_annual: float, portfolio_vol_annual: float) -> float` — `0.0` if
      `portfolio_vol_annual <= 0` else `min(1.0, target_vol_annual / portfolio_vol_annual)` (D-03).
    - `portfolio_vol(weights, asset_returns, *, halflife, min_obs=12) -> float` — the Pattern-5
      naive-no-covariance approach: if `len(aligned) >= min_obs`, EWMA-vol the blended weighted-return
      series; else the ponytail linear-sum fallback. Carry the exact `# ponytail:` comment naming the
      ceiling (ignores diversification → only ever MORE conservative) and the upgrade path
      (regime-conditional Ledoit-Wolf, L3-V2-01).
    - `regime_tilt_weights(regime, returns_by_regime, probs, *, min_obs_flag=6) -> pd.Series` — the
      naive, transparent, documented tilt (Claude's discretion): rank assets by within-regime
      annualized Sharpe for the active/blended regime, clip weights ≥ 0, normalize to sum 1; blend
      across regimes by the nowcaster `probs` (probability-weighted average of per-regime tilts).
      Document the construction in the docstring (it surfaces in the weekly report, Plan 05).
    - `vol_targeted_tilt(regime_or_probs, returns_by_regime, asset_returns, *, target_vol_annual=0.10,
      halflife, min_obs=12) -> dict` — assemble base tilt → compute portfolio_vol → scale →
      return `{"weights": scaled_asset_weights, "cash": 1 - scale, "scale": scale, "portfolio_vol": σ̂}`.
    Long-only, no-leverage everywhere; the scale cap at 1.0 is the leverage guard.
  </action>
  <verify>
    <automated>python3 -m pytest tests/unit/test_platform_tilt.py -x -q && python3 -m pytest tests/unit/test_platform_hysteresis.py tests/unit/test_platform_tilt.py -q</automated>
  </verify>
  <done>
    All tilt tests green; scale caps at 1.0 and degrades to 0.0 on non-positive vol; cash absorbs
    1 − scale; portfolio_vol falls back conservatively on short joint history.
  </done>
  <acceptance_criteria>
    - `vol_target_scale`: `(0.10,0.20)→0.5`, `(0.10,0.05)→1.0`, `(0.10,0.0)→0.0`, `(0.10,-1.0)→0.0`.
    - `portfolio_vol` imports `ewma_vol` from `platform/assets/vol.py` (grep confirms; no local EWMA loop) and carries the `# ponytail:` ceiling comment.
    - `vol_targeted_tilt` returns asset weights ≥ 0 summing to `scale`, with `cash == 1 − scale`; total (assets + cash) == 1.0 within tolerance.
    - `regime_tilt_weights` clips negatives to ≥ 0 and is deterministic.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nowcaster probs → allocation | Probabilities decide the tilt; a wrong hysteresis branch churns real money |
| persisted state → next run | The `hysteresis_state` checkpoint carries the acted-on regime across runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-07 | Tampering | hysteresis switching logic (argmax vs P(held)) | high | mitigate | The no-flip invariant test is the RED gate — an argmax mis-implementation fails the 0.65↔0.72 oscillation test before merge (Pitfall 3); `update_active_regime` is a pure, fully-tested function |
| T-04-08 | Tampering | leverage via scale > 1.0 | high | mitigate | `vol_target_scale` hard-caps at `min(1.0, …)` and returns 0.0 on non-positive vol; RED test asserts the cap and the zero-vol degrade — no code path can lever |
| T-04-09 | Repudiation | thresholds tuned off holdout data | medium | mitigate | v1 thresholds are literature-derived config defaults, NOT tuned (documented in objective + docstrings); a future tuning path must registry-log — flagged explicitly so the verifier can enforce it |
| T-04-10 | Tampering | stale/self-read hysteresis state | medium | mitigate | Load-before-save ordering (Pattern 2, diagnostics.py Pitfall 3) — the state machine reads the prior run's value, never its own just-written one; round-trip test covers it |
| T-04-SC | Tampering | pip/npm/cargo installs | low | accept | No new dependencies — no install step, no supply-chain surface |
</threat_model>

## Artifacts this plan produces

- **New package:** `src/trading_crab_lib/platform/allocation/` (`__init__.py`).
- **New module `allocation/hysteresis.py`:** `load_active_regime`, `save_active_regime`, `update_active_regime`.
- **New module `allocation/tilt.py`:** `vol_target_scale`, `portfolio_vol`, `regime_tilt_weights`, `vol_targeted_tilt`.
- **New checkpoint name:** `hysteresis_state`.
- **New tests:** `tests/unit/test_platform_hysteresis.py`, `tests/unit/test_platform_tilt.py`.

<verification>
- `python3 -m pytest tests/unit/test_platform_hysteresis.py tests/unit/test_platform_tilt.py -x -q` green.
- No-flip invariant and leverage-cap invariant both pass.
- Full platform suite green: `python3 -m pytest tests/unit/test_platform_*.py -q`.
</verification>

<success_criteria>
A naive vol-targeted regime-tilt allocation (L4-01) exists with a tested Schmitt-trigger
hysteresis state machine (no-flip invariant), a leverage-capped scale, and a cash residual —
consuming Plan 02's returns/vol and the nowcaster probabilities, persisting its state
load-before-save.
</success_criteria>

<output>
Create `.planning/phases/04-asset-prediction-allocation/04-03-SUMMARY.md` when done.
</output>
