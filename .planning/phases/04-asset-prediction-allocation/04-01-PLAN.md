---
phase: 04-asset-prediction-allocation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/platform_settings.yaml
  - src/trading_crab_lib/platform/ingestion/macro_daily.py
  - docs/splicing_rules.md
  - tests/unit/test_platform_macro_daily.py
autonomous: true
requirements: [L4-04]
user_setup:
  - service: FRED
    why: "Live DAAA/DBAA daily-credit fetch (deferred human item — synthetic tests cover logic)"
    env_vars:
      - name: FRED_API_KEY
        source: "fred.stlouisfed.org/docs/api/api_key.html (already used by incumbent)"
must_haves:
  truths:
    - "SPY is in universe.satellites so the existing fetch_universe_prices() path ingests it into daily_raw (Pitfall 1 closed — tripwire drawdown-from-peak has a real daily SPY series)"
    - "fetch_fred_daily(cfg) pulls DAAA/DBAA at native daily frequency (no resample) and persists them for the tripwire's credit-spread-velocity signal (Pitfall 2 closed)"
    - "allocation:, tripwire:, and report: config sections exist with documented literature-derived defaults, read defensively via cfg.get() (never added to _REQUIRED_PLATFORM_SECTIONS)"
  artifacts:
    - "config/platform_settings.yaml with SPY in universe.satellites, a fred_daily: block, and allocation:/tripwire:/report: sections"
    - "src/trading_crab_lib/platform/ingestion/macro_daily.py with _fetch_fred_daily, assemble_fred_daily, fetch_fred_daily, report/persist + __main__ self-check"
    - "tests/unit/test_platform_macro_daily.py (mocked-Fred assembly + graceful-failure + no-resample invariant)"
  key_links:
    - "macro_daily.fetch_fred_daily reads cfg['fred_daily']['series'] (mirrors fred_monthly.series shape) and persists checkpoint 'fred_daily_raw' in the platform namespace"
    - "SPY reaches daily_raw via prices_daily.universe_fetch_tickers = union(satellites, holdings, watchlist) − no_price_ingest — no edit to the frozen prices_daily.py needed"
---

<objective>
Close the two data gaps RESEARCH found by tracing the real data flow, and lay down the
config surface the rest of Phase 4 reads. (1) Add `SPY` to `universe.satellites` so the
already-built daily price path ingests it (the tripwire's drawdown-from-peak signal has no
daily SPY series today — Pitfall 1). (2) Add a `fred_daily:` block plus a new
`macro_daily.py` that fetches `DAAA`/`DBAA` at native daily frequency (FRED's `BAA`/`AAA` are
monthly-only — Pitfall 2). (3) Add `allocation:`, `tripwire:`, and `report:` config sections
with documented defaults (D-03 target_vol=0.10; D-04 tripwire thresholds; hysteresis 0.7/0.4)
so downstream plans read them via `cfg.get()` without any plan re-editing the YAML.

Purpose: These are cheap, additive, non-breaking changes that unblock L4-04's *daily cadence*
claim. Doing them in Wave 1 avoids a late-phase surprise when `daily_raw` turns out not to
contain what the tripwire needs. This plan owns EVERY edit to `config/platform_settings.yaml`
for the phase, keeping the YAML single-owner and letting Plans 02-05 stay YAML-free and parallel.

Output: config additions, `macro_daily.py`, a `docs/splicing_rules.md` note that SPY is now
dual-purpose plus the Fidelity-CSV placeholder seam, and a mocked-network test file.
</objective>

<execution_context>
@/home/user/claude-scratch-work/.claude/gsd-core/workflows/execute-plan.md
@/home/user/claude-scratch-work/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-asset-prediction-allocation/04-CONTEXT.md
@.planning/phases/04-asset-prediction-allocation/04-RESEARCH.md
@.planning/phases/04-asset-prediction-allocation/04-PATTERNS.md
@src/trading_crab_lib/platform/ingestion/macro_monthly.py
@src/trading_crab_lib/platform/ingestion/prices_daily.py
@config/platform_settings.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SPY + fred_daily + allocation/tripwire/report config sections</name>
  <files>config/platform_settings.yaml</files>
  <read_first>
    - config/platform_settings.yaml (existing universe.satellites list ~line 137; fred_monthly.series block ~lines 20-48 is the template for fred_daily)
    - .planning/phases/04-asset-prediction-allocation/04-RESEARCH.md (Pitfall 1, Pitfall 2, Assumptions A2/A3 for the documented threshold rationale)
    - .planning/phases/04-asset-prediction-allocation/04-CONTEXT.md (D-03 target_vol=0.10; D-04 tripwire trio; carried-forward hysteresis 0.7/0.4)
    - src/trading_crab_lib/platform/config.py (_REQUIRED_PLATFORM_SECTIONS — the new sections MUST NOT be added to it)
  </read_first>
  <action>
    Edit `config/platform_settings.yaml` (scoped Edits only; never Write the whole file):
    (a) Add `SPY` as the first entry under `universe.satellites` with an inline comment noting it
        is dual-purpose — tradable equities proxy (already `splice.equities.tradable: SPY`) AND
        the Phase 4 tripwire drawdown-from-peak input (Pitfall 1).
    (b) Add a top-level `fred_daily:` block mirroring the `fred_monthly.series` shape, with
        `DAAA` (name `fred_daaa`, shift false) and `DBAA` (name `fred_dbaa`, shift false), and a
        header comment citing FRED daily-frequency series (Pitfall 2, L4-04 credit family).
    (c) Add three top-level sections with documented, literature-derived defaults (per D-03/D-04
        and RESEARCH A2/A3):
        `allocation:` — `target_vol_annual: 0.10` (D-03), `ewma_halflife_months: 6`
        (A2 discretion: faster than RiskMetrics' ~22.8mo monthly halflife), `portfolio_vol_min_obs: 12`,
        and nested `hysteresis:` `act_threshold: 0.70` / `unwind_threshold: 0.40` (§5.3, carried-forward).
        `tripwire:` — `vol_halflife_days: 11.2` (RiskMetrics λ=0.94), `vol_spike_short_window_days: 21`,
        `vol_spike_baseline_days: 63`, `vol_spike_ratio: 1.5`, `credit_velocity_lookback_days: 5`,
        `credit_velocity_bps: 25`, `spy_drawdown_pct: 0.10` — each with a comment flagging the
        numeric thresholds as provisional-until-Phase-5-backtest (A3).
        `report:` — `trade_threshold_pct: 0.03` (flat no-trade band; §21 full band math is v2/L4-V2-01),
        `min_obs_flag: 6` (returns-by-regime low-confidence flag, D11 NULL-tolerance).
    Do NOT add `allocation`/`tripwire`/`report`/`fred_daily` to `_REQUIRED_PLATFORM_SECTIONS` in
    config.py — they are optional, defaulted sections read via `cfg.get()` (RESEARCH explicit constraint).
  </action>
  <verify>
    <automated>python3 -c "from trading_crab_lib.platform.config import load_platform_config as L; c=L(); assert 'SPY' in c['universe']['satellites']; assert 'DAAA' in c['fred_daily']['series'] and 'DBAA' in c['fred_daily']['series']; assert c['allocation']['target_vol_annual']==0.10; assert c['allocation']['hysteresis']['act_threshold']==0.70 and c['allocation']['hysteresis']['unwind_threshold']==0.40; assert set(['vol_spike_ratio','credit_velocity_bps','spy_drawdown_pct']) <= set(c['tripwire']); assert c['report']['trade_threshold_pct']==0.03; print('config OK')"</automated>
  </verify>
  <done>
    load_platform_config() returns SPY in satellites, a fred_daily block with DAAA+DBAA, and
    allocation/tripwire/report sections with the exact documented defaults; validate_platform_config
    still passes (no new required section).
  </done>
  <acceptance_criteria>
    - `universe.satellites` contains `SPY`; `fred_daily.series` contains `DAAA` and `DBAA` (both `shift: false`).
    - `allocation.target_vol_annual == 0.10`, `allocation.hysteresis.act_threshold == 0.70`, `allocation.hysteresis.unwind_threshold == 0.40`.
    - `tripwire` has keys `vol_halflife_days`, `vol_spike_short_window_days`, `vol_spike_baseline_days`, `vol_spike_ratio`, `credit_velocity_lookback_days`, `credit_velocity_bps`, `spy_drawdown_pct`.
    - `report.trade_threshold_pct == 0.03`, `report.min_obs_flag == 6`.
    - `_REQUIRED_PLATFORM_SECTIONS` in config.py is unchanged (verify by grep: the list still has exactly 6 entries).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Daily FRED credit ingestion module (macro_daily.py) + test</name>
  <files>src/trading_crab_lib/platform/ingestion/macro_daily.py, tests/unit/test_platform_macro_daily.py</files>
  <read_first>
    - src/trading_crab_lib/platform/ingestion/macro_monthly.py (lines 33-158 — _fetch_fred_monthly client construction, ThreadPoolExecutor + try/except-WARNING skeleton, pd.concat axis=1 outer-join assembly)
    - src/trading_crab_lib/platform/ingestion/prices_daily.py (no-internal-resample doctrine — the daily analog to mirror)
    - src/trading_crab_lib/platform/checkpoints.py (get_platform_checkpoint_manager)
    - .planning/phases/04-asset-prediction-allocation/04-PATTERNS.md (macro_daily.py section — exact-shape analog, no-resample)
    - src/trading_crab_lib/platform/honesty/gap_lag.py (module-shape template: compute + report/persist + __main__ self-check)
  </read_first>
  <action>
    Create `macro_daily.py` under `platform/ingestion/` with `from __future__ import annotations`.
    Reuse `macro_monthly.py`'s Fred client construction and try/except-WARNING skeleton verbatim,
    MINUS the resample (DAAA/DBAA are already daily):
    - `_fetch_fred_daily(fred, series_id, start, end) -> pd.Series` — `fred.get_series(...)` with NO
      `.resample()` call (native daily preserved; mirrors prices_daily's no-resample rule).
    - `assemble_fred_daily(series_map: dict[str, pd.Series]) -> pd.DataFrame` — PURE function:
      `pd.concat(list, axis=1)` outer join (NULL-tolerant; never `.join`/`.merge` defaults), columns
      named from each series' configured `name`. This is the unit-testable seam (no network).
    - `fetch_fred_daily(cfg) -> pd.DataFrame` — orchestrator: build `Fred(api_key=cfg['fred_daily'].get('api_key') or cfg['fred_monthly'].get('api_key'))`,
      ThreadPoolExecutor-fetch every `cfg['fred_daily']['series']` entry, WARN-and-skip on per-series
      failure (matches macro_monthly), assemble, and persist via
      `get_platform_checkpoint_manager().save(frame, "fred_daily_raw")`. On total failure, return an
      empty frame with a WARNING (graceful degradation, prices_daily pattern).
    - `report_fred_daily(frame, *, output_dir=None) -> Path` — optional human-readable summary +
      schema-stable parquet (gap_lag.py shape); acceptable to keep minimal.
    - `__main__` self-check: build a synthetic `series_map` (2 fake daily Series, no network), call
      `assemble_fred_daily`, print shape — proves the assembly path without FRED_API_KEY.
    Test file mocks `fredapi.Fred` (or injects a fake via the fetch seam): assert assemble produces a
    2-column daily frame with the configured names; assert a per-series exception is swallowed to a
    WARNING and the surviving series still appears; assert NO resample happened (a sub-monthly synthetic
    index keeps all its rows). This closes Pitfall 2 (L4-04 credit-spread-velocity daily source).
  </action>
  <verify>
    <automated>python3 -m pytest tests/unit/test_platform_macro_daily.py -x -q && python3 -m trading_crab_lib.platform.ingestion.macro_daily</automated>
  </verify>
  <done>
    Tests pass; `python3 -m ...macro_daily` prints its synthetic self-check with no network; the module
    persists to the `fred_daily_raw` platform checkpoint and never resamples daily input.
  </done>
  <acceptance_criteria>
    - `assemble_fred_daily({"a": s1, "b": s2})` returns a DataFrame with columns `["a","b"]` (or the configured `name`s) and preserves a sub-monthly index length (no resample).
    - A raised exception inside one series' fetch is caught, logged at WARNING, and the other series still lands in the frame (grep: `log.warning` present; no bare `except`).
    - `fetch_fred_daily` saves checkpoint name `"fred_daily_raw"` (grep the literal in the source).
    - `python3 -m trading_crab_lib.platform.ingestion.macro_daily` exits 0 with a printed self-check line, no network call.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Document SPY dual-purpose + Fidelity-CSV placeholder seam</name>
  <files>docs/splicing_rules.md</files>
  <read_first>
    - docs/splicing_rules.md (existing equities/tradable section — where to note SPY dual-purpose)
    - .planning/phases/04-asset-prediction-allocation/04-CONTEXT.md (D-01 Fidelity CSV parser documented as placeholder seam only; D-04 tripwire data provenance)
    - .planning/phases/04-asset-prediction-allocation/04-RESEARCH.md (Pitfall 1 how-to-avoid note)
  </read_first>
  <action>
    Append a short note to `docs/splicing_rules.md` (scoped Edit): SPY is now ingested as a
    daily universe ticker AND remains the documented tradable proxy for the `equities` research
    class — the monthly `equities_tr` splice is unchanged and still the modeling series; SPY's
    daily closes exist only to feed the Phase 4 tripwire's drawdown-from-peak signal. Add one line
    recording the DAAA/DBAA daily-credit series as the tripwire's credit-family source. Add the
    Fidelity positions-CSV parser as a documentation-only placeholder seam (v2, L4-V2-05): holdings
    in v1 come exclusively from manual per-account YAML (D-01) — no CSV parsing implemented.
  </action>
  <verify>
    <automated>grep -q "SPY" docs/splicing_rules.md && grep -qi "tripwire" docs/splicing_rules.md && grep -qi "Fidelity" docs/splicing_rules.md && echo "doc OK"</automated>
  </verify>
  <done>
    docs/splicing_rules.md documents SPY's dual purpose, the DAAA/DBAA tripwire credit source, and
    the Fidelity-CSV placeholder seam.
  </done>
  <acceptance_criteria>
    - `docs/splicing_rules.md` mentions SPY as both tradable proxy and tripwire input.
    - The doc records DAAA/DBAA as the daily credit-spread source and the Fidelity-CSV parser as a v2 placeholder (no v1 implementation).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FRED API → platform | Untrusted external time series enter via `macro_daily.fetch_fred_daily` |
| config YAML → all modules | Threshold values cross into decision logic; a wrong sign/inversion has real-money consequences |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Denial of Service | `fetch_fred_daily` network fetch | medium | mitigate | Per-series try/except → WARNING + skip; total failure returns empty frame, never raises (macro_monthly pattern) — a FRED outage cannot crash the daily tripwire pipeline |
| T-04-02 | Tampering | tripwire threshold config values | high | mitigate | Thresholds live in one owned YAML with provisional-value comments (A3); downstream modules read via `cfg.get()` with in-code defaults matching the YAML, so a deleted/garbled section degrades to safe defaults rather than silent mis-escalation. Correctness locked by Plan 04's OR-logic invariant test |
| T-04-03 | Information Disclosure | new daily checkpoints | low | accept | `fred_daily_raw`/`daily_raw` are raw market series (never revised, publicly available) — no holdout carve required (RESEARCH Pitfall 4: these are inputs, not tuned artifacts) |
| T-04-SC | Tampering | pip/npm/cargo installs | low | accept | Zero new dependencies this phase (RESEARCH Package Legitimacy Audit: N/A — `pandas`/`fredapi` already pinned); no install step, no supply-chain surface |
</threat_model>

## Artifacts this plan produces

- **Config keys (config/platform_settings.yaml):** `universe.satellites[SPY]`; `fred_daily.series.{DAAA,DBAA}`; `allocation.{target_vol_annual, ewma_halflife_months, portfolio_vol_min_obs}`; `allocation.hysteresis.{act_threshold, unwind_threshold}`; `tripwire.{vol_halflife_days, vol_spike_short_window_days, vol_spike_baseline_days, vol_spike_ratio, credit_velocity_lookback_days, credit_velocity_bps, spy_drawdown_pct}`; `report.{trade_threshold_pct, min_obs_flag}`.
- **New module:** `src/trading_crab_lib/platform/ingestion/macro_daily.py` with functions `_fetch_fred_daily`, `assemble_fred_daily`, `fetch_fred_daily`, `report_fred_daily`.
- **New checkpoint name:** `fred_daily_raw` (platform namespace).
- **New test:** `tests/unit/test_platform_macro_daily.py`.
- **Docs:** `docs/splicing_rules.md` note (SPY dual-purpose; DAAA/DBAA source; Fidelity-CSV v2 seam).

<verification>
- `python3 -m pytest tests/unit/test_platform_macro_daily.py -x -q` green.
- `load_platform_config()` exposes SPY, fred_daily, and allocation/tripwire/report sections.
- `python3 -m trading_crab_lib.platform.ingestion.macro_daily` runs the synthetic self-check with no network.
- Full suite still green: `python3 -m pytest tests/unit/test_platform_*.py -q`.
</verification>

<success_criteria>
SPY and DAAA/DBAA data gaps are closed (config + ingestion), the allocation/tripwire/report config
surface exists with documented defaults, and no frozen incumbent or Phase-1 file was modified.
</success_criteria>

<output>
Create `.planning/phases/04-asset-prediction-allocation/04-01-SUMMARY.md` when done.
</output>
