---
phase: 06-platform-notebook-suite
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/trading_crab_lib/platform/evaluation/report.py
  - tests/unit/test_platform_evaluation_report.py
  - outputs/reports/platform/backtest_full_sample_states.parquet
  - outputs/reports/platform/backtest_filtered_state_probs.parquet
autonomous: true
requirements: [NB-01]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The smoothed reference labeling and the walk-forward's per-date filtered state probabilities survive the end of run_full_backtest_evaluation() as parquet artifacts, so P3 and P6 load them instead of re-running a multi-minute walk-forward."
    - "Every artifact Phase 5 already produced is byte-for-byte unchanged after this plan lands."
    - "The persisted filtered-probability artifact reloads into the exact shape compute_sojourn_lag_headline consumes, so the headline can be recomputed without the backtest."
    - statement: "A full re-run of run_full_backtest_evaluation() writes the two new artifacts and reproduces the current reference numbers (strategy terminal log wealth 4.0265, max drawdown -21.24%, Brier 0.2087)."
      verification: backstop
  artifacts:
    - outputs/reports/platform/backtest_full_sample_states.parquet
    - outputs/reports/platform/backtest_filtered_state_probs.parquet
  key_links:
    - "run_full_backtest_evaluation()'s existing artifacts dict at the write_backtest_report() call site is the single place artifact persistence happens; the two new keys join it there and write_backtest_report() itself is unchanged."
    - "full_sample_states is already computed in-function from the ONE full-sample fit_jump_model over ref_cols; filtered_probs_matrix is already computed in-function by build_filtered_probs_matrix(per_step_metrics). Neither is recomputed."
    - "outputs/reports/platform/backtest_filtered_state_probs.parquet is the third A13 ingredient; the other two (the reference labeling, the active-feature-count timeline) are reconstructed cheaply in plan 06-03."
  prohibitions:
    - statement: "No logic, metric, threshold, or hyperparameter inside report.py changes — the edit is additive persistence only."
      status: pending
      verification: "Byte-identity of backtest_report.md, backtest_equity_curve_strategy.parquet, backtest_equity_curve_ablation.parquet, backtest_kpi_table.parquet, model_metrics_brier.parquet, model_metrics_calibration.parquet, model_metrics_confusion.parquet across a re-run."
    - statement: "write_backtest_report()'s signature, target_dir logic, and to_parquet call are not modified."
      status: pending
      verification: flagged-unverified
    - statement: "No existing outputs/reports/platform/ file is overwritten in place; the regeneration runs into a scratch directory first."
      status: pending
      verification: flagged-unverified
    - statement: "This plan does not resolve audit item A13 and no artifact, docstring, or test claims that it does."
      status: pending
      verification: flagged-unverified
---

<objective>
Persist the one A13 ingredient that exists nowhere on disk: the walk-forward's per-date filtered
state probabilities, alongside the full-sample smoothed reference labeling they are compared
against. Both are already computed inside `run_full_backtest_evaluation()`; this plan passes them
to the function that already writes evaluation artifacts.

Purpose: CONTEXT.md Amendment 2 item F makes the two-labeling side-by-side a stated P3
requirement, and Amendment 3 item H settles how its expensive ingredient is sourced. Without this,
P3 would have to run a multi-minute backtest to draw a labeling chart, or the comparison would
have to be deferred out of P3 entirely. Written once, read twice — P3 for the A13 panel, P6 for
the sojourn/lag headline and its resolved-transition count.

Output: two new parquet artifacts under `outputs/reports/platform/`, produced by an additive
change to a single dict literal in Phase-5-verified code, with byte-identity of every pre-existing
artifact proven rather than asserted in prose.

This does NOT resolve A13. It builds the surface on which A13 can be judged.
</objective>

<execution_context>
@/Users/glestryc/personal/github_repos/claude-scratch-work/.claude/gsd-core/workflows/execute-plan.md
@/Users/glestryc/personal/github_repos/claude-scratch-work/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-platform-notebook-suite/06-CONTEXT.md
@.planning/phases/06-platform-notebook-suite/06-PATTERNS.md
@.planning/BASELINE-v1-tracer-bullet.md
@src/trading_crab_lib/platform/evaluation/report.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add full_sample_states and filtered_state_probs to the existing artifacts dict</name>
  <files>src/trading_crab_lib/platform/evaluation/report.py, tests/unit/test_platform_evaluation_report.py</files>
  <read_first>
    - `src/trading_crab_lib/platform/evaluation/report.py` lines 306-345 — `write_backtest_report(markdown, artifacts, *, output_dir)`, which turns each dict key into `target_dir / f"backtest_{name}.parquet"` via `df.to_parquet(artifact_path, index=True)`. This function must not change.
    - `src/trading_crab_lib/platform/evaluation/report.py` lines 460-520 — `run_full_backtest_evaluation`'s docstring, including the Returns block that already lists `full_sample_states`.
    - `src/trading_crab_lib/platform/evaluation/report.py` lines 580-620 — where `full_sample_states` and `filtered_probs_matrix` are computed. `full_sample_states` is a `pd.Series` named `state` indexed by `X_df.index`; `filtered_probs_matrix` comes from `build_filtered_probs_matrix(per_step_metrics)` and is indexed by decision date with one integer-named column per observed state.
    - `src/trading_crab_lib/platform/evaluation/report.py` lines 655-682 — the `artifacts` dict literal passed to `write_backtest_report` and the function's return dict.
    - `src/trading_crab_lib/platform/evaluation/sojourn_lag.py` lines 54-96 — `build_filtered_probs_matrix`'s return contract, and lines 97-196 — `compute_sojourn_lag_headline`, which looks columns up by INTEGER state id (`if target_state not in filtered_probs_matrix.columns`). This is why the persisted column names must be reconvertible.
    - `tests/unit/test_platform_evaluation_report.py` lines 120-175 — `TestArtifactsWritten`, which calls `write_backtest_report` with its own artifacts dict and therefore is unaffected by a caller-side change.
    - `06-CONTEXT.md` AMENDMENT 3 item H — the exact boundary of the sanctioned edit.
    - `06-PATTERNS.md` section "platform/evaluation/report.py (MODIFIED — additive only, Amendment 3 item H)".
  </read_first>
  <action>
In `run_full_backtest_evaluation`, immediately before the existing `write_backtest_report(...)`
call, build two frames from variables that already exist in that scope. First,
`full_sample_states.to_frame()` — the Series is already named `state`, so the frame carries a
single `state` column indexed by the reference labeling's dates. Second, a parquet-safe copy of
`filtered_probs_matrix` whose integer state-id columns are renamed to the string form
`state_{k}`; do this with a dict comprehension over the existing columns so the column ORDER and
the underlying float values are untouched, and do not reindex, sort, fill, or round anything.

Add exactly two keys to the existing `artifacts` dict literal passed to `write_backtest_report`:
`full_sample_states` mapped to the first frame and `filtered_state_probs` mapped to the second.
Leave the three existing keys `equity_curve_strategy`, `equity_curve_ablation`, and `kpi_table`
in place, in their current order, mapped to their current values. Do not modify
`write_backtest_report` itself — not its signature, not its `target_dir` resolution, not its
`df.to_parquet(artifact_path, index=True)` call. Do not touch `assemble_backtest_report`,
`_build_kpi_table`, `_leg_kpis`, `_smoothed_hindsight_perf`, `_reference_label_columns`, or
`report_model_metrics`. Do not add, remove, or reorder any key in the function's RETURN dict.

Update `run_full_backtest_evaluation`'s docstring in two places: extend step (h) to say that the
smoothed reference states and the per-date filtered state probabilities are now persisted
alongside the equity curves and KPI table, and add a short paragraph naming the two written
filenames `backtest_full_sample_states.parquet` and `backtest_filtered_state_probs.parquet`,
stating that the filtered artifact's columns are stored as `state_{k}` strings for parquet
safety and must be converted back to integer state ids before being passed to
`compute_sojourn_lag_headline`, and stating that these two artifacts exist to let the regime and
evaluation notebooks render the two-labeling comparison without re-running the walk-forward.
State in that paragraph that persisting them does not resolve audit item A13 — it makes A13
inspectable.

In `tests/unit/test_platform_evaluation_report.py`, add a `TestNewLabelingArtifacts` class that
exercises `write_backtest_report` directly with a five-key artifacts dict shaped like the new
caller-side dict — three existing keys plus a one-column `state` frame and a `state_{k}`-columned
probability frame — writing into `tmp_path`, and asserts: all five parquet files appear with the
`backtest_` prefix; the states frame round-trips through `pd.read_parquet` with its DatetimeIndex
and integer `state` values intact; and the probability frame round-trips with `state_{k}` string
columns whose values sum to 1.0 per row within 1e-9. Add a second test that renames those string
columns back to integers and feeds the result plus the states Series to
`compute_sojourn_lag_headline`, asserting the returned dict carries the keys `median_sojourn`,
`median_lag`, `ratio`, `n_transitions`, `n_resolved`, `act_threshold` — this is the round trip P6
depends on. Keep both tests synthetic and network-free; do not read any real artifact from
`outputs/`.
  </action>
  <reversibility rating="costly">This modifies Phase-5-verified code that produced the project's first trustworthy baseline. The edit is additive and a `git revert` restores it, but two notebooks (P3, P6) come to depend on the artifacts, and regenerating them after a revert costs a multi-minute `run_full_backtest_evaluation()`. It is deliberately NOT rated one-way: Task 2 regenerates into a scratch directory and byte-compares before copying, so no non-version-controlled Phase-5 artifact is ever overwritten in place. That design choice is what removes the irreversible step.</reversibility>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_platform_evaluation_report.py -x -q && .venv/bin/python -m pytest tests/unit/test_platform_evaluation_sojourn_lag.py tests/unit/test_platform_backtest_driver.py tests/unit/test_platform_evaluation_kpis.py tests/unit/test_platform_evaluation_model_metrics.py -q && git diff --stat src/trading_crab_lib/platform/evaluation/report.py</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/unit/test_platform_evaluation_report.py -x -q` exits 0 with the pre-existing `TestArtifactsWritten` tests still passing unmodified.
    - `git diff src/trading_crab_lib/platform/evaluation/report.py` shows changes confined to (a) the two new frame constructions, (b) two added keys in the `artifacts` dict literal, (c) docstring text. It shows no change inside `write_backtest_report`, `assemble_backtest_report`, `_build_kpi_table`, `_leg_kpis`, `_smoothed_hindsight_perf`, `_reference_label_columns`, or the function's return dict.
    - `git diff --numstat src/trading_crab_lib/platform/evaluation/report.py` reports fewer than 40 added lines and 0 lines deleted other than the lines being extended.
    - A test asserts the round-tripped filtered-probability frame's rows sum to 1.0 within 1e-9 and that every value lies in [0, 1].
    - A test asserts `compute_sojourn_lag_headline` accepts the round-tripped pair and returns all six documented keys, with `ratio` finite or NaN and `n_resolved` an integer in [0, n_transitions].
    - `.venv/bin/python -m pytest tests/ -q` reports 0 failures and collects at least 1393 tests.
  </acceptance_criteria>
  <done>Two keys are added to one dict literal, the docstring names both artifacts and states the parquet-safe column convention, and the synthetic round trip into `compute_sojourn_lag_headline` is proven by test.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate into a scratch directory, prove byte-identity, copy only the new artifacts</name>
  <files>outputs/reports/platform/backtest_full_sample_states.parquet, outputs/reports/platform/backtest_filtered_state_probs.parquet</files>
  <read_first>
    - `src/trading_crab_lib/platform/evaluation/report.py` lines 686-706 — `main()`, which loads the config and the `monthly_features` / `monthly_raw` checkpoints and calls `run_full_backtest_evaluation` with no `output_dir` override. This task must instead call `run_full_backtest_evaluation(..., output_dir=<scratch>)` directly so nothing writes into `outputs/reports/platform/`.
    - `src/trading_crab_lib/platform/evaluation/model_metrics.py` `report_model_metrics(per_step_metrics, *, output_dir)` — confirms the three model-metrics artifacts also honour the `output_dir` override, so a scratch run captures all seven files.
    - `.planning/BASELINE-v1-tracer-bullet.md`, section "Current reference run — 2026-09-09" ONLY — the numbers this regeneration must reproduce. Everything from "## Historical" onward is superseded and must not be cited.
    - `06-VALIDATION.md` section "Plausibility bands — the numeric contract".
  </read_first>
  <action>
Run the regeneration as a one-off script under the scratchpad directory, never by invoking
`main()` (which writes straight into `outputs/reports/platform/`). The script loads
`load_platform_config()`, loads `monthly_features` and `monthly_raw` from
`get_platform_checkpoint_manager()`, and calls `run_full_backtest_evaluation(monthly_features,
monthly_raw, cfg, output_dir=SCRATCH_DIR)` exactly once. Expect a multi-minute run; the
walk-forward is 588 expanding steps of sklearn work.

Then compare, before copying anything: for each of the seven filenames already present in
`outputs/reports/platform/` — `backtest_report.md`, `backtest_equity_curve_strategy.parquet`,
`backtest_equity_curve_ablation.parquet`, `backtest_kpi_table.parquet`,
`model_metrics_brier.parquet`, `model_metrics_calibration.parquet`,
`model_metrics_confusion.parquet` — compute the SHA-256 of the scratch copy and of the live copy
and require them equal. Parquet writes in this environment were confirmed byte-deterministic for
the same frame, so a mismatch means the run is not reproducing Phase 5 and must be investigated,
not worked around. If any hash differs, stop and report which file and what changed, comparing
the two frames with `pandas.testing.assert_frame_equal` to characterise the difference; do not
copy anything and do not modify the live directory.

Only after all seven hashes match, copy `backtest_full_sample_states.parquet` and
`backtest_filtered_state_probs.parquet` from the scratch directory into
`outputs/reports/platform/`. Copy nothing else. The live directory must end with nine files, the
original seven untouched plus these two.

Finally, validate the two new artifacts against stated bands. The states artifact must carry a
single `state` column of integers drawn from the 5 canonical ids 0 through 4, over roughly 695
monthly rows starting in 1963 and ending on or before the holdout cutoff 2020-12-31, and its
occupancy shares must sum to 1.0 within 1e-9 and reproduce the current reference profile
1.6 / 14.0 / 31.9 / 40.6 / 11.9 percent within 0.5 percentage points per state. The probability
artifact must have 588 rows matching the walk-forward decision dates, five `state_{k}` columns,
every value in [0, 1], and every row summing to 1.0 within 1e-9. Recompute the headline from the
two artifacts via `compute_sojourn_lag_headline` and require it to reproduce the current
reference exactly: median sojourn 97.0 months, median detection lag 164.0 months, ratio 0.5914
within 1e-3, 4 resolved of 6 transitions. Record every one of these observed values in the plan
SUMMARY, and record alongside them that the ratio remains labelled not interpretable pending
audit item A13 — regenerating the inputs does not settle it.

`outputs/` is gitignored, so these two artifacts are build products, not committed files. Note
that in the SUMMARY together with the exact command an operator runs to reproduce them.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import pandas as pd, numpy as np
from pathlib import Path
d = Path('outputs/reports/platform')
s = pd.read_parquet(d / 'backtest_full_sample_states.parquet')
p = pd.read_parquet(d / 'backtest_filtered_state_probs.parquet')
assert list(s.columns) == ['state'], s.columns
assert set(int(v) for v in s['state'].unique()) <= {0,1,2,3,4}, s['state'].unique()
assert 650 <= len(s) <= 720, len(s)
assert s.index.max() <= pd.Timestamp('2020-12-31'), s.index.max()
occ = s['state'].value_counts(normalize=True).sort_index()
assert abs(occ.sum() - 1.0) < 1e-9, occ.sum()
assert len(p) == 588, len(p)
assert list(p.columns) == [f'state_{k}' for k in range(5)], list(p.columns)
assert float(p.to_numpy().min()) >= 0.0 and float(p.to_numpy().max()) <= 1.0
assert np.allclose(p.sum(axis=1).to_numpy(), 1.0, atol=1e-9)
from trading_crab_lib.platform.evaluation.sojourn_lag import compute_sojourn_lag_headline
h = compute_sojourn_lag_headline(s['state'].astype(int), p.rename(columns={f'state_{k}': k for k in range(5)}), act_threshold=0.70)
assert abs(h['median_sojourn'] - 97.0) < 1e-9, h
assert abs(h['median_lag'] - 164.0) < 1e-9, h
assert abs(h['ratio'] - 0.5914634146341463) < 1e-3, h
assert (h['n_resolved'], h['n_transitions']) == (4, 6), h
print('OK', h, dict(occ.round(4)))"</automated>
  </verify>
  <acceptance_criteria>
    - `outputs/reports/platform/` contains exactly nine files: the seven that existed before this plan plus `backtest_full_sample_states.parquet` and `backtest_filtered_state_probs.parquet`.
    - The SHA-256 of each of the seven pre-existing files is unchanged from before the regeneration; the before/after hashes are both recorded in the plan SUMMARY.
    - `backtest_full_sample_states.parquet` has one `state` column, integer values in {0,1,2,3,4}, between 650 and 720 rows, and an index max on or before 2020-12-31.
    - Occupancy shares from the states artifact sum to 1.0 within 1e-9 and match 1.6 / 14.0 / 31.9 / 40.6 / 11.9 percent within 0.5 percentage points per state.
    - `backtest_filtered_state_probs.parquet` has exactly 588 rows and five columns named `state_0` through `state_4`; every value lies in [0, 1] and every row sums to 1.0 within 1e-9.
    - `compute_sojourn_lag_headline` recomputed from the two artifacts returns median sojourn 97.0, median detection lag 164.0, ratio within 1e-3 of 0.5914634146341463, and `n_resolved` / `n_transitions` equal to 4 and 6.
    - The plan SUMMARY records the observed values above and states that the ratio remains not interpretable pending A13.
    - `.venv/bin/python -m pytest tests/ -q` reports 0 failures.
  </acceptance_criteria>
  <done>The two new artifacts exist on disk with values inside their stated bands, the seven pre-existing artifacts are provably byte-identical, and the headline recomputed from the persisted pair reproduces the current reference run exactly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evaluation run -> `outputs/reports/platform/` | This directory is gitignored, so anything overwritten here cannot be recovered from version control; it holds the evidence base for Phase 5's closure record |
| persisted parquet -> notebook consumer | P3 and P6 read these artifacts as trusted inputs; a silently wrong schema or column convention becomes a wrong chart rather than an error |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-06 | Tampering | regenerating evaluation artifacts overwrites non-version-controlled Phase-5 evidence in place | high | mitigate | Task 2 runs `run_full_backtest_evaluation(..., output_dir=<scratch>)` and copies only the two NEW filenames after all seven pre-existing files hash-match; the live directory is never written by the evaluation run |
| T-06-07 | Tampering | an additive edit unintentionally changing evaluation logic, silently altering the reference numbers | high | mitigate | SHA-256 byte-identity of all seven pre-existing artifacts is the acceptance criterion; `git diff` is constrained to the two frame constructions, two dict keys, and docstring text |
| T-06-08 | Information Disclosure | the persisted filtered-probability artifact exposing post-2020 data | low | accept | `run_backtest` applies `split_by_holdout_boundary` before constructing `expanding_steps`, and the reference labeling is fit on `split_by_holdout_boundary(monthly_features)`; both artifacts are structurally bounded at 2020-12-31, asserted by the index-max criterion |
| T-06-09 | Repudiation | a consumer treating the persisted 0.591 ratio as a settled go/no-go number | medium | mitigate | The docstring paragraph and the plan SUMMARY both state the artifacts make A13 inspectable rather than resolved; plans 06-03 and 06-06 render `core.A13_CAVEAT` wherever the ratio appears |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs zero new packages. `06-RESEARCH.md` records the Package Legitimacy Audit as not applicable for this phase. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/ -q` collects at least 1393 tests and reports 0 failures.
- The seven pre-existing `outputs/reports/platform/` files hash-match their pre-plan SHA-256 values.
- `outputs/reports/platform/backtest_full_sample_states.parquet` and `backtest_filtered_state_probs.parquet` exist and satisfy every band in Task 2's acceptance criteria.
- `compute_sojourn_lag_headline` recomputed from the persisted pair reproduces 97.0 / 164.0 / 0.5915 / 4 of 6.
</verification>

<success_criteria>
- `run_full_backtest_evaluation()` persists five backtest artifacts where it previously persisted three, with no change to any of the three.
- P3 and P6 can render the two-labeling comparison and the sojourn/lag headline by loading parquet, without running a walk-forward.
- Nothing in this plan claims A13 is resolved.
</success_criteria>

## Artifacts this phase produces

Symbols and files created by THIS plan (new; exclude from drift verification):

**New artifact filenames**
- `outputs/reports/platform/backtest_full_sample_states.parquet` — one `state` column, integer canonical state ids, DatetimeIndex over the reference labeling's months
- `outputs/reports/platform/backtest_filtered_state_probs.parquet` — five `state_0` .. `state_4` float columns, DatetimeIndex over the 588 walk-forward decision dates

**New test class**
- `tests/unit/test_platform_evaluation_report.py::TestNewLabelingArtifacts`

**New artifact dict keys** (inside the existing `artifacts` literal in `run_full_backtest_evaluation`)
- `full_sample_states`
- `filtered_state_probs`

<output>
Create `.planning/phases/06-platform-notebook-suite/06-02-SUMMARY.md` when done
</output>
