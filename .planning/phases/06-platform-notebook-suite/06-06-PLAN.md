---
phase: 06-platform-notebook-suite
plan: 06
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/trading_crab_lib/platform/plotting/allocation.py
  - notebooks/platform/P5_assets_allocation.ipynb
  - tests/unit/test_platform_plotting_allocation.py
autonomous: true
requirements: [NB-01]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "An operator opening P5 sees regime-conditional Sharpe/return statistics for the SAME 4-asset investable universe (SPY, TLT, IAU, USO) run_backtest actually tilts across, computed via returns_by_regime_stats over a full-sample smoothed labeling — never a hand-rolled parallel statistic."
    - "P5 shows each asset's trailing annualized EWMA vol over the full span, so a vol regime shift (e.g. a persistently higher VIX baseline) is visible by eye."
    - "P5 shows how vol_targeted_tilt's weights evolve month to month when driven by the full-sample smoothed labeling's regime confidences, and every rendered weight row satisfies drift.assert_portfolio_weights_plausible — each weight in [0,1], assets plus cash summing to 1.0."
    - "P5's weights-over-time panel is explicitly labeled as a smoothed/hindsight illustration of the allocation math, distinct from a live weekly recommendation — v1 has no live scoring path yet."
    - "An asset's return distribution whose current-era mean/vol has shifted relative to the pre-2021 fitted window appears in P5's drift table."
    - statement: "P5_assets_allocation.ipynb runs top-to-bottom against the real monthly_raw/monthly_features checkpoints without raising."
      verification: backstop
  artifacts:
    - src/trading_crab_lib/platform/plotting/allocation.py
    - notebooks/platform/P5_assets_allocation.ipynb
    - tests/unit/test_platform_plotting_allocation.py
  key_links:
    - "allocation.investable_asset_returns mirrors evaluation/report.py's asset_returns construction (lines 519-544) exactly — same splice_cfg dispatch, same tradable-ticker renaming, same cash exclusion — so P5's universe is provably the one the real strategy tilts across, not an approximation."
    - "allocation.compute_smoothed_tilt_weights_over_time calls only the public allocation.tilt.vol_targeted_tilt and assets.returns.returns_by_regime_stats — it reimplements no allocation math, mirroring (without importing) report.py's private _smoothed_hindsight_perf orchestration shape."
    - "drift.assert_portfolio_weights_plausible (06-01) is the single source of the weight-band check — P5 does not re-derive the [0,1]/sum-to-1 rule."
  prohibitions:
    - statement: "P5 sweeps no labeling or allocation hyperparameter (K, lambda, n_restarts, target_vol_annual, ewma_halflife_months, hysteresis thresholds) — it renders the shipped config's allocation math for the shipped labeling only (D-14's no-sweep discipline extended to L3/L4)."
      status: pending
      verification: flagged-unverified
    - statement: "P5 calls neither run_backtest() nor run_full_backtest_evaluation() anywhere — the weights-over-time panel is a cheap smoothed-labeling illustration, never the real walk-forward."
      status: pending
      verification: "tests/unit/test_platform_notebooks.py plus a source assertion that no P5 cell references either function name."
    - statement: "P5 introduces no sign-off cell and no per-run gate — that is P3's exclusive responsibility (D-15)."
      status: pending
      verification: "a source assertion in this plan's own test file confirms the notebook contains no 'Sign-Off' heading."
    - statement: "P5 writes no hysteresis_state, returns_by_regime, or any other production checkpoint from a notebook cell — its allocation.py functions are pure (DataFrame in, DataFrame/Figure out), never calling CheckpointManager.save."
      status: pending
      verification: "tests/unit/test_platform_plotting_allocation.py asserts allocation.py contains no CheckpointManager import and calls .save nowhere."
    - statement: "No task in this plan writes to data/checkpoints/platform/ or modifies any outputs/reports/platform/ artifact."
      status: pending
      verification: flagged-unverified
---

<objective>
Build P5 — the L3/L4 assets-and-allocation notebook. It shows the real 4-asset investable
universe's regime-conditional returns, each asset's trailing EWMA vol, and how the naive
vol-targeted regime tilt's weights would have evolved when driven by the full-sample smoothed
labeling — every weight row checked against the exact-band contract (each weight in [0,1], assets
plus cash summing to 1.0).

Purpose: `platform/allocation/tilt.py` and `platform/allocation/hysteresis.py` exist and are
tested in isolation, but nothing renders what they actually produce over real history. P5 is
where an operator sees the allocation layer's own math working (or not) against 59 years of
regime-conditional statistics, without re-running the expensive walk-forward P6 already owns.

Output: a new `platform/plotting/allocation.py` submodule (investable-universe construction,
returns-by-regime heatmap, EWMA vol timeline, smoothed weights-over-time), its test file, and the
committed `P5_assets_allocation.ipynb`.
</objective>

<execution_context>
@/Users/glestryc/personal/github_repos/claude-scratch-work/.claude/gsd-core/workflows/execute-plan.md
@/Users/glestryc/personal/github_repos/claude-scratch-work/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-platform-notebook-suite/06-CONTEXT.md
@.planning/phases/06-platform-notebook-suite/06-PATTERNS.md
@.planning/phases/06-platform-notebook-suite/06-VALIDATION.md
@.planning/BASELINE-v1-tracer-bullet.md
@.planning/phases/06-platform-notebook-suite/06-01-SUMMARY.md
@config/platform_settings.yaml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Investable-universe construction, returns-by-regime heatmap, EWMA vol timeline</name>
  <files>src/trading_crab_lib/platform/plotting/allocation.py, tests/unit/test_platform_plotting_allocation.py</files>
  <read_first>
    - `src/trading_crab_lib/platform/evaluation/report.py` lines 519-544 — the EXACT construction this task's `investable_asset_returns` mirrors: `research = build_core_research_series(monthly_raw, cfg)`; `returns = compute_monthly_returns(research)`; `asset_returns` built as `{params["tradable"]: returns[params["research_name"]] for name, params in splice_cfg.items() if name != "cash" and params["research_name"] in returns.columns}`; `cash_ret = returns[splice_cfg["cash"]["research_name"]]`. Read this as data flow only — do not import this private construction; re-derive it from the public `build_core_research_series`/`compute_monthly_returns` functions.
    - `src/trading_crab_lib/platform/splice.py` — `build_core_research_series(raw, cfg) -> pd.DataFrame` (pure function, no I/O side effects; returns columns `equities_tr`, `long_duration_tr`, `gold`, `oil`, `cash`).
    - `src/trading_crab_lib/platform/assets/returns.py` (full file) — `compute_monthly_returns(monthly_prices) -> pd.DataFrame`; `returns_by_regime_stats(returns, states) -> pd.DataFrame` (columns `regime`, `asset`, `mean_monthly_return`, `std_monthly_return`, `sharpe_annualized`, `hit_rate`, `max_drawdown`, `n_obs`).
    - `src/trading_crab_lib/platform/assets/vol.py` (full file) — `ewma_vol(returns, *, halflife, annualization_factor) -> pd.Series`; `MONTHLY_ANNUALIZATION = 12`.
    - `config/platform_settings.yaml` lines 127-181 (`splice:` block — the 5 classes and their `tradable` tickers: equities->SPY, long_duration->TLT, gold->IAU, oil->USO, cash's `research_name` is `cash`) and lines 336-346 (`allocation:` block — `target_vol_annual: 0.10`, `ewma_halflife_months: 6`).
    - `06-01-PLAN.md` Task 1's `<action>` block — `core._save_or_show`, `core._regime_color`, `core.REGIME_CMAP`.
    - `tests/unit/test_plotting.py` lines 1-80 — the Agg-backend header, seeded synthetic-frame fixtures, does-not-crash + empty-input class shape.
  </read_first>
  <behavior>
    - `investable_asset_returns(monthly_raw, cfg)` on the real dev checkpoint returns `(asset_returns, cash_ret)` where `asset_returns.columns` is exactly `{"SPY", "TLT", "IAU", "USO"}` and `cash_ret` is a Series aligned to the same index.
    - `plot_returns_by_regime_heatmap(stats_df)` returns a Figure for a populated `returns_by_regime_stats`-shaped frame and for an empty one.
    - `plot_ewma_vol_timeline(asset_returns, halflife=6)` returns a Figure for a populated multi-column frame and for a single-column frame.
  </behavior>
  <action>
Create `src/trading_crab_lib/platform/plotting/allocation.py` with `from __future__ import
annotations`, a module docstring, `log = logging.getLogger(__name__)`. Import `core` by module
path; import nothing from the legacy `trading_crab_lib.plotting` package (D-01).

`investable_asset_returns(monthly_raw: pd.DataFrame, cfg: dict) -> tuple[pd.DataFrame, pd.Series]`
mirrors `evaluation/report.py`'s asset-universe construction using ONLY public functions:
`research = build_core_research_series(monthly_raw, cfg)` (import from
`trading_crab_lib.platform.splice`); `returns = compute_monthly_returns(research)` (import from
`trading_crab_lib.platform.assets.returns`); builds `asset_returns` as a DataFrame with one
column per non-cash `cfg["splice"]` class, keyed by that class's `tradable` ticker, valued by
`returns[params["research_name"]]`, skipping any class whose `research_name` is absent from
`returns.columns` (an optional class like gold may be excluded when its source is unavailable —
log a WARNING naming the excluded class, mirroring `report.py`'s own `_excluded` handling);
returns `(asset_returns, cash_ret)` where `cash_ret = returns[cfg["splice"]["cash"]["research_name"]]`.
Document in the docstring that this is a read-only re-derivation of `report.py`'s asset-universe
construction (never an import of that private logic), kept in sync by both citing the same
`splice_cfg` dispatch rule.

`plot_returns_by_regime_heatmap(stats_df: pd.DataFrame, *, metric: str = "sharpe_annualized",
title: str | None = None, save_path: Path | None = None, show: bool = False) -> plt.Figure`
pivots `stats_df` via `stats_df.pivot(index="regime", columns="asset", values=metric)` and
renders an annotated heatmap; handles an empty `stats_df` via a "no data" annotation; finishes
through `core._save_or_show`.

`plot_ewma_vol_timeline(asset_returns: pd.DataFrame, *, halflife: float = 6, title: str =
"Trailing annualized EWMA volatility", save_path: Path | None = None, show: bool = False) ->
plt.Figure` renders one line per column of `ewma_vol(asset_returns[col].dropna(), halflife=halflife,
annualization_factor=MONTHLY_ANNUALIZATION)` (import `ewma_vol`/`MONTHLY_ANNUALIZATION` from
`trading_crab_lib.platform.assets.vol`), reindexed back onto `asset_returns.index` for a shared
x-axis; handles an empty or all-NaN input via a "no data" annotation; finishes through
`core._save_or_show`.

Create `tests/unit/test_platform_plotting_allocation.py` on the `tests/unit/test_plotting.py`
shape: Agg backend forced in the module header, a seeded `np.random.default_rng(42)` synthetic
monthly fixture (`pd.date_range(..., freq="ME")`, ~60 rows) with 4 asset return columns and a
cash column, one `TestXxx` class per public function with a does-not-crash case and an
empty-input case, the `TestInvestableAssetReturns` case above run against a synthetic `monthly_raw`
+ `cfg["splice"]` fixture (never the real checkpoint, so the test stays deterministic), and a
test asserting `allocation.py` contains no `CheckpointManager` import and no module reachable
from `trading_crab_lib.platform.plotting.allocation` has a `__name__` beginning
`trading_crab_lib.plotting` (D-01).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_platform_plotting_allocation.py -x -q && .venv/bin/python -c "
from trading_crab_lib.platform.config import load_platform_config
from trading_crab_lib.platform.checkpoints import get_platform_checkpoint_manager
from trading_crab_lib.platform.plotting import allocation as pallocation
cfg = load_platform_config()
mr = get_platform_checkpoint_manager().load('monthly_raw')
asset_returns, cash_ret = pallocation.investable_asset_returns(mr, cfg)
assert set(asset_returns.columns) == {'SPY', 'TLT', 'IAU', 'USO'}, set(asset_returns.columns)
assert len(cash_ret) == len(mr), (len(cash_ret), len(mr))
fig = pallocation.plot_ewma_vol_timeline(asset_returns)
assert fig is not None
print('P5 universe/vol smoke OK', asset_returns.shape)"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/unit/test_platform_plotting_allocation.py -x -q` exits 0.
    - `investable_asset_returns(monthly_raw, cfg)` run against the real dev checkpoint returns `asset_returns.columns` equal to exactly `{"SPY", "TLT", "IAU", "USO"}` and a `cash_ret` Series of the same length as `monthly_raw`.
    - `plot_returns_by_regime_heatmap(...)` and `plot_ewma_vol_timeline(...)` each return `matplotlib.figure.Figure` for both a populated and an empty input.
    - A test asserts `allocation.py` contains no `CheckpointManager` import and no module reachable from `trading_crab_lib.platform.plotting.allocation` has a `__name__` beginning `trading_crab_lib.plotting` (D-01).
    - `.venv/bin/python -m pytest tests/ -q` reports 0 failures.
  </acceptance_criteria>
  <done>The real 4-asset investable universe is re-derived read-only from public splice/returns functions, and the returns-by-regime heatmap and EWMA vol timeline both render for real and synthetic input.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Smoothed-labeling tilt weights over time and the exact weight-band contract</name>
  <files>src/trading_crab_lib/platform/plotting/allocation.py, tests/unit/test_platform_plotting_allocation.py</files>
  <read_first>
    - `src/trading_crab_lib/platform/allocation/tilt.py` (full file) — `vol_targeted_tilt(regime_or_probs, returns_by_regime, asset_returns, *, target_vol_annual=0.10, halflife, min_obs=12) -> dict` returning keys `weights` (`pd.Series`, index = asset tickers), `cash` (float), `scale`, `portfolio_vol`. Accepts a one-hot `{state: 1.0}` mapping for a single-regime point bet — this is the exact call shape this task's per-month loop uses.
    - `src/trading_crab_lib/platform/evaluation/report.py` lines 387-439 (`_smoothed_hindsight_perf`) — READ ONLY, for the per-decision-date loop SHAPE this task's function mirrors (one-hot probability on the smoothed state at each date, an inception-date asset-availability filter, `asset_returns.loc[:t, available]` as the expanding window `vol_targeted_tilt` scores against). Do NOT import this private function — this task builds its OWN version in `allocation.py` that returns full weight VECTORS over time (not a terminal wealth number), reusing only the PUBLIC `vol_targeted_tilt`/`returns_by_regime_stats`.
    - `06-01-PLAN.md` Task 2's `<action>` block — `drift.assert_portfolio_weights_plausible(weights: pd.Series, *, cash_weight: float = 0.0) -> None`, raising on any negative weight or a total departing 1.0 beyond tolerance — the single source of the weight-band contract this task's notebook applies; do not re-derive it.
    - `config/platform_settings.yaml` lines 336-346 (`allocation:` block) — `portfolio_vol_min_obs: 12`.
  </read_first>
  <behavior>
    - `compute_smoothed_tilt_weights_over_time(states, asset_returns, cfg)` on a synthetic 60-month, 3-asset, 2-state fixture returns a DataFrame indexed by a subset of `states.index` (warmup months excluded) with one column per asset plus `cash`; every row's asset columns plus `cash` sum to 1.0 within 1e-9.
    - Every row of that output passes `drift.assert_portfolio_weights_plausible` when the asset columns are passed as `weights` and the `cash` column as `cash_weight`.
    - `plot_tilt_weights_over_time(weights_df)` returns a Figure for a populated case and for an empty DataFrame.
  </behavior>
  <action>
Extend `src/trading_crab_lib/platform/plotting/allocation.py`.

`compute_smoothed_tilt_weights_over_time(states: pd.Series, asset_returns: pd.DataFrame, cfg:
dict, *, min_obs: int | None = None) -> pd.DataFrame` computes
`smoothed_stats = returns_by_regime_stats(asset_returns, states)` (import from
`trading_crab_lib.platform.assets.returns`) ONCE, then for every date `t` in `states.index`
starting after the first `min_obs` (default: `cfg.get("allocation", {}).get("portfolio_vol_min_obs",
12)`) months: builds `available = [col for col in asset_returns.columns if asset_returns[col]
.loc[:t].first_valid_index() is not None]` (the inception-date filter — an asset never gets
weight before it has traded), restricts `smoothed_stats` to `available` assets, calls
`vol_targeted_tilt({int(states.loc[t]): 1.0}, step_stats, asset_returns.loc[:t, available],
target_vol_annual=cfg.get("allocation", {}).get("target_vol_annual", 0.10),
halflife=cfg.get("allocation", {}).get("ewma_halflife_months", 6), min_obs=min_obs)` (import
`vol_targeted_tilt` from `trading_crab_lib.platform.allocation.tilt`), and records a row with one
column per asset in `tilt["weights"]` (reindexed onto the full `asset_returns.columns` with 0.0
fill for inactive assets) plus a `cash` column from `tilt["cash"]`. Returns a DataFrame indexed
by the visited dates. Document in the docstring that this is a SMOOTHED, hindsight-informed
illustration of the allocation math over history — driven by the full-sample smoothed labeling's
one-hot state, never the real-time walk-forward nowcaster's probabilities — and is therefore
DISTINCT from what the live strategy actually held at any date (that comparison is P6's).

`plot_tilt_weights_over_time(weights_df: pd.DataFrame, *, title: str = "Smoothed-labeling tilt
weights over time (illustrative, not the live strategy)", save_path: Path | None = None, show:
bool = False) -> plt.Figure` renders a stacked area chart of every column of `weights_df`
(assets + `cash`), using `core.CUSTOM_COLORS` for the asset bands and a neutral gray for `cash`;
handles an empty input via a "no data" annotation; finishes through `core._save_or_show`.

Extend `tests/unit/test_platform_plotting_allocation.py` with `TestComputeSmoothedTiltWeightsOverTime`
(the behavior cases above, including the drift-band pass-through check calling
`drift.assert_portfolio_weights_plausible` per row on the synthetic fixture's output) and
`TestPlotTiltWeightsOverTime` (does-not-crash + empty-input).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_platform_plotting_allocation.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/unit/test_platform_plotting_allocation.py -x -q` exits 0.
    - `compute_smoothed_tilt_weights_over_time` on the synthetic fixture returns a DataFrame whose every row's asset weights plus `cash` sum to 1.0 within 1e-9, and every row individually passes `drift.assert_portfolio_weights_plausible(row[assets], cash_weight=row["cash"])` — this pass-through is asserted directly in the test, not merely implied.
    - `plot_tilt_weights_over_time(...)` returns `matplotlib.figure.Figure` for both a populated and an empty DataFrame.
    - `allocation.py` contains no re-implementation of the `vol_targeted_tilt` allocation math itself — a test or `git diff` (once committed) confirms the module calls `vol_targeted_tilt` and `returns_by_regime_stats`, never re-derives their formulas.
    - `.venv/bin/python -m pytest tests/ -q` reports 0 failures.
  </acceptance_criteria>
  <done>The smoothed-labeling weights-over-time function exists as a tested library function, every output row provably passes the exact weight-band contract, and the stacked-area rendering works for both populated and empty input.</done>
</task>

<task type="auto">
  <name>Task 3: P5_assets_allocation notebook — returns-by-regime, vol, weights, plausibility, drift</name>
  <files>notebooks/platform/P5_assets_allocation.ipynb</files>
  <read_first>
    - `notebooks/platform/P1_data_spine.ipynb` (06-01) and `notebooks/platform/P3_regime_labeling.ipynb` (06-03) — cell structure and the `compute_regime_labeling` call convention P3 already established, reused here identically.
    - `src/trading_crab_lib/platform/plotting/loaders.py` (06-01) — `load_platform_checkpoint`, `load_full_span_checkpoint`, `redacted_config`, `compute_regime_labeling`.
    - `src/trading_crab_lib/platform/plotting/drift.py` (06-01) — `assert_portfolio_weights_plausible`, `drift_report`.
    - `src/trading_crab_lib/platform/plotting/allocation.py` (Tasks 1-2) — `investable_asset_returns`, `plot_returns_by_regime_heatmap`, `plot_ewma_vol_timeline`, `compute_smoothed_tilt_weights_over_time`, `plot_tilt_weights_over_time`.
    - `src/trading_crab_lib/platform/assets/returns.py` — `returns_by_regime_stats(returns, states) -> pd.DataFrame`, called directly in the notebook (a Phase-4 statistics function, not plotting logic).
    - `.planning/POST-2020-OBSERVATIONS.md` (06-01) — the record this notebook's full-span drift cell points to.
  </read_first>
  <action>
Create `notebooks/platform/P5_assets_allocation.ipynb` via the `nbformat` v4 programmatic API. No
cell may construct a figure, touch an axis, import a plotting library directly, write to a
checkpoint, or invoke the full walk-forward evaluation entrypoints. Every figure comes from
`platform/plotting/`. Run it once and commit it with outputs.

Cell sequence:

1. Markdown title `P5 — Assets & Allocation` with a one-paragraph purpose statement (L3/L4:
returns-by-regime, EWMA vol, the vol-targeted tilt and its weights) and the explicit prerequisite
line naming `python scripts/build_platform_data.py`.

2. Setup code cell: `%matplotlib inline`, logging at INFO, imports of `load_platform_config`,
`plotting as pplot`, `drift as pdrift`, `allocation as pallocation`, plus
`from trading_crab_lib.platform.assets.returns import returns_by_regime_stats`;
`cfg = load_platform_config()`. Display config only through `pplot.redacted_config(cfg)`.

3. Code cell: load `monthly_raw` and `monthly_features` (dev) via `pplot.load_platform_checkpoint`;
call `asset_returns, cash_ret = pallocation.investable_asset_returns(monthly_raw, cfg)`; print
`asset_returns.columns.tolist()` and its shape. Markdown states this is the SAME 4-asset universe
(SPY, TLT, IAU, USO) `run_backtest` tilts across — never a wider satellite universe.

4. Markdown then code: `labeling_result = pplot.compute_regime_labeling(monthly_features, cfg)`
(the same routed-at-`NOTEBOOK_SCRATCH_DIR` entrypoint P3/P4 use); `states =
labeling_result["regime_labels"]["state"]`; print its shape and span. Markdown states this reuses
P3's exact labeling entrypoint — P5 does not re-fit a second labeler.

5. Code cell: `stats_df = returns_by_regime_stats(asset_returns, states)`; print `stats_df`;
render `pallocation.plot_returns_by_regime_heatmap(stats_df, metric="sharpe_annualized")` and
`pallocation.plot_returns_by_regime_heatmap(stats_df, metric="mean_monthly_return")`.

6. Code cell: `pallocation.plot_ewma_vol_timeline(asset_returns)`. Markdown states what to look
for: a persistent post-1990s or post-2008 shift in baseline vol level for any of the four assets.

7. Markdown then code: the weights-over-time panel. State explicitly that this is a SMOOTHED,
hindsight-informed illustration of the allocation math driven by the full-sample labeling's
one-hot state — distinct from a live weekly recommendation (v1 has no live scoring path yet) and
distinct from what the real walk-forward strategy actually held (P6's domain). Call
`weights_df = pallocation.compute_smoothed_tilt_weights_over_time(states, asset_returns, cfg)`;
guard-check the LAST row before rendering:
`pdrift.assert_portfolio_weights_plausible(weights_df.iloc[-1][list(asset_returns.columns)],
cash_weight=float(weights_df.iloc[-1]["cash"]))` — an impossible weight stops the notebook here;
render `pallocation.plot_tilt_weights_over_time(weights_df)`.

8. Markdown then code: the D-06/D-07 full-span drift panel. Load
`full_raw = pplot.load_full_span_checkpoint("monthly_raw", rebuild_hint="python
scripts/build_platform_data.py")`; call
`full_asset_returns, _ = pallocation.investable_asset_returns(full_raw, cfg)`; call
`drift_df = pdrift.drift_report(full_asset_returns, list(full_asset_returns.columns),
cutoff=DEFAULT_HOLDOUT_CUTOFF)` (import `DEFAULT_HOLDOUT_CUTOFF` from
`trading_crab_lib.platform.honesty.holdout`); print the table. Markdown states any asset whose
current-era return distribution has shifted is a candidate for the D-09 drift check, and that a
decision-changing observation belongs in `.planning/POST-2020-OBSERVATIONS.md`.

9. Final markdown cell: summary of what an operator is checking (regime-conditional Sharpe by
asset, vol regime shifts, weight-band compliance, asset-return drift) — explicitly state P5
carries NO sign-off cell and NO per-run gate (D-15 stays P3-exclusive), and that the
weights-over-time panel is illustrative, never a live trading instruction.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_platform_notebooks.py -x -q && .venv/bin/python -c "
import nbformat, pathlib
nb = nbformat.read(pathlib.Path('notebooks/platform/P5_assets_allocation.ipynb'), as_version=4)
src = '\n'.join(c.source for c in nb.cells)
code_src = '\n'.join(c.source for c in nb.cells if c.cell_type == 'code')
for token in ['investable_asset_returns', 'compute_regime_labeling', 'assert_portfolio_weights_plausible', 'illustrative', 'load_full_span_checkpoint', 'POST-2020-OBSERVATIONS']:
    assert token in src, token
for banned in ['import matplotlib', 'import seaborn', '.save(', 'Sign-Off', 'run_full_backtest_evaluation', 'run_backtest(']:
    assert banned not in code_src, banned
print('P5 static checks OK', len(nb.cells), 'cells')"</automated>
    <human-check>Open `notebooks/platform/P5_assets_allocation.ipynb` in Jupyter, Run All against the real checkpoints, and confirm every cell completes with no exception.</human-check>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/unit/test_platform_notebooks.py -x -q` exits 0 with `P5_assets_allocation.ipynb` included in the parameterization.
    - The notebook's combined cell source contains `investable_asset_returns`, `compute_regime_labeling`, `assert_portfolio_weights_plausible`, `illustrative`, `load_full_span_checkpoint`, and `POST-2020-OBSERVATIONS`; no code cell contains `import matplotlib`, `import seaborn`, `.save(`, `Sign-Off`, `run_full_backtest_evaluation`, or `run_backtest(`.
    - Executing the notebook, `asset_returns.columns.tolist()` sorted equals `["IAU", "SPY", "TLT", "USO"]`.
    - Executing the notebook, the weights-over-time panel's last row passes `drift.assert_portfolio_weights_plausible` without raising.
    - The notebook is committed with its cell outputs, matching the storage convention of the twelve legacy notebooks.
    - `.venv/bin/python -m pytest tests/ -q` reports 0 failures.
  </acceptance_criteria>
  <done>P5 renders regime-conditional returns, EWMA vol, and a plausibility-checked weights-over-time illustration for the real 4-asset investable universe, plus an asset-return drift panel, all sourced from `platform/plotting/allocation.py` — with no sign-off cell.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed notebook -> git | P5's cell outputs become durable repository content, including anything a cell displays |
| notebook cell -> `data/checkpoints/platform/{hysteresis_state,returns_by_regime}` | Both are production checkpoint names the real strategy writes; P5 must never write either from a notebook |
| smoothed-labeling weights illustration -> operator judgement | The weights-over-time panel could be mistaken for a live trading instruction if not clearly labeled |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-23 | Information Disclosure | notebook cell displaying `load_platform_config()` output, which carries the injected `FRED_API_KEY` | high | mitigate | The notebook routes any config display through `loaders.redacted_config`; `tests/unit/test_platform_notebooks.py` check (4) from 06-01 fails the notebook otherwise |
| T-06-24 | Tampering | `compute_smoothed_tilt_weights_over_time` (or a future edit to it) calling `hysteresis.save_active_regime` / `report_returns_by_regime` and writing a production checkpoint on every notebook open | medium | mitigate | `allocation.py` imports no `CheckpointManager`-backed persistence function; Task 1's test asserts `allocation.py` contains no `CheckpointManager` import at all |
| T-06-25 | Repudiation | an operator or downstream reader treating P5's smoothed weights-over-time illustration as a live weekly allocation recommendation | medium | mitigate | Cell 7's markdown and the plot's own title state explicitly this is a hindsight-informed illustration, distinct from live scoring (which v1 does not yet implement) and distinct from the real walk-forward strategy (P6) |
| T-06-26 | Denial of Service | `compute_smoothed_tilt_weights_over_time` looping over ~700 months with a growing `asset_returns.loc[:t, ...]` slice each iteration | low | accept | Each iteration's `ewma_vol` call is O(n) over at most a handful of columns; total cost is bounded well under a minute for the full dev span, unlike the 588-refit walk-forward |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs zero new packages. `06-RESEARCH.md` records the Package Legitimacy Audit as not applicable for this phase. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/unit/test_platform_plotting_allocation.py tests/unit/test_platform_notebooks.py -x -q` exits 0.
- `.venv/bin/python -m pytest tests/ -q` collects at least 1393 tests and reports 0 failures.
- `git status --porcelain src/trading_crab_lib/platform/allocation/ src/trading_crab_lib/platform/assets/ outputs/reports/platform/ data/checkpoints/platform/` is empty — this plan reads Phase 4 code and produces no new checkpoints or report artifacts.
- Human check (end-of-phase): P5 runs top-to-bottom against real checkpoints.
</verification>

<success_criteria>
- `platform/plotting/allocation.py` exists with a read-only re-derivation of the real 4-asset investable universe, a returns-by-regime heatmap, an EWMA vol timeline, and a smoothed weights-over-time illustration.
- Every rendered weight row satisfies `drift.assert_portfolio_weights_plausible` exactly.
- P5 states its weights panel is illustrative, not a live recommendation.
- Every figure in P5 comes from `platform/plotting/` (ROADMAP criterion 3); P5 carries no sign-off cell (D-15 stays P3-exclusive).
</success_criteria>

## Artifacts this phase produces

Symbols and files created by THIS plan (new; exclude from drift verification):

**New modules**
- `src/trading_crab_lib/platform/plotting/allocation.py`

**New functions**
- `allocation.investable_asset_returns`, `allocation.plot_returns_by_regime_heatmap`, `allocation.plot_ewma_vol_timeline`, `allocation.compute_smoothed_tilt_weights_over_time`, `allocation.plot_tilt_weights_over_time`

**New files**
- `notebooks/platform/P5_assets_allocation.ipynb`
- `tests/unit/test_platform_plotting_allocation.py`

<output>
Create `.planning/phases/06-platform-notebook-suite/06-06-SUMMARY.md` when done
</output>