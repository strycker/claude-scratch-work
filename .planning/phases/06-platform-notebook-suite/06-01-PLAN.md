---
phase: 06-platform-notebook-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/trading_crab_lib/platform/plotting/__init__.py
  - src/trading_crab_lib/platform/plotting/core.py
  - src/trading_crab_lib/platform/plotting/loaders.py
  - src/trading_crab_lib/platform/plotting/data.py
  - src/trading_crab_lib/platform/plotting/drift.py
  - notebooks/platform/P1_data_spine.ipynb
  - tests/unit/test_platform_plotting.py
  - tests/unit/test_platform_plotting_data.py
  - tests/unit/test_platform_plotting_drift.py
  - tests/unit/test_platform_notebooks.py
  - .planning/POST-2020-OBSERVATIONS.md
autonomous: true
requirements: [NB-01]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A plot function called from a notebook cell returns a matplotlib Figure and renders inline without any RunConfig-shaped object existing on the platform side (D-02)."
    - "A missing platform checkpoint stops the notebook with a message naming the checkpoint AND the command that rebuilds it, not a bare FileNotFoundError (D-10)."
    - "A terminal log wealth of 111.06 raises rather than rendering (universal band abs(x) < 10.0)."
    - "A 60/40-shaped leg reporting -2.27% max drawdown raises, even though it sits inside the universal [-1, 0] bound (domain per-leg band)."
    - "A feature whose current-era distribution has shifted relative to the pre-2021 fitted window appears in a ranked drift table with a standardized mean shift, so gradual decay is visible."
    - "Every notebook under notebooks/platform/ parses as valid nbformat and defines no plotting logic in its own cells."
    - statement: "The tracer notebook P1_data_spine.ipynb runs top-to-bottom against the real monthly_raw checkpoint without raising."
      verification: backstop
  artifacts:
    - src/trading_crab_lib/platform/plotting/__init__.py
    - src/trading_crab_lib/platform/plotting/core.py
    - src/trading_crab_lib/platform/plotting/loaders.py
    - src/trading_crab_lib/platform/plotting/data.py
    - src/trading_crab_lib/platform/plotting/drift.py
    - notebooks/platform/P1_data_spine.ipynb
    - tests/unit/test_platform_plotting.py
    - tests/unit/test_platform_plotting_data.py
    - tests/unit/test_platform_plotting_drift.py
    - tests/unit/test_platform_notebooks.py
    - .planning/POST-2020-OBSERVATIONS.md
  key_links:
    - "platform/plotting/core.py::_save_or_show is the single finalization path every plot function in every later submodule calls."
    - "platform/plotting/loaders.py::load_full_span_checkpoint is the only full-span (post-2020) read path notebooks use; it wraps honesty.holdout.load_full_span rather than opening two managers by hand."
    - "platform/plotting/loaders.py::compute_regime_labeling routes labeling.diagnostics.label_regimes at a non-production scratch checkpoint namespace so no notebook writes data/checkpoints/platform/."
    - "tests/unit/test_platform_notebooks.py globs notebooks/platform/*.ipynb, so every notebook added by a later plan is gated by it without editing the test."
  prohibitions:
    - statement: "No task in this plan imports anything from the legacy trading_crab_lib.plotting package (D-01 — the legacy package is a pattern source only)."
      status: pending
      verification: "tests/unit/test_platform_plotting.py asserts the platform plotting package's module dependency set excludes the legacy plotting package."
    - statement: "No task in this plan changes any existing outputs/reports/platform/ artifact."
      status: pending
      verification: flagged-unverified
    - statement: "No task in this plan tunes K, lambda, n_restarts, feature selection, or asset selection (D-14)."
      status: pending
      verification: flagged-unverified
---

<objective>
Build the `platform/plotting/` package spine and prove it end-to-end with one production-quality
vertical slice: real checkpoint -> library loader -> library plot function -> committed notebook
-> static notebook gate. Then add the two check classes every later notebook depends on —
D-09 drift-against-the-pre-2021-baseline and the reinstated D-11 plausibility bands.

Purpose: `src/trading_crab_lib/platform/` today has zero matplotlib/seaborn imports across 48
modules and no `plotting/` subpackage. Everything the other five plans render sits on top of
what this plan creates. Getting the signature convention, the fail-loudly load path, and the
two numeric check classes right here is what stops five notebooks from having to be rewritten.

Output: a seven-symbol plotting spine (`core`, `loaders`, `data`, `drift`), a minimal but real
`P1_data_spine.ipynb`, three test files, and the D-07 post-2020 observation record.
</objective>

<execution_context>
@/Users/glestryc/personal/github_repos/claude-scratch-work/.claude/gsd-core/workflows/execute-plan.md
@/Users/glestryc/personal/github_repos/claude-scratch-work/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-platform-notebook-suite/06-CONTEXT.md
@.planning/phases/06-platform-notebook-suite/06-PATTERNS.md
@.planning/phases/06-platform-notebook-suite/06-VALIDATION.md
@.planning/BASELINE-v1-tracer-bullet.md
@CLAUDE.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "see the data spine" slice — loader, palette, one plot, one notebook</name>
  <files>src/trading_crab_lib/platform/plotting/__init__.py, src/trading_crab_lib/platform/plotting/core.py, src/trading_crab_lib/platform/plotting/loaders.py, src/trading_crab_lib/platform/plotting/data.py, notebooks/platform/P1_data_spine.ipynb, tests/unit/test_platform_plotting.py, tests/unit/test_platform_plotting_data.py</files>
  <read_first>
    - `src/trading_crab_lib/plotting/core.py` lines 19-90 — the pattern source for `_in_jupyter()`, the conditional `matplotlib.use("Agg")` guard, the ImportError install hint, `CUSTOM_COLORS`, `REGIME_CMAP`. Copy the idioms; import nothing from this package (D-01).
    - `src/trading_crab_lib/platform/config.py` lines 56-94 — proves `load_platform_config()` returns a plain dict and no config dataclass exists on the platform side; this is why the plot signature takes plain kwargs.
    - `src/trading_crab_lib/platform/checkpoints.py` — `PLATFORM_CHECKPOINT_DIR`, `get_platform_checkpoint_manager()`.
    - `src/trading_crab_lib/platform/honesty/holdout.py` lines 77-105 — `load_full_span(name, cutoff)`, its docstring's statement that a missing holdout checkpoint returns the dev side alone.
    - `src/trading_crab_lib/platform/labeling/diagnostics.py` lines 257-316 — `label_regimes(monthly_features, cfg, *, checkpoint_dir)`; note it calls `cm.save(...)` three times, which is why notebooks must pass a non-production `checkpoint_dir`.
    - `tests/unit/test_plotting.py` lines 1-80 — the Agg-backend header, the seeded synthetic-frame fixtures, and the does-not-crash + empty-input class shape.
    - `notebooks/01_ingestion.ipynb` — the committed-notebook convention: title markdown cell with an explicit prerequisite line, then a setup code cell, cell `id` fields present, outputs committed.
    - `06-PATTERNS.md` sections "platform/plotting/core.py", "platform/plotting/__init__.py", "Checkpoint load-or-raise-actionably (D-10)", "Full-span opt-in (D-06)", "notebooks/platform/*.ipynb".
  </read_first>
  <action>
Create `src/trading_crab_lib/platform/plotting/core.py` with `from __future__ import annotations`,
a module docstring, `log = logging.getLogger(__name__)`, and: `_in_jupyter() -> bool`; the
try/except matplotlib import block that calls `matplotlib.use("Agg")` only when `_in_jupyter()`
is false and re-raises ImportError with the hint `pip install 'trading-crab-lib[plotting]'`;
`CUSTOM_COLORS: list[str]` set to the five legacy hex values `#0000d0`, `#d00000`, `#f48c06`,
`#8338ec`, `#50a000`; `REGIME_CMAP = mcolors.ListedColormap(CUSTOM_COLORS)`;
`PLATFORM_PLOT_DIR: Path` = `OUTPUT_DIR / "plots" / "platform"` (import `OUTPUT_DIR` from
`trading_crab_lib`, the same root-package seam `platform/checkpoints.py` and
`platform/evaluation/report.py` already use); `_regime_color(state_id: int) -> str` indexing
`CUSTOM_COLORS` modulo its length; and
`_save_or_show(fig, *, save_path: Path | None, show: bool) -> plt.Figure` which creates
`save_path.parent`, calls `fig.savefig(save_path, dpi=150, bbox_inches="tight")` when
`save_path` is not None, calls `plt.show()` when `show` or `_in_jupyter()`, and RETURNS the
figure without closing it. Also define `A13_CAVEAT: str`, the single-source string every later
module and notebook uses wherever the sojourn/lag ratio appears; its text must state that the
ratio is not interpretable and name audit item A13.

Per D-02 the platform signature is `plot_x(data, *, save_path: Path | None = None,
show: bool = False) -> plt.Figure`. Do not define, import, or accept any run-configuration
dataclass parameter — `platform/config.py` has none to bind to. Do not port the legacy
`PLOT_DIR`-keyed staleness-cache helpers (`_plot_is_fresh`, `load_or_generate`,
`list_available_plots`); they exist to skip pipeline reruns and have no platform equivalent.

Create `src/trading_crab_lib/platform/plotting/loaders.py` with:
`NOTEBOOK_SCRATCH_DIR: Path` = `DATA_DIR / "checkpoints" / "platform_notebook"`;
`load_platform_checkpoint(name: str, *, rebuild_hint: str) -> pd.DataFrame` wrapping
`get_platform_checkpoint_manager().load(name)` and re-raising `FileNotFoundError` chained with
`from exc`, whose message names the checkpoint, the directory `data/checkpoints/platform/`, and
the literal `rebuild_hint` command (D-10);
`load_full_span_checkpoint(name: str, *, rebuild_hint: str) -> pd.DataFrame` which calls
`trading_crab_lib.platform.honesty.holdout.load_full_span(name)` (never a hand-rolled two-manager
concat), logs at WARNING the count of rows dated after `DEFAULT_HOLDOUT_CUTOFF` that the caller
is now looking at and points at `.planning/POST-2020-OBSERVATIONS.md` per D-07, and re-raises a
`rebuild_hint`-bearing `FileNotFoundError` on a missing dev checkpoint;
`load_report_artifact(filename: str, *, rebuild_hint: str) -> pd.DataFrame` reading
`OUTPUT_DIR / "reports" / "platform" / filename` with the same actionable-raise contract;
`load_full_sample_states(*, rebuild_hint: str = "python -m trading_crab_lib.platform.evaluation.report") -> pd.Series`
reading `backtest_full_sample_states.parquet` through `load_report_artifact` and returning its
`state` column as an int-valued Series indexed by date;
`load_filtered_state_probs(*, rebuild_hint: str = "python -m trading_crab_lib.platform.evaluation.report") -> pd.DataFrame`
reading `backtest_filtered_state_probs.parquet` through `load_report_artifact` and renaming its
parquet-safe `state_{k}` string columns back to integer state ids, because
`evaluation.sojourn_lag.compute_sojourn_lag_headline` looks columns up by integer state id — these
two artifacts are written by plan 06-02 and are absent until it lands, so this task verifies only
the actionable-raise path for them;
`redacted_config(cfg: dict) -> dict` returning a deep copy with every value under any key whose
name ends in `api_key` replaced by the literal string `<redacted>` (this is the function
notebooks call before displaying config, so a committed cell output can never carry a live FRED
credential);
and `compute_regime_labeling(monthly_features: pd.DataFrame, cfg: dict) -> dict` which calls
`label_regimes(monthly_features, cfg, checkpoint_dir=NOTEBOOK_SCRATCH_DIR)` and returns its dict
plus the `regime_labels` / `regime_confidences` / `regime_profiles` frames read back from that
scratch namespace. Routing at `NOTEBOOK_SCRATCH_DIR` is what keeps notebooks from writing the
production platform checkpoint namespace (D-10, legacy pitfall P20/D5); state that reason in the
function docstring.

Create `src/trading_crab_lib/platform/plotting/data.py` with exactly one function for this task:
`plot_coverage_timeline(df: pd.DataFrame, *, title: str = "Column coverage", max_columns: int = 60,
save_path: Path | None = None, show: bool = False) -> plt.Figure`. It renders a binary
availability grid (one row per column, x-axis the DataFrame's DatetimeIndex, filled where the
value is non-NaN), sorts rows by first-valid date, annotates each row with its first-valid year,
handles an empty DataFrame by returning a figure carrying a "no data" annotation rather than
raising, and finishes through `core._save_or_show`.

Create `src/trading_crab_lib/platform/plotting/__init__.py` re-exporting ONLY from `core` and
`loaders`: `CUSTOM_COLORS`, `REGIME_CMAP`, `PLATFORM_PLOT_DIR`, `A13_CAVEAT`,
`load_platform_checkpoint`, `load_full_span_checkpoint`, `load_report_artifact`,
`load_full_sample_states`, `load_filtered_state_probs`, `redacted_config`,
`compute_regime_labeling`, `NOTEBOOK_SCRATCH_DIR`, with an explicit `__all__`.
Per-layer plot functions are deliberately NOT re-exported here and are imported by their
submodule path (`from trading_crab_lib.platform.plotting import data as pdata`); this diverges
from the legacy barrel in `src/trading_crab_lib/plotting/__init__.py` so that the four
per-layer plans can be built in parallel without contending for this one file. Record that
reason in the module docstring.

Create `notebooks/platform/P1_data_spine.ipynb` via the `nbformat` v4 programmatic API
(`nbf.v4.new_notebook`, `new_markdown_cell`, `new_code_cell`, `nbf.write`) — never hand-written
JSON, never jupytext. Minimal tracer content, five cells: (1) markdown title "P1 — Data Spine"
with a one-paragraph purpose statement and the explicit prerequisite line naming
`python scripts/build_platform_data.py`; (2) a setup code cell with the `%matplotlib inline`
magic, `import logging`, `logging.basicConfig(level=logging.INFO)`,
`from trading_crab_lib.platform.config import load_platform_config`,
`from trading_crab_lib.platform import plotting as pplot`,
`from trading_crab_lib.platform.plotting import data as pdata`, and `cfg = load_platform_config()`;
(3) a code cell loading `monthly_raw` through `pplot.load_platform_checkpoint` with the rebuild
hint `python scripts/build_platform_data.py` and printing its shape and index bounds;
(4) a code cell calling `pdata.plot_coverage_timeline(monthly_raw, title="monthly_raw column
coverage")`; (5) a markdown cell stating what an operator is looking for (every series' first
valid date, and whether any series stops updating before the frame's last date). The notebook's
own cells must contain no figure construction, no axis manipulation, and no direct plotting-library
import — all of that lives in `platform/plotting/`, per ADR #11 applied to the platform. Run the
notebook once and commit it with its outputs, matching the storage convention of the twelve
legacy notebooks.

Create `tests/unit/test_platform_plotting.py` covering `core` and `loaders` on the
`tests/unit/test_plotting.py` shape: module header forcing the Agg backend before importing
pyplot; seeded `np.random.default_rng(42)` synthetic monthly fixtures built on
`pd.date_range(..., freq="ME")`; `TestSaveOrShow` asserting a PNG appears at an explicit
`save_path` under `tmp_path` and that the return value is a `plt.Figure`; a test that the
platform plotting package's transitive module set contains no module whose name starts with
`trading_crab_lib.plotting` (D-01); tests that `load_platform_checkpoint` and
`load_report_artifact` raise `FileNotFoundError` whose message contains the supplied rebuild
hint; a test that `redacted_config` removes an injected fake credential value from a nested
config dict; and a test that `NOTEBOOK_SCRATCH_DIR` is not `PLATFORM_CHECKPOINT_DIR`.

Create `tests/unit/test_platform_plotting_data.py` with `TestPlotCoverageTimeline` carrying a
does-not-crash case on the synthetic monthly fixture and an empty-input case on
`pd.DataFrame()`; both assert `isinstance(fig, plt.Figure)`.

Every new source file starts with `from __future__ import annotations`, carries a module
docstring, type-hints its public functions, uses `logging` rather than `print`, uses
`pathlib.Path` for every path, and stays within ruff's 127-character line length.
  </action>
  <reversibility rating="costly">The `plot_x(data, *, save_path, show) -> Figure` convention and the fresh-package boundary (D-01/D-02) are copied by every plot function in the five later plans; reversing either means rewriting every call site across six notebooks and re-coupling the platform to the legacy library, which invalidates MIGRATION-PLAN.md §2's four-seam count.</reversibility>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_platform_plotting.py tests/unit/test_platform_plotting_data.py -x -q && .venv/bin/python -c "import nbformat,pathlib; nb=nbformat.read(pathlib.Path('notebooks/platform/P1_data_spine.ipynb'),as_version=4); assert len(nb.cells)>=5" && .venv/bin/python -c "
from trading_crab_lib.platform.plotting import loaders, data
df = loaders.load_platform_checkpoint('monthly_raw', rebuild_hint='python scripts/build_platform_data.py')
assert df.shape[0] >= 700 and df.shape[1] >= 40, df.shape
fig = data.plot_coverage_timeline(df, title='smoke')
assert fig is not None"</automated>
    <human-check>Open `notebooks/platform/P1_data_spine.ipynb` in Jupyter, Run All, and confirm the coverage figure renders inline with no exception.</human-check>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/unit/test_platform_plotting.py tests/unit/test_platform_plotting_data.py -x -q` exits 0.
    - `src/trading_crab_lib/platform/plotting/core.py`, `loaders.py`, `data.py`, `__init__.py` all exist and each begins with `from __future__ import annotations`.
    - Importing `trading_crab_lib.platform.plotting` succeeds and `plotting.CUSTOM_COLORS` has exactly 5 entries, matching the platform's configured `labeling.K` of 5.
    - `data.plot_coverage_timeline(...)` returns an object satisfying `isinstance(fig, matplotlib.figure.Figure)` for both a populated frame and `pd.DataFrame()`.
    - `loaders.load_platform_checkpoint("monthly_raw", rebuild_hint=...)` returns a frame whose shape is at least 700 rows x 40 columns and whose index max is on or after 2020-12-31 (current on-disk reference: 776 x 45, 1962-01-31 to 2026-08-31).
    - `loaders.load_platform_checkpoint("does_not_exist", rebuild_hint="X")` raises `FileNotFoundError` whose `str()` contains both `does_not_exist` and `X`.
    - `loaders.load_full_span_checkpoint("monthly_features", rebuild_hint=...)` returns a frame with strictly more rows than `loaders.load_platform_checkpoint("monthly_features", ...)` (current on-disk reference: 776 vs 708) and an index max on or after 2021-01-31.
    - `loaders.NOTEBOOK_SCRATCH_DIR` resolves to a directory path different from `trading_crab_lib.platform.checkpoints.PLATFORM_CHECKPOINT_DIR`, asserted by a test in `tests/unit/test_platform_plotting.py`.
    - `loaders.load_full_sample_states()` and `loaders.load_filtered_state_probs()` each raise `FileNotFoundError` whose `str()` contains `trading_crab_lib.platform.evaluation.report` while those artifacts are absent (plan 06-02 produces them in the same wave); a test asserts the raise path with a monkeypatched empty artifact directory so it stays deterministic after 06-02 lands.
    - A test in `tests/unit/test_platform_plotting.py` asserts no module reachable from `trading_crab_lib.platform.plotting` has a `__name__` beginning `trading_crab_lib.plotting`.
    - `nbformat.read("notebooks/platform/P1_data_spine.ipynb", as_version=4)` succeeds and the notebook has at least 5 cells, at least one of which is a markdown cell whose source contains `scripts/build_platform_data.py`.
    - `.venv/bin/python -m pytest tests/ -q` collects at least 1393 tests (the current suite total) and reports 0 failures.
  </acceptance_criteria>
  <done>The platform plotting package exists, one real plot function renders the real `monthly_raw` checkpoint through the D-10 loader, the tracer notebook is committed with outputs, and the library-side tests are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Drift-against-baseline (D-09) and reinstated plausibility bands (D-11)</name>
  <files>src/trading_crab_lib/platform/plotting/drift.py, tests/unit/test_platform_plotting_drift.py</files>
  <read_first>
    - `src/trading_crab_lib/platform/splice.py` lines 113-155 — `_IMPLAUSIBLE_DECIMAL_YIELD`, `_SUSPICIOUSLY_LOW_MEDIAN_YIELD`, `assert_yield_units_plausible`. This is the exact raise/warn idiom to replicate: module-level named constants, a hard `ValueError` whose f-string explains why the value cannot exist and what to check, and a `log.warning` only where the failure direction is ambiguous.
    - `src/trading_crab_lib/platform/evaluation/model_metrics.py` lines 58-81 — `compute_brier_multiclass` computes `np.mean(diff * diff)` over the full (n, K) array. The bound is [0, 1], not the textbook [0, 2].
    - `src/trading_crab_lib/platform/labeling/diagnostics.py` line 67 — `_MIN_OCCUPANCY_THRESHOLD = 0.05`, the existing soft-warning threshold to mirror rather than re-invent.
    - `src/trading_crab_lib/platform/transforms_monthly.py` lines 106-160 — `_DISCONTINUITY_RATIO = 1.5` and `_warn_on_level_discontinuity`, the private detector whose public, testable counterpart this task adds.
    - `src/trading_crab_lib/platform/honesty/holdout.py` lines 31-56 — `DEFAULT_HOLDOUT_CUTOFF` and `split_by_holdout_boundary`, the split that defines D-09's pre-2021 fitted baseline.
    - `06-VALIDATION.md` section "Plausibility bands — the numeric contract" and the standing-lesson table above it.
    - `06-PATTERNS.md` section "platform/plotting/drift.py".
  </read_first>
  <behavior>
    - `assert_terminal_log_wealth_plausible(4.0265, leg="strategy")` returns None.
    - `assert_terminal_log_wealth_plausible(111.06, leg="strategy")` raises ValueError.
    - `assert_max_drawdown_plausible(-0.269625, leg="sixty_forty")` returns None.
    - `assert_max_drawdown_plausible(-0.0227, leg="sixty_forty")` raises ValueError even though -0.0227 is inside the universal [-1, 0] bound.
    - `assert_max_drawdown_plausible(-1.5, leg="spy_buy_hold")` raises ValueError on the universal bound.
    - `assert_brier_plausible(0.208746, n_classes=5)` returns a mapping whose no-skill value is 0.16 and whose beats-no-skill flag is False.
    - `assert_brier_plausible(1.7, n_classes=5)` raises ValueError (this codebase's Brier cannot exceed 1.0).
    - `assert_regime_occupancy_plausible` on shares summing to 1.0 returns the list of states below 0.05; on shares summing to 0.8 it raises.
    - `compute_drift` on two draws from the same distribution reports a standardized mean shift under 0.5; on a baseline shifted by three baseline sigmas it reports a shift of roughly 3.0 and flags.
    - `assert_no_level_discontinuity` on a smooth series returns an empty list; on a series that triples month over month it raises naming the offending date.
  </behavior>
  <action>
Create `src/trading_crab_lib/platform/plotting/drift.py` — pure functions only, no matplotlib
import, so it is importable and testable independently of any figure code.

Plausibility half (D-11, reinstated by CONTEXT.md Amendment 1 item A). Define module-level named
constants, never inline magic numbers: `_UNIVERSAL_LOG_WEALTH_ABS_MAX = 10.0`;
`_DOMAIN_LOG_WEALTH_RANGE = (-3.0, 12.0)`; `_UNIVERSAL_MAX_DRAWDOWN_RANGE = (-1.0, 0.0)`;
`_DOMAIN_MAX_DRAWDOWN_BANDS: dict[str, tuple[float, float]]` mapping leg name to (floor, ceiling)
with `spy_buy_hold` (-0.90, -0.30), `sixty_forty` (-0.80, -0.10), `faber_sma` (-0.50, -0.05),
`strategy` (-0.80, -0.05), `no_regime_ablation` (-0.80, -0.05); `_BRIER_RANGE = (0.0, 1.0)`;
`_TURNOVER_RANGE = (0.0, 2.0)` with `_TURNOVER_OPERATIONAL_MAX = 0.30`;
`_CVAR_RANGE = (-0.5, 0.0)` with `_CVAR_OPERATIONAL_RANGE = (-0.15, -0.01)`;
`_MIN_OCCUPANCY_WARN = 0.05` (mirroring `labeling/diagnostics.py`'s existing threshold);
`_OCCUPANCY_SUM_TOLERANCE = 1e-9`; `_LEVEL_DISCONTINUITY_RATIO = 1.5` (mirroring
`transforms_monthly.py`'s existing ratio).

Public functions, each raising `ValueError` with an f-string that states the observed value, the
band it violated, and what the operator should check:
`no_skill_brier(n_classes: int) -> float` returning `(K - 1) / K ** 2`;
`assert_terminal_log_wealth_plausible(value: float, *, leg: str) -> None` checking the universal
absolute bound first, then the domain range;
`assert_max_drawdown_plausible(value: float, *, leg: str) -> None` checking the universal range
first, then looking up the per-leg domain band and, when the leg is unknown to the mapping,
logging a warning and applying the universal bound only;
`assert_brier_plausible(value: float, *, n_classes: int) -> dict[str, float | bool]` returning
keys `value`, `no_skill`, `beats_no_skill`, raising outside `_BRIER_RANGE` and logging a warning
when the value is at or above the no-skill reference;
`assert_turnover_plausible(value: float) -> None`;
`assert_cvar_plausible(value: float) -> None`;
`assert_regime_occupancy_plausible(occupancy) -> list[int]` accepting a Series or mapping,
raising when any share falls outside [0, 1] or the sum departs 1.0 by more than the tolerance,
and returning the state ids below the warn threshold after logging them;
`assert_portfolio_weights_plausible(weights: pd.Series, *, cash_weight: float = 0.0) -> None`
raising on any negative weight (long-only by design) or a total departing 1.0 beyond tolerance;
`assert_no_level_discontinuity(series: pd.Series, *, name: str, ratio_threshold: float = _LEVEL_DISCONTINUITY_RATIO) -> list[pd.Timestamp]`
raising and naming the offending dates when a positive level series jumps by more than the ratio
month over month in either direction — this is the public counterpart of the private detector in
`transforms_monthly.py` and is the shape of the ALFRED rebasing defect closed as audit item A4;
and `assert_kpi_table_plausible(kpi_table: pd.DataFrame) -> pd.DataFrame` which iterates the
`leg` / `terminal_log_wealth` / `max_drawdown` columns of the persisted
`backtest_kpi_table.parquet` shape, collects EVERY violation before raising a single `ValueError`
listing them all (the `config.validate_config` collect-then-raise idiom), and on success returns
a tidy verdict frame with columns `leg`, `metric`, `value`, `universal_band`, `domain_band`,
`verdict` for a notebook to render.

Drift half (D-09). `baseline_and_current(full_df, cutoff: str = DEFAULT_HOLDOUT_CUTOFF)` returning
the `(baseline, current)` tuple by delegating to `honesty.holdout.split_by_holdout_boundary`, so
the baseline is always the pre-2021 fitted window and never a rolling trailing window (a rolling
baseline decays alongside the feature and never trips on gradual decay, which is the failure this
check exists to catch). `compute_drift(current: pd.Series, baseline: pd.Series) -> dict` returning
keys `n_baseline`, `n_current`, `baseline_mean`, `baseline_std`, `current_mean`,
`standardized_mean_shift` (current mean minus baseline mean, divided by baseline standard
deviation), `ks_statistic` and `ks_pvalue` from `scipy.stats.ks_2samp`, `pct_beyond_2sigma`
(share of current observations more than two baseline sigmas from the baseline mean), and `flag`
(true when the absolute standardized mean shift is at or above `_DRIFT_FLAG_THRESHOLD = 1.0`);
it must return NaN-valued keys rather than raise when either side is empty or the baseline
standard deviation is zero. `drift_report(full_df: pd.DataFrame, columns: list[str], *, cutoff: str = DEFAULT_HOLDOUT_CUTOFF) -> pd.DataFrame`
returning one row per column sorted by descending absolute standardized mean shift.

Document in the module docstring that drift and plausibility catch disjoint failure modes and
that neither substitutes for the other: a uniformly wrong series shows zero drift against itself,
which is why the percent-versus-decimal units defect was invisible to drift alone.

Create `tests/unit/test_platform_plotting_drift.py` with one `TestXxx` class per public function.
It MUST contain the two named regression cases as explicit in-band/out-of-band pairs: a
`sixty_forty` leg at -0.0227 max drawdown must raise while -0.269625 passes, and a terminal log
wealth of 111.06 must raise while 4.0265 passes. Add an in-band and an out-of-band case for every
other band, a drift test on two seeded same-distribution draws and on a three-sigma shifted draw,
and empty-input cases for `compute_drift` and `drift_report`.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_platform_plotting_drift.py -x -q && .venv/bin/python -c "
import pandas as pd
from trading_crab_lib.platform.plotting import drift
k = pd.read_parquet('outputs/reports/platform/backtest_kpi_table.parquet')
v = drift.assert_kpi_table_plausible(k)
print(v.to_string())
b = drift.assert_brier_plausible(float(pd.read_parquet('outputs/reports/platform/model_metrics_brier.parquet')['brier'].iloc[0]), n_classes=5)
assert abs(b['no_skill'] - 0.16) < 1e-12, b
assert b['beats_no_skill'] is False, b"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/unit/test_platform_plotting_drift.py -x -q` exits 0.
    - `assert_max_drawdown_plausible(-0.0227, leg="sixty_forty")` raises `ValueError`; `assert_max_drawdown_plausible(-0.269625, leg="sixty_forty")` returns None. Both cases are present in `tests/unit/test_platform_plotting_drift.py`.
    - `assert_terminal_log_wealth_plausible(111.06, leg="strategy")` raises `ValueError`; `assert_terminal_log_wealth_plausible(4.0265, leg="strategy")` returns None. Both cases are present in the same test file.
    - `no_skill_brier(5)` equals 0.16 exactly (to within 1e-12), and `assert_brier_plausible(0.208746, n_classes=5)` returns `beats_no_skill` False.
    - `assert_kpi_table_plausible` run against the live `outputs/reports/platform/backtest_kpi_table.parquet` returns a verdict frame with 5 leg rows and raises nothing; the frame's `value` column reproduces terminal log wealth in [-3, 12] and max drawdown in [-1, 0] for every leg (current reference: strategy 4.0265/-21.24%, ablation 3.6472/-19.81%, SPY 5.6805/-48.95%, 60/40 5.0471/-26.96%, Faber 6.3726/-18.94%).
    - `compute_drift` on two 400-point draws from `np.random.default_rng(0).normal(0, 1, ...)` returns `abs(standardized_mean_shift) < 0.5` and `flag` False; on a current draw shifted by 3.0 it returns `standardized_mean_shift` within 0.5 of 3.0 and `flag` True.
    - `compute_drift(pd.Series(dtype=float), pd.Series(dtype=float))` returns a dict without raising.
    - `drift.py` contains no matplotlib or seaborn import, asserted by a test that imports the module with `sys.modules` inspection or by `assert "matplotlib" not in drift.__dict__`.
    - `.venv/bin/python -m pytest tests/ -q` reports 0 failures.
  </acceptance_criteria>
  <done>Both check classes exist as pure functions with named-constant bands, the two historical regression cases fail their bands, and the live KPI table and Brier value pass their bands with the no-skill reference surfaced as 0.16.</done>
</task>

<task type="auto">
  <name>Task 3: Static notebook gate and the D-07 post-2020 observation record</name>
  <files>tests/unit/test_platform_notebooks.py, .planning/POST-2020-OBSERVATIONS.md</files>
  <read_first>
    - `notebooks/platform/P1_data_spine.ipynb` — the notebook this gate must pass against on creation.
    - `tests/test_pipeline_smoke.py` — the closest existing "parses and imports resolve without running" idiom in this repo.
    - `06-VALIDATION.md` section "Wave 0 Requirements", third bullet — the three checks this file owes.
    - `06-RESEARCH.md` section "Security Domain / Known Threat Patterns for this stack" — the two notebook-specific threats this gate is the control for.
    - `06-CONTEXT.md` decisions D-07, D-10, D-17.
  </read_first>
  <action>
Create `tests/unit/test_platform_notebooks.py` as a static-only gate — no cell execution, no
`nbmake`, no `papermill` (D-17). Parameterize every test over
`sorted(Path("notebooks/platform").glob("*.ipynb"))` so notebooks added by later plans are
covered without editing this file; skip the module when that directory holds no notebooks yet.

Checks, one test function each:
(1) `nbformat.read(path, as_version=4)` succeeds and the notebook has at least one cell — this
catches malformed JSON and a bad `nbformat_minor`.
(2) Criterion 3 enforcement: for every code cell, the cell source contains no direct import of
the matplotlib or seaborn top-level packages and no bare `plt.` or `sns.` attribute access, so
all figure construction routes through `platform/plotting/`. Build the forbidden-token list as a
module-level tuple constant so the assertion message can name which token was found in which
cell index.
(3) D-10 enforcement: no code cell source contains a `.save(` call, so a notebook can never write
a production checkpoint. Report the offending cell index in the failure message.
(4) Secret-hygiene enforcement (the Information Disclosure threat, T-06-01): no code cell displays
a whole platform config object. Assert that any cell whose source references the platform config
variable as its final expression or inside a `print` also routes it through
`redacted_config`. Implement this as: if a cell's source contains `load_platform_config`, the
notebook must contain at least one reference to `redacted_config` somewhere; and no cell source
may contain `print(cfg)` or a trailing bare `cfg` expression.
(5) Every notebook's first cell is a markdown cell whose source contains a prerequisite command,
asserted as the substring `scripts/` — the D-10 actionable-instructions convention applied to the
notebook header.
(6) Cross-notebook A13 discipline: for the notebooks named `P3_regime_labeling.ipynb`,
`P4_nowcaster.ipynb`, and `P6_backtest_evaluation.ipynb`, if the file exists, its combined cell
source must contain the substring `A13`. Guard each with an existence check so this test passes
in wave 1 and tightens automatically as those notebooks land.

Create `.planning/POST-2020-OBSERVATIONS.md`: the D-07 record, chosen as a markdown log because
CONTEXT.md's discretion note asks for the lightest option that stays greppable. Header explaining
that D-06 moved the fence from looking to fitting, that notebooks read the full span through
`load_full_span`, and that any post-2020 observation which changes a decision is recorded here
with its date so the influence stays traceable for a deflated-Sharpe denominator. Then a table
with columns `date`, `notebook`, `observation`, `decision changed`, `recorded by`, and one
seeded example row marked as a format example so the shape is unambiguous. State explicitly that
an empty table is a valid state and means no post-2020 observation has yet changed a decision.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_platform_notebooks.py -x -q && test -f .planning/POST-2020-OBSERVATIONS.md && grep -q 'load_full_span' .planning/POST-2020-OBSERVATIONS.md</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/unit/test_platform_notebooks.py -x -q` exits 0 with at least 5 tests collected against `notebooks/platform/P1_data_spine.ipynb`.
    - The test module contains no import of `nbmake` or `papermill` and calls no notebook-execution API, satisfying D-17.
    - Introducing a temporary notebook cell that constructs a figure directly makes `tests/unit/test_platform_notebooks.py` fail — demonstrate this once during execution by a scratch copy under `tmp_path`-style isolation, then discard it; record the observed failure in the plan SUMMARY.
    - Every test in the file is parameterized over a glob of `notebooks/platform/*.ipynb` rather than a hard-coded notebook list, verified by the absence of any literal notebook filename outside the three A13-discipline guards.
    - `.planning/POST-2020-OBSERVATIONS.md` exists, references `load_full_span`, and contains a table header with the columns `date`, `notebook`, `observation`, `decision changed`, `recorded by`.
    - `.venv/bin/python -m pytest tests/ -q` reports 0 failures.
  </acceptance_criteria>
  <done>Every notebook under `notebooks/platform/` is statically gated on parseability, criterion-3 compliance, checkpoint-write safety, and credential hygiene, and the D-07 record exists with a defined shape.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed notebook -> git | Notebook cell outputs are committed to the repository (verified convention of the twelve legacy notebooks), so anything a cell displays becomes durable, shareable content |
| notebook cell -> `data/checkpoints/platform/` | Notebook code can call the same `CheckpointManager` the fitting pipeline uses; a write here corrupts the fenced dev namespace every later phase depends on |
| local parquet on disk -> library loader | Checkpoint and report artifacts are read as untrusted-shaped data (schema may have drifted since they were written) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Information Disclosure | notebook cell displaying `load_platform_config()` output, which carries the injected `FRED_API_KEY` | high | mitigate | `loaders.redacted_config()` (Task 1) replaces every `*api_key` value with a literal placeholder; `tests/unit/test_platform_notebooks.py` check (4) (Task 3) fails any notebook that displays a whole config object without it |
| T-06-02 | Tampering | notebook cell calling `CheckpointManager.save` against `data/checkpoints/platform/` | medium | mitigate | `loaders.compute_regime_labeling()` routes `label_regimes` at `NOTEBOOK_SCRATCH_DIR` (Task 1); `tests/unit/test_platform_notebooks.py` check (3) (Task 3) fails any notebook whose cell source contains a save call |
| T-06-03 | Tampering | `platform/plotting/` re-coupling the platform to the legacy library, growing MIGRATION-PLAN.md §2's four-seam surface | low | mitigate | D-01 fresh package; a test in `tests/unit/test_platform_plotting.py` asserts no reachable module is under `trading_crab_lib.plotting` (Task 1) |
| T-06-04 | Repudiation | a post-2020 observation silently influencing a decision with no trace, voiding any later deflated-Sharpe denominator | medium | mitigate | `loaders.load_full_span_checkpoint` logs the post-cutoff row count at WARNING and names the record file; `.planning/POST-2020-OBSERVATIONS.md` created in Task 3 (D-07) |
| T-06-05 | Denial of Service | a plot function on a very wide frame producing thousands of axes and exhausting memory | low | accept | `plot_coverage_timeline` takes a `max_columns` bound; interactive notebook use is single-operator and locally bounded |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs zero new packages — matplotlib, seaborn, scipy and nbformat are already present in `.venv` via the existing `[plotting]` and core extras. `06-RESEARCH.md` records the Package Legitimacy Audit as not applicable for this reason. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/unit/test_platform_plotting.py tests/unit/test_platform_plotting_data.py tests/unit/test_platform_plotting_drift.py tests/unit/test_platform_notebooks.py -x -q` exits 0.
- `.venv/bin/python -m pytest tests/ -q` collects at least 1393 tests and reports 0 failures.
- `git status --porcelain outputs/reports/platform/` is empty — this plan changes no Phase-5 artifact.
- Human check (end-of-phase): `notebooks/platform/P1_data_spine.ipynb` runs top-to-bottom in Jupyter against the real `monthly_raw` checkpoint with no exception.
</verification>

<success_criteria>
- `platform/plotting/` exists with `core`, `loaders`, `data`, `drift`, and a barrel exporting only core and loader symbols.
- No module under `platform/plotting/` imports `trading_crab_lib.plotting`.
- The two named historical failures (60/40 at -2.27% max drawdown, terminal log wealth 111.06) both fail their bands in `tests/unit/test_platform_plotting_drift.py`.
- The live KPI table and Brier value pass their bands, with the K=5 no-skill Brier reference surfaced as 0.16 and the observed 0.2087 flagged as not beating it.
- Every notebook under `notebooks/platform/` is statically gated.
</success_criteria>

## Artifacts this phase produces

Symbols and files created by THIS plan (new; exclude from drift verification):

**New modules**
- `src/trading_crab_lib/platform/plotting/__init__.py`
- `src/trading_crab_lib/platform/plotting/core.py`
- `src/trading_crab_lib/platform/plotting/loaders.py`
- `src/trading_crab_lib/platform/plotting/data.py`
- `src/trading_crab_lib/platform/plotting/drift.py`

**New constants**
- `core.CUSTOM_COLORS`, `core.REGIME_CMAP`, `core.PLATFORM_PLOT_DIR`, `core.A13_CAVEAT`
- `loaders.NOTEBOOK_SCRATCH_DIR`
- `drift._UNIVERSAL_LOG_WEALTH_ABS_MAX`, `drift._DOMAIN_LOG_WEALTH_RANGE`, `drift._UNIVERSAL_MAX_DRAWDOWN_RANGE`, `drift._DOMAIN_MAX_DRAWDOWN_BANDS`, `drift._BRIER_RANGE`, `drift._TURNOVER_RANGE`, `drift._TURNOVER_OPERATIONAL_MAX`, `drift._CVAR_RANGE`, `drift._CVAR_OPERATIONAL_RANGE`, `drift._MIN_OCCUPANCY_WARN`, `drift._OCCUPANCY_SUM_TOLERANCE`, `drift._LEVEL_DISCONTINUITY_RATIO`, `drift._DRIFT_FLAG_THRESHOLD`

**New functions**
- `core._in_jupyter`, `core._regime_color`, `core._save_or_show`
- `loaders.load_platform_checkpoint`, `loaders.load_full_span_checkpoint`, `loaders.load_report_artifact`, `loaders.load_full_sample_states`, `loaders.load_filtered_state_probs`, `loaders.redacted_config`, `loaders.compute_regime_labeling`
- `data.plot_coverage_timeline`
- `drift.no_skill_brier`, `drift.assert_terminal_log_wealth_plausible`, `drift.assert_max_drawdown_plausible`, `drift.assert_brier_plausible`, `drift.assert_turnover_plausible`, `drift.assert_cvar_plausible`, `drift.assert_regime_occupancy_plausible`, `drift.assert_portfolio_weights_plausible`, `drift.assert_no_level_discontinuity`, `drift.assert_kpi_table_plausible`, `drift.baseline_and_current`, `drift.compute_drift`, `drift.drift_report`

**New files**
- `notebooks/platform/P1_data_spine.ipynb`
- `tests/unit/test_platform_plotting.py`
- `tests/unit/test_platform_plotting_data.py`
- `tests/unit/test_platform_plotting_drift.py`
- `tests/unit/test_platform_notebooks.py`
- `.planning/POST-2020-OBSERVATIONS.md`
- runtime scratch namespace `data/checkpoints/platform_notebook/` (created on first `compute_regime_labeling` call; gitignored)

<output>
Create `.planning/phases/06-platform-notebook-suite/06-01-SUMMARY.md` when done
</output>
