---
phase: 06-platform-notebook-suite
plan: 04
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/trading_crab_lib/platform/plotting/features.py
  - notebooks/platform/P2_features_taxonomy.ipynb
  - tests/unit/test_platform_plotting_features.py
autonomous: true
requirements: [NB-01]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "An operator opening P2 sees monthly_features' 53 columns grouped into fast/slow/agency/untagged tiers (per config/platform_settings.yaml's taxonomy block), so coverage gaps are visible per tier rather than in one undifferentiated grid."
    - "P2 states in prose that platform features are causal-only by construction and that a centered variant is architecturally forbidden by honesty/gating.py's FORBIDDEN_CENTERED_SUFFIXES guard — it never builds, seeks, or overlays a centered variant (Amendment 3 item I)."
    - "A feature whose observed range falls outside a named domain bound (fred_vix <= 0, a negative realized_vol_1m/3m, credit_spread_baa_aaa below -0.005, a non-positive cape_shiller/gold/oil, or div_yield outside [0, 0.15]) stops the notebook with a ValueError naming the feature and the bound it violated, rather than rendering a range chart over an impossible value."
    - "P2 re-runs the A4-style level-discontinuity guard against monthly_raw's agency-tier index columns (fred_cpi at minimum) on every open, so a rebuild that reintroduces an ALFRED rebasing discontinuity is caught here rather than discovered downstream in P3's labeling."
    - "P2 reads the full span (including post-2020) via the explicit holdout opt-in for its drift panel only, never for the plausibility/quality checks that gate on a possibly-impossible value, and its full-span cell reminds the operator that a decision-changing observation belongs in .planning/POST-2020-OBSERVATIONS.md (D-06/D-07)."
    - "A feature whose current-era distribution has shifted relative to the pre-2021 fitted window appears in P2's ranked drift table with a standardized mean shift, visible in a bar chart sorted by magnitude."
    - statement: "P2_features_taxonomy.ipynb runs top-to-bottom against the real monthly_features/monthly_raw checkpoints without raising."
      verification: backstop
  artifacts:
    - src/trading_crab_lib/platform/plotting/features.py
    - notebooks/platform/P2_features_taxonomy.ipynb
    - tests/unit/test_platform_plotting_features.py
  key_links:
    - "taxonomy.classify_feature/taxonomy.check_columns_tagged (platform/taxonomy.py, unmodified) are the single source of tier membership that features.tier_frames groups monthly_features columns by — never a re-derived tier list."
    - "features.py calls drift.assert_no_level_discontinuity and drift.drift_report (both from 06-01's platform/plotting/drift.py) rather than re-implementing plausibility or drift math."
    - "data.plot_coverage_timeline (06-01's platform/plotting/data.py) is reused directly per tier subset — P2 adds no second coverage-grid renderer."
  prohibitions:
    - statement: "P2 builds, seeks, or overlays no centered-feature variant — transforms_monthly.py has zero center/centered/causal occurrences and honesty/gating.py refuses any _centered/_c5/_zerophase column outright (Amendment 3 item I). The notebook's markdown prose MAY discuss FORBIDDEN_CENTERED_SUFFIXES by name; only CODE cells are constrained."
      status: pending
      verification: "tests/unit/test_platform_notebooks.py's per-code-cell check confirms no code cell contains the substring center=True (a two-sided rolling/ewm window) or uses a _centered/_c5/_zerophase-suffixed name as an actual column selector."
    - statement: "P2 carries no sign-off cell and no per-run gate — that is P3's exclusive responsibility (D-15)."
      status: pending
      verification: "tests/unit/test_platform_notebooks.py's existing glob-parameterized checks apply; a source assertion in this plan's own test file confirms the notebook contains no 'Sign-Off' heading."
    - statement: "P2 sweeps no hyperparameter and tunes nothing — it renders coverage, ranges, and quality for the shipped taxonomy and feature set only (D-14's no-sweep discipline extended to feature selection)."
      status: pending
      verification: flagged-unverified
    - statement: "P2 writes no checkpoint and calls no .save( in any code cell — it only reads via loaders.load_platform_checkpoint / loaders.load_full_span_checkpoint (D-10)."
      status: pending
      verification: "tests/unit/test_platform_notebooks.py's existing check (3) covers this notebook via its glob parameterization."
---

<objective>
Build P2 — the feature-taxonomy notebook. It groups `monthly_features`' columns into the
fast/slow/agency/untagged tiers the platform already declares in `config/platform_settings.yaml`,
shows per-tier coverage, flags any feature whose observed range is domain-impossible, re-checks
the A4-style level-discontinuity guard against the agency-tier raw series, and shows a ranked
drift table comparing the post-2020 window against the pre-2021 fitted baseline.

Purpose: `platform/plotting/` has zero taxonomy-aware rendering today — an operator cannot see,
at a glance, which of the 53 `monthly_features` columns are lean-set (fast/slow), which are
agency-tier (used sparingly), and which are untagged raw pass-through (the 21-ticker ETF universe
plus intermediate splice columns). This notebook is also this phase's one explicit answer to
Amendment 3 item I: it states in prose, rather than by omission, why no causal-vs-centered panel
exists.

Output: a new `platform/plotting/features.py` submodule (range table, tier coverage helper,
plausibility guard, discontinuity re-check, drift-summary plot), its test file, and the committed
`P2_features_taxonomy.ipynb`.
</objective>

<execution_context>
@/Users/glestryc/personal/github_repos/claude-scratch-work/.claude/gsd-core/workflows/execute-plan.md
@/Users/glestryc/personal/github_repos/claude-scratch-work/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-platform-notebook-suite/06-CONTEXT.md
@.planning/phases/06-platform-notebook-suite/06-PATTERNS.md
@.planning/phases/06-platform-notebook-suite/06-VALIDATION.md
@.planning/BASELINE-v1-tracer-bullet.md
@.planning/phases/06-platform-notebook-suite/06-01-SUMMARY.md
@config/platform_settings.yaml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Taxonomy tiering, coverage grouping, and the feature range table</name>
  <files>src/trading_crab_lib/platform/plotting/features.py, tests/unit/test_platform_plotting_features.py</files>
  <read_first>
    - `src/trading_crab_lib/platform/taxonomy.py` (full file, 86 lines) — `classify_feature(name, cfg) -> str | None`, `lean_feature_set(cfg) -> set[str]`, `check_columns_tagged(columns, cfg) -> list[str]`. These are the ONLY sources of tier membership; do not re-derive tier lists from `cfg["taxonomy"]` directly.
    - `config/platform_settings.yaml` lines 228-259 — the `taxonomy:` block: `fast` (10 columns: curve_10y3m, curve_10y2y, credit_spread_baa_aaa, fred_vix, gold, oil, trailing_return_1m, trailing_return_3m, realized_vol_1m, realized_vol_3m), `slow` (3: cape_shiller, div_yield, real_rate_level), `agency` (5: fred_gdp, fred_cpi, fred_unrate, fred_indpro, fred_payems).
    - `06-01-PLAN.md` Task 1's `<action>` block — the exact signature of `data.plot_coverage_timeline(df, *, title="Column coverage", max_columns=60, save_path=None, show=False) -> plt.Figure` this task reuses (imported by submodule path, never re-implemented) and the `core._save_or_show(fig, *, save_path, show) -> plt.Figure` contract every new function here must also finish through.
    - `tests/unit/test_plotting.py` lines 1-80 — the Agg-backend header, seeded synthetic-frame fixtures, does-not-crash + empty-input class shape this task's test file follows.
  </read_first>
  <behavior>
    - `tier_frames(df, cfg)` on a synthetic frame with 3 fast, 2 slow, 1 agency, 2 untagged columns returns a dict with keys "fast", "slow", "agency", "untagged", each a DataFrame slice with exactly the expected columns; an empty input returns four empty-column DataFrames without raising.
    - `feature_range_table(df, cfg)` on the same synthetic frame returns one row per column with `feature`, `tier` ("untagged" for the two untagged columns), `n_obs`, `first_valid`, `last_valid`, `min`, `max`, `mean`, `std`; a column that is entirely NaN reports `n_obs=0` and NaN range/mean/std rather than raising.
    - `plot_feature_ranges(range_table)` returns a Figure for a populated table, a tier-filtered subset, and an empty `pd.DataFrame()`.
  </behavior>
  <action>
Create `src/trading_crab_lib/platform/plotting/features.py` with `from __future__ import annotations`,
a module docstring, and `log = logging.getLogger(__name__)`. Import `core` and `taxonomy` by
module path (`from trading_crab_lib.platform.plotting import core`, `from trading_crab_lib.platform
import taxonomy`) — never import from the legacy `trading_crab_lib.plotting` package (D-01).

`tier_frames(df: pd.DataFrame, cfg: dict) -> dict[str, pd.DataFrame]` classifies every column of
`df` via `taxonomy.classify_feature(col, cfg)`, grouping columns whose classification is `None`
under the key `"untagged"`; returns a dict with exactly the four keys `"fast"`, `"slow"`,
`"agency"`, `"untagged"`, each a `df[[...]]` column-subset DataFrame (empty-columns DataFrame,
never `None`, when a tier has no members).

`feature_range_table(df: pd.DataFrame, cfg: dict) -> pd.DataFrame` builds one row per column of
`df`: `feature` (the column name), `tier` (`taxonomy.classify_feature(col, cfg)` or the literal
string `"untagged"` when `None`), `n_obs` (`col.notna().sum()`), `first_valid`
(`col.first_valid_index()`), `last_valid` (`col.last_valid_index()`), `min`, `max`, `mean`, `std`
(computed on `col.dropna()`; an all-NaN column reports `n_obs=0` and NaN for the numeric fields,
never raises). Sort rows by `tier` then `feature` for a stable, readable table.

`plot_feature_ranges(range_table: pd.DataFrame, *, tier: str | None = None, title: str =
"Feature ranges", save_path: Path | None = None, show: bool = False) -> plt.Figure` renders one
horizontal bar per feature spanning `[min, max]` with a marker at `mean`, optionally filtered to
rows where `range_table["tier"] == tier`; handles an empty (or empty-after-filter) table by
returning a figure carrying a "no data" annotation rather than raising; finishes through
`core._save_or_show`.

Every new function follows the platform signature `plot_x(data, *, save_path: Path | None = None,
show: bool = False) -> plt.Figure` where applicable, uses `pathlib.Path`, and stays within ruff's
127-character line length.

Create `tests/unit/test_platform_plotting_features.py` on the `tests/unit/test_plotting.py`
shape: Agg backend forced in the module header, a seeded `np.random.default_rng(42)` synthetic
monthly fixture (`pd.date_range(..., freq="ME")`, ~40 rows) with 3 fast-tagged, 2 slow-tagged, 1
agency-tagged, and 2 untagged columns (a synthetic `cfg["taxonomy"]` dict naming exactly those
6 tagged columns), one `TestXxx` class per public function with a does-not-crash case and an
empty-input case, plus the `TestTierFrames` case above and a test asserting `feature_range_table`
does not raise on an all-NaN column. Add a test asserting `features.py`'s module source contains
no direct top-level plotting-library import of its own — matplotlib access is routed exclusively
through the already-imported `core` module — and a test asserting no module reachable from
`trading_crab_lib.platform.plotting.features` has a `__name__` beginning `trading_crab_lib.plotting`
(D-01, mirroring 06-01's own per-package check).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_platform_plotting_features.py -x -q && .venv/bin/python -c "
from trading_crab_lib.platform.config import load_platform_config
from trading_crab_lib.platform.checkpoints import get_platform_checkpoint_manager
from trading_crab_lib.platform.plotting import features as pfeatures
cfg = load_platform_config()
mf = get_platform_checkpoint_manager().load('monthly_features')
tiers = pfeatures.tier_frames(mf, cfg)
assert set(tiers) == {'fast', 'slow', 'agency', 'untagged'}, set(tiers)
assert tiers['fast'].shape[1] == 10 and tiers['slow'].shape[1] == 3 and tiers['agency'].shape[1] == 5, {k: v.shape[1] for k, v in tiers.items()}
assert tiers['untagged'].shape[1] == 35, tiers['untagged'].shape[1]
rt = pfeatures.feature_range_table(mf, cfg)
assert len(rt) == 53, len(rt)
fig = pfeatures.plot_feature_ranges(rt, tier='fast')
assert fig is not None
print('P2 range/tier smoke OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/unit/test_platform_plotting_features.py -x -q` exits 0.
    - `tier_frames(monthly_features, cfg)` against the real dev checkpoint returns keys `{"fast","slow","agency","untagged"}` with column counts 10/3/5/35 respectively (current on-disk reference: 53 total columns).
    - `feature_range_table(monthly_features, cfg)` returns exactly 53 rows, one per column, with no unhandled exception on the columns that carry leading NaNs (e.g. `curve_10y2y`, first-valid 1976-06).
    - `plot_feature_ranges(...)` returns an object satisfying `isinstance(fig, matplotlib.figure.Figure)` for a populated table, a tier-filtered table, and `pd.DataFrame()`.
    - A test in `tests/unit/test_platform_plotting_features.py` asserts no module reachable from `trading_crab_lib.platform.plotting.features` has a `__name__` beginning `trading_crab_lib.plotting` (D-01).
    - `.venv/bin/python -m pytest tests/ -q` reports 0 failures.
  </acceptance_criteria>
  <done>`tier_frames` and `feature_range_table` exist as tested library functions and reproduce the real taxonomy's 10/3/5/35 tier split; `plot_feature_ranges` renders for both populated and empty input.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Plausibility guard, agency-level discontinuity re-check, and drift summary</name>
  <files>src/trading_crab_lib/platform/plotting/features.py, tests/unit/test_platform_plotting_features.py</files>
  <read_first>
    - `06-01-PLAN.md` Task 2's `<action>` block — the exact signatures this task reuses without re-implementing: `drift.assert_no_level_discontinuity(series, *, name, ratio_threshold=_LEVEL_DISCONTINUITY_RATIO) -> list[pd.Timestamp]` (raises `ValueError` naming the offending dates on a >1.5x month-over-month jump); `drift.compute_drift(current, baseline) -> dict` (keys `n_baseline`, `n_current`, `baseline_mean`, `baseline_std`, `current_mean`, `standardized_mean_shift`, `ks_statistic`, `ks_pvalue`, `pct_beyond_2sigma`, `flag`); `drift.drift_report(full_df, columns, *, cutoff=DEFAULT_HOLDOUT_CUTOFF) -> pd.DataFrame` (one row per column, sorted by descending absolute standardized mean shift); `drift.baseline_and_current(full_df, cutoff) -> tuple[pd.DataFrame, pd.DataFrame]`.
    - `src/trading_crab_lib/platform/splice.py` lines 124-155 (`assert_yield_units_plausible`) — the raise/warn idiom (module-level named constants, one collect-then-raise `ValueError`, `log.warning` only on the ambiguous direction) this task's `assert_feature_ranges_plausible` follows.
    - The real per-feature ranges captured during planning (verified against the live dev checkpoint): `fred_vix` min 9.51 max 59.89; `realized_vol_1m` min 0.0001 max 0.2014; `realized_vol_3m` min 0.0007 max 0.1132; `credit_spread_baa_aaa` min 0.32 max 3.38; `cape_shiller` min 6.64 max 44.19; `div_yield` min 0.0111 max 0.0624; `gold` min 254.60; `oil` min 10.25 — every one of these currently sits inside the bounds this task encodes, and the acceptance criteria assert exactly that.
    - `06-CONTEXT.md` amendment note D — the A4 CPI level-discontinuity defect (1970-12/1971-01 and 1988-01/1988-02 ~3x jumps in `fred_cpi`), now fixed; this task's discontinuity re-check is the regression guard that keeps it fixed.
  </read_first>
  <behavior>
    - `assert_feature_ranges_plausible(range_table)` on the real range table returns a verdict DataFrame with one row per checked feature and raises nothing.
    - `assert_feature_ranges_plausible` on a range table where `fred_vix`'s `min` is set to -5.0 raises `ValueError` naming `fred_vix`.
    - `assert_feature_ranges_plausible` on a range table where `div_yield`'s `max` is set to 0.40 raises `ValueError` naming `div_yield`.
    - `check_agency_level_discontinuities(monthly_raw, columns=("fred_cpi",))` on the real dev checkpoint returns an empty dict (A4 stays fixed).
    - `check_agency_level_discontinuities` on a synthetic series that triples month-over-month raises `ValueError` naming the offending column and date.
    - `plot_drift_summary(drift_report_df)` returns a Figure for a populated and an empty drift table.
  </behavior>
  <action>
Extend `src/trading_crab_lib/platform/plotting/features.py`.

Define module-level named constants (never inline magic numbers), sized from the real ranges
captured during planning with headroom, not tuned to make the check trivially pass:
`_VIX_MIN_PLAUSIBLE = 0.0` (the VIX index cannot be zero or negative by construction);
`_VOL_MIN_PLAUSIBLE = 0.0` (a realized volatility cannot be negative);
`_CREDIT_SPREAD_MIN_PLAUSIBLE = -0.005` (Baa-Aaa should not go meaningfully negative; small
numerical tolerance only); `_CAPE_MIN_PLAUSIBLE = 0.0` (a P/E ratio is positive);
`_COMMODITY_MIN_PLAUSIBLE = 0.0` (a spot commodity price is positive; applies to `gold`/`oil`);
`_DIV_YIELD_RANGE = (0.0, 0.15)` (S&P 500 dividend yield has never been negative or exceeded
15% in the platform's 1962+ history). Map feature name to (bound-kind, bound-value) in a single
`_RANGE_CHECKS: dict[str, dict]` module constant so the check function iterates data, not
branches.

`assert_feature_ranges_plausible(range_table: pd.DataFrame) -> pd.DataFrame` iterates
`_RANGE_CHECKS`, looks up each named feature's row in `range_table` by `feature` (skips silently
if that feature is absent from the table — e.g. a future taxonomy edit), collects EVERY
violation (not just the first) into a list, and raises ONE `ValueError` listing all violations
if any exist (the `config.validate_config` / `drift.assert_kpi_table_plausible` collect-then-raise
idiom) — the error message names the feature, its observed value, and the bound it violated.
On success returns a tidy verdict frame with columns `feature`, `bound`, `observed`, `verdict`.

`check_agency_level_discontinuities(monthly_raw: pd.DataFrame, *, columns: tuple[str, ...] =
("fred_cpi",)) -> dict[str, list[pd.Timestamp]]` calls `drift.assert_no_level_discontinuity`
once per column in `columns` present in `monthly_raw`, catching `ValueError` per column (never
letting the first violation short-circuit the rest), and re-raises ONE `ValueError` at the end
listing every offending column and its dates if any column violated — mirroring the collect-all
idiom above rather than the underlying function's per-call raise. Returns an empty dict when
every checked column is clean. This is the regression guard for audit item A4 (the CPI rebasing
discontinuity) — document that connection in the docstring.

`plot_drift_summary(drift_report_df: pd.DataFrame, *, title: str = "Feature drift vs pre-2021
baseline", save_path: Path | None = None, show: bool = False) -> plt.Figure` renders a horizontal
bar per feature of `standardized_mean_shift`, sorted by the input order (already
magnitude-sorted by `drift.drift_report`), color-flags bars at or beyond `drift._DRIFT_FLAG_THRESHOLD`
(imported from `trading_crab_lib.platform.plotting.drift`) in a distinct color from in-band bars,
and handles an empty input via a "no data" annotation; finishes through `core._save_or_show`.

Extend `tests/unit/test_platform_plotting_features.py` with the behavior cases above: an in-band
case against the real range table shape (synthetic fixture matching the real ranges, not the
live checkpoint, so the test stays network-free and deterministic) and one out-of-band case per
named bound (`fred_vix`, `realized_vol_1m`, `credit_spread_baa_aaa`, `cape_shiller`, `gold`,
`div_yield`); a discontinuity clean-case and a tripling-series raise-case; does-not-crash and
empty-input cases for `plot_drift_summary`.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_platform_plotting_features.py -x -q && .venv/bin/python -c "
from trading_crab_lib.platform.config import load_platform_config
from trading_crab_lib.platform.checkpoints import get_platform_checkpoint_manager
from trading_crab_lib.platform.plotting import features as pfeatures
cfg = load_platform_config()
mf = get_platform_checkpoint_manager().load('monthly_features')
mr = get_platform_checkpoint_manager().load('monthly_raw')
rt = pfeatures.feature_range_table(mf, cfg)
verdict = pfeatures.assert_feature_ranges_plausible(rt)
print(verdict.to_string())
clean = pfeatures.check_agency_level_discontinuities(mr, columns=('fred_cpi',))
assert clean == {}, clean
print('P2 plausibility/discontinuity smoke OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/unit/test_platform_plotting_features.py -x -q` exits 0.
    - `assert_feature_ranges_plausible` run against the real dev `monthly_features` range table raises nothing and returns a verdict frame covering every named feature present.
    - A synthetic range table with `fred_vix` min set to -5.0 makes `assert_feature_ranges_plausible` raise `ValueError` whose message contains `fred_vix`; a synthetic table with `div_yield` max set to 0.40 makes it raise `ValueError` whose message contains `div_yield`. Both cases are present in the test file.
    - `check_agency_level_discontinuities(monthly_raw, columns=("fred_cpi",))` run against the real dev checkpoint returns `{}` (A4 regression guard passes).
    - A synthetic series that triples month-over-month makes `check_agency_level_discontinuities` raise `ValueError` naming the column and the offending date; this case is present in the test file.
    - `plot_drift_summary(...)` returns a `matplotlib.figure.Figure` for both a populated and an empty `pd.DataFrame()`.
    - `features.py` contains no re-implementation of the discontinuity or drift math — `git diff` (once committed) shows only new function bodies calling `drift.assert_no_level_discontinuity` / `drift.compute_drift` / `drift.drift_report`, never duplicating their internals.
    - `.venv/bin/python -m pytest tests/ -q` reports 0 failures.
  </acceptance_criteria>
  <done>The named-bound feature-range plausibility check, the A4 discontinuity regression guard, and the drift-summary plot all exist as tested library functions, and the live dev checkpoint passes both checks cleanly.</done>
</task>

<task type="auto">
  <name>Task 3: P2_features_taxonomy notebook — tiered coverage, ranges, quality, drift, causal-only framing</name>
  <files>notebooks/platform/P2_features_taxonomy.ipynb</files>
  <read_first>
    - `notebooks/platform/P1_data_spine.ipynb` (from 06-01) — the cell structure, setup-cell import block, and the loader call convention to follow.
    - `src/trading_crab_lib/platform/plotting/loaders.py` (06-01) — `load_platform_checkpoint`, `load_full_span_checkpoint`, `redacted_config`.
    - `src/trading_crab_lib/platform/plotting/data.py` (06-01) — `plot_coverage_timeline(df, *, title, max_columns, save_path, show)`, reused here per tier.
    - `src/trading_crab_lib/platform/plotting/features.py` (Tasks 1-2) — `tier_frames`, `feature_range_table`, `plot_feature_ranges`, `assert_feature_ranges_plausible`, `check_agency_level_discontinuities`, `plot_drift_summary`.
    - `src/trading_crab_lib/platform/plotting/drift.py` (06-01) — `drift_report`, `DEFAULT_HOLDOUT_CUTOFF` (imported from `trading_crab_lib.platform.honesty.holdout`).
    - `src/trading_crab_lib/platform/honesty/gating.py` (full file, 104 lines) — `FORBIDDEN_CENTERED_SUFFIXES = ("_centered", "_c5", "_zerophase")`, `assert_causal_features`. This is the exact guard the notebook's causal-only prose cites.
    - `.planning/POST-2020-OBSERVATIONS.md` (06-01) — the record this notebook's full-span cell points to.
    - `06-CONTEXT.md` AMENDMENT 3 item I — the exact required prose framing.
  </read_first>
  <action>
Create `notebooks/platform/P2_features_taxonomy.ipynb` via the `nbformat` v4 programmatic API.
No cell may construct a figure, touch an axis, import a plotting library directly, or write to
a checkpoint. Every figure comes from `platform/plotting/`. Run it once and commit it with
outputs.

Cell sequence:

1. Markdown title `P2 — Features & Taxonomy` with a one-paragraph purpose statement and the
explicit prerequisite line naming `python scripts/build_platform_data.py`.

2. Setup code cell: `%matplotlib inline`, `import logging`, `logging.basicConfig(level=logging.INFO)`,
`from trading_crab_lib.platform.config import load_platform_config`,
`from trading_crab_lib.platform import plotting as pplot`,
`from trading_crab_lib.platform.plotting import data as pdata`,
`from trading_crab_lib.platform.plotting import features as pfeatures`,
`from trading_crab_lib.platform.plotting import drift as pdrift`,
`cfg = load_platform_config()`. Display config only through `pplot.redacted_config(cfg)` if
displayed at all.

3. Markdown: the causal-only framing (Amendment 3 item I), stated in these terms — platform
features are causal-only by construction (`transforms_monthly.py` has zero `center`/`centered`/
`causal` occurrences); a centered variant is architecturally forbidden by
`honesty.gating.FORBIDDEN_CENTERED_SUFFIXES`, which raises on sight of any `_centered`/`_c5`/
`_zerophase` column; the only intentional hindsight anywhere in the system is the L1 labeler's
own non-causal batch DP-decode, never the features themselves. State explicitly that this
notebook therefore carries no causal-vs-centered comparison panel, unlike the legacy quarterly
pipeline's ADR #1 split.

4. Code cell: load `monthly_features` and `monthly_raw` (dev) via `pplot.load_platform_checkpoint`
with rebuild hint `python scripts/build_platform_data.py`; print shapes and index bounds.

5. Markdown + code: tiered coverage. Call `pfeatures.tier_frames(monthly_features, cfg)`, then
call `pdata.plot_coverage_timeline` once per non-empty tier (`fast`, `slow`, `agency`,
`untagged`) with a tier-named title, e.g. `"monthly_features coverage — fast tier"`. Markdown
states what to look for: any lean-tier (fast/slow) feature whose coverage starts unexpectedly
late, and that agency-tier and untagged coverage gaps are lower-priority (agency is "used
sparingly" by design; untagged is raw pass-through).

6. Code cell: `range_table = pfeatures.feature_range_table(monthly_features, cfg)`; print the
full table; render `pfeatures.plot_feature_ranges(range_table, tier="fast")` and
`pfeatures.plot_feature_ranges(range_table, tier="slow")`.

7. Code cell: quality gate — `verdict = pfeatures.assert_feature_ranges_plausible(range_table)`
BEFORE any further rendering (an impossible range stops the notebook here); print `verdict`.
Then `clean = pfeatures.check_agency_level_discontinuities(monthly_raw, columns=("fred_cpi",))`;
print `clean` with a markdown note that an empty dict means the A4 CPI-rebasing defect remains
fixed.

8. Markdown then code: the D-06/D-07 full-span "looking" panel. State explicitly that this cell
crosses the holdout fence to LOOK, never to fit, and that any observation here which changes a
decision belongs in `.planning/POST-2020-OBSERVATIONS.md`. Load
`full_features = pplot.load_full_span_checkpoint("monthly_features", rebuild_hint="python
scripts/build_platform_data.py")`; build the lean column list via
`sorted(taxonomy.lean_feature_set(cfg) & set(full_features.columns))` (import
`trading_crab_lib.platform.taxonomy` directly in this cell); call
`pdrift.drift_report(full_features, lean_cols, cutoff=pdrift_cutoff)` where `pdrift_cutoff` comes
from `trading_crab_lib.platform.honesty.holdout.DEFAULT_HOLDOUT_CUTOFF`; print the table and
render `pfeatures.plot_drift_summary(drift_df)`.

9. Final markdown cell: summary of what an operator is checking (coverage gaps by tier, any
flagged implausible range, the A4 discontinuity guard result, and which lean features show the
largest pre/post-2020 drift) — explicitly state this notebook carries NO sign-off cell and NO
per-run gate (that is P3's exclusive responsibility, D-15); this is a periodic V&V surface.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_platform_notebooks.py -x -q && .venv/bin/python -c "
import nbformat, pathlib
nb = nbformat.read(pathlib.Path('notebooks/platform/P2_features_taxonomy.ipynb'), as_version=4)
src = '\n'.join(c.source for c in nb.cells)
code_src = '\n'.join(c.source for c in nb.cells if c.cell_type == 'code')
for token in ['FORBIDDEN_CENTERED_SUFFIXES', 'causal-only', 'load_full_span_checkpoint', 'POST-2020-OBSERVATIONS', 'assert_feature_ranges_plausible', 'check_agency_level_discontinuities', 'drift_report']:
    assert token in src, token
for banned in ['import matplotlib', 'import seaborn', '.save(', 'Sign-Off']:
    assert banned not in code_src, banned
assert 'center=True' not in code_src, 'code cell constructs a centered window'
print('P2 static checks OK', len(nb.cells), 'cells')"</automated>
    <human-check>Open `notebooks/platform/P2_features_taxonomy.ipynb` in Jupyter, Run All against the real checkpoints, and confirm every cell completes with no exception.</human-check>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/unit/test_platform_notebooks.py -x -q` exits 0 with `P2_features_taxonomy.ipynb` included in the parameterization.
    - The notebook's combined cell source contains `FORBIDDEN_CENTERED_SUFFIXES`, `causal-only`, `load_full_span_checkpoint`, `POST-2020-OBSERVATIONS`, `assert_feature_ranges_plausible`, `check_agency_level_discontinuities`, and `drift_report`; no code cell contains `import matplotlib`, `import seaborn`, `.save(`, `Sign-Off`, or `center=True`.
    - Executing the notebook, `tier_frames` reports 10/3/5/35 fast/slow/agency/untagged columns against the real 53-column dev `monthly_features`.
    - Executing the notebook, `assert_feature_ranges_plausible` raises nothing against the live data (every named feature passes its bound).
    - Executing the notebook, `check_agency_level_discontinuities(monthly_raw, columns=("fred_cpi",))` returns `{}`.
    - Executing the notebook, the full-span drift panel loads `monthly_features` via `load_full_span_checkpoint` (776 rows through 2026-08, current on-disk reference) and renders a non-empty drift table over the 13 lean columns.
    - The notebook is committed with its cell outputs, matching the storage convention of the twelve legacy notebooks.
    - `.venv/bin/python -m pytest tests/ -q` reports 0 failures.
  </acceptance_criteria>
  <done>P2 renders tiered coverage, ranges, a plausibility-gated quality check, the A4 discontinuity regression guard, and a full-span drift summary, all sourced from `platform/plotting/features.py`, and states the causal-only framing in place of a forbidden causal-vs-centered panel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed notebook -> git | P2's cell outputs become durable repository content, including anything a cell displays |
| local parquet on disk -> library loader | `monthly_features`/`monthly_raw` are read as untrusted-shaped data (schema may have drifted since written) |
| pre-2021 fitted window -> post-2020 look | P2's drift panel crosses the holdout fence to look, never to fit; the boundary is enforced by which loader function is called, not by any runtime guard |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-15 | Information Disclosure | notebook cell displaying `load_platform_config()` output, which carries the injected `FRED_API_KEY` | high | mitigate | The notebook routes any config display through `loaders.redacted_config`; `tests/unit/test_platform_notebooks.py` check (4) from 06-01 fails the notebook otherwise |
| T-06-16 | Repudiation | a post-2020 observation from the drift panel silently influencing a later decision with no trace | medium | mitigate | Cell 8's markdown explicitly names `.planning/POST-2020-OBSERVATIONS.md` as where a decision-changing observation is recorded (D-07); the full-span load is its own clearly-labeled cell, never blended into the fitting-fenced range/quality cells (D-06) |
| T-06-17 | Tampering | a rebuild silently reintroducing the A4 CPI-rebasing discontinuity (or a similar defect in another agency-tier series) without being noticed | medium | mitigate | `check_agency_level_discontinuities` re-runs `drift.assert_no_level_discontinuity` against `fred_cpi` on every notebook open and raises loudly on a violation, rather than relying on the fix having been made once |
| T-06-18 | Denial of Service | rendering a coverage grid or range chart over a very wide tier (e.g. the 35-column untagged tier) producing an oversized or slow figure | low | accept | `plot_coverage_timeline` (06-01) already bounds `max_columns`; `plot_feature_ranges` operates on one tier at a time via the `tier` filter, keeping any single figure to at most 35 rows |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs zero new packages — matplotlib, pandas, and nbformat are already present. `06-RESEARCH.md` records the Package Legitimacy Audit as not applicable for this reason. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/unit/test_platform_plotting_features.py tests/unit/test_platform_notebooks.py -x -q` exits 0.
- `.venv/bin/python -m pytest tests/ -q` collects at least 1393 tests and reports 0 failures.
- `git status --porcelain src/trading_crab_lib/platform/taxonomy.py src/trading_crab_lib/platform/transforms_monthly.py outputs/reports/platform/` is empty — this plan reads Phase 1-5 code and reference artifacts, never modifies them.
- Human check (end-of-phase): `notebooks/platform/P2_features_taxonomy.ipynb` runs top-to-bottom in Jupyter against the real `monthly_features`/`monthly_raw` checkpoints with no exception.
</verification>

<success_criteria>
- `platform/plotting/features.py` exists with tier grouping, range table, plausibility guard, discontinuity regression guard, and drift-summary rendering.
- `tier_frames` reproduces the real 10/3/5/35 fast/slow/agency/untagged split.
- The live dev checkpoint passes both the range-plausibility check and the A4 discontinuity regression guard cleanly.
- P2 states the causal-only framing in prose and carries no causal-vs-centered panel (Amendment 3 item I).
- Every figure in P2 comes from `platform/plotting/` (ROADMAP criterion 3); P2 carries no sign-off cell (D-15 stays P3-exclusive).
</success_criteria>

## Artifacts this phase produces

Symbols and files created by THIS plan (new; exclude from drift verification):

**New modules**
- `src/trading_crab_lib/platform/plotting/features.py`

**New constants**
- `features._VIX_MIN_PLAUSIBLE`, `features._VOL_MIN_PLAUSIBLE`, `features._CREDIT_SPREAD_MIN_PLAUSIBLE`, `features._CAPE_MIN_PLAUSIBLE`, `features._COMMODITY_MIN_PLAUSIBLE`, `features._DIV_YIELD_RANGE`, `features._RANGE_CHECKS`

**New functions**
- `features.tier_frames`, `features.feature_range_table`, `features.plot_feature_ranges`, `features.assert_feature_ranges_plausible`, `features.check_agency_level_discontinuities`, `features.plot_drift_summary`

**New files**
- `notebooks/platform/P2_features_taxonomy.ipynb`
- `tests/unit/test_platform_plotting_features.py`

<output>
Create `.planning/phases/06-platform-notebook-suite/06-04-SUMMARY.md` when done
</output>
