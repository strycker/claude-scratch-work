---
phase: 06-platform-notebook-suite
plan: 07
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified:
  - src/trading_crab_lib/platform/plotting/backtest.py
  - notebooks/platform/P6_backtest_evaluation.ipynb
  - tests/unit/test_platform_plotting_backtest.py
autonomous: true
requirements: [NB-01]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "An operator opening P6 sees equity curves for all five legs (strategy, no-regime ablation, SPY buy-and-hold, 60/40, Faber 10-month SMA) on one chart, the two ML legs loaded from Phase 5's persisted equity-curve artifacts and the three deterministic baselines recomputed cheaply from the same splice math report.py uses — never a re-run of run_full_backtest_evaluation()."
    - "P6 shows the no-regime-ablation delta (strategy minus ablation, both terminal log wealth and max drawdown) as an explicit computed pair of numbers, answering design section 8.7's 'does the regime layer pay rent' question."
    - "P6 renders a calibration summary from the SAME persisted model_metrics_calibration.parquet P4 loads, checked implicitly through drift.assert_brier_plausible on the persisted Brier value before any KPI table is displayed."
    - "P6 renders the sojourn/lag headline (median sojourn, median detection lag, ratio, resolved-transition count) recomputed from the two artifacts 06-02 persisted, and wherever the ratio appears it carries the verbatim core.A13_CAVEAT string plus the resolved-of-total transition count printed alongside it — no task claims A13 is resolved and no numeric band is presented as resolving it."
    - "P6 narrates that the Faber and 60/40 baseline legs were once implausible and are now corrected (citing UAT-AUDIT-2026-09-09.md for detail) without ever repeating the specific historical incorrect figures as if they were current — every KPI value P6 displays is read from the live backtest_kpi_table.parquet at render time, never hardcoded."
    - "Every KPI value P6 displays (terminal log wealth, max drawdown, per-leg) passes drift.assert_kpi_table_plausible before the KPI bar chart renders, so an impossible value stops the notebook rather than rendering silently."
    - "P6's drift panel shows whether the core research asset returns (equities_tr, long_duration_tr, gold, oil, cash) have shifted post-2020 relative to the pre-2021 fitted window."
    - statement: "P6_backtest_evaluation.ipynb runs top-to-bottom against the real monthly_raw checkpoint and the nine persisted outputs/reports/platform/ artifacts (seven from Phase 5, two from 06-02) without raising."
      verification: backstop
  artifacts:
    - src/trading_crab_lib/platform/plotting/backtest.py
    - notebooks/platform/P6_backtest_evaluation.ipynb
    - tests/unit/test_platform_plotting_backtest.py
  key_links:
    - "loaders.load_report_artifact / loaders.load_full_sample_states / loaders.load_filtered_state_probs (all 06-01) are the ONLY paths P6 uses to read the seven Phase-5 artifacts and 06-02's two new ones — P6 never calls run_backtest() or run_full_backtest_evaluation()."
    - "backtest.recompute_baseline_curves mirrors evaluation/report.py's baseline construction (lines 562-571) exactly — same split_by_holdout_boundary calls, same spy_buy_hold/sixty_forty/faber_sma calls, same cost_bps convention — so the three deterministic legs are provably the SAME curves report.py's KPI table summarizes, not an approximation."
    - "core.A13_CAVEAT (06-01) is rendered verbatim wherever the sojourn/lag ratio appears — the same string P3 (06-03) and P4 (06-05) render, so the caveat cannot drift between notebooks."
    - "06-02's backtest_full_sample_states.parquet / backtest_filtered_state_probs.parquet are the two artifacts that make the sojourn/lag headline recomputable here without a fresh walk-forward — this is why depends_on includes 06-02."
  prohibitions:
    - statement: "P6 calls neither run_backtest() nor run_full_backtest_evaluation() anywhere — every ML-strategy number comes from Phase 5's persisted parquet artifacts; only the three deterministic, no-tunable-parameter baseline curves (spy_buy_hold/sixty_forty/faber_sma) are recomputed in-notebook."
      status: pending
      verification: "tests/unit/test_platform_notebooks.py plus a source assertion that no P6 cell references either function name."
    - statement: "No task in this plan claims audit item A13 is resolved, and no task presents a numeric band around the sojourn/lag ratio as resolving it — a valid-looking number computed against a mismatched target is a third failure mode neither drift nor plausibility checks catch (06-VALIDATION.md's standing lesson)."
      status: pending
      verification: flagged-unverified
    - statement: "No task in this plan cites the historical void Faber/60-40 KPI figures as current — every displayed value is read from the live backtest_kpi_table.parquet at render/verification time."
      status: pending
      verification: flagged-unverified
    - statement: "P6 sweeps no hyperparameter and tunes nothing (D-14 extended to evaluation display)."
      status: pending
      verification: flagged-unverified
    - statement: "P6 introduces no sign-off cell and no per-run gate — that is P3's exclusive responsibility (D-15)."
      status: pending
      verification: "a source assertion in this plan's own test file confirms the notebook contains no 'Sign-Off' heading."
    - statement: "No task in this plan writes to data/checkpoints/platform/ or modifies any outputs/reports/platform/ artifact — recomputed baseline curves and any saved figures live only in notebook memory or under outputs/plots/platform/."
      status: pending
      verification: flagged-unverified
---

<objective>
Build P6 — the largest of the six notebooks and the phase's evaluation capstone. It renders all
five backtest legs' equity curves, the no-regime-ablation delta, a calibration summary, the KPI
table under its exact plausibility bands, and the sojourn/lag headline with its resolved-transition
count and the mandatory "not interpretable — see A13" caveat — assembling ROADMAP criterion 5's
five required elements from artifacts that already exist on disk, without re-running the
588-refit walk-forward.

Purpose: `run_full_backtest_evaluation()` is minutes of sklearn work per run and has already been
run once (Phase 5) plus regenerated once (06-02, additively). P6's job is to make what that run
already produced INSPECTABLE, not to run it again. The two hardest requirements are structural
honesty, not new computation: rendering the sojourn/lag ratio with its caveat intact everywhere
it appears, and narrating the Faber/60-40 anomaly's fix history without ever re-presenting the
now-void historical numbers as current.

Output: a new `platform/plotting/backtest.py` submodule (baseline recompute, equity-curve chart,
KPI bars, calibration summary, sojourn/lag headline chart), its test file, and the committed
`P6_backtest_evaluation.ipynb`.
</objective>

<execution_context>
@/Users/glestryc/personal/github_repos/claude-scratch-work/.claude/gsd-core/workflows/execute-plan.md
@/Users/glestryc/personal/github_repos/claude-scratch-work/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-platform-notebook-suite/06-CONTEXT.md
@.planning/phases/06-platform-notebook-suite/06-PATTERNS.md
@.planning/phases/06-platform-notebook-suite/06-VALIDATION.md
@.planning/BASELINE-v1-tracer-bullet.md
@.planning/phases/06-platform-notebook-suite/06-01-SUMMARY.md
@.planning/phases/06-platform-notebook-suite/06-02-SUMMARY.md
@config/platform_settings.yaml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deterministic baseline recompute, equity-curve chart, KPI bars, ablation delta</name>
  <files>src/trading_crab_lib/platform/plotting/backtest.py, tests/unit/test_platform_plotting_backtest.py</files>
  <read_first>
    - `src/trading_crab_lib/platform/evaluation/report.py` lines 519-571 — the EXACT construction this task's `recompute_baseline_curves` mirrors: `research = build_core_research_series(monthly_raw, cfg)`; `returns = compute_monthly_returns(research)`; `equity_ret/bond_ret/cash_ret/equity_level` from `research`/`returns`; `dev_equity_ret, _ = split_by_holdout_boundary(equity_ret, cutoff=DEFAULT_HOLDOUT_CUTOFF)` (same for bond/cash/level); `spy_ret = spy_buy_hold(dev_equity_ret)`; `sixty_forty_ret = sixty_forty(dev_equity_ret, dev_bond_ret, rebalance=..., cost_bps=baseline_cost_bps)`; `faber_ret = faber_sma(dev_equity_level, dev_cash_ret, cost_bps=baseline_cost_bps)`. Read this as data flow only — re-derive from the public functions, never import this private construction.
    - `src/trading_crab_lib/platform/backtest/baselines.py` (full file) — `spy_buy_hold`, `sixty_forty`, `faber_sma` (all pure, deterministic, log NO registry trial per the module's own docstring).
    - `src/trading_crab_lib/platform/honesty/holdout.py` — `split_by_holdout_boundary(df, cutoff=DEFAULT_HOLDOUT_CUTOFF)`, `DEFAULT_HOLDOUT_CUTOFF = "2020-12-31"`.
    - `06-01-PLAN.md` Task 1's `<action>` block — `loaders.load_report_artifact(filename, *, rebuild_hint) -> pd.DataFrame`, the ONLY path this task uses to read `backtest_equity_curve_strategy.parquet`, `backtest_equity_curve_ablation.parquet`, `backtest_kpi_table.parquet`.
    - `06-01-PLAN.md` Task 2's `<action>` block — `drift.assert_kpi_table_plausible(kpi_table) -> pd.DataFrame` (collect-then-raise across every leg's terminal_log_wealth/max_drawdown; verified against the live table this session: strategy 4.0265/-21.24%, ablation 3.6472/-19.81%, SPY 5.6805/-48.95%, 60/40 5.0471/-26.96%, Faber 6.3726/-18.94%).
    - Live schema verified this session: `backtest_kpi_table.parquet` is 5 rows x 3 columns (`leg`, `terminal_log_wealth`, `max_drawdown`); `backtest_equity_curve_strategy.parquet` / `backtest_equity_curve_ablation.parquet` are indexed by decision date with a `return` column (plus `turnover`, `cost`, `active_regime`, `scale`, `degraded`).
  </read_first>
  <behavior>
    - `recompute_baseline_curves(monthly_raw, cfg)` on the real dev checkpoint returns a dict with keys `spy_buy_hold`, `sixty_forty`, `faber_sma`, each a `pd.Series` whose index max is on or before 2020-12-31.
    - `plot_equity_curves(curves)` returns a Figure for a 5-leg dict and for a 1-leg dict.
    - `compute_ablation_delta(kpi_table)` on the real live table returns `{"wealth_delta": ..., "dd_delta": ...}` matching `strategy - no_regime_ablation` for both KPI columns.
    - `plot_kpi_table_bars(kpi_table)` returns a Figure for the real 5-row table and for an empty one.
  </behavior>
  <action>
Create `src/trading_crab_lib/platform/plotting/backtest.py` with `from __future__ import
annotations`, a module docstring, `log = logging.getLogger(__name__)`. Import `core` by module
path; import nothing from the legacy `trading_crab_lib.plotting` package (D-01).

`recompute_baseline_curves(monthly_raw: pd.DataFrame, cfg: dict) -> dict[str, pd.Series]` mirrors
`evaluation/report.py`'s baseline construction using ONLY public functions:
`research = build_core_research_series(monthly_raw, cfg)` (from `trading_crab_lib.platform.splice`);
`returns = compute_monthly_returns(research)` (from `trading_crab_lib.platform.assets.returns`);
resolve `equity_ret = returns[cfg["splice"]["equities"]["research_name"]]`,
`bond_ret = returns[cfg["splice"]["long_duration"]["research_name"]]`,
`cash_ret = returns[cfg["splice"]["cash"]["research_name"]]`,
`equity_level = research[cfg["splice"]["equities"]["research_name"]]`; apply
`split_by_holdout_boundary(..., cutoff=DEFAULT_HOLDOUT_CUTOFF)` (from
`trading_crab_lib.platform.honesty.holdout`) to each of the four series, keeping only the dev
side; read `rebalance = cfg.get("backtest", {}).get("sixty_forty_rebalance", "monthly")`,
`cost_bps = cfg.get("backtest", {}).get("cost_bps", 10)`,
`baseline_cost_bps = cost_bps if cfg.get("backtest", {}).get("apply_cost_to_baselines", True) else
0.0`; call `spy_buy_hold(dev_equity_ret)`, `sixty_forty(dev_equity_ret, dev_bond_ret,
rebalance=rebalance, cost_bps=baseline_cost_bps)`, `faber_sma(dev_equity_level, dev_cash_ret,
cost_bps=baseline_cost_bps)` (all imported from `trading_crab_lib.platform.backtest.baselines`).
Returns `{"spy_buy_hold": ..., "sixty_forty": ..., "faber_sma": ...}`. Document in the docstring
that this is a read-only re-derivation of `report.py`'s baseline construction (never an import of
that private logic) and that all three legs are deterministic price arithmetic with no tunable
parameter and log no registry trial, per `baselines.py`'s own documented scope.

`plot_equity_curves(curves: dict[str, pd.Series], *, title: str = "Equity curves (cumulative log
wealth)", save_path: Path | None = None, show: bool = False) -> plt.Figure` renders one line per
`curves` entry of `np.log1p(series.dropna()).cumsum()`, legended by key; handles an empty dict via
a "no data" annotation; finishes through `core._save_or_show`.

`compute_ablation_delta(kpi_table: pd.DataFrame) -> dict[str, float]` looks up the `strategy` and
`no_regime_ablation` rows by `leg`, returns
`{"wealth_delta": strategy_wealth - ablation_wealth, "dd_delta": strategy_dd - ablation_dd}`;
raises `ValueError` naming which leg is missing if either row is absent from `kpi_table`.

`plot_kpi_table_bars(kpi_table: pd.DataFrame, *, save_path: Path | None = None, show: bool =
False) -> plt.Figure` renders a grouped bar chart (one group per `leg`, one bar per metric:
`terminal_log_wealth` and `max_drawdown`); handles an empty `kpi_table` via a "no data"
annotation; finishes through `core._save_or_show`.

Create `tests/unit/test_platform_plotting_backtest.py` on the `tests/unit/test_plotting.py`
shape: Agg backend forced in the module header, a seeded `np.random.default_rng(42)` synthetic
monthly `monthly_raw`-shaped fixture with the columns `build_core_research_series` needs (or a
minimal `cfg["splice"]` fixture restricted to classes the synthetic frame supports), one `TestXxx`
class per public function with a does-not-crash case and an empty-input case, the
`TestComputeAblationDelta` case above built from a small synthetic 5-row kpi_table matching the
real schema, and a test asserting no module reachable from
`trading_crab_lib.platform.plotting.backtest` has a `__name__` beginning `trading_crab_lib.plotting`
(D-01).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_platform_plotting_backtest.py -x -q && .venv/bin/python -c "
import pandas as pd
from trading_crab_lib.platform.config import load_platform_config
from trading_crab_lib.platform.checkpoints import get_platform_checkpoint_manager
from trading_crab_lib.platform.plotting import loaders, backtest as pbacktest
cfg = load_platform_config()
mr = get_platform_checkpoint_manager().load('monthly_raw')
curves = pbacktest.recompute_baseline_curves(mr, cfg)
assert set(curves) == {'spy_buy_hold', 'sixty_forty', 'faber_sma'}, set(curves)
for name, s in curves.items():
    assert s.index.max() <= pd.Timestamp('2020-12-31'), (name, s.index.max())
kpi = loaders.load_report_artifact('backtest_kpi_table.parquet', rebuild_hint='python -m trading_crab_lib.platform.evaluation.report')
delta = pbacktest.compute_ablation_delta(kpi)
print('ablation delta', delta)
assert delta['wealth_delta'] > 0, delta
fig1 = pbacktest.plot_equity_curves(curves)
fig2 = pbacktest.plot_kpi_table_bars(kpi)
assert fig1 is not None and fig2 is not None
print('P6 baseline/KPI smoke OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/unit/test_platform_plotting_backtest.py -x -q` exits 0.
    - `recompute_baseline_curves(monthly_raw, cfg)` run against the real dev checkpoint returns exactly the keys `spy_buy_hold`, `sixty_forty`, `faber_sma`, each a `pd.Series` with index max on or before 2020-12-31.
    - `compute_ablation_delta` run against the real live `backtest_kpi_table.parquet` returns a `wealth_delta` strictly greater than 0 (current on-disk reference: strategy 4.0265 minus ablation 3.6472, a positive delta — the regime layer currently pays rent by this measure) — the exact numeric values are read live, never hardcoded in this plan's text.
    - `plot_equity_curves(...)` and `plot_kpi_table_bars(...)` each return `matplotlib.figure.Figure` for both a populated and an empty input.
    - A test asserts no module reachable from `trading_crab_lib.platform.plotting.backtest` has a `__name__` beginning `trading_crab_lib.plotting` (D-01).
    - `.venv/bin/python -m pytest tests/ -q` reports 0 failures.
  </acceptance_criteria>
  <done>The three deterministic baseline legs are re-derived read-only and dev-bounded, the ablation delta is computed from the live KPI table, and both the equity-curve and KPI-bar renderers work for real and edge-case input.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Calibration summary and the sojourn/lag headline with the mandatory A13 caveat</name>
  <files>src/trading_crab_lib/platform/plotting/backtest.py, tests/unit/test_platform_plotting_backtest.py</files>
  <read_first>
    - `src/trading_crab_lib/platform/evaluation/sojourn_lag.py` (full file) — `compute_sojourn_lag_headline(full_sample_states, filtered_probs_matrix, *, act_threshold=0.70) -> dict` returning keys `median_sojourn`, `median_lag`, `ratio`, `n_transitions`, `n_resolved`, `act_threshold`. This task calls it directly on the two 06-02 artifacts — it never recomputes `full_sample_states`/`filtered_probs_matrix` itself.
    - `06-01-PLAN.md` Task 1's `<action>` block — `loaders.load_full_sample_states(*, rebuild_hint=...) -> pd.Series` (returns the `state` column as an int-valued Series indexed by date) and `loaders.load_filtered_state_probs(*, rebuild_hint=...) -> pd.DataFrame` (renames `state_{k}` string columns back to integer state ids) — both read `backtest_full_sample_states.parquet` / `backtest_filtered_state_probs.parquet`, the two artifacts 06-02 persists.
    - `06-01-PLAN.md` Task 1's `<action>` block — `core.A13_CAVEAT: str`, the single-source string this task renders verbatim.
    - `06-02-PLAN.md` Task 2's acceptance criteria — the exact current reference values this task's live check reproduces: median sojourn 97.0 months, median detection lag 164.0 months, ratio within 1e-3 of 0.5914634146341463, `n_resolved`/`n_transitions` equal to 4 and 6.
    - `src/trading_crab_lib/platform/evaluation/model_metrics.py` lines 84-139 — the calibration artifact schema (`class_label`, `bin`, `bin_low`, `bin_high`, `predicted_prob_mean`, `observed_freq`, `n_in_bin`), reused here for a lighter summary view than P4's per-class detail panel.
  </read_first>
  <behavior>
    - `plot_calibration_summary(calibration_df)` on the real 21x7 calibration artifact returns a Figure with one scatter point per (class, bin) row, a 45-degree reference line, and point size scaled by `n_in_bin`; returns a Figure on an empty input too.
    - `plot_sojourn_lag_headline(headline)` on the real recomputed headline dict returns a Figure whose text includes the literal caveat string and the resolved/total transition count.
    - `plot_sojourn_lag_headline` raises no exception when `headline["ratio"]` is NaN (an all-unresolved degenerate case).
  </behavior>
  <action>
Extend `src/trading_crab_lib/platform/plotting/backtest.py`.

`plot_calibration_summary(calibration_df: pd.DataFrame, *, title: str = "Calibration summary
(walk-forward, all classes)", save_path: Path | None = None, show: bool = False) -> plt.Figure`
renders a single scatter of `predicted_prob_mean` (x) vs `observed_freq` (y) across every row of
`calibration_df`, sized by `n_in_bin` (normalized so the largest bubble is a fixed max size,
never zero-sized), colored by `class_label` via `core._regime_color(int(class_label))`, with a
dashed 45-degree reference line; handles an empty input via a "no data" annotation; finishes
through `core._save_or_show`. Document in the docstring that this is a lighter, all-classes-at-a-
glance companion to P4's per-class `nowcaster.plot_calibration_curve` — deliberately independent
code (no cross-module import between `backtest.py` and `nowcaster.py`), because the two notebooks
are built in the same wave and must not depend on each other's completion.

`plot_sojourn_lag_headline(headline: dict, *, caveat: str | None = None, save_path: Path | None =
None, show: bool = False) -> plt.Figure` renders a two-bar chart (`median_sojourn`,
`median_lag`) annotated with the computed `ratio` (or the literal string `"n/a (unresolved)"`
when `ratio` is NaN) and `f"{n_resolved} of {n_transitions} transitions resolved"`, then adds
`caveat` (defaulting to `core.A13_CAVEAT` when `None`) as a wrapped text box on the figure —
the caveat is ALWAYS rendered, never conditional on the ratio's value; finishes through
`core._save_or_show`.

Extend `tests/unit/test_platform_plotting_backtest.py` with `TestPlotCalibrationSummary`
(does-not-crash on a synthetic calibration frame matching the real schema, empty-input case) and
`TestPlotSojournLagHeadline` (does-not-crash on a synthetic headline dict with a finite ratio, a
does-not-crash case with `ratio=float("nan")`, and a test asserting the returned figure's text
content — collected via `fig.texts` plus every `Text` artist under its axes — contains both the
caveat substring and the `n_resolved`/`n_transitions` numbers).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_platform_plotting_backtest.py -x -q && .venv/bin/python -c "
from trading_crab_lib.platform.plotting import loaders, backtest as pbacktest
calib_df = loaders.load_report_artifact('model_metrics_calibration.parquet', rebuild_hint='python -m trading_crab_lib.platform.evaluation.report')
fig1 = pbacktest.plot_calibration_summary(calib_df)
assert fig1 is not None
states = loaders.load_full_sample_states()
probs = loaders.load_filtered_state_probs()
from trading_crab_lib.platform.evaluation.sojourn_lag import compute_sojourn_lag_headline
headline = compute_sojourn_lag_headline(states, probs, act_threshold=0.70)
assert abs(headline['median_sojourn'] - 97.0) < 1e-9, headline
assert abs(headline['median_lag'] - 164.0) < 1e-9, headline
assert abs(headline['ratio'] - 0.5914634146341463) < 1e-3, headline
assert (headline['n_resolved'], headline['n_transitions']) == (4, 6), headline
fig2 = pbacktest.plot_sojourn_lag_headline(headline)
assert fig2 is not None
print('P6 calibration/sojourn-lag smoke OK', headline)"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/unit/test_platform_plotting_backtest.py -x -q` exits 0.
    - `plot_calibration_summary(...)` run against the real `model_metrics_calibration.parquet` (21 rows) returns a `matplotlib.figure.Figure`; run against an empty frame it also returns a Figure without raising.
    - `compute_sojourn_lag_headline` recomputed from `loaders.load_full_sample_states()` and `loaders.load_filtered_state_probs()` reproduces the current reference exactly: median sojourn 97.0, median detection lag 164.0, ratio within 1e-3 of 0.5914634146341463, `n_resolved`/`n_transitions` equal to 4 and 6.
    - `plot_sojourn_lag_headline(...)` run against that real headline returns a Figure whose text content (collected from every `Text` artist) contains the substring `A13` and the substring `4 of 6` (the resolved-of-total count).
    - `plot_sojourn_lag_headline` run against a synthetic headline with `ratio=float("nan")` returns a Figure without raising, and its text still contains the caveat.
    - `.venv/bin/python -m pytest tests/ -q` reports 0 failures.
  </acceptance_criteria>
  <done>The calibration summary and the sojourn/lag headline chart both exist as tested library functions; the headline recomputed from the 06-02 artifacts reproduces the current reference exactly, and the A13 caveat plus the resolved-transition count are always rendered, never conditional.</done>
</task>

<task type="auto">
  <name>Task 3: P6_backtest_evaluation notebook — all five equity curves, ablation, calibration, sojourn/lag headline, anomaly history</name>
  <files>notebooks/platform/P6_backtest_evaluation.ipynb</files>
  <read_first>
    - `notebooks/platform/P1_data_spine.ipynb` (06-01) and `notebooks/platform/P3_regime_labeling.ipynb` (06-03) — cell structure and setup-cell import block to follow.
    - `src/trading_crab_lib/platform/plotting/loaders.py` (06-01) — `load_platform_checkpoint`, `load_report_artifact`, `load_full_sample_states`, `load_filtered_state_probs`, `redacted_config`.
    - `src/trading_crab_lib/platform/plotting/drift.py` (06-01) — `assert_kpi_table_plausible`, `assert_brier_plausible`, `drift_report`.
    - `src/trading_crab_lib/platform/plotting/core.py` (06-01) — `A13_CAVEAT`.
    - `src/trading_crab_lib/platform/plotting/backtest.py` (Tasks 1-2) — `recompute_baseline_curves`, `plot_equity_curves`, `compute_ablation_delta`, `plot_kpi_table_bars`, `plot_calibration_summary`, `plot_sojourn_lag_headline`.
    - `.planning/BASELINE-v1-tracer-bullet.md` section "Current reference run — 2026-09-09" ONLY — do not cite anything from "## Historical" onward.
    - `.planning/POST-2020-OBSERVATIONS.md` (06-01) — the record this notebook's full-span drift cell points to.
  </read_first>
  <action>
Create `notebooks/platform/P6_backtest_evaluation.ipynb` via the `nbformat` v4 programmatic API.
No cell may construct a figure, touch an axis, import a plotting library directly, write to a
checkpoint, or invoke the full walk-forward evaluation entrypoints. Every figure comes from
`platform/plotting/`. Run it once and commit it with outputs.

Cell sequence:

1. Markdown title `P6 — Backtest Evaluation` with a one-paragraph purpose statement (the
evaluation capstone: equity curves, baseline gauntlet, ablation delta, calibration, sojourn/lag
headline) and the explicit prerequisite line naming
`python -m trading_crab_lib.platform.evaluation.report` as the command that produces every
artifact this notebook reads — never re-run from inside the notebook.

2. Setup code cell: `%matplotlib inline`, logging at INFO, imports of `load_platform_config`,
`plotting as pplot`, `drift as pdrift`, `backtest as pbacktest`; `cfg = load_platform_config()`.
Display config only through `pplot.redacted_config(cfg)`.

3. Code cell: load the two ML-strategy equity curves via `pplot.load_report_artifact` —
`strategy_curve = pplot.load_report_artifact("backtest_equity_curve_strategy.parquet",
rebuild_hint="python -m trading_crab_lib.platform.evaluation.report")`,
`ablation_curve = pplot.load_report_artifact("backtest_equity_curve_ablation.parquet",
rebuild_hint=...)`. Load `monthly_raw` (dev) via `pplot.load_platform_checkpoint`; call
`baseline_curves = pbacktest.recompute_baseline_curves(monthly_raw, cfg)`. Assemble
`curves = {"strategy": strategy_curve["return"], "no_regime_ablation": ablation_curve["return"],
**baseline_curves}`; render `pbacktest.plot_equity_curves(curves)`. Markdown states the two
ML-strategy curves are loaded verbatim from Phase 5's persisted artifacts while the three
baseline curves are recomputed in-notebook from deterministic, no-tunable-parameter price
arithmetic (`backtest/baselines.py`) — never a re-run of the 588-refit walk-forward.

4. Markdown then code: the KPI table and its plausibility gate. Load
`kpi_table = pplot.load_report_artifact("backtest_kpi_table.parquet", rebuild_hint="python -m
trading_crab_lib.platform.evaluation.report")`; call
`verdict = pdrift.assert_kpi_table_plausible(kpi_table)` BEFORE any KPI rendering (an impossible
value stops the notebook here); print `verdict`; render `pbacktest.plot_kpi_table_bars(kpi_table)`.
Markdown narrates the Faber/60-40 fix history: both legs were once implausible (see
`UAT-AUDIT-2026-09-09.md` for the incident) and are now corrected, and states that every value
shown above is read live from `kpi_table` at render time — no historical figure is hardcoded in
this notebook.

5. Code cell: `delta = pbacktest.compute_ablation_delta(kpi_table)`; print `delta`. Markdown
states this answers design section 8.7's "does the regime layer pay rent" question, phrased
against whatever `delta`'s sign and magnitude actually are at render time — never a hardcoded
expectation.

6. Markdown then code: the calibration summary. Load
`calib_df = pplot.load_report_artifact("model_metrics_calibration.parquet", rebuild_hint="python
-m trading_crab_lib.platform.evaluation.report")` and
`brier_df = pplot.load_report_artifact("model_metrics_brier.parquet", rebuild_hint=...)`; call
`brier_verdict = pdrift.assert_brier_plausible(float(brier_df["brier"].iloc[0]), n_classes=5)`;
print `brier_verdict`; render `pbacktest.plot_calibration_summary(calib_df)`. Markdown points to
P4 for the full per-class calibration diagnostic and states this panel is the "does calibration
look sane at a glance" summary within the larger evaluation dashboard.

7. Markdown then code: the sojourn/lag headline — THE section carrying the mandatory A13
discipline. State up front, before any number is shown, that the ratio and lag below are NOT
interpretable pending audit item A13 (render `pplot.A13_CAVEAT` verbatim in this markdown cell).
Load `full_sample_states = pplot.load_full_sample_states()` and
`filtered_probs_matrix = pplot.load_filtered_state_probs()` (both from
`trading_crab_lib.platform.plotting.loaders`, both sourced from the two artifacts 06-02
persisted); call
`headline = compute_sojourn_lag_headline(full_sample_states, filtered_probs_matrix,
act_threshold=cfg.get("allocation", {}).get("hysteresis", {}).get("act_threshold", 0.70))`
(import `compute_sojourn_lag_headline` from
`trading_crab_lib.platform.evaluation.sojourn_lag`); print `headline`, explicitly printing the
`n_resolved`/`n_transitions` pair alongside the ratio; render
`pbacktest.plot_sojourn_lag_headline(headline)` (the caveat renders inside the figure too,
unconditionally). A markdown cell immediately below restates that no numeric band around this
ratio resolves A13 — the labeler's reference and the walk-forward's per-window feature sets are
fit on different columns, so the two series being compared are not tracking identical targets.

8. Markdown then code: the D-06/D-07 full-span drift panel over the core research series. Load
`full_raw = pplot.load_full_span_checkpoint("monthly_raw", rebuild_hint="python
scripts/build_platform_data.py")`; build `full_research = build_core_research_series(full_raw,
cfg)` (import from `trading_crab_lib.platform.splice`); call
`drift_df = pdrift.drift_report(full_research, list(full_research.columns),
cutoff=DEFAULT_HOLDOUT_CUTOFF)` (import `DEFAULT_HOLDOUT_CUTOFF` from
`trading_crab_lib.platform.honesty.holdout`); print the table. Markdown points to
`.planning/POST-2020-OBSERVATIONS.md` for recording any decision-changing observation.

9. Final markdown cell: summary restating ROADMAP criterion 5's five elements (equity curves,
baseline gauntlet, ablation delta, calibration, sojourn/lag headline with resolved-transition
count) as delivered above, restating the A13 caveat one final time, and explicitly stating P6
carries NO sign-off cell and NO per-run gate (D-15 stays P3-exclusive).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_platform_notebooks.py -x -q && .venv/bin/python -c "
import nbformat, pathlib
nb = nbformat.read(pathlib.Path('notebooks/platform/P6_backtest_evaluation.ipynb'), as_version=4)
src = '\n'.join(c.source for c in nb.cells)
code_src = '\n'.join(c.source for c in nb.cells if c.cell_type == 'code')
for token in ['A13', 'not interpretable', 'n_resolved', 'compute_ablation_delta', 'assert_kpi_table_plausible', 'assert_brier_plausible', 'UAT-AUDIT-2026-09-09', 'load_full_span_checkpoint', 'POST-2020-OBSERVATIONS']:
    assert token in src, token
for banned in ['import matplotlib', 'import seaborn', '.save(', 'Sign-Off', 'run_full_backtest_evaluation', 'run_backtest(']:
    assert banned not in code_src, banned
print('P6 static checks OK', len(nb.cells), 'cells')"</automated>
    <human-check>Open `notebooks/platform/P6_backtest_evaluation.ipynb` in Jupyter, Run All against the real checkpoints and all nine persisted `outputs/reports/platform/` artifacts, and confirm every cell completes with no exception.</human-check>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/unit/test_platform_notebooks.py -x -q` exits 0 with `P6_backtest_evaluation.ipynb` included in the parameterization and passing the cross-notebook A13 discipline check (06-01 Task 3's check 6).
    - The notebook's combined cell source contains `A13`, `not interpretable`, `n_resolved`, `compute_ablation_delta`, `assert_kpi_table_plausible`, `assert_brier_plausible`, `UAT-AUDIT-2026-09-09`, `load_full_span_checkpoint`, and `POST-2020-OBSERVATIONS`; no code cell contains `import matplotlib`, `import seaborn`, `.save(`, `Sign-Off`, `run_full_backtest_evaluation`, or `run_backtest(`.
    - Executing the notebook, `pdrift.assert_kpi_table_plausible(kpi_table)` raises nothing and the 5-leg equity-curve chart renders all five legs.
    - Executing the notebook, `headline["n_resolved"]` and `headline["n_transitions"]` equal 4 and 6 respectively (current reference), printed explicitly alongside the ratio.
    - The notebook's source contains no numeral matching the void historical Faber/60-40 figures — every KPI value referenced in prose is described as "read live from kpi_table", never a literal hardcoded percentage.
    - The notebook is committed with its cell outputs, matching the storage convention of the twelve legacy notebooks.
    - `.venv/bin/python -m pytest tests/ -q` reports 0 failures.
  </acceptance_criteria>
  <done>P6 renders all five equity-curve legs, the ablation delta, a calibration summary, and the sojourn/lag headline with its A13 caveat and resolved-transition count intact — narrating the Faber/60-40 fix history without ever citing the void historical figures as current, and carrying no sign-off cell.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed notebook -> git | P6's cell outputs become durable repository content, including anything a cell displays |
| Phase 5 + 06-02 persisted artifacts -> P6 | P6 trusts nine `outputs/reports/platform/` files (seven from Phase 5, two from 06-02) to be exactly what the honest evaluation run produced; a schema drift becomes a wrong chart, not an error |
| sojourn/lag ratio -> operator judgement | The single most consequential number in the notebook is explicitly not a settled go/no-go signal (A13) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-27 | Information Disclosure | notebook cell displaying `load_platform_config()` output, which carries the injected `FRED_API_KEY` | high | mitigate | The notebook routes any config display through `loaders.redacted_config`; `tests/unit/test_platform_notebooks.py` check (4) from 06-01 fails the notebook otherwise |
| T-06-28 | Repudiation | the sojourn/lag ratio being read as a settled go/no-go signal because it sits inside every plausibility band | high | mitigate | `pplot.A13_CAVEAT` is rendered in markdown BEFORE the number appears, again inside `plot_sojourn_lag_headline`'s figure unconditionally, and again in the closing summary; `n_resolved`/`n_transitions` are always printed alongside the ratio; `tests/unit/test_platform_notebooks.py`'s cross-notebook A13 check (06-01 Task 3, check 6) fails the notebook if the substring `A13` is absent |
| T-06-29 | Repudiation | a reader mistaking the notebook's narration of the now-corrected Faber/60-40 anomaly for a claim that the historical incorrect figures are still live | medium | mitigate | Every KPI value is read from the live `backtest_kpi_table.parquet` at render time and described in prose as "read live", never as a hardcoded historical figure; no task text in this plan repeats the void historical percentages |
| T-06-30 | Tampering | a notebook cell calling `run_backtest()`/`run_full_backtest_evaluation()` on open, turning a viewing notebook into a multi-minute walk-forward every time it is opened | medium | mitigate | Every ML-strategy number is loaded from persisted artifacts via `loaders.load_report_artifact`/`load_full_sample_states`/`load_filtered_state_probs`; a source assertion forbids `run_backtest`/`run_full_backtest_evaluation` in any cell |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs zero new packages. `06-RESEARCH.md` records the Package Legitimacy Audit as not applicable for this phase. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/unit/test_platform_plotting_backtest.py tests/unit/test_platform_notebooks.py -x -q` exits 0.
- `.venv/bin/python -m pytest tests/ -q` collects at least 1393 tests and reports 0 failures.
- `git status --porcelain src/trading_crab_lib/platform/evaluation/ src/trading_crab_lib/platform/backtest/ outputs/reports/platform/ data/checkpoints/platform/` is empty — this plan reads Phase 3/5 code and artifacts, never modifies them.
- The sojourn/lag headline recomputed from the 06-02 artifacts reproduces the current reference exactly (97.0 / 164.0 / 0.5915 / 4 of 6).
- Human check (end-of-phase): P6 runs top-to-bottom against real checkpoints and all nine persisted artifacts.
</verification>

<success_criteria>
- `platform/plotting/backtest.py` exists with a read-only re-derivation of the three deterministic baseline curves, an all-five-leg equity-curve chart, KPI bars under a plausibility gate, an ablation-delta computation, a calibration summary, and a sojourn/lag headline chart.
- P6 renders ALL FIVE of ROADMAP criterion 5's elements: equity curves, the baseline gauntlet, the ablation delta, calibration, and the sojourn/lag headline with its resolved-transition count.
- The A13 caveat and the resolved-of-total transition count appear wherever the ratio is shown; nothing in P6 claims A13 is resolved.
- P6 narrates the Faber/60-40 fix history without ever presenting the void historical figures as current.
- Every figure in P6 comes from `platform/plotting/` (ROADMAP criterion 3); P6 carries no sign-off cell (D-15 stays P3-exclusive).
</success_criteria>

## Artifacts this phase produces

Symbols and files created by THIS plan (new; exclude from drift verification):

**New modules**
- `src/trading_crab_lib/platform/plotting/backtest.py`

**New functions**
- `backtest.recompute_baseline_curves`, `backtest.plot_equity_curves`, `backtest.compute_ablation_delta`, `backtest.plot_kpi_table_bars`, `backtest.plot_calibration_summary`, `backtest.plot_sojourn_lag_headline`

**New files**
- `notebooks/platform/P6_backtest_evaluation.ipynb`
- `tests/unit/test_platform_plotting_backtest.py`

<output>
Create `.planning/phases/06-platform-notebook-suite/06-07-SUMMARY.md` when done
</output>
