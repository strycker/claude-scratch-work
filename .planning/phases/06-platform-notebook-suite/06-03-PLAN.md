---
phase: 06-platform-notebook-suite
plan: 03
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified:
  - src/trading_crab_lib/platform/plotting/history.py
  - src/trading_crab_lib/platform/plotting/regime.py
  - notebooks/platform/P3_regime_labeling.ipynb
  - tests/unit/test_platform_plotting_history.py
  - tests/unit/test_platform_plotting_regime.py
autonomous: true
requirements: [NB-01]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "An operator opening P3 sees the 5 regimes drawn against NBER recession bars and a dated list of named economic eras, so the question 'are these labels real' can be answered by eye (ROADMAP criterion 4)."
    - "P3 shows the smoothed reference labeling and the walk-forward's per-window filtered labeling side by side, with their disagreement quantified and the seven feature-set change dates marked (CONTEXT.md Amendment 2 item F)."
    - "P3 also shows that the shipped label_regimes() labeling covers only 1990-01 onward while the evaluation reference covers 1963-02 onward, because they are fit on different feature sets."
    - "Wherever the sojourn/lag ratio appears in P3 it carries the single-source A13 caveat string, and nothing in the notebook claims A13 is resolved."
    - "If the FRED USREC fetch fails, P3 warns loudly and names what degraded; it never renders an unshaded chart that looks complete (D-10)."
    - "P3 ends in a plain markdown sign-off cell where an operator records date, verdict, and reasoning, and a negative verdict is a valid recorded outcome (D-15, D-16)."
    - statement: "P3_regime_labeling.ipynb runs top-to-bottom against real checkpoints and the two persisted labeling artifacts without raising."
      verification: backstop
    - statement: "The regime timeline read against dated economic history supports or contradicts the labeling — a human judgement, not a computed verdict."
      verification: backstop
  artifacts:
    - src/trading_crab_lib/platform/plotting/history.py
    - src/trading_crab_lib/platform/plotting/regime.py
    - notebooks/platform/P3_regime_labeling.ipynb
    - tests/unit/test_platform_plotting_history.py
    - tests/unit/test_platform_plotting_regime.py
  key_links:
    - "backtest_full_sample_states.parquet (plan 06-02) is the labeling P3's economic-history overlay draws, because it is the only one of the three spanning 1963 and therefore the only one that reaches the 1973 oil shock and 1981 Volcker transitions."
    - "backtest_filtered_state_probs.parquet (plan 06-02) is the A13 side-by-side's second series; its argmax over the 588 decision dates is the filtered labeling."
    - "backtest.driver._window_active_features driven by honesty.walkforward.expanding_steps reproduces the seven feature-set change dates without re-running the walk-forward."
    - "core.A13_CAVEAT is the single string both P3 and P6 render, so the caveat cannot drift between notebooks."
  prohibitions:
    - statement: "P3 runs no (K, lambda, n_restarts) sweep and changes no hyperparameter — it renders diagnostics for the shipped K=5, lambda=52.0, n_restarts=10 configuration only (D-14)."
      status: pending
      verification: flagged-unverified
    - statement: "P3 introduces no sign_off() helper and no machine-readable sign-off ledger; the sign-off is a markdown cell (D-15)."
      status: pending
      verification: flagged-unverified
    - statement: "P3 calls run_full_backtest_evaluation() or run_backtest() nowhere — it loads the persisted artifacts instead."
      status: pending
      verification: "tests/unit/test_platform_notebooks.py plus a source assertion that no P3 cell references either function name."
    - statement: "No task in this plan writes to data/checkpoints/platform/ or modifies any outputs/reports/platform/ artifact."
      status: pending
      verification: flagged-unverified
    - statement: "No task in this plan claims audit item A13 is resolved."
      status: pending
      verification: flagged-unverified
---

<objective>
Build P3 — the one notebook that carries a cold-start human decision. It draws the regime
labeling against dated economic history so an operator can judge whether the five states are
real, and it renders the three labelings the codebase actually produces side by side so audit
item A13 becomes inspectable rather than merely reported.

Purpose: CONTEXT.md Amendment 2 item E flipped the expectation for this notebook. The corrected
labeling maps onto recognizable history — 1973-09 oil shock, 1981-10 Volcker, 1988-09
disinflation, 1996-08 late-90s expansion, 2008-07 into the 1.6%-occupancy crisis state, 2009-06
recovery. Build P3 expecting a genuine question, not a foregone answer. D-16 remains the safety
valve: a negative verdict is recorded and does not block.

Output: a history-overlay module, a regime plotting module including the A13 comparison, and the
sign-off notebook.
</objective>

<execution_context>
@/Users/glestryc/personal/github_repos/claude-scratch-work/.claude/gsd-core/workflows/execute-plan.md
@/Users/glestryc/personal/github_repos/claude-scratch-work/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-platform-notebook-suite/06-CONTEXT.md
@.planning/phases/06-platform-notebook-suite/06-PATTERNS.md
@.planning/phases/06-platform-notebook-suite/06-VALIDATION.md
@.planning/BASELINE-v1-tracer-bullet.md
@.planning/phases/06-platform-notebook-suite/06-01-SUMMARY.md
@.planning/phases/06-platform-notebook-suite/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Economic-history overlay data and the regime-by-era contingency table</name>
  <files>src/trading_crab_lib/platform/plotting/history.py, tests/unit/test_platform_plotting_history.py</files>
  <read_first>
    - `src/trading_crab_lib/platform/config.py` lines 56-94 — `load_platform_config()` injects `FRED_API_KEY` into `cfg["fred_monthly"]["api_key"]` and warns when it is unset. This is the credential path the USREC fetch reuses; no new secret is introduced.
    - `src/trading_crab_lib/platform/ingestion/macro_monthly.py` lines 105-130 — how the platform already constructs `fredapi.Fred(api_key=...)` and resamples a FRED series to month-end. Match that call shape.
    - `src/trading_crab_lib/platform/splice.py` lines 124-155 — the loud-failure idiom this module's degradation warning follows in spirit (name the failure, name the consequence, name the fix).
    - `src/trading_crab_lib/platform/plotting/core.py` (from plan 06-01) — `_save_or_show`, `CUSTOM_COLORS`, `_regime_color`.
    - `06-CONTEXT.md` decisions D-10, D-12, D-13.
  </read_first>
  <behavior>
    - `ECONOMIC_EVENTS` contains 6 entries, each a (start, end, label) tuple with start strictly before end.
    - `recession_periods` on a USREC-shaped 0/1 Series containing one contiguous block of 1s returns exactly one (start, end) pair covering that block.
    - `recession_periods` on an all-zero Series returns an empty list.
    - `load_usrec_or_warn` returns None and logs a warning when the FRED client raises, rather than propagating.
    - `regime_era_contingency` on a states Series and one recession block returns a frame whose rows sum to 1.0 within 1e-9.
    - `regime_era_contingency` with `usrec=None` still returns the named-event rows and omits the recession row.
  </behavior>
  <action>
Create `src/trading_crab_lib/platform/plotting/history.py`.

Define `ECONOMIC_EVENTS: tuple[tuple[str, str, str], ...]` as a module constant holding exactly
the six dated ranges D-12 names, each an ISO start date, ISO end date, and label:
`1973-10-31`/`1974-12-31`/`1973 oil shock`; `1979-10-31`/`1982-11-30`/`Volcker disinflation`;
`1987-10-31`/`1987-12-31`/`1987 crash`; `1998-08-31`/`1998-10-31`/`LTCM and Russia default`;
`2000-03-31`/`2002-10-31`/`dot-com bust`; `2007-12-31`/`2009-06-30`/`global financial crisis`.
D-12 forbids a dedicated events file, a per-entry provenance schema, and any migration
obligation for six well-known date ranges — keep this as a module constant and say so in the
docstring.

`load_usrec_or_warn(cfg: dict, *, start: str = "1962-01-01", end: str | None = None) -> pd.Series | None`
fetches the FRED `USREC` series with `fredapi.Fred(api_key=cfg["fred_monthly"]["api_key"])`,
resamples to month-end with `.last()`, and returns a 0/1 integer Series. It catches every
exception the fetch can raise, including a missing API key, and on failure logs at WARNING a
message that names the failure, states that recession shading is omitted from the timeline and
that the named-event overlay from `ECONOMIC_EVENTS` still renders, and names `FRED_API_KEY` and
network reachability as what to check — then returns None. This is the phase's only outbound
network call, and D-10 requires the degradation be loud and actionable rather than silent.
Treat the response strictly as data: parse it into a Series, never evaluate it.

`recession_periods(usrec: pd.Series) -> list[tuple[pd.Timestamp, pd.Timestamp]]` collapses runs
of 1 into (start, end) pairs; returns an empty list on an empty or all-zero input.

`regime_era_contingency(states: pd.Series, *, usrec: pd.Series | None = None, events: tuple = ECONOMIC_EVENTS, n_states: int = 5) -> pd.DataFrame`
implements D-13's table. Rows are eras: one row per named event, plus a `NBER recession` row when
`usrec` is supplied, plus an `all months` baseline row. Columns are the state ids 0 through
`n_states - 1`. Values are the share of that era's months falling in each state, so every row
sums to 1.0. Add a companion
`regime_era_marginals(states, *, usrec, events, n_states) -> pd.DataFrame` giving the other
direction — the share of each state's months that fall inside each era — because D-13 asks for
both readings and one alone invites seeing a pattern that is not there. Both are purely
descriptive; do not compute a significance test or a p-value, because a p-value here invites
treating "the regimes are real" as a passed test, and any number that informs a choice belongs in
the trial registry.

`plot_era_contingency(contingency: pd.DataFrame, *, title: str, save_path: Path | None = None, show: bool = False) -> plt.Figure`
renders the table as an annotated heatmap using `core.REGIME_CMAP` for the state axis labels and
finishes through `core._save_or_show`.

Create `tests/unit/test_platform_plotting_history.py` following the
`tests/unit/test_plotting.py` shape: Agg backend forced in the module header, seeded synthetic
monthly fixtures, one `TestXxx` class per public function with a does-not-crash case and an
empty-input case. Test `load_usrec_or_warn`'s degradation path by monkeypatching the Fred client
to raise, asserting the return is None and that a WARNING record was emitted; no test may make a
real network call.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_platform_plotting_history.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/unit/test_platform_plotting_history.py -x -q` exits 0.
    - `len(history.ECONOMIC_EVENTS) == 6` and every entry parses as two `pd.Timestamp` values with start strictly less than end.
    - `history.load_usrec_or_warn(cfg)` with a monkeypatched raising client returns `None` and emits exactly one WARNING record whose message contains `USREC` and `FRED_API_KEY`; no test performs network I/O.
    - `history.regime_era_contingency(states, usrec=usrec)` returns a frame whose every row sums to 1.0 within 1e-9 and whose every value lies in [0, 1]; a test asserts both bounds.
    - `history.regime_era_contingency(states, usrec=None)` returns a frame with no `NBER recession` row and does not raise.
    - `history.plot_era_contingency(...)` returns a `matplotlib.figure.Figure` for both a populated contingency frame and an empty `pd.DataFrame()`.
    - `.venv/bin/python -m pytest tests/ -q` reports 0 failures.
  </acceptance_criteria>
  <done>The overlay data source, its loud degradation path, and both directions of the D-13 contingency table exist as tested library functions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Regime plotting module including the A13 three-labeling comparison</name>
  <files>src/trading_crab_lib/platform/plotting/regime.py, tests/unit/test_platform_plotting_regime.py</files>
  <read_first>
    - `src/trading_crab_lib/platform/backtest/driver.py` lines 98-118 — `_window_active_features(features, cols, *, min_history)`, the private single source of truth for which features enter the regime model in a given window. Read its docstring: a late-starting feature enters once it has accumulated `min_history` non-NaN months.
    - `src/trading_crab_lib/platform/honesty/walkforward.py` lines 38-50 — `expanding_steps(index, *, min_train, step)` yielding `(t, train_index, test_index)` with `train_index` strictly before `t`.
    - `src/trading_crab_lib/platform/evaluation/report.py` lines 442-458 — `_reference_label_columns`, and its docstring's statement that the reference set is deliberately distinct from the walk-forward's per-window `min_history` set. This is the A13 finding stated in the code itself.
    - `src/trading_crab_lib/platform/labeling/diagnostics.py` lines 62-134 — `occupancy_and_sojourns` (returns `occupancy_pct`, `sojourns`, `overall_median_sojourn_months`) and `label_churn`; lines 180-199 — `auto_profile`.
    - `src/trading_crab_lib/platform/prediction/transition_matrix.py` — `empirical_transition_matrix(states) -> pd.DataFrame`.
    - `src/trading_crab_lib/plotting/regime.py` — the legacy quarterly analogs `plot_regime_timeline`, `plot_transition_matrix`, `plot_soft_probabilities`. Adapt the ideas; the data are monthly and the signature has no config object.
    - `src/trading_crab_lib/platform/plotting/core.py` and `history.py` (Task 1) — `_save_or_show`, `_regime_color`, `REGIME_CMAP`, `A13_CAVEAT`, `recession_periods`, `ECONOMIC_EVENTS`.
    - `config/platform_settings.yaml` `backtest` section — `feature_min_history` and `min_train_months`, the two parameters the active-feature-count reconstruction needs.
  </read_first>
  <behavior>
    - `plot_regime_timeline` on a synthetic states Series returns a Figure with `recessions=None` and with a one-block recession list.
    - `active_feature_count_timeline` on a synthetic frame whose second column starts late reports a count that increases at the expected step date.
    - `feature_set_change_dates` on a constant-count timeline returns an empty list.
    - `label_disagreement` on two identical Series reports `pct_disagree` of 0.0; on two Series differing in half their positions it reports 0.5.
    - `label_disagreement` on Series with disjoint indexes reports `n_compared` of 0 without raising.
    - `plot_label_comparison` renders with two series of different lengths and different index spans.
  </behavior>
  <action>
Create `src/trading_crab_lib/platform/plotting/regime.py`. Every public function follows the
platform signature `plot_x(data, *, ..., save_path: Path | None = None, show: bool = False) -> plt.Figure`
and finishes through `core._save_or_show`.

Diagnostic panels for the shipped configuration (D-14 — render, never tune):
`plot_regime_timeline(states, *, recessions=None, events=None, title, save_path, show)` draws the
state series as a coloured band per month using `core._regime_color`, shades each recession
interval, and marks each `ECONOMIC_EVENTS` range with a labelled span. This is the panel ROADMAP
criterion 4 rests on, so the event labels must be legible alongside the bands rather than in a
detached legend.
`plot_occupancy_and_sojourn(states, *, n_states=5, ...)` renders occupancy shares as a bar chart
and the sojourn distribution as a box or strip plot beside it, both sourced from
`labeling.diagnostics.occupancy_and_sojourns` rather than recomputed.
`plot_transition_matrix(matrix, ...)` renders an annotated heatmap of an
`empirical_transition_matrix` output.
`plot_soft_confidences(confidences, ...)` renders a stacked area chart over the confidence
frame's `state_{k}` columns.
`plot_regime_profiles(profiles_df, ...)` renders `auto_profile`'s per-state description strings as
a table figure.

A13 comparison surface (CONTEXT.md Amendment 2 item F, sourced per Amendment 3 item H):
`active_feature_count_timeline(dev_features, cols, *, min_history, min_train) -> pd.DataFrame`
walks `expanding_steps(dev_features.index, min_train=min_train)` and, at each step, calls
`_window_active_features(dev_features.loc[train_index], cols, min_history=min_history)`, returning
a frame indexed by decision date with columns `n_active` and `active` (the sorted column list).
Import `_window_active_features` from `backtest.driver` rather than duplicating its body: it is
the single source of truth for that rule, and duplicating it would let the two drift apart if
`min_history` semantics change. Note in the docstring that the import reaches a private name
deliberately, read-only, and that promoting it was rejected because this phase has exactly one
sanctioned edit to Phase-5 code and it is the artifact write in plan 06-02.
`feature_set_change_dates(timeline) -> list[pd.Timestamp]` returns the dates where `n_active`
changes, including the first step.
`plot_active_feature_count(timeline, ...)` renders `n_active` as a step chart with each change
date annotated with its new count.
`label_disagreement(reference_states, comparison_states) -> dict` intersects the two indexes and
returns `n_compared`, `n_disagree`, `pct_disagree`, `first_common_date`, `last_common_date`, and
`per_state_confusion` (a frame of reference state by comparison state counts). It must return
zeros rather than raise when the intersection is empty.
`plot_label_comparison(labelings: dict[str, pd.Series], *, change_dates=None, title, ...)` draws
one horizontal coloured band track per named labeling on a shared time axis, a disagreement strip
under them marking months where the tracks differ, and a vertical rule at each supplied change
date. It must accept two or three labelings with different spans and different lengths without
reindexing them onto a common index silently — where a labeling has no value the track is drawn
blank, because the fact that one labeling starts in 1990 and another in 1963 is exactly what this
panel exists to show.
`plot_sojourn_distribution(states, *, n_states=5, ...)` renders per-state sojourn lengths.

Create `tests/unit/test_platform_plotting_regime.py` on the `tests/unit/test_plotting.py` shape:
Agg backend header, seeded synthetic monthly fixtures, one `TestXxx` class per public function
with a does-not-crash case and an empty-input case. Add a dedicated
`TestActiveFeatureCountTimeline` with a synthetic frame where one column starts late, asserting
the count steps up on the expected date. Add a real-data test guarded by
`pytest.mark.skipif` on the existence of
`data/checkpoints/platform/monthly_features.parquet` that reconstructs the timeline from the real
dev checkpoint using the config's `feature_min_history` and `min_train_months` and asserts the
seven change points reproduce exactly: 1972-01-31 to 4, 1972-02-29 to 6, 1972-04-30 to 8,
1973-02-28 to 9, 1986-06-30 to 10, 1995-02-28 to 12, 2000-01-31 to 13. No test makes a network
call.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_platform_plotting_regime.py -x -q && .venv/bin/python -c "
import pandas as pd
from trading_crab_lib.platform.config import load_platform_config
from trading_crab_lib.platform.checkpoints import get_platform_checkpoint_manager
from trading_crab_lib.platform.taxonomy import lean_feature_set
from trading_crab_lib.platform.plotting import regime as pregime
cfg = load_platform_config(); bt = cfg['backtest']
mf = get_platform_checkpoint_manager().load('monthly_features')
cols = sorted(lean_feature_set(cfg) & set(mf.columns))
tl = pregime.active_feature_count_timeline(mf, cols, min_history=bt['feature_min_history'], min_train=bt['min_train_months'])
assert len(tl) == 588, len(tl)
chg = pregime.feature_set_change_dates(tl)
got = [(str(d.date()), int(tl.loc[d, 'n_active'])) for d in chg]
exp = [('1972-01-31',4),('1972-02-29',6),('1972-04-30',8),('1973-02-28',9),('1986-06-30',10),('1995-02-28',12),('2000-01-31',13)]
assert got == exp, got
print('A13 timeline OK', got)"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/unit/test_platform_plotting_regime.py -x -q` exits 0.
    - `active_feature_count_timeline` run against the real dev `monthly_features` with the config's `feature_min_history` and `min_train_months` returns exactly 588 rows, `n_active` values bounded within [4, 13], and change points equal to 1972-01-31 to 4, 1972-02-29 to 6, 1972-04-30 to 8, 1973-02-28 to 9, 1986-06-30 to 10, 1995-02-28 to 12, 2000-01-31 to 13 — seven changes, matching the audit's reported sequence.
    - `feature_set_change_dates` returns exactly 7 dates for that real-data timeline and an empty list for a constant-count synthetic timeline.
    - `label_disagreement(s, s)` returns `pct_disagree` 0.0 and `n_compared` equal to `len(s)`; `label_disagreement` on Series with disjoint indexes returns `n_compared` 0 without raising; every returned `pct_disagree` lies in [0, 1].
    - Every public function in `regime.py` returns a `matplotlib.figure.Figure` for both its populated fixture and its empty input.
    - `regime.py` imports `_window_active_features` from `trading_crab_lib.platform.backtest.driver` and contains no reimplementation of that rule; a test asserts the imported object is the same function object as the one on the driver module.
    - `git status --porcelain src/trading_crab_lib/platform/backtest/driver.py` is empty — the driver is read, never edited.
    - `.venv/bin/python -m pytest tests/ -q` reports 0 failures.
  </acceptance_criteria>
  <done>The regime panels and the A13 comparison surface exist as tested library functions, and the seven feature-set change points reproduce exactly against real data.</done>
</task>

<task type="auto">
  <name>Task 3: P3_regime_labeling notebook with the cold-start sign-off cell</name>
  <files>notebooks/platform/P3_regime_labeling.ipynb</files>
  <read_first>
    - `notebooks/platform/P1_data_spine.ipynb` (from plan 06-01) — the cell structure, setup-cell import block, and the loader call convention to follow.
    - `src/trading_crab_lib/platform/plotting/loaders.py` (from plan 06-01) — `load_platform_checkpoint`, `load_full_span_checkpoint`, `load_full_sample_states`, `load_filtered_state_probs`, `compute_regime_labeling`, `redacted_config`.
    - `src/trading_crab_lib/platform/plotting/regime.py` and `history.py` (Tasks 1-2) — the exact function names and signatures the notebook calls.
    - `src/trading_crab_lib/platform/plotting/drift.py` (from plan 06-01) — `assert_regime_occupancy_plausible`, `no_skill_brier` is not needed here but occupancy is.
    - `.planning/BASELINE-v1-tracer-bullet.md`, section "Current reference run — 2026-09-09" ONLY — the reference occupancy and transition table. Everything from "## Historical" onward is superseded.
    - `06-CONTEXT.md` AMENDMENT 2 items E and F, and AMENDMENT 3 item H.
    - `06-PATTERNS.md` section "notebooks/platform/P3_regime_labeling.ipynb — sign-off cell (D-15)".
  </read_first>
  <action>
Create `notebooks/platform/P3_regime_labeling.ipynb` with the `nbformat` v4 programmatic API.
No cell may construct a figure, touch an axis, import a plotting library directly, call
`run_backtest` or `run_full_backtest_evaluation`, or write a checkpoint. Every figure comes from
`platform/plotting/`. Run it once and commit it with outputs.

Cell sequence:

1. Markdown title `P3 — Regime Labeling` stating the notebook's two jobs (periodic check that the
regimes are still well-defined, plus the one cold-start sign-off) and the prerequisite line
naming `python scripts/build_platform_data.py` and
`python -m trading_crab_lib.platform.evaluation.report`.

2. Setup code cell: `%matplotlib inline`, logging configured at INFO, imports of
`load_platform_config`, the plotting package as `pplot`, and `regime`, `history`, `drift` as
submodule aliases; `cfg = load_platform_config()`. Display config through
`pplot.redacted_config(cfg)` if config is displayed at all — never the raw dict, because notebook
outputs are committed.

3. Markdown explaining that three labelings exist and why, then a code cell that loads all three:
the evaluation's smoothed reference via `pplot.load_full_sample_states()`; the walk-forward's
filtered path via `pplot.load_filtered_state_probs()` reduced to a hard labeling by row-wise
argmax over its state columns; and the shipped labeler via
`pplot.compute_regime_labeling(monthly_features_dev, cfg)`, where `monthly_features_dev` comes
from `pplot.load_platform_checkpoint("monthly_features", ...)`. Print each one's row count and
index span. State in the accompanying markdown the measured fact that the reference spans 695
months from 1963-02 on 9 feature columns, the filtered path covers 588 decision dates from
1972-01, and the shipped labeler covers 372 months from 1990-01 on all 13 lean columns because
`fred_vix` begins in 1990 and the labeler drops any row with a missing feature.

4. Markdown then code: the ROADMAP criterion 4 panel. Fetch recession bars with
`history.load_usrec_or_warn(cfg)`, convert with `history.recession_periods`, and call
`regime.plot_regime_timeline(reference_states, recessions=..., events=history.ECONOMIC_EVENTS,
title=...)`. If the fetch returned None, the code cell must print the degradation explicitly
before plotting so the operator cannot mistake an unshaded chart for a complete one. Follow with
a markdown cell listing the six dated transitions an operator is checking the chart against —
1973-09 oil shock, 1981-10 Volcker, 1988-09 post-Volcker disinflation, 1996-08 late-90s
expansion, 2008-07 into the crisis state, 2009-06 recovery — and stating explicitly that the
expectation is a genuine question rather than a foregone negative.

5. Code cell: occupancy and sojourn via `regime.plot_occupancy_and_sojourn`, with
`drift.assert_regime_occupancy_plausible` called on the shares first so an impossible occupancy
stops the notebook rather than rendering. Print the observed shares and the median sojourn.

6. Code cell: `regime.plot_transition_matrix(empirical_transition_matrix(reference_states))` plus
`regime.plot_sojourn_distribution(reference_states)`.

7. Code cell: D-13's contingency tables — `history.regime_era_contingency` and
`history.regime_era_marginals`, each rendered via `history.plot_era_contingency` and also
displayed as a frame. Markdown above them stating both readings are descriptive and that no
significance test is computed, per D-13.

8. Code cell: soft confidences and per-state profiles from the shipped labeler's output
(`regime.plot_soft_confidences`, `regime.plot_regime_profiles`), with markdown noting these cover
the 1990-onward span only.

9. Markdown then code: the A13 side-by-side. Build the active-feature-count timeline with
`regime.active_feature_count_timeline` using the config's `feature_min_history` and
`min_train_months`, get its change dates with `regime.feature_set_change_dates`, render
`regime.plot_active_feature_count`, then render
`regime.plot_label_comparison` over all three labelings with `change_dates` supplied, and print
`regime.label_disagreement` between the reference and the filtered labeling. The markdown must
state the A13 finding in its own terms — the reference is fit on a fixed 9 columns while the
walk-forward's active set changes seven times across the backtest, so their disagreement is not
purely detection delay — and must render `pplot.A13_CAVEAT` so the caveat text is identical to
P6's. State that this notebook makes A13 inspectable and does not resolve it.

10. Markdown: the design section 4.4 acceptance criteria for the shipped configuration, presented
report-only per D-14, with the observed occupancy, sojourn, and churn values from the cells above.
No cell may sweep K, lambda, or n_restarts.

11. Final markdown cell: the cold-start sign-off, per D-15 a plain markdown cell with no helper
and no ledger. It carries the heading `Cold-Start Sign-Off` and blank fields `Date:`,
`Verdict:` with the three allowed values accept, reject, accept-with-caveats, and `Reasoning:`,
followed by a line recording D-16 — a negative verdict is recorded and does not block the phase —
and a line stating that after cold start this cell remains but is not required per run, because
subsequent use of P3 is behaviour-checking rather than re-signing.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_platform_notebooks.py -x -q && .venv/bin/python -c "
import nbformat, pathlib
nb = nbformat.read(pathlib.Path('notebooks/platform/P3_regime_labeling.ipynb'), as_version=4)
src = '\n'.join(c.source for c in nb.cells)
for token in ['Cold-Start Sign-Off', 'Verdict:', 'Reasoning:', 'A13', '1973-09', '2008-07', 'ECONOMIC_EVENTS', 'label_disagreement', 'active_feature_count_timeline']:
    assert token in src, token
for banned in ['run_full_backtest_evaluation', 'run_backtest(']:
    assert banned not in src, banned
print('P3 static checks OK', len(nb.cells), 'cells')"</automated>
    <human-check>Open `notebooks/platform/P3_regime_labeling.ipynb`, Run All against the real checkpoints, confirm every cell completes, read the regime timeline against the six dated transitions, and record date, verdict, and reasoning in the final sign-off cell. A negative verdict is a valid outcome and does not block the phase (D-16).</human-check>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/unit/test_platform_notebooks.py -x -q` exits 0 with `P3_regime_labeling.ipynb` included in the parameterization.
    - The notebook's combined cell source contains the substrings `Cold-Start Sign-Off`, `Verdict:`, `Reasoning:`, and `A13`, and contains neither `run_full_backtest_evaluation` nor `run_backtest(`.
    - Executing the notebook, the reference labeling loaded from `backtest_full_sample_states.parquet` has 695 rows spanning 1963-02-28 to 2020-12-31 with occupancy shares 1.58 / 13.96 / 31.94 / 40.58 / 11.94 percent within 0.5 percentage points per state, summing to 1.0 within 1e-9, and median sojourn 97.0 months.
    - Executing the notebook, the reference labeling's transition dates are exactly 1963-02-28 into state 2, 1973-09-30 into 1, 1981-10-31 into 4, 1988-09-30 into 2, 1996-08-31 into 3, 2008-07-31 into 0, 2009-06-30 into 3.
    - Executing the notebook, the shipped `compute_regime_labeling` output covers 372 rows from 1990-01-31 to 2020-12-31 with 5 distinct states, and the notebook prints that span alongside the reference's.
    - The A13 panel's active-feature-count timeline has 588 rows with 7 change dates and `n_active` bounded within [4, 13]; `label_disagreement` between the reference and filtered labelings reports `n_compared` equal to 588 and `pct_disagree` in [0, 1]. The observed `pct_disagree` is recorded in the plan SUMMARY; no band is claimed around it, because a plausible-looking disagreement number computed against a mismatched target is exactly what A13 is.
    - The occupancy panel calls `drift.assert_regime_occupancy_plausible` before rendering, so occupancy shares that do not sum to 1.0 stop the notebook.
    - The notebook is committed with its cell outputs, matching the storage convention of the twelve legacy notebooks.
    - `.venv/bin/python -m pytest tests/ -q` reports 0 failures.
  </acceptance_criteria>
  <done>P3 renders the regime labeling against dated economic history, shows all three labelings side by side with the seven feature-set change dates marked, carries the A13 caveat, and ends in an editable markdown sign-off cell.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FRED USREC endpoint -> notebook | The phase's only outbound network call; the response is untrusted external data |
| committed notebook -> git | P3's cell outputs become durable repository content, including anything a cell displays |
| persisted labeling artifacts -> P3 | P3 trusts `backtest_full_sample_states.parquet` and `backtest_filtered_state_probs.parquet` to be the series the evaluation actually used |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-10 | Spoofing | a substituted or malformed USREC response producing wrong recession shading, which an operator then signs off against | medium | mitigate | `load_usrec_or_warn` parses the response strictly as a 0/1 monthly Series and never evaluates it; the shading is drawn alongside the independent `ECONOMIC_EVENTS` constant, so a wrong USREC contradicts the event overlay visibly rather than silently |
| T-06-11 | Information Disclosure | P3 displaying `load_platform_config()` output, which carries the injected `FRED_API_KEY`, into a committed cell output | high | mitigate | The notebook routes any config display through `loaders.redacted_config`; `tests/unit/test_platform_notebooks.py` check (4) from plan 06-01 fails the notebook otherwise |
| T-06-12 | Tampering | `compute_regime_labeling` writing `regime_labels` into the fenced production checkpoint namespace when P3 is opened | medium | mitigate | `loaders.compute_regime_labeling` passes `checkpoint_dir=NOTEBOOK_SCRATCH_DIR` so `label_regimes`'s three save calls land in a non-production namespace (D-10, legacy pitfall P20/D5) |
| T-06-13 | Repudiation | a cold-start sign-off recorded against a chart whose recession shading silently failed to load | medium | mitigate | D-10 loud degradation: the fetch failure prints before the plot and the warning names what is missing, so the sign-off is never made against a chart that looks complete but is not |
| T-06-14 | Denial of Service | P3 triggering a multi-minute walk-forward on every open | low | mitigate | P3 loads the persisted artifacts from plan 06-02; a source assertion forbids `run_backtest` and `run_full_backtest_evaluation` in any cell |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs zero new packages. `06-RESEARCH.md` records the Package Legitimacy Audit as not applicable for this phase. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/unit/test_platform_plotting_history.py tests/unit/test_platform_plotting_regime.py tests/unit/test_platform_notebooks.py -x -q` exits 0.
- `.venv/bin/python -m pytest tests/ -q` collects at least 1393 tests and reports 0 failures.
- The real-data active-feature-count reconstruction reproduces the seven documented change points exactly.
- `git status --porcelain src/trading_crab_lib/platform/backtest/driver.py outputs/reports/platform/ data/checkpoints/platform/` is empty.
- Human check (end-of-phase): P3 runs top-to-bottom against real checkpoints and the operator records a verdict in the sign-off cell.
</verification>

<success_criteria>
- P3 shows the 5 regimes against NBER recession bars and six dated economic eras (ROADMAP criterion 4).
- P3 shows the reference, filtered, and shipped labelings side by side with the seven feature-set change dates marked, and quantifies reference-versus-filtered disagreement.
- P3 ends in a markdown sign-off cell with date, verdict, and reasoning fields (ROADMAP criterion 2).
- Every figure in P3 comes from `platform/plotting/` (ROADMAP criterion 3).
- Nothing in P3 claims A13 is resolved, and no hyperparameter is swept.
</success_criteria>

## Artifacts this phase produces

Symbols and files created by THIS plan (new; exclude from drift verification):

**New modules**
- `src/trading_crab_lib/platform/plotting/history.py`
- `src/trading_crab_lib/platform/plotting/regime.py`

**New constants**
- `history.ECONOMIC_EVENTS`

**New functions**
- `history.load_usrec_or_warn`, `history.recession_periods`, `history.regime_era_contingency`, `history.regime_era_marginals`, `history.plot_era_contingency`
- `regime.plot_regime_timeline`, `regime.plot_occupancy_and_sojourn`, `regime.plot_transition_matrix`, `regime.plot_soft_confidences`, `regime.plot_regime_profiles`, `regime.plot_sojourn_distribution`, `regime.active_feature_count_timeline`, `regime.feature_set_change_dates`, `regime.plot_active_feature_count`, `regime.label_disagreement`, `regime.plot_label_comparison`

**New files**
- `notebooks/platform/P3_regime_labeling.ipynb`
- `tests/unit/test_platform_plotting_history.py`
- `tests/unit/test_platform_plotting_regime.py`

<output>
Create `.planning/phases/06-platform-notebook-suite/06-03-SUMMARY.md` when done
</output>
