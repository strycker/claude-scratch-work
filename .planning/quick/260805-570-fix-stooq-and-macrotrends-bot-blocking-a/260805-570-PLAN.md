---
phase: quick/260805-570
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [DATA-03, DATA-05]
files_modified:
  - src/trading_crab_lib/ingestion/http.py
  - src/trading_crab_lib/ingestion/macrotrends.py
  - src/trading_crab_lib/platform/ingestion/prices_daily.py
  - src/trading_crab_lib/platform/ingestion/alfred.py
  - scripts/build_platform_data.py
  - tests/unit/test_ingestion_http.py
  - tests/unit/test_macrotrends.py
  - tests/unit/test_platform_prices_ingest.py
  - tests/unit/test_platform_alfred.py
  - tests/unit/test_build_platform_data_guard.py

estimate:
  tokens: 78000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Stooq and macrotrends fetches go through a browser-impersonating HTTP client instead of plain `requests`."
    - "The impersonating helper verifies TLS by default and does not import the assets.py SSL-bypass factory (P22 is not widened)."
    - "Stooq's existing non-CSV guard still rejects the JS challenge page rather than parsing it as prices."
    - "`_detect_vintage_columns` accepts the real three-column fredapi all-releases frame and still maps realtime_end when present."
    - "`scripts/build_platform_data.py` returns non-zero and never prints BUILD OK when the `daily_raw` checkpoint is empty."
    - "yfinance daily fetch chunks the universe, retries rate-limited chunks with backoff, and keeps partial results."
  artifacts:
    - src/trading_crab_lib/ingestion/http.py
    - tests/unit/test_ingestion_http.py
    - tests/unit/test_build_platform_data_guard.py
  key_links:
    - "prices_daily._batch_stooq_daily -> ingestion.http.http_get"
    - "macrotrends._scrape_series -> ingestion.http.http_get"
    - "build_platform_data.main -> platform checkpoint manager `daily_raw` row/column assertion"
---

<objective>
Fix five verified defects: two bot-blocked scrapers (Stooq JS challenge, macrotrends
Cloudflare 403), an ALFRED schema constant that has made vintage ingestion fail 0/5 on
every run since it shipped, a build-success guard that reports OK on an empty price
universe, and a yfinance batch that discards everything when Yahoo rate-limits it.

Purpose: `daily_raw` is currently 0 rows x 0 columns while the build script prints
"BUILD OK", and ALFRED point-in-time vintages have never once been fetched. Both are
silent-wrong failures in the data layer the whole platform sits on.

Output: one shared browser-impersonating HTTP helper, two rewired scrapers, a corrected
ALFRED required-column contract with honest tests, a real build assertion, and chunked
yfinance fetching with backoff.
</objective>

<execution_context>
@/Users/glestryc/personal/github_repos/claude-scratch-work/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@src/trading_crab_lib/platform/ingestion/prices_daily.py
@src/trading_crab_lib/ingestion/macrotrends.py
@src/trading_crab_lib/platform/ingestion/alfred.py
@scripts/build_platform_data.py
@tests/unit/test_platform_alfred.py
@tests/unit/test_platform_prices_ingest.py
@tests/unit/test_macrotrends.py

**Established diagnosis — do NOT re-investigate:**
- Stooq: HTTP 200 + a ~796-byte JS browser-verification page. Not a credential problem.
- macrotrends: HTTP 403 Cloudflare "Just a moment...". Not a credential problem.
- Both use plain `requests`, whose TLS fingerprint is trivially bot-detected.
- `curl_cffi` 0.16.0 is installed and already declared in the library `[ingestion]` extra
  and in `requirements.txt`. `curl_cffi.requests.Session(impersonate="chrome", verify=True)`
  constructs successfully. `curl_cffi.requests.exceptions.RequestException` subclasses
  `OSError`, as does `requests.exceptions.RequestException`.
- fredapi's `get_series_all_releases()` returns only `realtime_start`, `date`, `value`
  (realtime_end is commented out in fredapi's own source). `realtime_end` is never read by
  any downstream code.
- `pyproject.toml` sets `pythonpath = ["src", "scripts"]`, so `import build_platform_data`
  works from tests (same pattern as `tests/test_scripts_weekly_report.py`).

**Network verification is impossible in this container.** The agent proxy MITM-terminates
TLS and resets curl_cffi's impersonated ClientHello (verified: "Connection reset by peer"
against both hosts, with and without explicit proxy config). Tasks 1's fixes are verified by
mocked-transport unit tests plus a documented manual step for Glenn's own machine. Nothing
in this plan may claim A/B are confirmed working against the live sites.

**Conventions (mandatory):** `from __future__ import annotations` at top of every source
file; module docstring explaining purpose; type hints on public functions;
`log = logging.getLogger(__name__)` and logging (never `print`) in library code; no bare
`except`; broad network catches marked `# noqa: BLE001`; line length 127.

**Test honesty (project rule):** every new or edited test must fail against the OLD code and
pass against the NEW code. No tautological assertions. Network stays mocked. Defect C
survived 1157 passing tests precisely because its test asserted the broken behavior was
correct — do not repeat that.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Shared browser-impersonating HTTP helper, wired into Stooq and macrotrends</name>
  <files>src/trading_crab_lib/ingestion/http.py, src/trading_crab_lib/platform/ingestion/prices_daily.py, src/trading_crab_lib/ingestion/macrotrends.py, tests/unit/test_ingestion_http.py, tests/unit/test_platform_prices_ingest.py, tests/unit/test_macrotrends.py</files>

  <behavior>
    - `browser_session()` constructs the curl_cffi session with `impersonate="chrome"` and
      TLS verification left enabled.
    - `browser_session()` returns a plain `requests.Session` carrying browser headers when
      curl_cffi is unavailable.
    - `browser_session()` returns None and logs a WARNING when neither client is importable.
    - `http_get(url)` issues the request through an impersonating session and returns the response.
    - `_batch_stooq_daily` calls the helper, never plain `requests.get`.
    - `_batch_stooq_daily` sleeps between tickers (>= n-1 sleeps for n tickers).
    - `_batch_stooq_daily` still returns `{}` for a JS challenge page (existing guard intact).
    - `_batch_stooq_daily` still parses a genuine CSV body into a Close Series.
    - `_scrape_series` calls the helper, never plain `requests.get`.
    - macrotrends still falls back to the HTML-table parser when no embedded JSON is present.
    - macrotrends still degrades to an empty frame with a WARNING when a fetch raises.
  </behavior>

  <action>
Create `src/trading_crab_lib/ingestion/http.py`. Module docstring states its purpose:
a shared browser-impersonating HTTP client for public data sources that bot-block plain
`requests` by TLS fingerprint (Stooq serves a JavaScript browser-verification page; macrotrends
returns a Cloudflare interstitial). The docstring must state that this helper leaves TLS
verification enabled by default and is deliberately NOT the SSL-bypass session factory in
`ingestion/assets.py`, which is tracked tech debt (pitfall P22) and must not be widened.

Module contents:
  - Module-level `_curl_requests` holding `curl_cffi.requests` when importable, else None,
    set in a try/except ImportError; `_CURL_CFFI_AVAILABLE` bool alongside it. A module-level
    `_requests` reference to `requests` under the same treatment.
  - `BROWSER_HEADERS: dict[str, str]` — a current Chrome desktop User-Agent plus
    Accept / Accept-Language. Used for the plain-requests fallback path and merged under any
    caller-supplied headers.
  - `DEFAULT_IMPERSONATE = "chrome"` and `DEFAULT_TIMEOUT = 30.0`.
  - `HTTP_ERRORS: tuple[type[BaseException], ...]` — the transport exception classes call
    sites should catch. Build it from whichever clients imported, always including `OSError`
    (both clients' request exceptions subclass it). Exported so call sites need no
    client-specific imports and no bare except.
  - `impersonation_available() -> bool` — True when curl_cffi is importable.
  - `browser_session(*, impersonate: str = DEFAULT_IMPERSONATE, verify: bool = True) -> Any | None`
    — when curl_cffi is available, construct `_curl_requests.Session(impersonate=impersonate,
    verify=verify)`; on TypeError (older curl_cffi that rejects the impersonate kwarg) retry
    without it and log at DEBUG. When curl_cffi is absent, return `_requests.Session()` with
    `BROWSER_HEADERS` applied and its verify flag set from the `verify` argument, logging at
    DEBUG that impersonation is unavailable. When neither client imports, log a WARNING and
    return None.
    The `verify` parameter defaults to enabled and no code path in this module may hand the
    client a falsy verify value on its own initiative — only an explicit caller argument.
  - `http_get(url: str, *, session: Any = None, headers: dict[str, str] | None = None,
    timeout: float = DEFAULT_TIMEOUT, impersonate: str = DEFAULT_IMPERSONATE,
    verify: bool = True) -> Any` — reuse `session` when given, otherwise build a one-shot
    one via `browser_session`. Raise `OSError` with a clear message if no client is available.
    Merge `BROWSER_HEADERS` under `headers` and return the response object.
  Do not import anything from `trading_crab_lib.ingestion.assets` in this module.

Rewire `prices_daily._batch_stooq_daily`:
  - Add module-level `import time` and
    `from trading_crab_lib.ingestion.http import HTTP_ERRORS, browser_session, http_get`.
    Add module constant `_STOOQ_RATE_LIMIT_SECONDS = 1.5`.
  - Delete the lazy in-function `import requests` and its ImportError branch; instead build ONE
    session via `browser_session()` before the ticker loop and return `{}` with a WARNING if it
    is None.
  - Reuse that single session for every ticker via `http_get(url, session=session,
    headers=..., timeout=30)`. Sleep `_STOOQ_RATE_LIMIT_SECONDS` between tickers (skip the
    sleep before the first) — 22 rapid-fire requests is itself a bot signal.
  - Catch `HTTP_ERRORS` around the fetch, marked `# noqa: BLE001`, log WARNING, continue.
  - LEAVE THE NON-CSV GUARD EXACTLY AS IT IS: the check that the body starts with the CSV
    date header before parsing, and the skip-with-WARNING otherwise. That guard is correct and
    is the reason a challenge page was never mistaken for prices. Do not relax or reorder it.
  - Update the function docstring to note the impersonating client is now used, while keeping
    the existing explanation of the challenge-page guard.

Rewire `macrotrends`:
  - Replace the top-level hard `try: import requests / raise ImportError` block with
    `from trading_crab_lib.ingestion.http import browser_session, http_get`. The module must no
    longer reference `requests` directly.
  - Give `_scrape_series` an added keyword parameter `session: Any = None` and replace its
    `requests.get(...)` call with `http_get(url, session=session, headers=HEADERS, timeout=30)`.
    Keep `resp.raise_for_status()` — a Cloudflare 403 then raises into `fetch_all`'s existing
    broad handler, which logs a WARNING and moves on, preserving graceful degradation.
  - In `fetch_all`, build one session via `browser_session()` before the series loop and pass it
    into every `_scrape_series` call.
  - Leave untouched: `_DATA_PATTERN`, the embedded-JSON path, the
    `_scrape_series_html_table` fallback chain, `HEADERS`, and `RATE_LIMIT_SECONDS = 3.0`.
  - Update the module docstring to record that macrotrends fronts its pages with a Cloudflare
    bot check, which is why the impersonating client is required.

Tests — new `tests/unit/test_ingestion_http.py`:
  - `test_browser_session_requests_chrome_impersonation_with_tls_verification` — monkeypatch
    `http._curl_requests` with a fake module whose `Session(**kwargs)` records kwargs; assert
    the recorded impersonate kwarg is "chrome" and the recorded verify kwarg is truthy.
    (Assert on the recorded constructor kwargs, not on `session.verify` — curl_cffi rewrites
    that attribute from the ambient CA-bundle environment.)
  - `test_browser_session_falls_back_to_requests_without_curl_cffi` — set
    `http._CURL_CFFI_AVAILABLE` False; assert the returned object is a `requests.Session` and
    carries a browser User-Agent header.
  - `test_browser_session_returns_none_without_any_client` — both client references absent;
    assert None returned.
  - `test_http_get_reuses_supplied_session` — pass a recording fake session; assert its `.get`
    received the url and a timeout, and that no new session was constructed.
  - `test_module_does_not_import_the_ssl_bypass_factory` — read this module's own source and
    assert it contains no import from the assets ingestion module.

Tests — edit `tests/unit/test_platform_prices_ingest.py`:
  - Repoint `test_batch_stooq_daily_parses_csv_close` and
    `test_batch_stooq_daily_skips_js_challenge_page` from `@patch("requests.get")` to
    `@patch("trading_crab_lib.platform.ingestion.prices_daily.http_get")`, and patch
    `prices_daily.browser_session` to return a MagicMock and `prices_daily.time.sleep`.
    The challenge-page test is the anti-regression guard for the non-CSV check and must keep
    asserting an empty dict.
  - ADD `test_batch_stooq_daily_uses_impersonating_client_not_plain_requests` — patch
    `prices_daily.http_get` to return a CSV response, and patch `requests.get` with a mock;
    assert the impersonating helper was called and `requests.get` was not.
  - ADD `test_batch_stooq_daily_rate_limits_between_tickers` — three tickers, `http_get`
    returning CSV, `prices_daily.time.sleep` patched; assert sleep was called at least twice.

Tests — edit `tests/unit/test_macrotrends.py`:
  - Repoint all five `@patch("trading_crab_lib.ingestion.macrotrends.requests.get")` decorators
    to `@patch("trading_crab_lib.ingestion.macrotrends.http_get")`, patching
    `macrotrends.browser_session` where a session is needed. The HTML-table fallback test and
    the graceful-failure test must keep asserting the same outcomes as before.
  - ADD `test_scrape_series_uses_impersonating_client` — assert the patched helper received the
    macrotrends URL.

Every repointed and added test above fails against the pre-change code (the patch targets do
not exist, or no sleep occurs) and passes after — verify that ordering holds before committing.

Commit: `fix(ingestion): use browser-impersonating HTTP for Stooq and macrotrends`
  </action>

  <verify>
    <automated>python -m pytest tests/unit/test_ingestion_http.py tests/unit/test_macrotrends.py tests/unit/test_platform_prices_ingest.py -q</automated>
    <automated>python -c "import ast,sys; src=open('src/trading_crab_lib/ingestion/http.py').read(); ast.parse(src); sys.exit(0 if 'ingestion.assets' not in src else 1)"</automated>
    <automated>grep -v '^\s*#' src/trading_crab_lib/platform/ingestion/prices_daily.py | grep -c 'requests.get' | grep -qx 0</automated>
    <automated>grep -v '^\s*#' src/trading_crab_lib/ingestion/macrotrends.py | grep -c 'requests.get' | grep -qx 0</automated>
    <automated>python -m ruff check src/trading_crab_lib/ingestion/http.py src/trading_crab_lib/ingestion/macrotrends.py src/trading_crab_lib/platform/ingestion/prices_daily.py</automated>
    <human-check>
      Live-network confirmation is NOT possible in this container (the agent proxy resets
      curl_cffi's impersonated TLS handshake). On your own machine, run:
        python -c "from trading_crab_lib.ingestion.http import http_get; r=http_get('https://stooq.com/q/d/l/?s=spy.us&i=d'); print(r.text[:80])"
      Expect a line beginning with the CSV date header, not HTML. Then:
        python -c "import logging;logging.basicConfig(level=logging.INFO);from trading_crab_lib.ingestion.macrotrends import fetch_all;print(fetch_all({}).shape)"
      Expect a non-empty frame. Until this is run, fixes A and B are unconfirmed against
      the live sites — the unit tests prove wiring, not that the impersonation defeats
      the bot checks.
    </human-check>
  </verify>

  <done>
`ingestion/http.py` exists with `browser_session`, `http_get`, `HTTP_ERRORS`,
`impersonation_available`, and no import from the assets module. Neither `prices_daily` nor
`macrotrends` calls `requests.get`. Stooq sleeps between tickers and still rejects non-CSV
bodies. The three affected test files pass; ruff is clean. One commit.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Correct the ALFRED required-vintage-column contract and the tests that enshrined the bug</name>
  <files>src/trading_crab_lib/platform/ingestion/alfred.py, tests/unit/test_platform_alfred.py</files>

  <behavior>
    - `_detect_vintage_columns` accepts a frame with only realtime_start / date / value and
      returns a three-key mapping.
    - `_detect_vintage_columns` still maps realtime_end when the frame carries it (four-key
      mapping), case-insensitively.
    - `_detect_vintage_columns` still raises naming the missing role when `value` is absent.
    - `fetch_vintage_series` returns without raising for a three-column all-releases response.
    - `value_as_of` reconstructs correct point-in-time values from a three-column frame.
  </behavior>

  <action>
In `src/trading_crab_lib/platform/ingestion/alfred.py`:
  - Narrow `_REQUIRED_VINTAGE_ROLES` to `("realtime_start", "date", "value")` and add a sibling
    `_OPTIONAL_VINTAGE_ROLES = ("realtime_end",)`.
  - In `_detect_vintage_columns`, keep the existing required-role loop and its raise, then map
    each optional role into the returned mapping only when present in the frame — callers whose
    frames do carry realtime_end keep receiving it.
  - Replace the docstring sentence that describes this as guarding an assumed all-releases
    column schema. The new text records the observed fact: fredapi's `get_series_all_releases()`
    returns three columns — realtime_start, date, value — because realtime_end is commented out
    in fredapi's own source. Requiring it made every ALFRED fetch fail 0/5 on every machine and
    every network since the module shipped; realtime_end is read by no downstream code and is
    therefore mapped opportunistically, never required.
  - Mirror the correction in the module docstring's bulk-fetch section so the recorded schema
    matches reality.
  - Change nothing in `value_as_of`, `align_with_fallback`, or `fetch_all_vintages` — they read
    only realtime_start, date, and value.

In `tests/unit/test_platform_alfred.py`:
  - Add a helper `_make_three_column_all_releases_df()` producing the REAL fredapi shape —
    realtime_start / date / value only — with the same two-revision structure as the existing
    four-column helper (2020-01-01 revised from 100 to 150, plus a 2020-06-01 observation).
  - REWRITE `test_detect_vintage_columns_raises_on_missing_role`: it currently builds the real
    fredapi shape and asserts it MUST raise, which is exactly the bug. Point it instead at a
    frame missing `value` (columns realtime_start and date only) and match on that role name.
  - ADD `test_detect_vintage_columns_accepts_real_fredapi_three_column_shape` — the three-column
    frame maps to exactly realtime_start / date / value, with no realtime_end key present.
  - ADD `test_detect_vintage_columns_maps_realtime_end_when_present` if not already covered by
    the case-insensitive test; keep `test_detect_vintage_columns_case_insensitive` passing
    unchanged for the four-column frame.
  - ADD `test_fetch_vintage_series_accepts_three_column_response` — mock Fred returning the
    three-column frame; assert the call returns a DataFrame and does not raise.
  - ADD `test_value_as_of_on_three_column_frame` — assert the same two point-in-time answers the
    existing four-column test asserts (100 before the revision, 150 after), proving the
    reconstruction genuinely works on the real shape rather than only on the padded fixture.

The rewritten and added tests must fail against the pre-change module (the three-column frame
raises today) and pass after.

Commit: `fix(alfred): stop requiring realtime_end absent from fredapi all-releases`
  </action>

  <verify>
    <automated>python -m pytest tests/unit/test_platform_alfred.py -q</automated>
    <automated>python -c "import pandas as pd; from trading_crab_lib.platform.ingestion.alfred import _detect_vintage_columns as d; m=d(pd.DataFrame(columns=['realtime_start','date','value'])); assert set(m)=={'realtime_start','date','value'}, m; print('three-column shape accepted')"</automated>
    <automated>python -m ruff check src/trading_crab_lib/platform/ingestion/alfred.py</automated>
  </verify>

  <done>
`_REQUIRED_VINTAGE_ROLES` is the three-role tuple; realtime_end maps only when present. The
real fredapi three-column frame is accepted by `_detect_vintage_columns`, `fetch_vintage_series`,
and `value_as_of`. The missing-role test now targets a genuinely required role. Docstrings record
the observed schema instead of the assumed one. `tests/unit/test_platform_alfred.py` passes.
One commit.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Real build-success assertion and chunked yfinance fetching with backoff</name>
  <files>scripts/build_platform_data.py, src/trading_crab_lib/platform/ingestion/prices_daily.py, tests/unit/test_build_platform_data_guard.py, tests/unit/test_platform_prices_ingest.py</files>

  <behavior>
    - The build guard reports a failure for a zero-row frame.
    - The build guard reports a failure for a frame with rows but zero columns.
    - The build guard reports a failure when the daily_raw checkpoint is absent.
    - The build guard passes for a frame with rows and columns.
    - `_batch_yfinance_daily` issues more than one download call for a universe larger than the
      chunk size, each call carrying at most chunk-size tickers.
    - `_batch_yfinance_daily` returns the successful chunk's tickers when a later chunk
      exhausts its retries, rather than an empty dict.
    - `_batch_yfinance_daily` retries a rate-limited chunk with increasing sleep intervals and
      keeps the result when a retry succeeds.
  </behavior>

  <action>
In `scripts/build_platform_data.py`:
  - Add a module-level pure helper
    `check_price_coverage(daily_raw: pd.DataFrame | None) -> str | None` returning None when the
    frame has both rows and columns, and otherwise a specific failure message naming what was
    wrong (frame absent / zero rows / zero columns). Type-hinted, docstringed, no I/O — this is
    what the unit tests exercise.
  - In `main()`, delete the substring heuristic that scans column names for price-like tokens.
    That heuristic is why the script printed BUILD OK over a 0x0 `daily_raw`: the "tr" token
    matches almost any column name, and the "equit" token matches the multpl-derived
    `equities_tr` column, which is not produced by price ingestion at all.
  - Replace it with: load the `daily_raw` checkpoint from the platform checkpoint manager inside
    a try/except for a missing-file error (passing None to the helper on that path), call
    `check_price_coverage`, and on a non-None message `log.error` it together with the
    "a data source was unreachable — re-run from a machine with normal internet" guidance and
    `return 1` before any success output. Apply the same row/column check to `monthly_features`.
    The BUILD OK line must be unreachable whenever either check fails.

In `prices_daily._batch_yfinance_daily`:
  - Add module constants `_YF_CHUNK_SIZE = 5`, `_YF_MAX_RETRIES = 3`,
    `_YF_BACKOFF_BASE_SECONDS = 2.0`, and `_RATE_LIMIT_SIGNATURES` holding the substrings that
    identify Yahoo throttling responses ("Too Many Requests", "Rate limit", "rate limited",
    "429"). Define them locally — do not import the private constant from `ingestion/assets.py`.
  - Extract the existing MultiIndex/flat-column parsing block verbatim into a private helper
    `_extract_close_series(raw: pd.DataFrame, tickers: list[str]) -> dict[str, pd.Series]`,
    preserving the tz-strip, the dropna, the all-NaN WARNING, and the no-resample rule.
  - Rewrite the body to iterate the ticker list in chunks of `_YF_CHUNK_SIZE`. For each chunk,
    attempt up to `_YF_MAX_RETRIES` downloads; treat both a raised transport/rate-limit error and
    an empty/None result as retryable, sleeping `_YF_BACKOFF_BASE_SECONDS * 2 ** attempt` between
    attempts. On success merge the chunk's series into the accumulating result dict; on final
    failure log a WARNING naming the chunk and CONTINUE to the next chunk — partial results are
    never discarded. Sleep briefly between chunks. Keep the existing exception tuple and its
    `# noqa: BLE001` marking; keep the `_YFINANCE_AVAILABLE` early return unchanged.
  - Log a summary line reporting how many of the requested tickers were recovered.
  - Do not change `fetch_universe_prices`'s Stooq-fallback ordering or its graceful empty-frame
    degradation; the chunked fetch still returns a plain dict.

Tests — new `tests/unit/test_build_platform_data_guard.py`:
  - `import build_platform_data as build` (scripts is on pytest's pythonpath, same pattern as
    `tests/test_scripts_weekly_report.py`).
  - Four cases against `check_price_coverage`: zero-row frame -> message, rows-but-no-columns
    frame -> message, None -> message, populated frame -> None. Assert the failure messages are
    non-empty strings, not just truthiness.

Tests — add to `tests/unit/test_platform_prices_ingest.py`:
  - `test_yfinance_daily_fetches_in_small_chunks` — patch `prices_daily.yf` and
    `prices_daily._YFINANCE_AVAILABLE`, and `prices_daily.time.sleep`; request twelve tickers;
    assert `yf.download` was called more than once and that no call carried more than
    `_YF_CHUNK_SIZE` tickers.
  - `test_yfinance_daily_keeps_partial_results_when_a_chunk_exhausts_retries` — first chunk
    returns a valid frame, every later attempt raises a rate-limit error; assert the returned
    dict contains the first chunk's tickers and is not empty.
  - `test_yfinance_daily_retries_rate_limited_chunk_with_growing_backoff` — one chunk raises a
    rate-limit error twice then succeeds; assert `yf.download` was called three times for it and
    that the patched sleep received strictly increasing intervals.
  Each of these fails against the pre-change single-call implementation (one call, empty dict on
  failure, no sleeps) and passes after.

Commit: `fix(build): assert non-empty daily_raw and chunk yfinance fetches with backoff`
  </action>

  <verify>
    <automated>python -m pytest tests/unit/test_build_platform_data_guard.py tests/unit/test_platform_prices_ingest.py -q</automated>
    <automated>python -c "import pandas as pd, build_platform_data as b, sys; sys.path.insert(0,'scripts'); assert b.check_price_coverage(pd.DataFrame()) , 'empty frame must report failure'; assert b.check_price_coverage(pd.DataFrame({'SPY':[1.0]})) is None; print('guard rejects empty daily_raw')"</automated>
    <automated>python -m ruff check scripts/build_platform_data.py src/trading_crab_lib/platform/ingestion/prices_daily.py</automated>
    <automated>python -m pytest tests/ -q</automated>
  </verify>

  <done>
`check_price_coverage` exists and is unit-tested for all four cases; `main()` returns non-zero
without printing BUILD OK on an empty `daily_raw` or `monthly_features`, and the column-substring
heuristic is gone. `_batch_yfinance_daily` fetches in chunks, retries rate-limited chunks with
exponential backoff, and merges partial results. Full suite passes with no new failures.
One commit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stooq.com / macrotrends.net -> parser | Untrusted third-party HTML/CSV crosses into pandas parsing |
| Yahoo Finance -> yfinance -> frame | Untrusted upstream response shapes the price universe |
| FRED/ALFRED -> fredapi -> vintage frame | Untrusted column schema drives point-in-time reconstruction |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-570-01 | Tampering | `ingestion/http.py` | high | mitigate | TLS verification stays enabled by default; the module does not import or replicate the assets.py SSL-bypass factory, so pitfall P22's blast radius is unchanged (Task 1 verify gate asserts the absent import) |
| T-570-02 | Spoofing | `_batch_stooq_daily` | medium | mitigate | The existing CSV-header guard is preserved verbatim so a challenge/error page can never be parsed as price data (Task 1 explicitly forbids relaxing it; the challenge-page test remains) |
| T-570-03 | Information disclosure | `ingestion/http.py` | low | mitigate | Only URLs and ticker/series names are logged; no headers, cookies, or API keys are logged on any path |
| T-570-04 | Denial of service | Stooq / Yahoo endpoints | medium | mitigate | Per-ticker sleep for Stooq and chunked fetching with exponential backoff for yfinance — polite pacing that also reduces the caller's own bot signature |
| T-570-05 | Tampering | `alfred._detect_vintage_columns` | medium | accept | The required-role check is narrowed, not removed: realtime_start / date / value are still enforced before any point-in-time reconstruction, and realtime_end was never read downstream |
| T-570-06 | Repudiation | `scripts/build_platform_data.py` | high | mitigate | The build no longer reports success over an empty price universe; failure is logged at ERROR with a non-zero exit, so a silent-wrong data layer cannot propagate to the report |
</threat_model>

<verification>
- `python -m pytest tests/ -q` — full suite, no new failures versus the pre-change baseline.
- `python -m ruff check src/ scripts/` — clean.
- Three commits on the branch, one per task, conventional-commit format.
- Fixes A and B remain UNCONFIRMED against the live sites until the Task 1 `<human-check>` is
  run on Glenn's own machine. No commit message, summary, or STATE entry may claim otherwise.
</verification>

<success_criteria>
- Stooq and macrotrends fetch through the shared impersonating client; neither module calls
  `requests.get`; the Stooq non-CSV guard is byte-for-byte intact.
- The helper defaults to verified TLS and contains no import from the assets ingestion module.
- ALFRED accepts fredapi's real three-column all-releases frame; the test that asserted the
  opposite is repointed at a genuinely required role, and a positive test covers the real shape.
- `scripts/build_platform_data.py` exits non-zero on an empty `daily_raw` and never prints
  BUILD OK in that case.
- yfinance fetches in chunks with backoff and preserves partial results.
- Every new/edited test fails against the old code and passes against the new.
</success_criteria>

<output>
Create `.planning/quick/260805-570-fix-stooq-and-macrotrends-bot-blocking-a/260805-570-SUMMARY.md` when done.
</output>
