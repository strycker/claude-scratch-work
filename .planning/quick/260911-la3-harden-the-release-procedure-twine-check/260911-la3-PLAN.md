---
phase: quick-260911-la3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/publish-pypi.yml
  - src/trading_crab_lib/pyproject.toml
  - src/trading_crab_lib/README.md
  - src/trading_crab_lib/MANIFEST.in
  - docs/RELEASING.md
autonomous: true
requirements: [QUICK-260911-la3]

user_setup:
  - service: TestPyPI (test.pypi.org)
    why: "Task 3 adds a workflow_dispatch dry-run path that uploads to TestPyPI. Without this secret, a testpypi-targeted dispatch run correctly fails the token-presence guard by design (it must not fall back to a production token) -- creating the secret is what makes the rehearsal path usable, not required for this plan's tasks to complete or verify."
    env_vars:
      - name: TEST_PYPI_API_TOKEN
        source: "test.pypi.org -> Account settings -> API tokens (account-scoped is fine for a rehearsal token, or scope to the project once it exists there). Add as a GitHub repository secret, or as an environment secret on the 'pypi' environment -- same two locations PYPI_APP_TOKEN and PYPI_LIB_TOKEN already use."

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 4
  confidence: low        # gsd-tools estimate-calibration: sample_count=0, factor=1.0, applied=false

must_haves:
  truths:
    - "trading-crab-lib's PyPI project page renders a full description: the wheel and sdist both carry README.md as long_description with content-type text/markdown, proven by a real `twine check --strict` PASSED result captured against freshly built artifacts (the pre-fix baseline, already captured this session, FAILED with long_description / long_description_content_type warnings)."
    - "`twine check --strict` runs as its own named, gated step on real dist/ output before any upload step, for both matrix legs, and fails the job on any metadata warning or error rather than allowing a broken package past CI."
    - "A workflow_dispatch run with the target input set to testpypi uploads using TEST_PYPI_API_TOKEN and TWINE_REPOSITORY_URL=https://test.pypi.org/legacy/; a tag-push run (v*, lib-v*, both-v*) always resolves target=pypi and always uses the production secret and the default PyPI endpoint, regardless of any input -- proven by driving the gate step's extracted bash with both event shapes, not by inspection alone."
    - "The token-presence guard checks whichever secret the resolved target actually needs (TEST_PYPI_API_TOKEN for testpypi, PYPI_LIB_TOKEN/PYPI_APP_TOKEN for pypi) by selecting the secret NAME via ternary and indexing secrets[...] exactly once -- never a value-level secrets.A || secrets.B pattern, which would silently fall through to the wrong package's production token whenever the intended secret is the one that's missing."
    - "The Publish ... to PyPI step's run: body is byte-identical to its pre-task form -- the TestPyPI path is implemented entirely through TWINE_REPOSITORY_URL in env:, verified by a live-executed twine.settings.Settings probe showing an empty TWINE_REPOSITORY_URL resolves to the real production upload URL and the TestPyPI literal resolves to TestPyPI, not by argument."
    - "A developer following docs/RELEASING.md can: read the tag-convention trap (a v* tag silently skips the lib leg, reporting GREEN), bump both pyproject.toml versions before tagging, validate a token without publishing (upload an already-published artifact without --skip-existing: 400 = good token, 403 = bad token, and --skip-existing defeats this check), lay out a four-section ~/.pypirc, and trigger the TestPyPI rehearsal -- using only placeholder secret values, never a real one."
    - "Nothing else in the workflow regresses: tag conventions, the matrix, the gate's existing publish/skip logic, the dist path line, the Verify built artifacts version guard, --skip-existing, both package versions (0.1.4), and python-package.yml's 3.10-3.13 matrix are all unchanged."
    - "No real PyPI, TestPyPI, or other secret value appears in any committed file, workflow log excerpt, or scratchpad artifact referenced by the plan or summary."
  artifacts:
    - .github/workflows/publish-pypi.yml
    - src/trading_crab_lib/pyproject.toml
    - src/trading_crab_lib/README.md
    - docs/RELEASING.md
  key_links:
    - "src/trading_crab_lib/pyproject.toml [project] readme = \"README.md\" <-> src/trading_crab_lib/README.md existing on disk <-> twine check --strict PASSED on real built artifacts (Task 1 fixes the exact defect Task 2's new gate then enforces going forward)"
    - "gate step new target output <-> token-guard step's target-aware secret-NAME ternary <-> publish step's byte-identical secret-NAME ternary and TWINE_REPOSITORY_URL ternary -- all three keyed off the identical steps.gate.outputs.target value, and the guard/publish TWINE_PASSWORD expressions must be string-identical or the guard proves nothing about what publish receives"
    - "workflow_dispatch target input default (testpypi, the safe choice) <-> gate step's hardcoded TARGET=\"pypi\" branch for any non-workflow_dispatch event -- a tag push has no target input to read at all, so production is the only reachable outcome on that path, not a default being overridden"
    - "TWINE_REPOSITORY_URL=\"\" (env, false branch) <-> twine's EnvironmentDefault/get_repository_from_config falsy-check <-> production https://upload.pypi.org/legacy/ -- this mechanism is asserted by live execution of twine's own Settings class in Task 3's verification, not by reading its source and trusting it"
---

<objective>
Harden the PyPI release procedure end to end: give `trading-crab-lib` a real README so its PyPI
page is no longer blank, add a `twine check --strict` gate that catches the class of metadata
defect the README fix just closed, add a TestPyPI dry-run path reachable only via manual dispatch
(a tag push must always publish for real), and write down the release checklist and traps
discovered while building all of this.

Purpose: `trading-crab-lib` currently builds and uploads cleanly but with a blank PyPI
description (`twine check` on the current 0.1.4 build: PASSED WITH WARNINGS --
`long_description` and `long_description_content_type` both missing, confirmed this session).
Separately, the only way to test a real upload today is a real, irreversible publish -- there is
no rehearsal path, and no written procedure exists for the traps already hit once (the `v*` tag
silently skipping the lib leg while reporting green; a burned, unrecoverable PyPI version).

Output: `src/trading_crab_lib/README.md` (new) wired into `[project] readme` in that package's
`pyproject.toml`; a new `twine check --strict` step in `.github/workflows/publish-pypi.yml`; a
new `workflow_dispatch` `target` input (`testpypi` default, `pypi` option) with the gate,
token-presence guard, and publish steps all made target-aware; and `docs/RELEASING.md` capturing
the full procedure and every trap listed above.
</objective>

<execution_context>
@/Users/glestryc/personal/github_repos/claude-scratch-work/.claude/gsd-core/workflows/execute-plan.md
@/Users/glestryc/personal/github_repos/claude-scratch-work/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.github/workflows/publish-pypi.yml
@src/trading_crab_lib/pyproject.toml
@src/trading_crab_lib/MANIFEST.in
@.planning/quick/260911-kkj-add-a-token-presence-guard-to-the-pypi-p/260911-kkj-PLAN.md
@CLAUDE.md

**Facts established this session -- do not re-derive, build directly on these:**

- Current branch is `claude/exciting-keller-2xy11u`. Stay on it. Both packages are at version
  0.1.4 (unpublished) -- no version changes in this plan.
- `./.venv/bin/python` has PyYAML 6.0.3, `build` 1.6.1, and `twine` importable -- confirmed by
  running all of the checks below with it this session.
- **Baseline `twine check --strict` result, captured this session by actually building both
  packages fresh:** `trading-crab` (root `pyproject.toml`) PASSES clean. `trading-crab-lib`
  (`src/trading_crab_lib/pyproject.toml`) FAILS with exactly two warnings: `long_description_content_type`
  missing (defaults to `text/x-rst`) and `long_description` missing. Cause: no `readme` key in
  that package's `[project]` table and no README file in that directory.
- **Fix confirmed by a real build, this session, in an isolated scratch copy:** adding
  `readme = "README.md"` to `[project]` plus a real README.md file, with **no MANIFEST.in
  change**, produced a wheel whose METADATA correctly showed `Description-Content-Type:
  text/markdown` (auto-inferred from the `.md` extension -- no explicit content-type key needed)
  and a description body containing the README's own text, and an sdist tarball that already
  listed `README.md` at its top level with the MANIFEST.in file completely unchanged. `twine
  check --strict` on both PASSED. Task 1 must reproduce this on the real package, not just trust
  this note -- but the mechanism and the "MANIFEST.in needs no edit" conclusion are settled.
- **`TWINE_REPOSITORY_URL` behavior, confirmed by live-executing `twine.settings.Settings` this
  session (source: `twine/utils.py:EnvironmentDefault`, `twine/utils.py:get_repository_from_config`),
  not by reading source alone:** with `TWINE_REPOSITORY_URL=""` in the environment, twine's own
  `EnvironmentDefault` argparse action reads it via `os.environ.get(env, default)` -- since the
  key is present (even though empty), it does NOT fall back to argparse's `default=None`, so
  `args.repository_url` becomes `""`. `get_repository_from_config` then tests `if repository_url:`
  -- an empty string is falsy -- so it falls through to the named-repository lookup, which
  resolves the literal name `"pypi"` to `https://upload.pypi.org/legacy/` via twine's own
  hardcoded fallback (no `.pypirc` needed). Setting `TWINE_REPOSITORY_URL` to the TestPyPI legacy
  URL resolves directly to that URL instead. Both outcomes were observed by actually constructing
  `Settings(...)` and printing `settings.repository_config["repository"]` -- not asserted from
  reading the code. **Conclusion for this plan: the `Publish ... to PyPI` step's `run:` body
  needs zero changes -- the TestPyPI path is implemented entirely via one new `env:` entry.**
- **GitHub Actions ternary trap, reasoned through and avoided by construction in this plan's
  design:** `condition && secrets.A || secrets.B` returns `secrets.B` whenever `secrets.A` is
  itself an empty/missing secret, even when `condition` is true -- because `&&`/`||` in GH Actions
  expressions return operand *values*, and an empty string is falsy, so the `||` treats
  `condition && secrets.A` (which equals `secrets.A`, i.e. `""`) as false and falls through. This
  plan's target-aware expressions therefore ternary the secret *name* (always a non-empty
  literal) and index `secrets[...]` exactly once at the end -- confirmed to parse and produce the
  intended literal strings via a `yaml.safe_load` round-trip this session.
- Established convention on this file (from the two preceding quick tasks, 260910-vyi and
  260911-kkj): any new or edited `run:` body must contain ZERO `${{ }}` expressions -- hoist
  matrix/context/secret values into that step's own `env:` block, so the shipped bash stays
  extractable via `yaml.safe_load` and runnable standalone. Any `${{ }}` this plan adds therefore
  lives only in `env:` values, never in a `run:` string.
- The scratchpad already contains files from the immediately preceding quick task (260911-kkj):
  `extract_token_guard.py`, `gate.sh`, `guard.sh`, `token_guard.sh`,
  `run_token_guard_scenarios.sh`, and assorted `.log` files. **Use distinct new names for every
  script this plan's tasks write** (see each task's action) so nothing here is overwritten or
  confused with the prior task's evidence.
- Scratchpad for all temporary scripts and captured output:
  `/tmp/claude-0/-home-user-claude-scratch-work/2b704a4a-9590-5ab7-b2d2-213af4621a1f/scratchpad`
- This repo has a documented history of sign-offs against evidence that could not detect a wrong
  value (`.planning/UAT-AUDIT-2026-09-09.md`). Extract-and-execute the real bash; never retype a
  paraphrase. Build the real artifacts; never assert from the presence of a config key alone.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Give trading-crab-lib a real README and prove twine check goes from WARNING to PASSED</name>
  <files>src/trading_crab_lib/pyproject.toml, src/trading_crab_lib/README.md, src/trading_crab_lib/MANIFEST.in</files>
  <reversibility rating="reversible">Two new/edited files scoped to one package directory; revert is `git checkout` of this task's files.</reversibility>
  <action>
Wire the one path that matters end to end: a real library README, wired into the real package
metadata, proven by a real build and a real `twine check --strict` run -- going from the
documented WARNING baseline to a clean PASSED.

**Add `readme = "README.md"` to `src/trading_crab_lib/pyproject.toml`'s `[project]` table**,
placed on its own line immediately after the existing `description = "..."` line. Also add
`readme = "README.md"` to the `[tool.poetry]` table in the same file (it already carries `name`
and `packages` for the `poetry install -E` convenience path documented in that file's own
comments) -- one extra line, for metadata consistency between the two build paths that read this
file, even though CI publishes exclusively via `python -m build` (setuptools backend) and never
invokes `poetry build`.

**Write `src/trading_crab_lib/README.md` as a library-first README** -- not a copy of the root
`README.md`, which is app/pipeline-oriented and answers a different question (how do I run the
whole trading-crab pipeline) than this file must answer (how do I `pip install trading-crab-lib`
and call it from my own code). Before writing a single sentence, re-verify every function name,
class name, and behavior claim against the real source (`src/trading_crab_lib/__init__.py`,
`config.py`, `transforms.py`, `checkpoints.py`) -- do not carry over any claim from this plan's
own prose without independently confirming it against the file on disk, since a future signature
change would silently make an unverified README claim wrong. At minimum, cover:

1. **What it provides**, grouped by the real module layout: feature engineering (`transforms.py`
   plus `divergence.py`/`momentum.py`/`indicators.py`/`yield_curve_features.py`), clustering
   (`clustering.py` PCA/KMeans, `gmm.py`, `hmm.py`, `markov.py`, `density.py`, `spectral.py`,
   `cluster_comparison.py`), regime profiling (`regime.py`), supervised prediction
   (`prediction/` -- note the flat production API in `prediction/__init__.py` versus the bundle
   test-support API in `prediction/classifier.py`, per this repo's ADR #12, so a reader importing
   from the wrong module isn't surprised by a different return shape), reporting/diagnostics/
   tactics (`reporting.py`, `diagnostics.py`, `tactics.py`, `email.py`), plotting
   (`plotting/` package, matplotlib/seaborn-based), optional data ingestion (`ingestion/` --
   FRED/multpl.com/yfinance/macrotrends, gated behind the `[ingestion]` extra), and checkpointing
   (`checkpoints.py` -- parquet for DataFrames, joblib for models).
2. **Install instructions** covering the base install (`pip install trading-crab-lib`; core deps
   are pandas/numpy/scikit-learn/scipy/pyarrow/pyyaml/joblib/python-dotenv, no network/viz deps)
   and every real optional extra read directly from the `[project.optional-dependencies]` table
   in this same `pyproject.toml` at the time of writing -- `ingestion`, `plotting`, `hmm`,
   `clustering-extras`, `boosting`, `browser`, `all`, `dev` -- each with a one-line purpose and its
   actual constituent packages (e.g. `browser` needs a one-time `playwright install chromium`
   after installing the extra, per that extra's own comment in the file). Do not invent an extra
   that isn't in the file, and do not omit one that is.
3. **A short, accurate usage example** built only from real, checkable signatures -- showing
   `trading_crab_lib.load()` (note it now accepts `None` / a `Path` or `str` / or a `dict`
   directly, bypassing all file I/O, per this package's own `config.py` docstring) and
   `trading_crab_lib.transforms.engineer_all(df, cfg, causal=...)`, explaining the `causal` flag
   in one sentence (`False` for clustering/regime labeling, `True` for supervised learning and
   live scoring -- never mix the two, per this repo's look-ahead-bias guard). State plainly that
   functions expecting a full project layout (e.g. `CheckpointManager`, the `ingestion/` fetchers)
   look for `config/`, `data/`, `outputs/` directories that are overridable via
   `TC_ROOT_DIR`/`TC_CONFIG_DIR`/`TC_DATA_DIR`/`TC_OUTPUT_DIR` environment variables, or avoided
   entirely by passing a config dict straight to `load()`.
4. **A link back to the GitHub repo** -- reuse the exact `Homepage` URL already present in this
   file's `[project.urls]` table (do not invent a different URL) -- and one sentence pointing to
   the sibling `trading-crab` application package (same monorepo, `src/trading_crab/`) for the
   CLI and full pipeline orchestration this library does not itself provide.
5. **License line** -- MIT, matching the `license` key already in this file.

Accuracy matters more than length. Every named identifier in the finished README must be
independently checkable against the real source at the time of writing.

**Verify empirically whether `src/trading_crab_lib/MANIFEST.in` needs an `include README.md`
line** -- do not presuppose the answer from this plan's context note. Build the real package's
sdist into a scratch output directory and inspect the actual tarball listing for `README.md`. If
it is present without any MANIFEST.in edit (expected, based on this session's scratch-copy test,
but confirm on the real files), leave MANIFEST.in untouched and record that finding in the
summary. Only add the include line if the real sdist is missing the file.

Clean up every build artifact this task produces (`dist/`, `*.egg-info/`, scratch output dirs)
before finishing -- nothing from this task's builds may be committed.
  </action>
  <verify>
    <automated>cd /home/user/claude-scratch-work && rm -rf /tmp/la3_lib_dist src/trading_crab_lib/dist src/trading_crab_lib/*.egg-info && mkdir -p /tmp/la3_lib_dist && ./.venv/bin/python -m build --outdir /tmp/la3_lib_dist src/trading_crab_lib 2>&1 | tail -5 && echo "--- twine check --strict on real freshly-built lib artifacts ---" && ./.venv/bin/python -m twine check --strict /tmp/la3_lib_dist/* && echo "LIB CHECK: PASSED" && echo "--- sdist contents ---" && tar tzf /tmp/la3_lib_dist/*.tar.gz | grep -i readme && echo "README present in sdist" && echo "--- wheel METADATA content-type ---" && rm -rf /tmp/la3_lib_wheel_extract && mkdir -p /tmp/la3_lib_wheel_extract && cd /tmp/la3_lib_wheel_extract && unzip -q /tmp/la3_lib_dist/*.whl && grep -q "^Description-Content-Type: text/markdown" *.dist-info/METADATA && echo "content-type OK" && grep -qv "^$" *.dist-info/METADATA && cd /home/user/claude-scratch-work && rm -rf /tmp/la3_lib_dist /tmp/la3_lib_wheel_extract src/trading_crab_lib/*.egg-info && echo "cleanup OK"</automated>
  </verify>
  <done>`src/trading_crab_lib/pyproject.toml` carries `readme = "README.md"` in both `[project]` and `[tool.poetry]`; `src/trading_crab_lib/README.md` exists, is library-scoped (not a copy of the root README), and every named identifier in it was independently verified against the real source; a freshly built wheel and sdist for the real package both pass `twine check --strict` cleanly (captured output shows `PASSED`, not the pre-fix `PASSED WITH WARNINGS` baseline captured earlier this session); the wheel's METADATA shows `Description-Content-Type: text/markdown` with a non-empty description body; the sdist tarball lists `README.md`; `MANIFEST.in` is edited only if the real build empirically required it, and that finding is recorded either way; all build artifacts are removed afterward.</done>
</task>

<task type="auto">
  <name>Task 2: Add a twine check --strict gate and prove it passes on real artifacts for both packages</name>
  <files>.github/workflows/publish-pypi.yml</files>
  <reversibility rating="reversible">One additive workflow step; revert is a `git checkout` of a single file.</reversibility>
  <action>
Add a new, separately-named, gated step to `.github/workflows/publish-pypi.yml` that runs
`twine check --strict` against the real built artifacts, positioned after `Verify built
artifacts` and before `Publish ... to PyPI`.

**Placement/naming decision -- a separate step, not folded into `Verify built artifacts`.**
`Verify built artifacts` already has one job: dist non-empty plus tag-vs-version agreement.
Metadata rendering (missing long_description, bad classifiers, malformed URLs) is a different
failure category with a different fix (edit the package's README/pyproject, not its tag or
version). Folding both into one step would mean a single failing step name covers two unrelated
root causes -- exactly the anti-pattern the immediately preceding quick task (260911-kkj) reasoned
against when it gave the token-presence check its own step rather than folding it into
`Publish ... to PyPI`. Name the new step so the name itself is the diagnosis, e.g.
`Check package metadata with twine`.

**The step.** `if: steps.gate.outputs.publish == 'true'`, character-for-character identical to
the other publish-path steps' condition. Its `env:` block hoists exactly one value,
`BUILD_DIR: ${{ matrix.build_dir }}` (matching the existing hoisting convention on this file; the
`run:` body must contain zero `${{ }}` expressions). The `run:` body: enable `set -euo pipefail`,
then run `twine check --strict` against every file under `"$BUILD_DIR"/dist/*`.

**Why `--strict`, and why it is safe now.** `--strict` promotes twine's warnings (missing
long_description, missing content-type, etc.) to hard failures. Before Task 1, applying
`--strict` today would break every future `trading-crab-lib` release on the exact warnings
already known and accepted this session. Task 1 fixes that specific defect first, so by the time
this step lands, both packages pass `--strict` clean -- making `--strict` a real regression gate
(catching, for example, someone later deleting the `readme` key) rather than a gate that starts
the workflow in a permanently-broken state. This is why Task 1 precedes this task in this plan's
ordering.

**Header comment.** Add one short paragraph to the file's top comment block, after the existing
tag-conventions block and before the "Requires ... repository secrets" line: state that every
artifact is now validated with `twine check --strict` before upload, and that a package missing
README metadata fails the job here instead of silently rendering a blank PyPI project page after
a real publish.

**Nothing else changes.** Do not touch `Verify built artifacts`, the gate step, the dist path,
`--skip-existing`, the matrix, tag conventions, or `python-package.yml`.

Then prove the shipped bash actually runs against real artifacts for BOTH packages. Write a
Python extractor into the scratchpad as `extract_twine_check_step.py` (a new name -- do not reuse
or overwrite any 260911-kkj script already present there). It must load the workflow with
`yaml.safe_load`, index `w["jobs"]["publish"]["steps"]`, locate the new step by its exact static
name, and assert before writing anything: zero template expressions in the `run:` body; `if:`
string identical to `Verify built artifacts`'s; position strictly after `Verify built artifacts`
and strictly before `Publish ... to PyPI` in the step list. Only then write the `run:` body
verbatim to `twine_check_step.sh`.

Build BOTH real packages fresh (`python -m build` for `.` and for `src/trading_crab_lib`) into
scratch output directories laid out the same way the real workflow lays them out (an
`app_build/dist/` and a `lib_build/dist/` pair, so `BUILD_DIR` plus `/dist/*` matches exactly what
`matrix.build_dir` plus `/dist/*` resolves to in CI), then drive `twine_check_step.sh` once per
package with `BUILD_DIR` pointed at each real build directory, capturing output. Both runs must
exit 0 and both captured outputs must contain the literal word `PASSED` for every artifact --
proving Task 1's fix is what makes the library pass this new gate, not merely that a `readme` key
exists in a file somewhere. Clean up all dist/egg-info artifacts afterward.
  </action>
  <verify>
    <automated>cd /home/user/claude-scratch-work && SP=/tmp/claude-0/-home-user-claude-scratch-work/2b704a4a-9590-5ab7-b2d2-213af4621a1f/scratchpad && ./.venv/bin/python -c "import yaml; yaml.safe_load(open('.github/workflows/publish-pypi.yml')); print('YAML OK')" && ./.venv/bin/python "$SP/extract_twine_check_step.py" "$SP" && test -s "$SP/twine_check_step.sh" && echo "extraction OK"</automated>
    <automated>cd /home/user/claude-scratch-work && SP=/tmp/claude-0/-home-user-claude-scratch-work/2b704a4a-9590-5ab7-b2d2-213af4621a1f/scratchpad && rm -rf /tmp/la3_probe && mkdir -p /tmp/la3_probe/app_build/dist /tmp/la3_probe/lib_build/dist && ./.venv/bin/python -m build --outdir /tmp/la3_probe/app_build/dist . 2>&1 | tail -3 && ./.venv/bin/python -m build --outdir /tmp/la3_probe/lib_build/dist src/trading_crab_lib 2>&1 | tail -3 && BUILD_DIR=/tmp/la3_probe/app_build bash "$SP/twine_check_step.sh" && BUILD_DIR=/tmp/la3_probe/lib_build bash "$SP/twine_check_step.sh" && rm -rf /tmp/la3_probe dist src/trading_crab_lib/dist *.egg-info src/trading_crab_lib/*.egg-info 2>/dev/null; echo "twine-check-step drive OK on both packages, cleanup done"</automated>
  </verify>
  <done>A new, statically-named `Check package metadata with twine` step exists in the committed
  YAML, positioned strictly between `Verify built artifacts` and `Publish ... to PyPI`, gated
  identically to the surrounding steps, with a `run:` body containing zero template expressions;
  its extracted body, driven against freshly built real artifacts for BOTH `trading-crab` and
  `trading-crab-lib`, exits 0 and prints `PASSED` for each -- proving Task 1's README fix is what
  makes the library pass this new gate, not merely that a `readme` key is present somewhere.</done>
</task>

<task type="auto">
  <name>Task 3: Add a TestPyPI dry-run path reachable only via workflow_dispatch</name>
  <files>.github/workflows/publish-pypi.yml</files>
  <reversibility rating="reversible">Additive workflow_dispatch input plus target-aware env expressions on three existing steps; revert is a `git checkout` of this one file.</reversibility>
  <action>
Add a `target` input to the existing `workflow_dispatch` trigger, thread a new `target` output
through the gate step, and make the token-presence guard and publish step read whichever secret
and endpoint that target implies -- without touching the publish step's `run:` body at all.

**New `workflow_dispatch` input.** Alongside the existing `package` input, add `target`:
description explaining `testpypi` is a safe rehearsal upload and `pypi` is a real publish;
`required: true`; `default: "testpypi"` -- the safe default, so an operator who dispatches the
workflow without touching this field gets a rehearsal, never a real publish; `type: choice`;
`options: ["testpypi", "pypi"]`.

**Gate step (`Decide whether to publish this package`) -- compute and emit `target`.** Add
`INPUT_TARGET: ${{ github.event.inputs.target }}` to its existing `env:` block, alongside the
values already hoisted there. At the very top of the `run:` body, before the existing `TAG=...`
line and its publish/skip branching (so this runs on every invocation regardless of which branch
decides to publish or skip), compute: if `EVENT_NAME` equals `workflow_dispatch`,
`TARGET="$INPUT_TARGET"`; otherwise (any tag push -- `v*`, `lib-v*`, or `both-v*`),
`TARGET="pypi"` unconditionally. This is not a default being silently overridden: a tag push
carries no `target` input at all, so hardcoding `pypi` on that branch is the only reachable
outcome, guaranteeing a TAG-triggered run always targets production. Emit `target=$TARGET` to
`$GITHUB_OUTPUT` and one log line naming the package and the resolved target (e.g.
`TARGET: $PKG will publish to '$TARGET' (event=$EVENT_NAME)`). This becomes a new gate output,
`steps.gate.outputs.target`, read by both steps below. Leave every existing line in this step's
publish/skip branching untouched.

**Token-presence guard (`Check PyPI token is present`) -- read the target-appropriate secret.**
Add `TARGET: ${{ steps.gate.outputs.target }}` to its `env:` block. Replace its `SECRET_NAME` and
`TWINE_PASSWORD` env values with target-aware expressions that select the secret NAME first and
index `secrets[...]` exactly once at the end -- never a value-level fallback. Specifically:
`SECRET_NAME` becomes `${{ steps.gate.outputs.target == 'testpypi' && 'TEST_PYPI_API_TOKEN' ||
matrix.secret_name }}`, and `TWINE_PASSWORD` becomes `${{ secrets[ steps.gate.outputs.target ==
'testpypi' && 'TEST_PYPI_API_TOKEN' || matrix.secret_name ] }}`. Do not write this as
`secrets.TEST_PYPI_API_TOKEN || secrets[matrix.secret_name]` or any other value-level ternary:
GitHub Actions' `&&`/`||` return operand values, and an empty secret is falsy, so a value-level
ternary would silently fall through to the production secret whenever the intended (testpypi)
secret is the one that's actually missing -- defeating the guard's entire purpose for that
scenario. The key-name-then-single-lookup shape sidesteps this because both ternary branches (the
two literal/matrix secret NAMES) are always non-empty strings. Update the `run:` body's messages
to include the target -- e.g. prefix the existing `$PKG` references with `$PKG (target: $TARGET)`
-- while keeping every existing remedy phrase from the prior quick task (`repository secret`,
`environment secret`, `pypi`, `empty string`) verbatim; they still apply correctly to whichever
secret name was resolved.

**Publish step (`Publish ... to PyPI`) -- add the endpoint override, touch nothing else.** Its
`TWINE_PASSWORD` env value becomes the byte-identical expression to the guard's new one (same
key-selection ternary, same `secrets[...]` shape) -- this identity is itself a checkable invariant
proving the guard validates the exact credential the upload will use. Add one new env entry,
`TWINE_REPOSITORY_URL: ${{ steps.gate.outputs.target == 'testpypi' &&
'https://test.pypi.org/legacy/' || '' }}`. Do not add a `--repository-url` flag or any other
change to the `run:` body -- twine reads `TWINE_REPOSITORY_URL` directly from the environment
(confirmed by this session's live `Settings` probe, recorded in this plan's context section), and
an empty value on the `pypi` branch resolves identically in practice to an unset one because
twine's own falsy-check on `repository_url` falls through to the `pypi`-named default. This is
why the `run:` body needs zero edits: leave `DIST_DIR="${{ matrix.build_dir }}/dist"` and
`twine upload --skip-existing "$DIST_DIR"/*` exactly as they are today.

**Header comment.** Update the "Requires ... repository secrets" block to "Requires three
repository secrets", adding `TEST_PYPI_API_TOKEN -- TestPyPI token shared by both packages for
workflow_dispatch dry runs (not used for tag-triggered publishes)`. Add one short paragraph above
it explaining the `target` input, that its default is the safe (`testpypi`) choice, and that a
tag-triggered run always targets production regardless of this input because that event has no
such input to read -- pointing to `docs/RELEASING.md` (created in Task 4) for the full
walkthrough.

**Do not touch** `Verify built artifacts` (its workflow_dispatch early-exit on the version check
is unrelated and must remain exactly as is), the twine-check step from Task 2, the matrix, tag
conventions, the dist path resolution mechanism, or `--skip-existing`.

Then prove the target-resolution mechanism with real, driven evidence -- not inspection. Write
`extract_release_gate_steps.py` into the scratchpad (a new name, distinct from every 260911-kkj
and Task-2 script). It must load the committed YAML with `yaml.safe_load`, index the three
touched steps by exact name, and assert: the gate step's `run:` body still contains zero template
expressions after the edit; the guard's and publish step's `TWINE_PASSWORD` env expression
strings are byte-identical to each other; the publish step's `TWINE_REPOSITORY_URL` expression
string contains both `steps.gate.outputs.target == 'testpypi'` and the literal
`https://test.pypi.org/legacy/`; the workflow_dispatch `target` input exists with
`default: testpypi` and both `testpypi` and `pypi` in its `options`. Only after those assertions
pass, write the gate step's `run:` body verbatim to `release_gate.sh` and the guard step's `run:`
body verbatim to `release_guard.sh`.

Drive `release_gate.sh` through both event shapes and capture the `GITHUB_OUTPUT` file contents
each time: (1) `EVENT_NAME=push`, a `GITHUB_REF` under `refs/tags/v0.1.4` (or another real tag
shape), `INPUT_PKG` and `INPUT_TARGET` both unset -- assert the emitted `target` value is exactly
`pypi`; (2) `EVENT_NAME=workflow_dispatch`, `INPUT_PKG=both`, `INPUT_TARGET=testpypi` -- assert the
emitted `target` value is exactly `testpypi`. This is the observed proof that a tag push always
resolves to production even with no dispatch input present at all, and that a dispatch run honors
an explicit testpypi selection.

Re-run the existing token-guard regression scenarios (token unset, token empty) against
`release_guard.sh`, once for `TARGET=pypi` (using each matrix leg's production secret name) and
once for `TARGET=testpypi` (using `TEST_PYPI_API_TOKEN`) -- all four combinations must exit
non-zero and name the correct secret for that combination.

Finally, reproduce this session's live `twine.settings.Settings` probe as its own script,
`probe_twine_repository_url.py`, in the scratchpad: construct `Settings` once with
`TWINE_REPOSITORY_URL` unset from the environment (simulating the `pypi` branch) and once with it
set to `https://test.pypi.org/legacy/`, and assert the resolved
`settings.repository_config["repository"]` is `https://upload.pypi.org/legacy/` in the first case
and the TestPyPI URL in the second. State explicitly in the summary that this probe -- not a live
GitHub Actions run, which cannot be executed in this sandbox -- is the evidence for the
registry-resolution mechanism, and that the GH Actions expression engine's own evaluation of the
ternaries is verified structurally (string assertions on the parsed YAML) rather than by live
execution; mark that boundary as the honest limit of what could be proven here.
  </action>
  <verify>
    <automated>cd /home/user/claude-scratch-work && SP=/tmp/claude-0/-home-user-claude-scratch-work/2b704a4a-9590-5ab7-b2d2-213af4621a1f/scratchpad && ./.venv/bin/python -c "import yaml; d=yaml.safe_load(open('.github/workflows/publish-pypi.yml')); t=d['on']['workflow_dispatch']['inputs']['target']; assert t['default']=='testpypi', t; assert set(t['options'])=={'testpypi','pypi'}, t; print('workflow_dispatch target input OK')" && ./.venv/bin/python "$SP/extract_release_gate_steps.py" "$SP" && test -s "$SP/release_gate.sh" && test -s "$SP/release_guard.sh" && echo "extraction+structural OK"</automated>
    <automated>cd /home/user/claude-scratch-work && SP=/tmp/claude-0/-home-user-claude-scratch-work/2b704a4a-9590-5ab7-b2d2-213af4621a1f/scratchpad && rm -f "$SP/gh_output_tagpush.txt" "$SP/gh_output_dispatch.txt" && GITHUB_OUTPUT="$SP/gh_output_tagpush.txt" GITHUB_REF="refs/tags/v0.1.4" EVENT_NAME=push PKG=trading-crab TAG_PREFIX=v INPUT_PKG= INPUT_TARGET= bash "$SP/release_gate.sh" > /tmp/la3_gate_tagpush.log 2>&1; grep -q '^target=pypi$' "$SP/gh_output_tagpush.txt" && echo "tag-push resolves to pypi OK" && GITHUB_OUTPUT="$SP/gh_output_dispatch.txt" GITHUB_REF="refs/heads/claude/exciting-keller-2xy11u" EVENT_NAME=workflow_dispatch PKG=trading-crab-lib TAG_PREFIX=lib-v INPUT_PKG=both INPUT_TARGET=testpypi bash "$SP/release_gate.sh" > /tmp/la3_gate_dispatch.log 2>&1; grep -q '^target=testpypi$' "$SP/gh_output_dispatch.txt" && echo "workflow_dispatch testpypi resolves to testpypi OK"</automated>
    <automated>cd /home/user/claude-scratch-work && SP=/tmp/claude-0/-home-user-claude-scratch-work/2b704a4a-9590-5ab7-b2d2-213af4621a1f/scratchpad && (TARGET=pypi PKG=trading-crab SECRET_NAME=PYPI_APP_TOKEN bash "$SP/release_guard.sh"; echo "exit:$?") 2>&1 | tee /tmp/la3_guard_pypi_unset.log | grep -q "PYPI_APP_TOKEN" && (TARGET=testpypi PKG=trading-crab SECRET_NAME=TEST_PYPI_API_TOKEN bash "$SP/release_guard.sh"; echo "exit:$?") 2>&1 | tee /tmp/la3_guard_testpypi_unset.log | grep -q "TEST_PYPI_API_TOKEN" && (TARGET=pypi PKG=trading-crab SECRET_NAME=PYPI_APP_TOKEN TWINE_PASSWORD="" bash "$SP/release_guard.sh"; echo "exit:$?") 2>&1 | grep -q "empty string" && echo "target-aware guard regression OK (unset + empty, both targets)"</automated>
    <automated>cd /home/user/claude-scratch-work && SP=/tmp/claude-0/-home-user-claude-scratch-work/2b704a4a-9590-5ab7-b2d2-213af4621a1f/scratchpad && ./.venv/bin/python "$SP/probe_twine_repository_url.py" | tee /tmp/la3_twine_probe.log && grep -q "https://upload.pypi.org/legacy/" /tmp/la3_twine_probe.log && grep -q "https://test.pypi.org/legacy/" /tmp/la3_twine_probe.log && echo "live twine.settings.Settings probe OK"</automated>
  </verify>
  <done>`workflow_dispatch` gains a `target` input defaulting to `testpypi` with options `testpypi`
  and `pypi`; the gate step's extracted bash, driven directly, proves a tag push always resolves
  `target=pypi` (no dispatch input present at all) and a workflow_dispatch run with
  `target=testpypi` resolves accordingly; the token guard and publish step read the token through
  byte-identical target-aware expressions that select the secret KEY (never the VALUE) via
  ternary, avoiding the empty-value fallthrough trap, and the guard's extracted body still fails
  correctly (naming the right secret) for both targets and both unset/empty scenarios; the
  publish step's `run:` body is untouched; a standalone, live-executed
  `twine.settings.Settings` probe confirms `TWINE_REPOSITORY_URL=""` resolves to production and
  the TestPyPI literal resolves to TestPyPI; the header comment documents all three secrets and
  the tag-always-production rule; the boundary of what could not be executed (the GH Actions
  expression engine itself) is stated explicitly rather than glossed over.</done>
</task>

<task type="auto">
  <name>Task 4: Write docs/RELEASING.md -- the release checklist and every trap discovered</name>
  <files>docs/RELEASING.md</files>
  <reversibility rating="reversible">New documentation file; revert is `git rm` or `git checkout` of this one file.</reversibility>
  <action>
Create `docs/RELEASING.md` (the `docs/` directory already exists at the repo root, holding
topic-specific reference docs -- this is a natural, minimally-disruptive home for a release
procedure that is neither the broad multi-format overview in `DISTRIBUTION.md` nor something that
belongs restructured into the root `README.md`, which this plan leaves untouched per its
constraints). This file is the single source of truth for releasing either package; write every
section against the ACTUAL state of `.github/workflows/publish-pypi.yml` after Tasks 1-3 land,
not against this plan's description of the intended design -- re-read the committed file before
writing each section that quotes a secret name, a step name, or an input default.

Cover, at minimum, as clearly separated sections:

1. **Tag conventions and the v* trap.** State the three tag prefixes (`v*` publishes
   `trading-crab` only, `lib-v*` publishes `trading-crab-lib` only, `both-v*` publishes both) and,
   explicitly, the trap already hit once on this project: a `v*` tag SKIPS the lib leg of the
   matrix silently, and a skipped matrix leg reports GREEN in the Actions UI -- indistinguishable
   at a glance from an intentional, successful publish of that package. Tell the reader exactly
   how to check which legs actually ran (read the per-leg job log for the `PUBLISH:`/`SKIP:` line
   the gate step prints).
2. **Version bumping discipline.** Both `pyproject.toml` files (root `trading-crab`, and
   `src/trading_crab_lib/pyproject.toml` for `trading-crab-lib`) must be bumped BEFORE tagging.
   State that the `Verify built artifacts` step now enforces tag-vs-artifact version agreement and
   fails the job if they disagree.
3. **Burned versions are permanent.** A version once uploaded to PyPI can never be re-uploaded,
   even after deletion -- cite 0.1.3 as the real, already-burned example on this project.
4. **Validating a token without publishing.** Describe the actual technique: upload an
   ALREADY-PUBLISHED artifact WITHOUT `--skip-existing`; a `400 File already exists` response
   means the token authenticated successfully (good token, existing file); a `403 Forbidden` means
   the token itself is bad. State the trap this plan's context already identified: `--skip-existing`
   calls an UNAUTHENTICATED pre-check first and `continue`s before ever authenticating, so leaving
   `--skip-existing` on defeats this validation technique entirely -- it must be OFF for this
   specific manual check, never as a permanent change to the workflow.
5. **Local `~/.pypirc` layout.** Describe a four-section layout, one section per
   (package x registry) combination -- e.g. `trading-crab-app`, `trading-crab-lib`,
   `trading-crab-app-test`, `trading-crab-lib-test` -- each with `repository`,
   `username = __token__`, and `password` keys, using an obvious placeholder for every password
   value, never a real one. State explicitly why all four need an explicit `repository = ` line:
   twine's own config loader only auto-fills the endpoint URL for the two literal section names
   `pypi` and `testpypi`; any custom section name (which this four-way split requires, since there
   is no way to have two different `pypi` sections) must specify its `repository = ` URL
   explicitly, or the upload fails with a missing-config error.
6. **The TestPyPI rehearsal path.** Describe how to trigger it: run the workflow via
   `workflow_dispatch`, select the target package(s), and set the `target` input to `testpypi`
   (its default) rather than `pypi`; state that a tag push can never reach this path regardless of
   any input, by design. Cross-reference the required `TEST_PYPI_API_TOKEN` secret from Task 3's
   header-comment update.
7. **Where the secrets live, and the empty-string trap.** Name all three secrets exactly as they
   appear in the committed workflow's header comment after Task 3 (`PYPI_APP_TOKEN`,
   `PYPI_LIB_TOKEN`, `TEST_PYPI_API_TOKEN`) and the two valid locations for each (a repository
   secret, or an environment secret on the `pypi` environment). State plainly that GitHub's
   dynamic `secrets[...]` lookup resolves an unknown or mistyped secret name to an empty string
   rather than erroring -- so a typo in a secret's name is indistinguishable from the secret never
   having been created, and the workflow's own token-presence guard exists specifically to turn
   that silent case into a named, fast failure.
8. **The `twine check --strict` gate.** One short paragraph noting that every release now
   validates package metadata before upload, and that this is what would have caught the
   blank-PyPI-page defect this plan's Task 1 fixed, had it existed sooner.

State plainly, in a short note near the top or bottom of the file, that this document's
tag-convention and secret-lookup sections may be superseded by future workflow edits -- readers
should treat the committed `.github/workflows/publish-pypi.yml` as the ultimate source of truth if
the two ever disagree.

Never write a real token, real secret value, or anything that looks like one into this file --
placeholders only, and make every placeholder obviously fake (e.g. `<PYPI_APP_TOKEN value>`, not a
plausible-looking random string).
  </action>
  <verify>
    <automated>F=/home/user/claude-scratch-work/docs/RELEASING.md && test -f "$F" && grep -q "lib-v" "$F" && grep -q "both-v" "$F" && grep -qi "SKIP" "$F" && grep -qi "GREEN" "$F" && grep -q "400" "$F" && grep -q "403" "$F" && grep -q -- "--skip-existing" "$F" && grep -qi "empty string" "$F" && grep -q "PYPI_APP_TOKEN" "$F" && grep -q "PYPI_LIB_TOKEN" "$F" && grep -q "TEST_PYPI_API_TOKEN" "$F" && grep -q "twine check --strict" "$F" && grep -qi "pypirc" "$F" && echo "content coverage OK"</automated>
    <automated>F=/home/user/claude-scratch-work/docs/RELEASING.md && ! grep -Eq "(pypi-AgEIcHlwaS|sk-[A-Za-z0-9]{20,}|[A-Za-z0-9_-]{40,})" "$F" && echo "no real-looking secret pattern found OK"</automated>
  </verify>
  <done>`docs/RELEASING.md` exists and accurately covers, against the real post-Task-3 workflow:
  the tag-convention trap (v* silently skips lib, skipped legs report GREEN), version-bump-before-
  tag discipline, burned-version permanence, the 400-vs-403 token-validation technique and the
  --skip-existing trap that defeats it, a four-section ~/.pypirc layout with the explicit-
  repository-line rationale, the TestPyPI rehearsal trigger, all three real secret names and their
  two valid locations plus the empty-string-on-mismatch behavior, and the new twine check --strict
  gate -- with no real secret value anywhere in the file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub secrets store -> workflow step environment | A third long-lived token (TEST_PYPI_API_TOKEN) now crosses into shell environments that also write to a publicly readable job log, alongside the two production tokens. |
| Package source (README.md, transforms.py, etc.) -> public PyPI project page | trading-crab-lib's long_description is now rendered, unsanitized by PyPI itself, on a public page -- content control is entirely this repo's responsibility. |
| workflow_dispatch input -> upload destination | A human-selected `target` value now determines which of two independent package registries (production PyPI vs TestPyPI) receives the upload. |
| runner -> PyPI / TestPyPI | The runner holds tokens that can publish irreversibly (PyPI) or semi-irreversibly (TestPyPI also disallows reuse of a version number) under the project's name. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-la3-01 | Information Disclosure | TEST_PYPI_API_TOKEN in guard + publish step environments | high | mitigate | Reuses the established no-echo, emptiness-test-only handling from 260911-kkj unchanged; the new secret is injected only into the same two steps the production secrets already reach, gated by the identical publish condition. |
| T-la3-02 | Spoofing / wrong-registry publish | gate step's `target` computation | critical | mitigate | A tag-triggered run has no `target` input to read at all and the gate step hardcodes `TARGET="pypi"` on every non-workflow_dispatch path -- proven in Task 3 by driving the extracted gate bash with `EVENT_NAME=push` and no dispatch input and observing `target=pypi` in the captured GITHUB_OUTPUT, not by inspection. |
| T-la3-03 | Spoofing / credential-target mismatch | token-presence guard and publish step secret-selection ternary | high | mitigate | The ternary selects the secret NAME (always a non-empty literal) and indexes `secrets[...]` exactly once, avoiding the value-level `A \|\| B` falsy-fallthrough trap identified in this plan's context; the guard's and publish step's TWINE_PASSWORD expressions are asserted string-identical so a passing guard is proof about the exact credential the upload will use. |
| T-la3-04 | Tampering | trading-crab-lib's newly-rendered long_description on the public PyPI page | medium | mitigate | Every claim in the new README is verified against real source in Task 1 before being written; no external or user-controlled input flows into this file. |
| T-la3-05 | Denial of Service (self-inflicted) | `twine check --strict` gate | low | accept | Intended fail-closed behavior: a metadata warning should block a release until fixed, not be silently ignored. Task 1 lands first specifically so this gate does not start the workflow in an already-broken state. |
| T-la3-06 | Information Disclosure | docs/RELEASING.md and scratchpad token-validation walkthrough | medium | mitigate | Every credential-shaped value in the checklist and in every scratchpad script this plan's tasks write is an obviously fake placeholder; no task performs a real upload or a real token-validation probe against the live registries. |
| T-la3-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no new packages -- `build`, `twine`, and `PyYAML` are already present and importable in the committed `./.venv`, confirmed this session. No `## Package Legitimacy Audit` is required. |
</threat_model>

<verification>
- `.github/workflows/publish-pypi.yml` parses under `yaml.safe_load` after every task; steps
  indexed via `w["jobs"]["publish"]["steps"]` (the `on:` key parses as boolean `True`).
- Every new or edited `run:` body is extracted verbatim from the committed YAML and executed
  standalone -- never retyped -- using script names distinct from every artifact the immediately
  preceding quick task (260911-kkj) left in the scratchpad.
- Task 1's fix is proven by a real build: `twine check --strict` on freshly built
  `trading-crab-lib` artifacts goes from this session's captured WARNING baseline to a captured
  PASSED, with the wheel's METADATA and the sdist's tarball listing both inspected directly (not
  inferred from the presence of a `readme` key).
- Task 2's gate is proven by driving its extracted body against real freshly-built artifacts for
  BOTH packages, not just the one Task 1 fixed.
- Task 3's registry-resolution is proven two ways: (a) the gate step's extracted bash, driven
  with both a tag-push and a workflow_dispatch event shape, produces the correct `target` output
  each time; (b) a standalone, live-executed `twine.settings.Settings` probe confirms
  `TWINE_REPOSITORY_URL=""` resolves to production and the TestPyPI literal resolves to TestPyPI.
  The GH Actions expression engine itself cannot be executed in this sandbox -- that boundary is
  stated explicitly, not glossed over, and the ternary/secret-name-identity claims are instead
  proven by string assertions on the parsed YAML.
- Regression scope asserted after Task 3: tag conventions, the matrix, the gate's pre-existing
  publish/skip branching, the dist path line, `Verify built artifacts`, `--skip-existing`, both
  0.1.4 versions, and `python-package.yml`'s 3.10-3.13 matrix are all unchanged from this plan's
  starting point.
- `docs/RELEASING.md` is checked for the presence of every required topic and the absence of any
  real-secret-shaped string.
- Any check that cannot run for an environment reason is reported as unproven, never as passing.
- All build artifacts (`dist/`, `*.egg-info/`, scratch output directories) are removed before
  each task is considered done; nothing from this plan's builds is committed.
</verification>

<success_criteria>
- `trading-crab-lib` passes `twine check --strict` cleanly on a freshly built wheel and sdist, up
  from this session's captured WARNING baseline, with the README rendering correctly in both
  artifact types.
- `.github/workflows/publish-pypi.yml` runs `twine check --strict` on every leg's real artifacts
  before upload, gated identically to the existing publish-path steps.
- A workflow_dispatch run can rehearse a publish against TestPyPI via a `target` input defaulting
  to the safe (`testpypi`) choice; a tag-triggered run always targets production PyPI with no way
  to redirect it.
- The token-presence guard and the publish step read the target-appropriate secret through
  provably identical, falsy-fallthrough-safe expressions.
- `docs/RELEASING.md` exists and accurately documents the tag-convention trap, version-bump
  discipline, burned-version permanence, token validation without publishing, the local
  `.pypirc` layout, the TestPyPI rehearsal path, and where all three secrets live.
- Tag conventions, the matrix, the gate's pre-existing logic, the dist path, `Verify built
  artifacts`, `--skip-existing`, both package versions, and `python-package.yml`'s Python matrix
  are all unchanged.
- No real PyPI, TestPyPI, or other secret value appears in any committed file or scratchpad
  artifact.
- Committed on branch `claude/exciting-keller-2xy11u`. No new branch, no tag, no push, no PR.
</success_criteria>

<output>
Create `.planning/quick/260911-la3-harden-the-release-procedure-twine-check/260911-la3-SUMMARY.md`
when done.

The summary must include: the actual captured before/after `twine check --strict` output for
`trading-crab-lib` (Task 1); the actual captured `target` output values from driving the gate
step's extracted bash under both event shapes (Task 3); the actual captured output of the live
`twine.settings.Settings` probe for both the empty and TestPyPI-set `TWINE_REPOSITORY_URL` cases
(Task 3); and an explicit statement of which parts of Task 3's design (the GH Actions expression
engine's own ternary evaluation) could not be executed in this sandbox and were therefore
verified structurally rather than by live run.

Reminder for the executor: do NOT create a pull request, do NOT create or push a git tag, do NOT
push, do NOT change any package version. Stay on `claude/exciting-keller-2xy11u`. Never write a
real token or secret value into any file, committed or scratchpad.
</output>