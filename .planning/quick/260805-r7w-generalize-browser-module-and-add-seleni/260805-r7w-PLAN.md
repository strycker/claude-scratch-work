---
phase: quick-260805-r7w
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/trading_crab_lib/ingestion/browser.py
  - tests/unit/test_ingestion_browser.py
  - src/trading_crab_lib/ingestion/macrotrends.py
  - src/trading_crab_lib/platform/ingestion/macro_monthly.py
  - tests/unit/test_macrotrends.py
  - tests/unit/test_platform_macro_ingest.py
  - src/trading_crab_lib/pyproject.toml
  - .planning/STATE.md
autonomous: true
requirements: [QUICK-260805-r7w]

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "fetch_page_html returns the rendered HTML of an arbitrary URL, and returns None (not a partial page) when a REQUIRED wait_for_selector never appears"
    - "fetch_stooq_csvs behaves exactly as before — same warm-up, same download-then-in-page-fetch order, same consecutive-non-CSV early bail — and all 13 pre-existing browser tests pass with zero edits"
    - "A second engine (Selenium) renders pages when Playwright is unavailable, selectable by argument or TC_BROWSER_ENGINE, with Playwright preferred under 'auto'"
    - "fetch_urls_as_text is Playwright-only and degrades to {} with a clear WARNING when only Selenium is available — no faked Selenium download path exists"
    - "Both macrotrends call sites try plain HTTP FIRST and reach the browser only when the HTTP body yields neither the embedded JSON array nor a parseable table"
    - "Neither macrotrends module's resample rule changes — quarterly stays QE, monthly stays ME"
    - "No unit test launches a browser or touches the network; both engines are mocked entirely"
    - "python -m pytest tests/ -q passes"
  artifacts:
    - src/trading_crab_lib/ingestion/browser.py
    - tests/unit/test_ingestion_browser.py
    - src/trading_crab_lib/ingestion/macrotrends.py
    - src/trading_crab_lib/platform/ingestion/macro_monthly.py
    - tests/unit/test_macrotrends.py
    - tests/unit/test_platform_macro_ingest.py
    - src/trading_crab_lib/pyproject.toml
  key_links:
    - "fetch_stooq_csvs -> fetch_urls_as_text (thin wrapper; prices_daily._batch_stooq_daily import unchanged)"
    - "fetch_page_html -> _resolve_engine -> _fetch_page_html_playwright | _fetch_page_html_selenium"
    - "macrotrends._scrape_series -> _html_yields_data -> fetch_page_html (fallback only)"
    - "macro_monthly._scrape_macrotrends_monthly -> macrotrends._html_yields_data -> fetch_page_html (fallback only)"
    - "src/trading_crab_lib/pyproject.toml [browser] extra -> playwright + selenium, reachable via [all]"
---

<objective>
Generalize `ingestion/browser.py` from a Stooq-shaped CSV fetcher into a source-agnostic
browser-rendering capability, add Selenium as a second engine, and route macrotrends
through it as a fallback behind its existing HTTP attempt.

Purpose: the browser capability added by quick task 260805-jt2 works and has 13 tests, but
its only public entry point is `fetch_stooq_csvs(urls)` — the shape is Stooq's, so
macrotrends cannot reuse it. macrotrends currently returns Cloudflare 403 to every
non-browser client tried (plain requests, curl_cffi impersonating chrome AND safari,
WebFetch), and its chart/table toggle does not change the URL, so there is no table-view
URL to target — the series is already in the page and JavaScript re-renders it. Rendering
the page is the only remaining lever.

Output: `fetch_page_html` / `fetch_urls_as_text` as the general API with `fetch_stooq_csvs`
kept as a backwards-compatible wrapper, a Selenium engine with explicit engine selection,
macrotrends routed through the fallback at both call sites, and `selenium` added to the
`[browser]` packaging extra.
</objective>

<execution_context>
@/home/user/claude-scratch-work/.claude/gsd-core/workflows/execute-plan.md
@/home/user/claude-scratch-work/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.claude/CLAUDE.md

@src/trading_crab_lib/ingestion/browser.py
@tests/unit/test_ingestion_browser.py
@src/trading_crab_lib/ingestion/macrotrends.py
@src/trading_crab_lib/platform/ingestion/macro_monthly.py
@src/trading_crab_lib/ingestion/http.py
@src/trading_crab_lib/hmm.py
@src/trading_crab_lib/pyproject.toml
</context>

<constraints>
Repo conventions are mandatory and non-negotiable for every file touched:

- `from __future__ import annotations` at the top of every source file
- Module docstring stating purpose
- Type hints on all public functions
- `log = logging.getLogger(__name__)`; logging only, never `print()`, in library code
- No bare `except:`; broad network/driver catches carry `# noqa: BLE001`
- Line length 127
- `pathlib.Path` for any filesystem path

ESTABLISHED FACTS — do not re-investigate, do not add a diagnostic task:

- macrotrends returns Cloudflare 403 to plain requests, to curl_cffi impersonating chrome
  AND safari, and to WebFetch. Its chart/table toggle does NOT change the URL — there is no
  table-view URL to fetch.
- Stooq's CSV endpoint refuses Playwright's `context.request.get` with "Access denied" but
  serves a real `page.goto` navigation as a file download. That download flow is preserved
  EXACTLY; do not "simplify" it back to the request API.

<!-- planner-discipline-allow: networkidle -->
FORBIDDEN WAIT PREDICATE: `wait_until="networkidle"` waits for 500 ms of zero network
traffic, which pages carrying analytics, ads, or polling never reach — so it times out on
pages that loaded perfectly. This already cost one wasted diagnostic run. Use
`wait_until="domcontentloaded"` plus a settle or a selector wait. Do NOT introduce
`networkidle` anywhere in this codebase; a grep gate enforces it.

TEST HONESTY (project rule, enforced): every new or edited test must FAIL against
pre-change code and PASS after. The executor verifies this with a `git stash` round-trip on
the test files and records the RED evidence in the summary. No tautological assertions —
nothing that asserts a mock returns what the test just configured it to return without
exercising the code under test. BOTH engines are mocked ENTIRELY: no browser launch, no
network, in any unit test. Note that `playwright` IS importable in this environment, so an
unpatched fallback path in a test would launch a real browser — every test that can reach
`fetch_page_html` must patch it at the consuming module's namespace.

BACKWARDS COMPATIBILITY (hard): the 13 existing tests in
`tests/unit/test_ingestion_browser.py` must pass with ZERO edits. New tests are APPENDED to
that file. This pins the following, which must not change:

- Module-level names `_PLAYWRIGHT_AVAILABLE`, `sync_playwright`, `PlaywrightError`, `time`,
  `_ABORT_AFTER_CONSECUTIVE_NON_CSV` all keep their exact spelling and module path.
- `page.goto` on the `fetch_urls_as_text` path is called as `page.goto(url, timeout=...)`
  and with NO other keyword. The test fakes define `goto(self, url, timeout=None)`; adding
  `wait_until=` there raises TypeError, which the broad handler swallows into an empty
  result and silently reddens the suite.
- The `fetch_urls_as_text` path never calls `page.content()` or `page.wait_for_selector()`
  — the fakes do not define them.
- The unavailable-engine WARNING emitted by `fetch_stooq_csvs` / `fetch_urls_as_text` still
  contains the substring `pip install`.
- `browser.py` still imports no pandas.

NO EVASION CLAIMS: no comment, docstring, log line, commit message, or summary may state or
imply that Selenium is a way past a block Playwright cannot pass, or that macrotrends-via-
browser is confirmed working. Neither claim is true and neither is testable from here.
</constraints>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Generalize browser.py — fetch_page_html + fetch_urls_as_text + viewport, fetch_stooq_csvs as wrapper</name>
  <files>src/trading_crab_lib/ingestion/browser.py, tests/unit/test_ingestion_browser.py</files>

  <read_first>
    - src/trading_crab_lib/ingestion/browser.py in full — every constant, the download-then-
      in-page-fetch order, and the early-bail counter are load-bearing and are being MOVED,
      not rewritten.
    - tests/unit/test_ingestion_browser.py in full — the fakes define the exact call shapes
      the generalized code must keep emitting.
  </read_first>

  <behavior>
    - `fetch_page_html(url)` with a working fake page returns that page's `content()`;
      exactly one Chromium is launched and `browser.close()` is called.
    - `fetch_page_html(url, wait_for_selector="table.x")` calls `page.wait_for_selector`
      with that selector before reading content.
    - `fetch_page_html(url, wait_for_selector="table.x")` where the selector never appears
      (wait raises) returns **None** — a timeout is information; a fixed sleep would
      silently proceed with an empty page.
    - `fetch_page_html(url, wait_for_selector="table.x", require_selector=False)` where the
      selector never appears returns the rendered HTML anyway, with a WARNING.
    - `fetch_page_html` returns None and logs a WARNING (never raises) when no engine is
      available, and when `chromium.launch` raises.
    - `browser.new_context` receives an explicit 1280x800 viewport on BOTH the
      `fetch_page_html` path and the `fetch_urls_as_text` path.
    - `fetch_urls_as_text(urls)` with no `expected_prefix` attempts EVERY url even when all
      of them return unrecognised bodies — the consecutive-mismatch early bail is a
      Stooq-specific opt-in, not baked into the generic path.
    - `fetch_stooq_csvs` still aborts after `_ABORT_AFTER_CONSECUTIVE_NON_CSV` non-CSV
      bodies (existing test) — i.e. the wrapper opts in to the prefix guard.
    - All 13 pre-existing tests pass unedited.
  </behavior>

  <action>
Rewrite `src/trading_crab_lib/ingestion/browser.py` as a source-agnostic module. Keep every
existing constant and its exact name. Add:

- `_VIEWPORT = {"width": 1280, "height": 800}` — an explicit viewport, because a default or
  absent viewport is itself a distinguishing signal and some layouts render differently
  without one.
- `_WAIT_UNTIL = "domcontentloaded"` — the navigation predicate for `fetch_page_html`. Add
  a comment recording WHY the idle-network predicate (the one that waits for 500 ms of zero
  network traffic) is banned here: pages with analytics, ads, or polling never reach it, so
  it times out on pages that loaded fine. See `<constraints>` for the banned literal; a
  grep gate enforces its absence.

Rewrite the module docstring. Keep the two recorded verdicts (Stooq's JS challenge defeats
TLS impersonation; Stooq's CSV endpoint answers a navigation-as-download but refuses the
request API — the "Download is starting" raise is the SUCCESS path). Reframe the opening:
this module renders pages in a real browser for ANY source whose content is gated behind
JavaScript execution, and it stays a SIBLING of `ingestion/http.py` — the cheap HTTP path
is always tried first by every caller. State that `fetch_stooq_csvs` is retained as a
named wrapper because its download-interception flow is endpoint-specific.

Public API — four functions:

1. `playwright_available() -> bool` — unchanged.

2. `fetch_page_html(url: str, *, wait_for_selector: str | None = None, require_selector:
   bool = True, settle_ms: int = _CHALLENGE_SETTLE_MS, timeout_ms: int = _NAV_TIMEOUT_MS,
   engine: str | None = None) -> str | None`

   Renders *url* and returns the page's HTML, or None.

   `require_selector` exists because a selector may be a GUESS. When True (default) a
   selector that never appears returns None — the caller asked for a specific element and
   not getting it is a real failure. When False, the miss is logged at WARNING and whatever
   rendered is returned, so an unconfirmed selector cannot throw away a page that may still
   carry the data in a script tag. Document both branches in the docstring.

   `engine` is accepted now and resolved through a `_resolve_engine(engine: str | None) ->
   str | None` helper. In THIS task `_resolve_engine` knows only Playwright: it returns
   `"playwright"` when `_PLAYWRIGHT_AVAILABLE` else logs a WARNING naming the install
   commands and returns None. Task 2 extends it; do not stub Selenium here.

   Playwright implementation `_fetch_page_html_playwright(...)`: `with sync_playwright() as
   pw`, launch with the shared `_launch_kwargs()` helper (headless + `_STEALTH_LAUNCH_ARGS`
   + `executable_path` only when `TC_CHROMIUM_PATH` is set and non-empty — factor the
   existing logic out of `fetch_urls_as_text` into that helper, keeping the key ABSENT when
   unset), `try:` / `finally: browser.close()`, `context = browser.new_context(viewport=
   dict(_VIEWPORT))`, `context.add_init_script(_STEALTH_INIT_SCRIPT)`, `page.goto(url,
   timeout=timeout_ms, wait_until=_WAIT_UNTIL)`. Then, if `wait_for_selector`, call
   `page.wait_for_selector(wait_for_selector, timeout=timeout_ms)` inside its own
   `try/except PlaywrightError` implementing the `require_selector` branch above
   (Playwright's TimeoutError subclasses `Error`, so one except clause covers it). Then
   `page.wait_for_timeout(settle_ms)` when `settle_ms` is truthy, and return
   `page.content() or None`. Wrap the whole thing in a broad `except Exception` carrying
   `# noqa: BLE001`, log a WARNING, return None. This function NEVER raises.

3. `fetch_urls_as_text(urls: dict[str, str], *, warmup_url: str | None = None,
   rate_limit_seconds: float = _BROWSER_RATE_LIMIT_SECONDS, expected_prefix: str | None =
   None, abort_after_consecutive_mismatch: int = _ABORT_AFTER_CONSECUTIVE_NON_CSV) ->
   dict[str, str]`

   This is the EXISTING `fetch_stooq_csvs` body, renamed off Stooq and parameterised.
   Preserve verbatim: the empty-`urls` early return before anything is launched, the
   availability guard whose WARNING contains `pip install`, one browser per run, the single
   warm-up navigation (now skipped when `warmup_url` is None), the settle after warm-up,
   per-URL pacing via `time.sleep` for `i > 0` only, the download-first then in-page-fetch
   order, the per-URL try/except that keeps one bad URL from aborting the batch,
   `browser.close()` in `finally`, and the outer broad `except Exception` with
   `# noqa: BLE001`. Add `viewport=dict(_VIEWPORT)` to the `new_context(...)` call
   alongside the existing `accept_downloads=True`.

   Generalise ONLY the early-bail: the counter increments when `expected_prefix` is set and
   the body does not `lstrip().lower().startswith(expected_prefix)`. When `expected_prefix`
   is None the counter never increments, so the generic path attempts every URL. Reword the
   two Stooq-named log messages to name the URL/key instead of "Stooq", and keep the
   `pip install` substring in the availability warning.

   `_fetch_one_via_download` and `_fetch_one_via_page_fetch` keep their names, bodies, and
   the swallowed-`PlaywrightError` semantics exactly.

4. `fetch_stooq_csvs(urls: dict[str, str], *, warmup_url: str = _STOOQ_WARMUP_URL,
   rate_limit_seconds: float = _BROWSER_RATE_LIMIT_SECONDS) -> dict[str, str]` — a thin
   wrapper that returns `fetch_urls_as_text(urls, warmup_url=warmup_url,
   rate_limit_seconds=rate_limit_seconds, expected_prefix="date,")`. Keep the existing
   docstring's substance (why navigate-and-take-the-download rather than the request API;
   why no cookie is harvested and replayed). `prices_daily.py` is NOT edited — its
   `from ...browser import fetch_stooq_csvs, playwright_available` keeps working.

Append new tests to `tests/unit/test_ingestion_browser.py` under a new section divider. Do
NOT edit the 13 existing tests. Add a `_FakeHtmlPage` fake exposing `goto(url, timeout=None,
wait_until=None)`, `wait_for_selector(sel, timeout=None)` (raising
`browser_mod.PlaywrightError` when configured to), `wait_for_timeout(ms)`, and
`content()`; reuse `_build_fake_playwright` for the launch chain. Cover every case in
`<behavior>`, asserting on recorded calls (selector actually passed, viewport kwarg
actually present, `content()` actually returned, `close()` actually called) rather than on
mock configuration. For the generic-no-early-bail case, feed 10 URLs whose bodies are all
unrecognised HTML and assert every one was attempted — contrast with the existing
`test_abandons_browser_path_after_consecutive_non_csv`, which proves the wrapper opts in.
  </action>

  <verify>
    <automated>python -m pytest tests/unit/test_ingestion_browser.py -q</automated>
    <automated>test "$(grep -cE 'wait_until[[:space:]]*=[[:space:]]*.networkidle' src/trading_crab_lib/ingestion/browser.py || true)" = "0"</automated>
    <automated>test "$(grep -cE '^(import|from) pandas' src/trading_crab_lib/ingestion/browser.py || true)" = "0"</automated>
    <automated>python -m pytest tests/unit/test_platform_prices_ingest.py -q</automated>
  </verify>

  <done>
`fetch_page_html` and `fetch_urls_as_text` exist and are source-agnostic; `fetch_stooq_csvs`
is a wrapper over `fetch_urls_as_text`; an explicit 1280x800 viewport is set on both paths;
a required selector that never appears yields None and a non-required one degrades to the
rendered HTML; the 13 pre-existing browser tests and the prices_daily tests pass unedited.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Selenium as a second ENGINE — availability flag, engine selection, hardened driver</name>
  <files>src/trading_crab_lib/ingestion/browser.py, tests/unit/test_ingestion_browser.py</files>

  <read_first>
    - src/trading_crab_lib/hmm.py lines 33-58 — the repo's optional-dependency pattern:
      module-level `_X_AVAILABLE` flag set inside `try/except ImportError`.
    - The existing playwright import block in browser.py — note it BINDS the names in the
      except branch (`sync_playwright = None`, `PlaywrightError = Exception`) so tests can
      patch them. Selenium must do the same; `selenium` is NOT installed in this
      environment, so unbound names would make every Selenium test un-patchable.
  </read_first>

  <behavior>
    - `selenium_available()` reflects `_SELENIUM_AVAILABLE`, both True and False.
    - Engine 'auto' with both engines available uses Playwright; the Selenium `webdriver`
      module object is never touched.
    - Engine 'auto' with Playwright unavailable and Selenium available uses Selenium and
      returns its `page_source`.
    - `TC_BROWSER_ENGINE=selenium` with both available uses Selenium; `sync_playwright` is
      never called.
    - `TC_BROWSER_ENGINE=playwright` with both available uses Playwright.
    - `TC_BROWSER_ENGINE=playwright` while Playwright is unavailable returns None with a
      WARNING and does NOT silently substitute Selenium — an explicit request is honored,
      not overridden.
    - An explicit `engine="selenium"` argument beats `TC_BROWSER_ENGINE=playwright`.
    - An unrecognised `TC_BROWSER_ENGINE` value logs a WARNING and behaves as 'auto'.
    - Neither engine available: `fetch_page_html` returns None with a WARNING naming both
      install paths.
    - Selenium options carry `--headless=new`, `--disable-blink-features=
      AutomationControlled`, an explicit window size, and an explicit user agent.
    - Selenium waits for a selector via `WebDriverWait(...).until(...)`, not a fixed sleep.
    - Selenium selector timeout with `require_selector=True` returns None; with
      `require_selector=False` returns `page_source`.
    - `driver.quit()` is called even when `driver.get` raises, and the raise never escapes.
    - `TC_CHROMIUM_PATH` sets `options.binary_location` on the Selenium path.
    - `fetch_urls_as_text` with only Selenium available returns `{}`, logs a WARNING stating
      the download flow is Playwright-only and containing `pip install`, and never touches
      `webdriver`.
  </behavior>

  <action>
Add the Selenium engine to `src/trading_crab_lib/ingestion/browser.py`.

Optional import block mirroring the playwright one, binding EVERY name in the except branch:

    _SELENIUM_AVAILABLE = False
    try:
        from selenium import webdriver
        from selenium.common.exceptions import TimeoutException as SeleniumTimeout
        from selenium.webdriver.chrome.options import Options as ChromeOptions
        from selenium.webdriver.common.by import By
        from selenium.webdriver.support import expected_conditions as EC
        from selenium.webdriver.support.ui import WebDriverWait
        _SELENIUM_AVAILABLE = True
    except ImportError:
        webdriver = None
        SeleniumTimeout = Exception
        ChromeOptions = None
        By = None
        EC = None
        WebDriverWait = None

Extend the module docstring with a section that states PLAINLY, in the module's own voice,
the honest reason the second engine exists. Required substance, no softening and no
implication of the opposite:

- Selenium is NOT bot-evasion redundancy.
- Both engines drive a real Chromium and present near-identical automation signals.
- ChromeDriver historically leaks `$cdc_` properties into the page that fingerprinting
  scripts specifically look for, which makes Selenium if anything MORE detectable than
  Playwright, not less.
- Its value is ENGINE redundancy — a way to still render a page when playwright is not
  installed, or when the installed playwright package and the ambient Chromium build do not
  match (a failure mode this repo has already hit, which is why `TC_CHROMIUM_PATH` exists).
- It is not a second attempt at a block Playwright could not pass.

New constants:

- `_BROWSER_ENGINE_ENV = "TC_BROWSER_ENGINE"`, accepted values `"playwright"`,
  `"selenium"`, `"auto"`.
- `_SELENIUM_USER_AGENT` — a current, realistic desktop Chrome UA string. Comment that this
  is pinned in code so the value is visible and stable across ambient Chrome builds; it is
  hygiene, not evasion.
- `_SELENIUM_ARGS = ["--headless=new", "--disable-blink-features=AutomationControlled",
  "--window-size=1280,800", f"--user-agent={_SELENIUM_USER_AGENT}"]` — the window size
  mirrors `_VIEWPORT`.

`selenium_available() -> bool` returns `_SELENIUM_AVAILABLE`, placed next to
`playwright_available()`.

Extend `_resolve_engine(engine: str | None) -> str | None`:

    requested = (engine or os.environ.get(_BROWSER_ENGINE_ENV) or "auto").strip().lower()

- `"playwright"` -> `"playwright"` if available, else WARNING + None (do NOT fall through to
  Selenium; an explicit request that cannot be honored is a failure the operator must see).
- `"selenium"` -> `"selenium"` if available, else WARNING + None.
- `"auto"` -> `"playwright"` if available, else `"selenium"` if available, else a WARNING
  naming BOTH install paths (`pip install 'playwright>=1.40'` then `playwright install
  chromium`; `pip install 'selenium>=4.15'` plus a Chrome/Chromium binary) and None.
- Any other value -> WARNING naming the accepted values, then treat as `"auto"`.

`_fetch_page_html_selenium(url, *, wait_for_selector, require_selector, settle_ms,
timeout_ms) -> str | None`:

- `options = ChromeOptions()`; `for arg in _SELENIUM_ARGS: options.add_argument(arg)`.
- `options.binary_location = chromium_path` only when `os.environ.get(_CHROMIUM_PATH_ENV)`
  is set and non-empty (same escape hatch as the Playwright path).
- `driver = webdriver.Chrome(options=options)`, `driver.set_page_load_timeout(timeout_ms /
  1000.0)`, `driver.get(url)`.
- When `wait_for_selector`: `WebDriverWait(driver, timeout_ms / 1000.0).until(
  EC.presence_of_element_located((By.CSS_SELECTOR, wait_for_selector)))` inside
  `try/except SeleniumTimeout`, implementing the same `require_selector` branch as the
  Playwright path. An explicit wait, never a fixed sleep — the whole point is that a
  timeout is information.
- `time.sleep(settle_ms / 1000.0)` when `settle_ms` is truthy, then return
  `driver.page_source or None`.
- Broad `except Exception` with `# noqa: BLE001` -> WARNING -> None, and a `finally` that
  calls `driver.quit()` when a driver was constructed, wrapped in
  `contextlib.suppress(Exception)` with a one-line comment (an orphaned chromedriver
  process per failed run is worse than a swallowed teardown error). Never raise.

`fetch_page_html` dispatches on the resolved engine name. Selenium implements
`fetch_page_html` ONLY.

`fetch_urls_as_text` stays Playwright-only: guard on `_PLAYWRIGHT_AVAILABLE` directly (NOT
on `_resolve_engine`), and when it is False return `{}` after logging a WARNING that (a)
contains `pip install` — the pre-existing availability test asserts that substring — and
(b) states the download-interception flow is Playwright-specific, so Selenium cannot serve
this call even when it is installed. Do NOT fake a Selenium download path. Add a docstring
line saying so.

Append Selenium tests to `tests/unit/test_ingestion_browser.py` covering every case in
`<behavior>`. Mock Selenium ENTIRELY: patch `browser_mod._SELENIUM_AVAILABLE`,
`browser_mod.webdriver`, `browser_mod.ChromeOptions`, `browser_mod.WebDriverWait`,
`browser_mod.By`, `browser_mod.EC`, and `browser_mod.SeleniumTimeout` as needed. Build a
`_FakeDriver` recording `get` urls, exposing `page_source`, and recording `quit()`. Assert
engine choice by which fake was touched (e.g. `mock_sync_pw.assert_not_called()` for the
Selenium cases and `fake_webdriver.Chrome.assert_not_called()` for the Playwright cases) —
that is observable behavior, not restated configuration. Use `monkeypatch.setenv` /
`delenv` for `TC_BROWSER_ENGINE` and `TC_CHROMIUM_PATH`, and make sure every test that does
not care about the env var deletes it, so a stray export cannot flip the suite.
  </action>

  <verify>
    <automated>python -m pytest tests/unit/test_ingestion_browser.py -q</automated>
    <automated>test "$(grep -cE 'wait_until[[:space:]]*=[[:space:]]*.networkidle' src/trading_crab_lib/ingestion/browser.py || true)" = "0"</automated>
    <automated>python -c "from trading_crab_lib.ingestion import browser; assert browser.selenium_available() is False; assert browser.playwright_available() in (True, False); print('engine flags ok')"</automated>
  </verify>

  <done>
`selenium_available()` exists beside `playwright_available()`; `fetch_page_html` selects an
engine by argument then `TC_BROWSER_ENGINE` then Playwright-first auto; Selenium renders
with hardened options and an explicit `WebDriverWait`; `fetch_urls_as_text` remains
Playwright-only and says so; the module docstring states plainly that Selenium is engine
redundancy and not bot-evasion redundancy; all browser tests pass including the original 13.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Route macrotrends through the browser fallback at both call sites + packaging</name>
  <files>src/trading_crab_lib/ingestion/macrotrends.py, src/trading_crab_lib/platform/ingestion/macro_monthly.py, tests/unit/test_macrotrends.py, tests/unit/test_platform_macro_ingest.py, src/trading_crab_lib/pyproject.toml, .planning/STATE.md</files>

  <read_first>
    - src/trading_crab_lib/ingestion/macrotrends.py `_scrape_series` and
      `_scrape_series_html_table` — the `_extract_json_data` -> `read_html` chain and the
      `QE` resample that must stay frozen (D-01).
    - src/trading_crab_lib/platform/ingestion/macro_monthly.py
      `_scrape_macrotrends_monthly` — the same chain with an `ME` resample, and the existing
      import-not-copy pattern (`macrotrends._extract_json_data`, `multpl._SUFFIX_MAP`).
    - tests/unit/test_macrotrends.py and tests/unit/test_platform_macro_ingest.py — the
      existing failure tests raise from `http_get` itself, so they never reach the parse
      chain and must stay green untouched.
  </read_first>

  <behavior>
    - `_scrape_series` with an HTTP body carrying the embedded JSON array parses it and
      never calls `fetch_page_html`.
    - `_scrape_series` with an HTTP body carrying no JSON but a parseable table uses the
      `read_html` fallback and never calls `fetch_page_html`.
    - `_scrape_series` with an HTTP body carrying neither (a Cloudflare interstitial) calls
      `fetch_page_html` exactly once with the full series URL, and parses the rendered HTML
      it returns.
    - That fallback call passes `require_selector=False`, so an absent/wrong selector cannot
      discard a page that may still carry the data.
    - `_scrape_series` with an interstitial AND `fetch_page_html` returning None raises, and
      `fetch_all` degrades to an empty DataFrame with a WARNING rather than propagating.
    - `_scrape_series` output is still quarter-end indexed; `_scrape_macrotrends_monthly`
      output is still month-end indexed.
    - All four behaviors above hold identically for `_scrape_macrotrends_monthly`.
  </behavior>

  <action>
**`src/trading_crab_lib/ingestion/macrotrends.py`**

Add `from trading_crab_lib.ingestion.browser import fetch_page_html` at module level (so
tests patch it at `trading_crab_lib.ingestion.macrotrends.fetch_page_html`).

Add a module-level constant:

    # CANDIDATE selector, taken from a third-party snippet and NEVER confirmed against a
    # live macrotrends page — it may simply be wrong. It is therefore only ever passed with
    # require_selector=False: if it matches we get a real render-complete signal, and if it
    # does not we still keep whatever HTML rendered, which may carry the data in a script
    # tag even when no such table exists.
    BROWSER_WAIT_SELECTOR = "table.historical_data_table"

Add a helper used by both modules (defined here, imported there — the existing
import-not-copy convention):

    def _html_yields_data(html: str) -> bool:
        """True when *html* carries either the embedded JSON array or a table read_html can parse."""

Implementation: return True if `_extract_json_data(html)` is truthy; otherwise
`pd.read_html(StringIO(html))` inside `try/except (ValueError, ImportError)` returning
`bool(tables)` / False. Docstring must state what this encodes: a Cloudflare interstitial
carries neither, which is exactly why a browser render is worth its cost, and why a
response that DOES carry either must never pay it.

In `_scrape_series`, after `resp.raise_for_status()`:

    html = resp.text
    if not _html_yields_data(html):
        log.warning(
            "macrotrends HTTP response for %s carried neither embedded JSON nor a parseable "
            "table (bot interstitial?) — falling back to a rendered page", column_name,
        )
        rendered = fetch_page_html(url, wait_for_selector=BROWSER_WAIT_SELECTOR, require_selector=False)
        if rendered:
            html = rendered

Then run the EXISTING `_extract_json_data(html)` -> `_scrape_series_html_table(html, ...)`
chain against `html` instead of `resp.text`. Nothing else changes. Both `QE` resample rules
stay exactly as they are (D-01). Update the module docstring to note the two-step fetch:
plain HTTP first — it is cheap and becomes correct again the day Cloudflare stops
interfering — then a rendered page only when the HTTP body was not the data. State that
whether a real browser gets past this particular Cloudflare deployment is NOT confirmed.

**`src/trading_crab_lib/platform/ingestion/macro_monthly.py`**

Add `from trading_crab_lib.ingestion.browser import fetch_page_html` at module level (tests
patch it at `trading_crab_lib.platform.ingestion.macro_monthly.fetch_page_html`). Apply the
identical guard inside `_scrape_macrotrends_monthly` after `resp.raise_for_status()`, using
`macrotrends._html_yields_data` and `macrotrends.BROWSER_WAIT_SELECTOR` by reference (do
not re-declare either). Feed the resulting `html` into the existing
`macrotrends._extract_json_data` -> `_scrape_macrotrends_html_table_monthly` chain. The
`"ME"` resample rules are untouched. Extend the function docstring to describe the fallback
and to keep its existing explanation of why the interstitial surfaces as a parse failure
rather than an HTTP error.

**Tests**

`tests/unit/test_macrotrends.py`: add a `SAMPLE_INTERSTITIAL_HTML` fixture with no embedded
JSON and no `<table>` (a "Just a moment..." style body). Add tests for every case in
`<behavior>`, patching `trading_crab_lib.ingestion.macrotrends.fetch_page_html` in EVERY
test that can reach the fallback — playwright is importable in this environment, so an
unpatched path would launch a real browser. Assert `fetch_page_html.assert_not_called()` on
the two HTTP-success cases (this is the "HTTP is tried first" proof), assert it is called
once with the full URL on the interstitial case, and assert `require_selector is False` in
its kwargs. Add a quarter-end index assertion as a frozen-resample regression guard.

`tests/unit/test_platform_macro_ingest.py`: the same set against
`_scrape_macrotrends_monthly` / `_fetch_macrotrends_monthly_all`, patching
`trading_crab_lib.platform.ingestion.macro_monthly.fetch_page_html`, with a month-end index
assertion as the frozen-resample guard.

Do not edit the existing macrotrends tests — their failures are raised from `http_get`, so
they never reach the new branch.

**`src/trading_crab_lib/pyproject.toml`**

Add `"selenium>=4.15"` to the existing `[project.optional-dependencies] browser` extra
alongside `playwright>=1.40`, and add `selenium = ">=4.15"` to
`[tool.poetry.group.browser.dependencies]`. Rewrite the extra's comment to name both
engines and state the honest split: Playwright is the primary engine, Selenium is a
fallback for when Playwright is unavailable or its browser build mismatches — not a second
attempt at a block Playwright could not pass. Keep the note that the wheel ships no browser
binaries. `[all]` already pulls `browser`; do not touch it.

**`.planning/STATE.md`**

Append a row to the `Quick Tasks Completed` table for `260805-r7w`, and add a
Blockers/Concerns bullet recording plainly that macrotrends-via-browser is WIRING-verified
by mocked tests only and NOT live-verified: the container's egress is proxy-reset and
Chromium here fails even on `example.com`, and a diagnostic on the user's machine is still
pending on whether a real browser gets past macrotrends' Cloudflare at all. If it does not,
this plumbing does not rescue macrotrends. Also record that `BROWSER_WAIT_SELECTOR` is an
unconfirmed candidate selector, used only with `require_selector=False` for that reason.
  </action>

  <verify>
    <automated>python -m pytest tests/unit/test_macrotrends.py tests/unit/test_platform_macro_ingest.py -q</automated>
    <automated>python -m pytest tests/ -q</automated>
    <automated>test "$(grep -c 'resample("QE")' src/trading_crab_lib/ingestion/macrotrends.py || true)" = "4"</automated>
    <automated>python -c "import tomllib,pathlib; d=tomllib.loads(pathlib.Path('src/trading_crab_lib/pyproject.toml').read_text()); b=d['project']['optional-dependencies']['browser']; assert any('selenium' in x for x in b) and any('playwright' in x for x in b), b; print('browser extra ok:', b)"</automated>
    <human-check>
      On a residential connection (NOT this container — all egress here is proxy-reset and
      Chromium fails even on example.com), run:

        pip install 'trading-crab-lib[browser]'   # or: pip install playwright selenium
        playwright install chromium
        python -c "from trading_crab_lib.ingestion.browser import fetch_page_html; \
                   h=fetch_page_html('https://www.macrotrends.net/1333/historical-gold-prices-100-year-chart'); \
                   print(len(h) if h else 'None'); print((h or '')[:400])"

      Then repeat with TC_BROWSER_ENGINE=selenium.

      What this answers: whether a real browser gets past macrotrends' Cloudflare AT ALL.
      Until it is answered, nothing in the code, commits, STATE.md, or the summary may claim
      macrotrends-via-browser works. A short body or a "Just a moment..." title means it
      does not, and this plumbing did not rescue macrotrends — record that outcome as-is.
    </human-check>
  </verify>

  <done>
Both macrotrends call sites try plain HTTP first and reach `fetch_page_html` only when the
HTTP body yields neither embedded JSON nor a parseable table; the candidate selector is
passed with `require_selector=False`; both resample rules are unchanged; `selenium` is in
the `[browser]` extra with an honest comment; STATE.md records the not-live-verified status;
`python -m pytest tests/ -q` passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| macrotrends.net -> headless Chromium | Untrusted remote JavaScript is now fetched AND EXECUTED locally for a SECOND source — the boundary widened by 260805-jt2 now covers macrotrends too |
| rendered HTML -> `_extract_json_data` / `pandas.read_html` | Untrusted, JS-generated markup crosses into regex + JSON + HTML parsing and then into price data |
| operator environment -> `TC_BROWSER_ENGINE`, `TC_CHROMIUM_PATH` | Env vars select which automation stack and which binary are launched |
| PyPI -> `selenium` in the `[browser]` extra | A new third-party dependency is declared in library packaging |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-r7w-01 | Tampering | rendered HTML -> `_extract_json_data` / `read_html` in both macrotrends modules | medium | mitigate | The browser is reached ONLY after `_html_yields_data` proves the HTTP body was not the data, and the rendered HTML goes through the exact same JSON-then-table parse chain and the same numeric coercion (`pd.to_numeric(errors="coerce")` + `dropna`) as the HTTP body — a rendered page gets no parsing privilege the HTTP path lacks |
| T-r7w-02 | Elevation of Privilege | `_fetch_page_html_playwright` / `_fetch_page_html_selenium` executing remote JS | medium | mitigate | Headless launch, a fresh context/driver per call with no persistent profile and no stored credentials, navigation limited to the single caller-supplied URL, `browser.close()` in `finally` on the Playwright path and `driver.quit()` in `finally` on the Selenium path so no orphan browser or chromedriver process survives a failure |
| T-r7w-03 | Denial of Service | browser cost on every macrotrends run | medium | mitigate | The render is gated on the HTTP body yielding neither JSON nor a table, so the browser is never launched while the cheap path still works; `_html_yields_data` is a pure local check with no network cost; per-series 3s pacing (`RATE_LIMIT_SECONDS`) is unchanged |
| T-r7w-04 | Denial of Service | selector wait hanging a run | low | mitigate | Every wait is bounded by `timeout_ms` (Playwright `wait_for_selector` timeout, Selenium `WebDriverWait`); a timeout returns None or the partial render and the per-series `except` degrades that series only. The idle-network wait predicate is banned by a grep gate precisely because it never fires on ad/analytics-bearing pages |
| T-r7w-05 | Spoofing | `TC_CHROMIUM_PATH` binary selection (now also `options.binary_location`) | low | accept | Operator-controlled, carrying exactly the trust level of the interpreter already running the pipeline; it exists because ambient Chromium builds mismatch the installed playwright package. Documented, not validated — unchanged posture from 260805-jt2 |
| T-r7w-06 | Spoofing | `TC_BROWSER_ENGINE` engine selection | low | accept | Operator-controlled and restricted to three recognised values; an unrecognised value logs a WARNING and falls back to 'auto' rather than being passed through to a driver |
| T-r7w-SC | Tampering | supply chain — `selenium` added to the `[browser]` extra | low | accept | No package-manager install task exists in this plan; `selenium` is declared in packaging metadata behind an OPTIONAL extra with a version floor and is NOT installed in this environment (its tests run against mocks bound to `None` in the ImportError branch). It is the long-established first-party SeleniumHQ package (`github.com/SeleniumHQ/selenium`), not a newly-surfaced name. The operator installs it deliberately via `[browser]` |
| T-r7w-07 | Information Disclosure | log output from the browser paths | low | mitigate | The module logs URLs and dict keys only — no cookies, no response bodies, no page source, and no env var VALUES are logged |
</threat_model>

<verification>
1. `python -m pytest tests/ -q` passes — the whole suite, not just the touched files.
2. The 13 pre-existing tests in `tests/unit/test_ingestion_browser.py` are byte-for-byte
   unedited (`git diff` on that file shows only appended content).
3. Test honesty: for each of the three test files touched, the executor stashes the SOURCE
   changes, runs the new tests, and records that they FAIL (missing symbol, wrong call
   count, or wrong return) before restoring — RED evidence goes in the summary. Any new test
   that passes against pre-change code is tautological and must be rewritten.
4. `wait_until="networkidle"` appears nowhere in `ingestion/browser.py` (grep gate in the
   Task 1 and Task 2 verify blocks).
5. `browser.py` imports no pandas and never parses, validates, or interprets fetched text.
6. `fetch_urls_as_text` has no Selenium code path; `webdriver` is unreachable from it.
7. `prices_daily.py` is NOT in `files_modified` and its import of `fetch_stooq_csvs` /
   `playwright_available` still resolves — proven by
   `tests/unit/test_platform_prices_ingest.py` passing unedited.
8. Both macrotrends resample rules are unchanged: `QE` in `ingestion/macrotrends.py`, `ME`
   in `platform/ingestion/macro_monthly.py` (D-01).
9. No comment, docstring, log line, commit message, or summary claims that Selenium defeats
   a block Playwright cannot, or that macrotrends-via-browser is confirmed working.
10. NOT verifiable here and deliberately not attempted: whether a real browser gets past
    macrotrends' Cloudflare at all. All container egress is proxy-reset and Chromium here
    fails even on example.com. The Task 3 `<human-check>` is the only way to answer it.
</verification>

<success_criteria>
- `ingestion/browser.py` is a source-agnostic browser-rendering capability, not a Stooq
  fetcher, and `fetch_stooq_csvs` still works for its existing caller with zero edits there.
- A required selector that never appears returns None; an unconfirmed one passed with
  `require_selector=False` degrades to the rendered HTML instead of discarding the page.
- Two engines exist with an explicit, testable preference order and env override, and the
  module says out loud that the second one is engine redundancy, not evasion redundancy.
- macrotrends gets a rendering fallback at both call sites without paying for it whenever
  plain HTTP still returns the data, and without either module's cadence changing.
- Full test suite green; every new test proven RED against pre-change code.
</success_criteria>

<output>
Create `.planning/quick/260805-r7w-generalize-browser-module-and-add-seleni/260805-r7w-SUMMARY.md` when done.
</output>
</content>
</invoke>
